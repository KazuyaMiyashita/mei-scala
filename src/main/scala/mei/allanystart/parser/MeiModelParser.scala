package mei.allanystart.parser

// This file is auto-generated from MEI RelaxNG schema.
// Do not edit this file manually.

import scala.xml.{Node, NodeSeq, Elem}
import mei.allanystart.*

/** Although this abstraction is not strictly necessary, it is provided to prevent the linter from warning about an unused variable in def parse for a class that has no fields. */
trait MeiModelDummyParser {
  def parse(node: Node): Any
}

object SvgParser extends MeiModelDummyParser {
  def parse(node: Node): Svg = {
    Svg(
      node.child.collect {
        case n: Elem if n.label == "a"                => mei.allanystart.parser.AParser.parse(n)
        case n: Elem if n.label == "altGlyphDef"      => mei.allanystart.parser.AltGlyphDefParser.parse(n)
        case n: Elem if n.label == "animate"          => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "animateColor"     => mei.allanystart.parser.AnimateColorParser.parse(n)
        case n: Elem if n.label == "animateMotion"    => mei.allanystart.parser.AnimateMotionParser.parse(n)
        case n: Elem if n.label == "animateTransform" => mei.allanystart.parser.AnimateTransformParser.parse(n)
        case n: Elem if n.label == "circle"           => mei.allanystart.parser.CircleParser.parse(n)
        case n: Elem if n.label == "clipPath"         => mei.allanystart.parser.ClipPathParser.parse(n)
        case n: Elem if n.label == "color-profile"    => mei.allanystart.parser.ColorProfileParser.parse(n)
        case n: Elem if n.label == "cursor"           => mei.allanystart.parser.CursorParser.parse(n)
        case n: Elem if n.label == "defs"             => mei.allanystart.parser.DefsParser.parse(n)
        case n: Elem if n.label == "desc"             => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "ellipse"          => mei.allanystart.parser.EllipseParser.parse(n)
        case n: Elem if n.label == "filter"           => mei.allanystart.parser.FilterParser.parse(n)
        case n: Elem if n.label == "font"             => mei.allanystart.parser.FontParser.parse(n)
        case n: Elem if n.label == "font-face"        => mei.allanystart.parser.FontFaceParser.parse(n)
        case n: Elem if n.label == "g"                => mei.allanystart.parser.GParser.parse(n)
        case n: Elem if n.label == "image"            => mei.allanystart.parser.ImageParser.parse(n)
        case n: Elem if n.label == "line"             => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "linearGradient"   => mei.allanystart.parser.LinearGradientParser.parse(n)
        case n: Elem if n.label == "marker"           => mei.allanystart.parser.MarkerParser.parse(n)
        case n: Elem if n.label == "mask"             => mei.allanystart.parser.MaskParser.parse(n)
        case n: Elem if n.label == "metadata"         => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "path"             => mei.allanystart.parser.PathParser.parse(n)
        case n: Elem if n.label == "pattern"          => mei.allanystart.parser.PatternParser.parse(n)
        case n: Elem if n.label == "polygon"          => mei.allanystart.parser.PolygonParser.parse(n)
        case n: Elem if n.label == "polyline"         => mei.allanystart.parser.PolylineParser.parse(n)
        case n: Elem if n.label == "radialGradient"   => mei.allanystart.parser.RadialGradientParser.parse(n)
        case n: Elem if n.label == "rect"             => mei.allanystart.parser.RectParser.parse(n)
        case n: Elem if n.label == "script"           => mei.allanystart.parser.ScriptParser.parse(n)
        case n: Elem if n.label == "set"              => mei.allanystart.parser.SetParser.parse(n)
        case n: Elem if n.label == "style"            => mei.allanystart.parser.StyleParser.parse(n)
        case n: Elem if n.label == "svg"              => mei.allanystart.parser.SvgParser.parse(n)
        case n: Elem if n.label == "switch"           => mei.allanystart.parser.SwitchParser.parse(n)
        case n: Elem if n.label == "symbol"           => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "text"             => mei.allanystart.parser.TextParser.parse(n)
        case n: Elem if n.label == "title"            => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "use"              => mei.allanystart.parser.UseParser.parse(n)
        case n: Elem if n.label == "view"             => mei.allanystart.parser.ViewParser.parse(n)
      }.toList,
    )
  }
}
object GParser extends MeiModelDummyParser {
  def parse(node: Node): G = {
    G(
      node.child.collect {
        case n: Elem if n.label == "a"                => mei.allanystart.parser.AParser.parse(n)
        case n: Elem if n.label == "altGlyphDef"      => mei.allanystart.parser.AltGlyphDefParser.parse(n)
        case n: Elem if n.label == "animate"          => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "animateColor"     => mei.allanystart.parser.AnimateColorParser.parse(n)
        case n: Elem if n.label == "animateMotion"    => mei.allanystart.parser.AnimateMotionParser.parse(n)
        case n: Elem if n.label == "animateTransform" => mei.allanystart.parser.AnimateTransformParser.parse(n)
        case n: Elem if n.label == "circle"           => mei.allanystart.parser.CircleParser.parse(n)
        case n: Elem if n.label == "clipPath"         => mei.allanystart.parser.ClipPathParser.parse(n)
        case n: Elem if n.label == "color-profile"    => mei.allanystart.parser.ColorProfileParser.parse(n)
        case n: Elem if n.label == "cursor"           => mei.allanystart.parser.CursorParser.parse(n)
        case n: Elem if n.label == "defs"             => mei.allanystart.parser.DefsParser.parse(n)
        case n: Elem if n.label == "desc"             => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "ellipse"          => mei.allanystart.parser.EllipseParser.parse(n)
        case n: Elem if n.label == "filter"           => mei.allanystart.parser.FilterParser.parse(n)
        case n: Elem if n.label == "font"             => mei.allanystart.parser.FontParser.parse(n)
        case n: Elem if n.label == "font-face"        => mei.allanystart.parser.FontFaceParser.parse(n)
        case n: Elem if n.label == "g"                => mei.allanystart.parser.GParser.parse(n)
        case n: Elem if n.label == "image"            => mei.allanystart.parser.ImageParser.parse(n)
        case n: Elem if n.label == "line"             => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "linearGradient"   => mei.allanystart.parser.LinearGradientParser.parse(n)
        case n: Elem if n.label == "marker"           => mei.allanystart.parser.MarkerParser.parse(n)
        case n: Elem if n.label == "mask"             => mei.allanystart.parser.MaskParser.parse(n)
        case n: Elem if n.label == "metadata"         => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "path"             => mei.allanystart.parser.PathParser.parse(n)
        case n: Elem if n.label == "pattern"          => mei.allanystart.parser.PatternParser.parse(n)
        case n: Elem if n.label == "polygon"          => mei.allanystart.parser.PolygonParser.parse(n)
        case n: Elem if n.label == "polyline"         => mei.allanystart.parser.PolylineParser.parse(n)
        case n: Elem if n.label == "radialGradient"   => mei.allanystart.parser.RadialGradientParser.parse(n)
        case n: Elem if n.label == "rect"             => mei.allanystart.parser.RectParser.parse(n)
        case n: Elem if n.label == "script"           => mei.allanystart.parser.ScriptParser.parse(n)
        case n: Elem if n.label == "set"              => mei.allanystart.parser.SetParser.parse(n)
        case n: Elem if n.label == "style"            => mei.allanystart.parser.StyleParser.parse(n)
        case n: Elem if n.label == "svg"              => mei.allanystart.parser.SvgParser.parse(n)
        case n: Elem if n.label == "switch"           => mei.allanystart.parser.SwitchParser.parse(n)
        case n: Elem if n.label == "symbol"           => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "text"             => mei.allanystart.parser.TextParser.parse(n)
        case n: Elem if n.label == "title"            => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "use"              => mei.allanystart.parser.UseParser.parse(n)
        case n: Elem if n.label == "view"             => mei.allanystart.parser.ViewParser.parse(n)
      }.toList,
    )
  }
}
object DefsParser extends MeiModelDummyParser {
  def parse(node: Node): Defs = {
    Defs(
      node.child.collect {
        case n: Elem if n.label == "a"                => mei.allanystart.parser.AParser.parse(n)
        case n: Elem if n.label == "altGlyphDef"      => mei.allanystart.parser.AltGlyphDefParser.parse(n)
        case n: Elem if n.label == "animate"          => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "animateColor"     => mei.allanystart.parser.AnimateColorParser.parse(n)
        case n: Elem if n.label == "animateMotion"    => mei.allanystart.parser.AnimateMotionParser.parse(n)
        case n: Elem if n.label == "animateTransform" => mei.allanystart.parser.AnimateTransformParser.parse(n)
        case n: Elem if n.label == "circle"           => mei.allanystart.parser.CircleParser.parse(n)
        case n: Elem if n.label == "clipPath"         => mei.allanystart.parser.ClipPathParser.parse(n)
        case n: Elem if n.label == "color-profile"    => mei.allanystart.parser.ColorProfileParser.parse(n)
        case n: Elem if n.label == "cursor"           => mei.allanystart.parser.CursorParser.parse(n)
        case n: Elem if n.label == "defs"             => mei.allanystart.parser.DefsParser.parse(n)
        case n: Elem if n.label == "desc"             => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "ellipse"          => mei.allanystart.parser.EllipseParser.parse(n)
        case n: Elem if n.label == "filter"           => mei.allanystart.parser.FilterParser.parse(n)
        case n: Elem if n.label == "font"             => mei.allanystart.parser.FontParser.parse(n)
        case n: Elem if n.label == "font-face"        => mei.allanystart.parser.FontFaceParser.parse(n)
        case n: Elem if n.label == "g"                => mei.allanystart.parser.GParser.parse(n)
        case n: Elem if n.label == "image"            => mei.allanystart.parser.ImageParser.parse(n)
        case n: Elem if n.label == "line"             => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "linearGradient"   => mei.allanystart.parser.LinearGradientParser.parse(n)
        case n: Elem if n.label == "marker"           => mei.allanystart.parser.MarkerParser.parse(n)
        case n: Elem if n.label == "mask"             => mei.allanystart.parser.MaskParser.parse(n)
        case n: Elem if n.label == "metadata"         => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "path"             => mei.allanystart.parser.PathParser.parse(n)
        case n: Elem if n.label == "pattern"          => mei.allanystart.parser.PatternParser.parse(n)
        case n: Elem if n.label == "polygon"          => mei.allanystart.parser.PolygonParser.parse(n)
        case n: Elem if n.label == "polyline"         => mei.allanystart.parser.PolylineParser.parse(n)
        case n: Elem if n.label == "radialGradient"   => mei.allanystart.parser.RadialGradientParser.parse(n)
        case n: Elem if n.label == "rect"             => mei.allanystart.parser.RectParser.parse(n)
        case n: Elem if n.label == "script"           => mei.allanystart.parser.ScriptParser.parse(n)
        case n: Elem if n.label == "set"              => mei.allanystart.parser.SetParser.parse(n)
        case n: Elem if n.label == "style"            => mei.allanystart.parser.StyleParser.parse(n)
        case n: Elem if n.label == "svg"              => mei.allanystart.parser.SvgParser.parse(n)
        case n: Elem if n.label == "switch"           => mei.allanystart.parser.SwitchParser.parse(n)
        case n: Elem if n.label == "symbol"           => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "text"             => mei.allanystart.parser.TextParser.parse(n)
        case n: Elem if n.label == "title"            => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "use"              => mei.allanystart.parser.UseParser.parse(n)
        case n: Elem if n.label == "view"             => mei.allanystart.parser.ViewParser.parse(n)
      }.toList,
    )
  }
}
object DescParser extends MeiModelDummyParser {
  def parse(node: Node): Desc = {
    Desc(
      node.child.collect {
        case n if n.text.trim.nonEmpty => n.text
      }.toList,
    )
  }
}
object TitleParser extends MeiModelDummyParser {
  def parse(node: Node): Title = {
    Title(
      node.child.collect {
        case n if n.text.trim.nonEmpty => n.text
      }.toList,
    )
  }
}
object MetadataParser extends MeiModelDummyParser {
  def parse(node: Node): Metadata = {
    Metadata(
      node.child.collect {
        case n if n.text.trim.nonEmpty => n.text
      }.toList,
    )
  }
}
object SymbolParser extends MeiModelDummyParser {
  def parse(node: Node): Symbol = {
    Symbol(
      node.child.collect {
        case n: Elem if n.label == "a"                => mei.allanystart.parser.AParser.parse(n)
        case n: Elem if n.label == "altGlyphDef"      => mei.allanystart.parser.AltGlyphDefParser.parse(n)
        case n: Elem if n.label == "animate"          => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "animateColor"     => mei.allanystart.parser.AnimateColorParser.parse(n)
        case n: Elem if n.label == "animateMotion"    => mei.allanystart.parser.AnimateMotionParser.parse(n)
        case n: Elem if n.label == "animateTransform" => mei.allanystart.parser.AnimateTransformParser.parse(n)
        case n: Elem if n.label == "circle"           => mei.allanystart.parser.CircleParser.parse(n)
        case n: Elem if n.label == "clipPath"         => mei.allanystart.parser.ClipPathParser.parse(n)
        case n: Elem if n.label == "color-profile"    => mei.allanystart.parser.ColorProfileParser.parse(n)
        case n: Elem if n.label == "cursor"           => mei.allanystart.parser.CursorParser.parse(n)
        case n: Elem if n.label == "defs"             => mei.allanystart.parser.DefsParser.parse(n)
        case n: Elem if n.label == "desc"             => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "ellipse"          => mei.allanystart.parser.EllipseParser.parse(n)
        case n: Elem if n.label == "filter"           => mei.allanystart.parser.FilterParser.parse(n)
        case n: Elem if n.label == "font"             => mei.allanystart.parser.FontParser.parse(n)
        case n: Elem if n.label == "font-face"        => mei.allanystart.parser.FontFaceParser.parse(n)
        case n: Elem if n.label == "g"                => mei.allanystart.parser.GParser.parse(n)
        case n: Elem if n.label == "image"            => mei.allanystart.parser.ImageParser.parse(n)
        case n: Elem if n.label == "line"             => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "linearGradient"   => mei.allanystart.parser.LinearGradientParser.parse(n)
        case n: Elem if n.label == "marker"           => mei.allanystart.parser.MarkerParser.parse(n)
        case n: Elem if n.label == "mask"             => mei.allanystart.parser.MaskParser.parse(n)
        case n: Elem if n.label == "metadata"         => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "path"             => mei.allanystart.parser.PathParser.parse(n)
        case n: Elem if n.label == "pattern"          => mei.allanystart.parser.PatternParser.parse(n)
        case n: Elem if n.label == "polygon"          => mei.allanystart.parser.PolygonParser.parse(n)
        case n: Elem if n.label == "polyline"         => mei.allanystart.parser.PolylineParser.parse(n)
        case n: Elem if n.label == "radialGradient"   => mei.allanystart.parser.RadialGradientParser.parse(n)
        case n: Elem if n.label == "rect"             => mei.allanystart.parser.RectParser.parse(n)
        case n: Elem if n.label == "script"           => mei.allanystart.parser.ScriptParser.parse(n)
        case n: Elem if n.label == "set"              => mei.allanystart.parser.SetParser.parse(n)
        case n: Elem if n.label == "style"            => mei.allanystart.parser.StyleParser.parse(n)
        case n: Elem if n.label == "svg"              => mei.allanystart.parser.SvgParser.parse(n)
        case n: Elem if n.label == "switch"           => mei.allanystart.parser.SwitchParser.parse(n)
        case n: Elem if n.label == "symbol"           => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "text"             => mei.allanystart.parser.TextParser.parse(n)
        case n: Elem if n.label == "title"            => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "use"              => mei.allanystart.parser.UseParser.parse(n)
        case n: Elem if n.label == "view"             => mei.allanystart.parser.ViewParser.parse(n)
      }.toList,
    )
  }
}
object UseParser extends MeiModelDummyParser {
  def parse(node: Node): Use = {
    Use(
      node.child.collect {
        case n: Elem if n.label == "animate"          => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "animateColor"     => mei.allanystart.parser.AnimateColorParser.parse(n)
        case n: Elem if n.label == "animateMotion"    => mei.allanystart.parser.AnimateMotionParser.parse(n)
        case n: Elem if n.label == "animateTransform" => mei.allanystart.parser.AnimateTransformParser.parse(n)
        case n: Elem if n.label == "desc"             => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "metadata"         => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "set"              => mei.allanystart.parser.SetParser.parse(n)
        case n: Elem if n.label == "title"            => mei.allanystart.parser.TitleParser.parse(n)
      }.toList,
    )
  }
}
object SwitchParser extends MeiModelDummyParser {
  def parse(node: Node): Switch = {
    Switch(
      node.child.collect {
        case n: Elem if n.label == "a"                => mei.allanystart.parser.AParser.parse(n)
        case n: Elem if n.label == "animate"          => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "animateColor"     => mei.allanystart.parser.AnimateColorParser.parse(n)
        case n: Elem if n.label == "animateMotion"    => mei.allanystart.parser.AnimateMotionParser.parse(n)
        case n: Elem if n.label == "animateTransform" => mei.allanystart.parser.AnimateTransformParser.parse(n)
        case n: Elem if n.label == "circle"           => mei.allanystart.parser.CircleParser.parse(n)
        case n: Elem if n.label == "desc"             => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "ellipse"          => mei.allanystart.parser.EllipseParser.parse(n)
        case n: Elem if n.label == "foreignObject"    => mei.allanystart.parser.ForeignObjectParser.parse(n)
        case n: Elem if n.label == "g"                => mei.allanystart.parser.GParser.parse(n)
        case n: Elem if n.label == "image"            => mei.allanystart.parser.ImageParser.parse(n)
        case n: Elem if n.label == "line"             => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "metadata"         => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "path"             => mei.allanystart.parser.PathParser.parse(n)
        case n: Elem if n.label == "polygon"          => mei.allanystart.parser.PolygonParser.parse(n)
        case n: Elem if n.label == "polyline"         => mei.allanystart.parser.PolylineParser.parse(n)
        case n: Elem if n.label == "rect"             => mei.allanystart.parser.RectParser.parse(n)
        case n: Elem if n.label == "set"              => mei.allanystart.parser.SetParser.parse(n)
        case n: Elem if n.label == "svg"              => mei.allanystart.parser.SvgParser.parse(n)
        case n: Elem if n.label == "switch"           => mei.allanystart.parser.SwitchParser.parse(n)
        case n: Elem if n.label == "text"             => mei.allanystart.parser.TextParser.parse(n)
        case n: Elem if n.label == "title"            => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "use"              => mei.allanystart.parser.UseParser.parse(n)
      }.toList,
    )
  }
}
object ImageParser extends MeiModelDummyParser {
  def parse(node: Node): Image = {
    Image(
      node.child.collect {
        case n: Elem if n.label == "animate"          => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "animateColor"     => mei.allanystart.parser.AnimateColorParser.parse(n)
        case n: Elem if n.label == "animateMotion"    => mei.allanystart.parser.AnimateMotionParser.parse(n)
        case n: Elem if n.label == "animateTransform" => mei.allanystart.parser.AnimateTransformParser.parse(n)
        case n: Elem if n.label == "desc"             => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "metadata"         => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "set"              => mei.allanystart.parser.SetParser.parse(n)
        case n: Elem if n.label == "title"            => mei.allanystart.parser.TitleParser.parse(n)
      }.toList,
    )
  }
}
object StyleParser extends MeiModelDummyParser {
  def parse(node: Node): Style = {
    Style(
      node.child.collect {
        case n if n.text.trim.nonEmpty => n.text
      }.toList,
    )
  }
}
object PathParser extends MeiModelDummyParser {
  def parse(node: Node): Path = {
    Path(
      node.child.collect {
        case n: Elem if n.label == "animate"          => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "animateColor"     => mei.allanystart.parser.AnimateColorParser.parse(n)
        case n: Elem if n.label == "animateMotion"    => mei.allanystart.parser.AnimateMotionParser.parse(n)
        case n: Elem if n.label == "animateTransform" => mei.allanystart.parser.AnimateTransformParser.parse(n)
        case n: Elem if n.label == "desc"             => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "metadata"         => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "set"              => mei.allanystart.parser.SetParser.parse(n)
        case n: Elem if n.label == "title"            => mei.allanystart.parser.TitleParser.parse(n)
      }.toList,
    )
  }
}
object RectParser extends MeiModelDummyParser {
  def parse(node: Node): Rect = {
    Rect(
      node.child.collect {
        case n: Elem if n.label == "animate"          => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "animateColor"     => mei.allanystart.parser.AnimateColorParser.parse(n)
        case n: Elem if n.label == "animateMotion"    => mei.allanystart.parser.AnimateMotionParser.parse(n)
        case n: Elem if n.label == "animateTransform" => mei.allanystart.parser.AnimateTransformParser.parse(n)
        case n: Elem if n.label == "desc"             => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "metadata"         => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "set"              => mei.allanystart.parser.SetParser.parse(n)
        case n: Elem if n.label == "title"            => mei.allanystart.parser.TitleParser.parse(n)
      }.toList,
    )
  }
}
object CircleParser extends MeiModelDummyParser {
  def parse(node: Node): Circle = {
    Circle(
      node.child.collect {
        case n: Elem if n.label == "animate"          => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "animateColor"     => mei.allanystart.parser.AnimateColorParser.parse(n)
        case n: Elem if n.label == "animateMotion"    => mei.allanystart.parser.AnimateMotionParser.parse(n)
        case n: Elem if n.label == "animateTransform" => mei.allanystart.parser.AnimateTransformParser.parse(n)
        case n: Elem if n.label == "desc"             => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "metadata"         => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "set"              => mei.allanystart.parser.SetParser.parse(n)
        case n: Elem if n.label == "title"            => mei.allanystart.parser.TitleParser.parse(n)
      }.toList,
    )
  }
}
object LineParser extends MeiModelDummyParser {
  def parse(node: Node): Line = {
    Line(
      node.child.collect {
        case n: Elem if n.label == "animate"          => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "animateColor"     => mei.allanystart.parser.AnimateColorParser.parse(n)
        case n: Elem if n.label == "animateMotion"    => mei.allanystart.parser.AnimateMotionParser.parse(n)
        case n: Elem if n.label == "animateTransform" => mei.allanystart.parser.AnimateTransformParser.parse(n)
        case n: Elem if n.label == "desc"             => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "metadata"         => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "set"              => mei.allanystart.parser.SetParser.parse(n)
        case n: Elem if n.label == "title"            => mei.allanystart.parser.TitleParser.parse(n)
      }.toList,
    )
  }
}
object EllipseParser extends MeiModelDummyParser {
  def parse(node: Node): Ellipse = {
    Ellipse(
      node.child.collect {
        case n: Elem if n.label == "animate"          => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "animateColor"     => mei.allanystart.parser.AnimateColorParser.parse(n)
        case n: Elem if n.label == "animateMotion"    => mei.allanystart.parser.AnimateMotionParser.parse(n)
        case n: Elem if n.label == "animateTransform" => mei.allanystart.parser.AnimateTransformParser.parse(n)
        case n: Elem if n.label == "desc"             => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "metadata"         => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "set"              => mei.allanystart.parser.SetParser.parse(n)
        case n: Elem if n.label == "title"            => mei.allanystart.parser.TitleParser.parse(n)
      }.toList,
    )
  }
}
object PolylineParser extends MeiModelDummyParser {
  def parse(node: Node): Polyline = {
    Polyline(
      node.child.collect {
        case n: Elem if n.label == "animate"          => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "animateColor"     => mei.allanystart.parser.AnimateColorParser.parse(n)
        case n: Elem if n.label == "animateMotion"    => mei.allanystart.parser.AnimateMotionParser.parse(n)
        case n: Elem if n.label == "animateTransform" => mei.allanystart.parser.AnimateTransformParser.parse(n)
        case n: Elem if n.label == "desc"             => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "metadata"         => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "set"              => mei.allanystart.parser.SetParser.parse(n)
        case n: Elem if n.label == "title"            => mei.allanystart.parser.TitleParser.parse(n)
      }.toList,
    )
  }
}
object PolygonParser extends MeiModelDummyParser {
  def parse(node: Node): Polygon = {
    Polygon(
      node.child.collect {
        case n: Elem if n.label == "animate"          => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "animateColor"     => mei.allanystart.parser.AnimateColorParser.parse(n)
        case n: Elem if n.label == "animateMotion"    => mei.allanystart.parser.AnimateMotionParser.parse(n)
        case n: Elem if n.label == "animateTransform" => mei.allanystart.parser.AnimateTransformParser.parse(n)
        case n: Elem if n.label == "desc"             => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "metadata"         => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "set"              => mei.allanystart.parser.SetParser.parse(n)
        case n: Elem if n.label == "title"            => mei.allanystart.parser.TitleParser.parse(n)
      }.toList,
    )
  }
}
object TextParser extends MeiModelDummyParser {
  def parse(node: Node): Text = {
    Text(
      node.child.collect {
        case n: Elem if n.label == "a"                => mei.allanystart.parser.AParser.parse(n)
        case n: Elem if n.label == "altGlyph"         => mei.allanystart.parser.AltGlyphParser.parse(n)
        case n: Elem if n.label == "animate"          => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "animateColor"     => mei.allanystart.parser.AnimateColorParser.parse(n)
        case n: Elem if n.label == "animateMotion"    => mei.allanystart.parser.AnimateMotionParser.parse(n)
        case n: Elem if n.label == "animateTransform" => mei.allanystart.parser.AnimateTransformParser.parse(n)
        case n: Elem if n.label == "desc"             => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "metadata"         => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "set"              => mei.allanystart.parser.SetParser.parse(n)
        case n: Elem if n.label == "textPath"         => mei.allanystart.parser.TextPathParser.parse(n)
        case n: Elem if n.label == "title"            => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "tref"             => mei.allanystart.parser.TrefParser.parse(n)
        case n: Elem if n.label == "tspan"            => mei.allanystart.parser.TspanParser.parse(n)
        case n if n.text.trim.nonEmpty                => n.text
      }.toList,
    )
  }
}
object TspanParser extends MeiModelDummyParser {
  def parse(node: Node): Tspan = {
    Tspan(
      node.child.collect {
        case n: Elem if n.label == "a"            => mei.allanystart.parser.AParser.parse(n)
        case n: Elem if n.label == "altGlyph"     => mei.allanystart.parser.AltGlyphParser.parse(n)
        case n: Elem if n.label == "animate"      => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "animateColor" => mei.allanystart.parser.AnimateColorParser.parse(n)
        case n: Elem if n.label == "desc"         => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "metadata"     => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "set"          => mei.allanystart.parser.SetParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "tref"         => mei.allanystart.parser.TrefParser.parse(n)
        case n: Elem if n.label == "tspan"        => mei.allanystart.parser.TspanParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
    )
  }
}
object TrefParser extends MeiModelDummyParser {
  def parse(node: Node): Tref = {
    Tref(
      node.child.collect {
        case n: Elem if n.label == "animate"      => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "animateColor" => mei.allanystart.parser.AnimateColorParser.parse(n)
        case n: Elem if n.label == "desc"         => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "metadata"     => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "set"          => mei.allanystart.parser.SetParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
      }.toList,
    )
  }
}
object TextPathParser extends MeiModelDummyParser {
  def parse(node: Node): TextPath = {
    TextPath(
      node.child.collect {
        case n: Elem if n.label == "a"            => mei.allanystart.parser.AParser.parse(n)
        case n: Elem if n.label == "altGlyph"     => mei.allanystart.parser.AltGlyphParser.parse(n)
        case n: Elem if n.label == "animate"      => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "animateColor" => mei.allanystart.parser.AnimateColorParser.parse(n)
        case n: Elem if n.label == "desc"         => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "metadata"     => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "set"          => mei.allanystart.parser.SetParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "tref"         => mei.allanystart.parser.TrefParser.parse(n)
        case n: Elem if n.label == "tspan"        => mei.allanystart.parser.TspanParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
    )
  }
}
object AltGlyphParser extends MeiModelDummyParser {
  def parse(node: Node): AltGlyph = {
    AltGlyph(
      node.child.collect {
        case n if n.text.trim.nonEmpty => n.text
      }.toList,
    )
  }
}
object AltGlyphDefParser extends MeiModelDummyParser {
  def parse(node: Node): AltGlyphDef = {
    AltGlyphDef(
      node.child.collect {
        case n: Elem if n.label == "altGlyphItem" => mei.allanystart.parser.AltGlyphItemParser.parse(n)
        case n: Elem if n.label == "glyphRef"     => mei.allanystart.parser.GlyphRefParser.parse(n)
      }.toList,
    )
  }
}
object AltGlyphItemParser extends MeiModelDummyParser {
  def parse(node: Node): AltGlyphItem = {
    AltGlyphItem(
      node.child.collect {
        case n: Elem if n.label == "glyphRef" => mei.allanystart.parser.GlyphRefParser.parse(n)
      }.toList,
    )
  }
}
object GlyphRefParser extends MeiModelDummyParser {
  def parse(node: Node): GlyphRef = {
    GlyphRef(
    )
  }
}
object MarkerParser extends MeiModelDummyParser {
  def parse(node: Node): Marker = {
    Marker(
      node.child.collect {
        case n: Elem if n.label == "a"                => mei.allanystart.parser.AParser.parse(n)
        case n: Elem if n.label == "altGlyphDef"      => mei.allanystart.parser.AltGlyphDefParser.parse(n)
        case n: Elem if n.label == "animate"          => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "animateColor"     => mei.allanystart.parser.AnimateColorParser.parse(n)
        case n: Elem if n.label == "animateMotion"    => mei.allanystart.parser.AnimateMotionParser.parse(n)
        case n: Elem if n.label == "animateTransform" => mei.allanystart.parser.AnimateTransformParser.parse(n)
        case n: Elem if n.label == "circle"           => mei.allanystart.parser.CircleParser.parse(n)
        case n: Elem if n.label == "clipPath"         => mei.allanystart.parser.ClipPathParser.parse(n)
        case n: Elem if n.label == "color-profile"    => mei.allanystart.parser.ColorProfileParser.parse(n)
        case n: Elem if n.label == "cursor"           => mei.allanystart.parser.CursorParser.parse(n)
        case n: Elem if n.label == "defs"             => mei.allanystart.parser.DefsParser.parse(n)
        case n: Elem if n.label == "desc"             => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "ellipse"          => mei.allanystart.parser.EllipseParser.parse(n)
        case n: Elem if n.label == "filter"           => mei.allanystart.parser.FilterParser.parse(n)
        case n: Elem if n.label == "font"             => mei.allanystart.parser.FontParser.parse(n)
        case n: Elem if n.label == "font-face"        => mei.allanystart.parser.FontFaceParser.parse(n)
        case n: Elem if n.label == "g"                => mei.allanystart.parser.GParser.parse(n)
        case n: Elem if n.label == "image"            => mei.allanystart.parser.ImageParser.parse(n)
        case n: Elem if n.label == "line"             => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "linearGradient"   => mei.allanystart.parser.LinearGradientParser.parse(n)
        case n: Elem if n.label == "marker"           => mei.allanystart.parser.MarkerParser.parse(n)
        case n: Elem if n.label == "mask"             => mei.allanystart.parser.MaskParser.parse(n)
        case n: Elem if n.label == "metadata"         => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "path"             => mei.allanystart.parser.PathParser.parse(n)
        case n: Elem if n.label == "pattern"          => mei.allanystart.parser.PatternParser.parse(n)
        case n: Elem if n.label == "polygon"          => mei.allanystart.parser.PolygonParser.parse(n)
        case n: Elem if n.label == "polyline"         => mei.allanystart.parser.PolylineParser.parse(n)
        case n: Elem if n.label == "radialGradient"   => mei.allanystart.parser.RadialGradientParser.parse(n)
        case n: Elem if n.label == "rect"             => mei.allanystart.parser.RectParser.parse(n)
        case n: Elem if n.label == "script"           => mei.allanystart.parser.ScriptParser.parse(n)
        case n: Elem if n.label == "set"              => mei.allanystart.parser.SetParser.parse(n)
        case n: Elem if n.label == "style"            => mei.allanystart.parser.StyleParser.parse(n)
        case n: Elem if n.label == "svg"              => mei.allanystart.parser.SvgParser.parse(n)
        case n: Elem if n.label == "switch"           => mei.allanystart.parser.SwitchParser.parse(n)
        case n: Elem if n.label == "symbol"           => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "text"             => mei.allanystart.parser.TextParser.parse(n)
        case n: Elem if n.label == "title"            => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "use"              => mei.allanystart.parser.UseParser.parse(n)
        case n: Elem if n.label == "view"             => mei.allanystart.parser.ViewParser.parse(n)
      }.toList,
    )
  }
}
object ColorProfileParser extends MeiModelDummyParser {
  def parse(node: Node): ColorProfile = {
    ColorProfile(
      node.child.collect {
        case n: Elem if n.label == "desc"     => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "metadata" => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "title"    => mei.allanystart.parser.TitleParser.parse(n)
      }.toList,
    )
  }
}
object LinearGradientParser extends MeiModelDummyParser {
  def parse(node: Node): LinearGradient = {
    LinearGradient(
      node.child.collect {
        case n: Elem if n.label == "animate"          => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "animateTransform" => mei.allanystart.parser.AnimateTransformParser.parse(n)
        case n: Elem if n.label == "desc"             => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "metadata"         => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "set"              => mei.allanystart.parser.SetParser.parse(n)
        case n: Elem if n.label == "stop"             => mei.allanystart.parser.StopParser.parse(n)
        case n: Elem if n.label == "title"            => mei.allanystart.parser.TitleParser.parse(n)
      }.toList,
    )
  }
}
object RadialGradientParser extends MeiModelDummyParser {
  def parse(node: Node): RadialGradient = {
    RadialGradient(
      node.child.collect {
        case n: Elem if n.label == "animate"          => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "animateTransform" => mei.allanystart.parser.AnimateTransformParser.parse(n)
        case n: Elem if n.label == "desc"             => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "metadata"         => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "set"              => mei.allanystart.parser.SetParser.parse(n)
        case n: Elem if n.label == "stop"             => mei.allanystart.parser.StopParser.parse(n)
        case n: Elem if n.label == "title"            => mei.allanystart.parser.TitleParser.parse(n)
      }.toList,
    )
  }
}
object StopParser extends MeiModelDummyParser {
  def parse(node: Node): Stop = {
    Stop(
      node.child.collect {
        case n: Elem if n.label == "animate"      => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "animateColor" => mei.allanystart.parser.AnimateColorParser.parse(n)
        case n: Elem if n.label == "set"          => mei.allanystart.parser.SetParser.parse(n)
      }.toList,
    )
  }
}
object PatternParser extends MeiModelDummyParser {
  def parse(node: Node): Pattern = {
    Pattern(
      node.child.collect {
        case n: Elem if n.label == "a"                => mei.allanystart.parser.AParser.parse(n)
        case n: Elem if n.label == "altGlyphDef"      => mei.allanystart.parser.AltGlyphDefParser.parse(n)
        case n: Elem if n.label == "animate"          => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "animateColor"     => mei.allanystart.parser.AnimateColorParser.parse(n)
        case n: Elem if n.label == "animateMotion"    => mei.allanystart.parser.AnimateMotionParser.parse(n)
        case n: Elem if n.label == "animateTransform" => mei.allanystart.parser.AnimateTransformParser.parse(n)
        case n: Elem if n.label == "circle"           => mei.allanystart.parser.CircleParser.parse(n)
        case n: Elem if n.label == "clipPath"         => mei.allanystart.parser.ClipPathParser.parse(n)
        case n: Elem if n.label == "color-profile"    => mei.allanystart.parser.ColorProfileParser.parse(n)
        case n: Elem if n.label == "cursor"           => mei.allanystart.parser.CursorParser.parse(n)
        case n: Elem if n.label == "defs"             => mei.allanystart.parser.DefsParser.parse(n)
        case n: Elem if n.label == "desc"             => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "ellipse"          => mei.allanystart.parser.EllipseParser.parse(n)
        case n: Elem if n.label == "filter"           => mei.allanystart.parser.FilterParser.parse(n)
        case n: Elem if n.label == "font"             => mei.allanystart.parser.FontParser.parse(n)
        case n: Elem if n.label == "font-face"        => mei.allanystart.parser.FontFaceParser.parse(n)
        case n: Elem if n.label == "g"                => mei.allanystart.parser.GParser.parse(n)
        case n: Elem if n.label == "image"            => mei.allanystart.parser.ImageParser.parse(n)
        case n: Elem if n.label == "line"             => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "linearGradient"   => mei.allanystart.parser.LinearGradientParser.parse(n)
        case n: Elem if n.label == "marker"           => mei.allanystart.parser.MarkerParser.parse(n)
        case n: Elem if n.label == "mask"             => mei.allanystart.parser.MaskParser.parse(n)
        case n: Elem if n.label == "metadata"         => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "path"             => mei.allanystart.parser.PathParser.parse(n)
        case n: Elem if n.label == "pattern"          => mei.allanystart.parser.PatternParser.parse(n)
        case n: Elem if n.label == "polygon"          => mei.allanystart.parser.PolygonParser.parse(n)
        case n: Elem if n.label == "polyline"         => mei.allanystart.parser.PolylineParser.parse(n)
        case n: Elem if n.label == "radialGradient"   => mei.allanystart.parser.RadialGradientParser.parse(n)
        case n: Elem if n.label == "rect"             => mei.allanystart.parser.RectParser.parse(n)
        case n: Elem if n.label == "script"           => mei.allanystart.parser.ScriptParser.parse(n)
        case n: Elem if n.label == "set"              => mei.allanystart.parser.SetParser.parse(n)
        case n: Elem if n.label == "style"            => mei.allanystart.parser.StyleParser.parse(n)
        case n: Elem if n.label == "svg"              => mei.allanystart.parser.SvgParser.parse(n)
        case n: Elem if n.label == "switch"           => mei.allanystart.parser.SwitchParser.parse(n)
        case n: Elem if n.label == "symbol"           => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "text"             => mei.allanystart.parser.TextParser.parse(n)
        case n: Elem if n.label == "title"            => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "use"              => mei.allanystart.parser.UseParser.parse(n)
        case n: Elem if n.label == "view"             => mei.allanystart.parser.ViewParser.parse(n)
      }.toList,
    )
  }
}
object ClipPathParser extends MeiModelDummyParser {
  def parse(node: Node): ClipPath = {
    ClipPath(
      node.child.collect {
        case n: Elem if n.label == "altGlyphDef"      => mei.allanystart.parser.AltGlyphDefParser.parse(n)
        case n: Elem if n.label == "animate"          => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "animateColor"     => mei.allanystart.parser.AnimateColorParser.parse(n)
        case n: Elem if n.label == "animateMotion"    => mei.allanystart.parser.AnimateMotionParser.parse(n)
        case n: Elem if n.label == "animateTransform" => mei.allanystart.parser.AnimateTransformParser.parse(n)
        case n: Elem if n.label == "circle"           => mei.allanystart.parser.CircleParser.parse(n)
        case n: Elem if n.label == "desc"             => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "ellipse"          => mei.allanystart.parser.EllipseParser.parse(n)
        case n: Elem if n.label == "line"             => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "metadata"         => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "path"             => mei.allanystart.parser.PathParser.parse(n)
        case n: Elem if n.label == "polygon"          => mei.allanystart.parser.PolygonParser.parse(n)
        case n: Elem if n.label == "polyline"         => mei.allanystart.parser.PolylineParser.parse(n)
        case n: Elem if n.label == "rect"             => mei.allanystart.parser.RectParser.parse(n)
        case n: Elem if n.label == "set"              => mei.allanystart.parser.SetParser.parse(n)
        case n: Elem if n.label == "text"             => mei.allanystart.parser.TextParser.parse(n)
        case n: Elem if n.label == "title"            => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "use"              => mei.allanystart.parser.UseParser.parse(n)
      }.toList,
    )
  }
}
object MaskParser extends MeiModelDummyParser {
  def parse(node: Node): Mask = {
    Mask(
      node.child.collect {
        case n: Elem if n.label == "a"                => mei.allanystart.parser.AParser.parse(n)
        case n: Elem if n.label == "altGlyphDef"      => mei.allanystart.parser.AltGlyphDefParser.parse(n)
        case n: Elem if n.label == "animate"          => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "animateColor"     => mei.allanystart.parser.AnimateColorParser.parse(n)
        case n: Elem if n.label == "animateMotion"    => mei.allanystart.parser.AnimateMotionParser.parse(n)
        case n: Elem if n.label == "animateTransform" => mei.allanystart.parser.AnimateTransformParser.parse(n)
        case n: Elem if n.label == "circle"           => mei.allanystart.parser.CircleParser.parse(n)
        case n: Elem if n.label == "clipPath"         => mei.allanystart.parser.ClipPathParser.parse(n)
        case n: Elem if n.label == "color-profile"    => mei.allanystart.parser.ColorProfileParser.parse(n)
        case n: Elem if n.label == "cursor"           => mei.allanystart.parser.CursorParser.parse(n)
        case n: Elem if n.label == "defs"             => mei.allanystart.parser.DefsParser.parse(n)
        case n: Elem if n.label == "desc"             => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "ellipse"          => mei.allanystart.parser.EllipseParser.parse(n)
        case n: Elem if n.label == "filter"           => mei.allanystart.parser.FilterParser.parse(n)
        case n: Elem if n.label == "font"             => mei.allanystart.parser.FontParser.parse(n)
        case n: Elem if n.label == "font-face"        => mei.allanystart.parser.FontFaceParser.parse(n)
        case n: Elem if n.label == "g"                => mei.allanystart.parser.GParser.parse(n)
        case n: Elem if n.label == "image"            => mei.allanystart.parser.ImageParser.parse(n)
        case n: Elem if n.label == "line"             => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "linearGradient"   => mei.allanystart.parser.LinearGradientParser.parse(n)
        case n: Elem if n.label == "marker"           => mei.allanystart.parser.MarkerParser.parse(n)
        case n: Elem if n.label == "mask"             => mei.allanystart.parser.MaskParser.parse(n)
        case n: Elem if n.label == "metadata"         => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "path"             => mei.allanystart.parser.PathParser.parse(n)
        case n: Elem if n.label == "pattern"          => mei.allanystart.parser.PatternParser.parse(n)
        case n: Elem if n.label == "polygon"          => mei.allanystart.parser.PolygonParser.parse(n)
        case n: Elem if n.label == "polyline"         => mei.allanystart.parser.PolylineParser.parse(n)
        case n: Elem if n.label == "radialGradient"   => mei.allanystart.parser.RadialGradientParser.parse(n)
        case n: Elem if n.label == "rect"             => mei.allanystart.parser.RectParser.parse(n)
        case n: Elem if n.label == "script"           => mei.allanystart.parser.ScriptParser.parse(n)
        case n: Elem if n.label == "set"              => mei.allanystart.parser.SetParser.parse(n)
        case n: Elem if n.label == "style"            => mei.allanystart.parser.StyleParser.parse(n)
        case n: Elem if n.label == "svg"              => mei.allanystart.parser.SvgParser.parse(n)
        case n: Elem if n.label == "switch"           => mei.allanystart.parser.SwitchParser.parse(n)
        case n: Elem if n.label == "symbol"           => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "text"             => mei.allanystart.parser.TextParser.parse(n)
        case n: Elem if n.label == "title"            => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "use"              => mei.allanystart.parser.UseParser.parse(n)
        case n: Elem if n.label == "view"             => mei.allanystart.parser.ViewParser.parse(n)
      }.toList,
    )
  }
}
object FilterParser extends MeiModelDummyParser {
  def parse(node: Node): Filter = {
    Filter(
      node.child.collect {
        case n: Elem if n.label == "animate"             => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "desc"                => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "feBlend"             => mei.allanystart.parser.FeBlendParser.parse(n)
        case n: Elem if n.label == "feColorMatrix"       => mei.allanystart.parser.FeColorMatrixParser.parse(n)
        case n: Elem if n.label == "feComponentTransfer" => mei.allanystart.parser.FeComponentTransferParser.parse(n)
        case n: Elem if n.label == "feComposite"         => mei.allanystart.parser.FeCompositeParser.parse(n)
        case n: Elem if n.label == "feConvolveMatrix"    => mei.allanystart.parser.FeConvolveMatrixParser.parse(n)
        case n: Elem if n.label == "feDiffuseLighting"   => mei.allanystart.parser.FeDiffuseLightingParser.parse(n)
        case n: Elem if n.label == "feDisplacementMap"   => mei.allanystart.parser.FeDisplacementMapParser.parse(n)
        case n: Elem if n.label == "feFlood"             => mei.allanystart.parser.FeFloodParser.parse(n)
        case n: Elem if n.label == "feGaussianBlur"      => mei.allanystart.parser.FeGaussianBlurParser.parse(n)
        case n: Elem if n.label == "feImage"             => mei.allanystart.parser.FeImageParser.parse(n)
        case n: Elem if n.label == "feMerge"             => mei.allanystart.parser.FeMergeParser.parse(n)
        case n: Elem if n.label == "feMorphology"        => mei.allanystart.parser.FeMorphologyParser.parse(n)
        case n: Elem if n.label == "feOffset"            => mei.allanystart.parser.FeOffsetParser.parse(n)
        case n: Elem if n.label == "feSpecularLighting"  => mei.allanystart.parser.FeSpecularLightingParser.parse(n)
        case n: Elem if n.label == "feTile"              => mei.allanystart.parser.FeTileParser.parse(n)
        case n: Elem if n.label == "feTurbulence"        => mei.allanystart.parser.FeTurbulenceParser.parse(n)
        case n: Elem if n.label == "metadata"            => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "set"                 => mei.allanystart.parser.SetParser.parse(n)
        case n: Elem if n.label == "title"               => mei.allanystart.parser.TitleParser.parse(n)
      }.toList,
    )
  }
}
object FeBlendParser extends MeiModelDummyParser {
  def parse(node: Node): FeBlend = {
    FeBlend(
      node.child.collect {
        case n: Elem if n.label == "animate" => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "set"     => mei.allanystart.parser.SetParser.parse(n)
      }.toList,
    )
  }
}
object FeColorMatrixParser extends MeiModelDummyParser {
  def parse(node: Node): FeColorMatrix = {
    FeColorMatrix(
      node.child.collect {
        case n: Elem if n.label == "animate" => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "set"     => mei.allanystart.parser.SetParser.parse(n)
      }.toList,
    )
  }
}
object FeComponentTransferParser extends MeiModelDummyParser {
  def parse(node: Node): FeComponentTransfer = {
    FeComponentTransfer(
      node.child.collect {
        case n: Elem if n.label == "feFuncA" => mei.allanystart.parser.FeFuncAParser.parse(n)
        case n: Elem if n.label == "feFuncB" => mei.allanystart.parser.FeFuncBParser.parse(n)
        case n: Elem if n.label == "feFuncG" => mei.allanystart.parser.FeFuncGParser.parse(n)
        case n: Elem if n.label == "feFuncR" => mei.allanystart.parser.FeFuncRParser.parse(n)
      }.toList,
    )
  }
}
object FeCompositeParser extends MeiModelDummyParser {
  def parse(node: Node): FeComposite = {
    FeComposite(
      node.child.collect {
        case n: Elem if n.label == "animate" => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "set"     => mei.allanystart.parser.SetParser.parse(n)
      }.toList,
    )
  }
}
object FeConvolveMatrixParser extends MeiModelDummyParser {
  def parse(node: Node): FeConvolveMatrix = {
    FeConvolveMatrix(
      node.child.collect {
        case n: Elem if n.label == "animate" => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "set"     => mei.allanystart.parser.SetParser.parse(n)
      }.toList,
    )
  }
}
object FeDiffuseLightingParser extends MeiModelDummyParser {
  def parse(node: Node): FeDiffuseLighting = {
    FeDiffuseLighting(
      node.child.collect {
        case n: Elem if n.label == "animate"        => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "animateColor"   => mei.allanystart.parser.AnimateColorParser.parse(n)
        case n: Elem if n.label == "feDistantLight" => mei.allanystart.parser.FeDistantLightParser.parse(n)
        case n: Elem if n.label == "fePointLight"   => mei.allanystart.parser.FePointLightParser.parse(n)
        case n: Elem if n.label == "feSpotLight"    => mei.allanystart.parser.FeSpotLightParser.parse(n)
        case n: Elem if n.label == "set"            => mei.allanystart.parser.SetParser.parse(n)
      }.toList,
    )
  }
}
object FeDisplacementMapParser extends MeiModelDummyParser {
  def parse(node: Node): FeDisplacementMap = {
    FeDisplacementMap(
      node.child.collect {
        case n: Elem if n.label == "animate" => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "set"     => mei.allanystart.parser.SetParser.parse(n)
      }.toList,
    )
  }
}
object FeFloodParser extends MeiModelDummyParser {
  def parse(node: Node): FeFlood = {
    FeFlood(
      node.child.collect {
        case n: Elem if n.label == "animate"      => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "animateColor" => mei.allanystart.parser.AnimateColorParser.parse(n)
        case n: Elem if n.label == "set"          => mei.allanystart.parser.SetParser.parse(n)
      }.toList,
    )
  }
}
object FeGaussianBlurParser extends MeiModelDummyParser {
  def parse(node: Node): FeGaussianBlur = {
    FeGaussianBlur(
      node.child.collect {
        case n: Elem if n.label == "animate" => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "set"     => mei.allanystart.parser.SetParser.parse(n)
      }.toList,
    )
  }
}
object FeImageParser extends MeiModelDummyParser {
  def parse(node: Node): FeImage = {
    FeImage(
      node.child.collect {
        case n: Elem if n.label == "animate"          => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "animateTransform" => mei.allanystart.parser.AnimateTransformParser.parse(n)
        case n: Elem if n.label == "set"              => mei.allanystart.parser.SetParser.parse(n)
      }.toList,
    )
  }
}
object FeMergeParser extends MeiModelDummyParser {
  def parse(node: Node): FeMerge = {
    FeMerge(
      node.child.collect {
        case n: Elem if n.label == "feMergeNode" => mei.allanystart.parser.FeMergeNodeParser.parse(n)
      }.toList,
    )
  }
}
object FeMergeNodeParser extends MeiModelDummyParser {
  def parse(node: Node): FeMergeNode = {
    FeMergeNode(
      node.child.collect {
        case n: Elem if n.label == "animate" => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "set"     => mei.allanystart.parser.SetParser.parse(n)
      }.toList,
    )
  }
}
object FeMorphologyParser extends MeiModelDummyParser {
  def parse(node: Node): FeMorphology = {
    FeMorphology(
      node.child.collect {
        case n: Elem if n.label == "animate" => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "set"     => mei.allanystart.parser.SetParser.parse(n)
      }.toList,
    )
  }
}
object FeOffsetParser extends MeiModelDummyParser {
  def parse(node: Node): FeOffset = {
    FeOffset(
      node.child.collect {
        case n: Elem if n.label == "animate" => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "set"     => mei.allanystart.parser.SetParser.parse(n)
      }.toList,
    )
  }
}
object FeSpecularLightingParser extends MeiModelDummyParser {
  def parse(node: Node): FeSpecularLighting = {
    FeSpecularLighting(
      node.child.collect {
        case n: Elem if n.label == "animate"        => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "animateColor"   => mei.allanystart.parser.AnimateColorParser.parse(n)
        case n: Elem if n.label == "feDistantLight" => mei.allanystart.parser.FeDistantLightParser.parse(n)
        case n: Elem if n.label == "fePointLight"   => mei.allanystart.parser.FePointLightParser.parse(n)
        case n: Elem if n.label == "feSpotLight"    => mei.allanystart.parser.FeSpotLightParser.parse(n)
        case n: Elem if n.label == "set"            => mei.allanystart.parser.SetParser.parse(n)
      }.toList,
    )
  }
}
object FeTileParser extends MeiModelDummyParser {
  def parse(node: Node): FeTile = {
    FeTile(
      node.child.collect {
        case n: Elem if n.label == "animate" => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "set"     => mei.allanystart.parser.SetParser.parse(n)
      }.toList,
    )
  }
}
object FeTurbulenceParser extends MeiModelDummyParser {
  def parse(node: Node): FeTurbulence = {
    FeTurbulence(
      node.child.collect {
        case n: Elem if n.label == "animate" => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "set"     => mei.allanystart.parser.SetParser.parse(n)
      }.toList,
    )
  }
}
object FeDistantLightParser extends MeiModelDummyParser {
  def parse(node: Node): FeDistantLight = {
    FeDistantLight(
      node.child.collect {
        case n: Elem if n.label == "animate" => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "set"     => mei.allanystart.parser.SetParser.parse(n)
      }.toList,
    )
  }
}
object FePointLightParser extends MeiModelDummyParser {
  def parse(node: Node): FePointLight = {
    FePointLight(
      node.child.collect {
        case n: Elem if n.label == "animate" => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "set"     => mei.allanystart.parser.SetParser.parse(n)
      }.toList,
    )
  }
}
object FeSpotLightParser extends MeiModelDummyParser {
  def parse(node: Node): FeSpotLight = {
    FeSpotLight(
      node.child.collect {
        case n: Elem if n.label == "animate" => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "set"     => mei.allanystart.parser.SetParser.parse(n)
      }.toList,
    )
  }
}
object FeFuncRParser extends MeiModelDummyParser {
  def parse(node: Node): FeFuncR = {
    FeFuncR(
      node.child.collect {
        case n: Elem if n.label == "animate" => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "set"     => mei.allanystart.parser.SetParser.parse(n)
      }.toList,
    )
  }
}
object FeFuncGParser extends MeiModelDummyParser {
  def parse(node: Node): FeFuncG = {
    FeFuncG(
      node.child.collect {
        case n: Elem if n.label == "animate" => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "set"     => mei.allanystart.parser.SetParser.parse(n)
      }.toList,
    )
  }
}
object FeFuncBParser extends MeiModelDummyParser {
  def parse(node: Node): FeFuncB = {
    FeFuncB(
      node.child.collect {
        case n: Elem if n.label == "animate" => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "set"     => mei.allanystart.parser.SetParser.parse(n)
      }.toList,
    )
  }
}
object FeFuncAParser extends MeiModelDummyParser {
  def parse(node: Node): FeFuncA = {
    FeFuncA(
      node.child.collect {
        case n: Elem if n.label == "animate" => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "set"     => mei.allanystart.parser.SetParser.parse(n)
      }.toList,
    )
  }
}
object CursorParser extends MeiModelDummyParser {
  def parse(node: Node): Cursor = {
    Cursor(
      node.child.collect {
        case n: Elem if n.label == "desc"     => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "metadata" => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "title"    => mei.allanystart.parser.TitleParser.parse(n)
      }.toList,
    )
  }
}
object AParser extends MeiModelDummyParser {
  def parse(node: Node): A = {
    A(
      node.child.collect {
        case n: Elem if n.label == "a"                => mei.allanystart.parser.AParser.parse(n)
        case n: Elem if n.label == "altGlyphDef"      => mei.allanystart.parser.AltGlyphDefParser.parse(n)
        case n: Elem if n.label == "animate"          => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "animateColor"     => mei.allanystart.parser.AnimateColorParser.parse(n)
        case n: Elem if n.label == "animateMotion"    => mei.allanystart.parser.AnimateMotionParser.parse(n)
        case n: Elem if n.label == "animateTransform" => mei.allanystart.parser.AnimateTransformParser.parse(n)
        case n: Elem if n.label == "circle"           => mei.allanystart.parser.CircleParser.parse(n)
        case n: Elem if n.label == "clipPath"         => mei.allanystart.parser.ClipPathParser.parse(n)
        case n: Elem if n.label == "color-profile"    => mei.allanystart.parser.ColorProfileParser.parse(n)
        case n: Elem if n.label == "cursor"           => mei.allanystart.parser.CursorParser.parse(n)
        case n: Elem if n.label == "defs"             => mei.allanystart.parser.DefsParser.parse(n)
        case n: Elem if n.label == "desc"             => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "ellipse"          => mei.allanystart.parser.EllipseParser.parse(n)
        case n: Elem if n.label == "filter"           => mei.allanystart.parser.FilterParser.parse(n)
        case n: Elem if n.label == "font"             => mei.allanystart.parser.FontParser.parse(n)
        case n: Elem if n.label == "font-face"        => mei.allanystart.parser.FontFaceParser.parse(n)
        case n: Elem if n.label == "g"                => mei.allanystart.parser.GParser.parse(n)
        case n: Elem if n.label == "image"            => mei.allanystart.parser.ImageParser.parse(n)
        case n: Elem if n.label == "line"             => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "linearGradient"   => mei.allanystart.parser.LinearGradientParser.parse(n)
        case n: Elem if n.label == "marker"           => mei.allanystart.parser.MarkerParser.parse(n)
        case n: Elem if n.label == "mask"             => mei.allanystart.parser.MaskParser.parse(n)
        case n: Elem if n.label == "metadata"         => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "path"             => mei.allanystart.parser.PathParser.parse(n)
        case n: Elem if n.label == "pattern"          => mei.allanystart.parser.PatternParser.parse(n)
        case n: Elem if n.label == "polygon"          => mei.allanystart.parser.PolygonParser.parse(n)
        case n: Elem if n.label == "polyline"         => mei.allanystart.parser.PolylineParser.parse(n)
        case n: Elem if n.label == "radialGradient"   => mei.allanystart.parser.RadialGradientParser.parse(n)
        case n: Elem if n.label == "rect"             => mei.allanystart.parser.RectParser.parse(n)
        case n: Elem if n.label == "script"           => mei.allanystart.parser.ScriptParser.parse(n)
        case n: Elem if n.label == "set"              => mei.allanystart.parser.SetParser.parse(n)
        case n: Elem if n.label == "style"            => mei.allanystart.parser.StyleParser.parse(n)
        case n: Elem if n.label == "svg"              => mei.allanystart.parser.SvgParser.parse(n)
        case n: Elem if n.label == "switch"           => mei.allanystart.parser.SwitchParser.parse(n)
        case n: Elem if n.label == "symbol"           => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "text"             => mei.allanystart.parser.TextParser.parse(n)
        case n: Elem if n.label == "title"            => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "use"              => mei.allanystart.parser.UseParser.parse(n)
        case n: Elem if n.label == "view"             => mei.allanystart.parser.ViewParser.parse(n)
        case n if n.text.trim.nonEmpty                => n.text
      }.toList,
    )
  }
}
object ViewParser extends MeiModelDummyParser {
  def parse(node: Node): View = {
    View(
      node.child.collect {
        case n: Elem if n.label == "desc"     => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "metadata" => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "title"    => mei.allanystart.parser.TitleParser.parse(n)
      }.toList,
    )
  }
}
object ScriptParser extends MeiModelDummyParser {
  def parse(node: Node): Script = {
    Script(
      node.child.collect {
        case n if n.text.trim.nonEmpty => n.text
      }.toList,
    )
  }
}
object AnimateParser extends MeiModelDummyParser {
  def parse(node: Node): Animate = {
    Animate(
      node.child.collect {
        case n: Elem if n.label == "desc"     => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "metadata" => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "title"    => mei.allanystart.parser.TitleParser.parse(n)
      }.toList,
    )
  }
}
object SetParser extends MeiModelDummyParser {
  def parse(node: Node): Set = {
    Set(
      node.child.collect {
        case n: Elem if n.label == "desc"     => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "metadata" => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "title"    => mei.allanystart.parser.TitleParser.parse(n)
      }.toList,
    )
  }
}
object AnimateMotionParser extends MeiModelDummyParser {
  def parse(node: Node): AnimateMotion = {
    AnimateMotion(
      node.child.collect {
        case n: Elem if n.label == "desc"     => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "metadata" => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "mpath"    => mei.allanystart.parser.MpathParser.parse(n)
        case n: Elem if n.label == "title"    => mei.allanystart.parser.TitleParser.parse(n)
      }.toList,
    )
  }
}
object AnimateColorParser extends MeiModelDummyParser {
  def parse(node: Node): AnimateColor = {
    AnimateColor(
      node.child.collect {
        case n: Elem if n.label == "desc"     => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "metadata" => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "title"    => mei.allanystart.parser.TitleParser.parse(n)
      }.toList,
    )
  }
}
object AnimateTransformParser extends MeiModelDummyParser {
  def parse(node: Node): AnimateTransform = {
    AnimateTransform(
      node.child.collect {
        case n: Elem if n.label == "desc"     => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "metadata" => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "title"    => mei.allanystart.parser.TitleParser.parse(n)
      }.toList,
    )
  }
}
object MpathParser extends MeiModelDummyParser {
  def parse(node: Node): Mpath = {
    Mpath(
      node.child.collect {
        case n: Elem if n.label == "desc"     => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "metadata" => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "title"    => mei.allanystart.parser.TitleParser.parse(n)
      }.toList,
    )
  }
}
object FontParser extends MeiModelDummyParser {
  def parse(node: Node): Font = {
    Font(
      node.child.collect {
        case n: Elem if n.label == "desc"          => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "font-face"     => mei.allanystart.parser.FontFaceParser.parse(n)
        case n: Elem if n.label == "glyph"         => mei.allanystart.parser.GlyphParser.parse(n)
        case n: Elem if n.label == "hkern"         => mei.allanystart.parser.HkernParser.parse(n)
        case n: Elem if n.label == "metadata"      => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "missing-glyph" => mei.allanystart.parser.MissingGlyphParser.parse(n)
        case n: Elem if n.label == "title"         => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "vkern"         => mei.allanystart.parser.VkernParser.parse(n)
      }.toList,
    )
  }
}
object FontFaceParser extends MeiModelDummyParser {
  def parse(node: Node): FontFace = {
    FontFace(
      node.child.collect {
        case n: Elem if n.label == "definition-src" => mei.allanystart.parser.DefinitionSrcParser.parse(n)
        case n: Elem if n.label == "desc"           => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "font-face-src"  => mei.allanystart.parser.FontFaceSrcParser.parse(n)
        case n: Elem if n.label == "metadata"       => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "title"          => mei.allanystart.parser.TitleParser.parse(n)
      }.toList,
    )
  }
}
object GlyphParser extends MeiModelDummyParser {
  def parse(node: Node): Glyph = {
    Glyph(
      node.child.collect {
        case n: Elem if n.label == "a"                => mei.allanystart.parser.AParser.parse(n)
        case n: Elem if n.label == "altGlyphDef"      => mei.allanystart.parser.AltGlyphDefParser.parse(n)
        case n: Elem if n.label == "animate"          => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "animateColor"     => mei.allanystart.parser.AnimateColorParser.parse(n)
        case n: Elem if n.label == "animateMotion"    => mei.allanystart.parser.AnimateMotionParser.parse(n)
        case n: Elem if n.label == "animateTransform" => mei.allanystart.parser.AnimateTransformParser.parse(n)
        case n: Elem if n.label == "circle"           => mei.allanystart.parser.CircleParser.parse(n)
        case n: Elem if n.label == "clipPath"         => mei.allanystart.parser.ClipPathParser.parse(n)
        case n: Elem if n.label == "color-profile"    => mei.allanystart.parser.ColorProfileParser.parse(n)
        case n: Elem if n.label == "cursor"           => mei.allanystart.parser.CursorParser.parse(n)
        case n: Elem if n.label == "defs"             => mei.allanystart.parser.DefsParser.parse(n)
        case n: Elem if n.label == "desc"             => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "ellipse"          => mei.allanystart.parser.EllipseParser.parse(n)
        case n: Elem if n.label == "filter"           => mei.allanystart.parser.FilterParser.parse(n)
        case n: Elem if n.label == "font"             => mei.allanystart.parser.FontParser.parse(n)
        case n: Elem if n.label == "font-face"        => mei.allanystart.parser.FontFaceParser.parse(n)
        case n: Elem if n.label == "g"                => mei.allanystart.parser.GParser.parse(n)
        case n: Elem if n.label == "image"            => mei.allanystart.parser.ImageParser.parse(n)
        case n: Elem if n.label == "line"             => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "linearGradient"   => mei.allanystart.parser.LinearGradientParser.parse(n)
        case n: Elem if n.label == "marker"           => mei.allanystart.parser.MarkerParser.parse(n)
        case n: Elem if n.label == "mask"             => mei.allanystart.parser.MaskParser.parse(n)
        case n: Elem if n.label == "metadata"         => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "path"             => mei.allanystart.parser.PathParser.parse(n)
        case n: Elem if n.label == "pattern"          => mei.allanystart.parser.PatternParser.parse(n)
        case n: Elem if n.label == "polygon"          => mei.allanystart.parser.PolygonParser.parse(n)
        case n: Elem if n.label == "polyline"         => mei.allanystart.parser.PolylineParser.parse(n)
        case n: Elem if n.label == "radialGradient"   => mei.allanystart.parser.RadialGradientParser.parse(n)
        case n: Elem if n.label == "rect"             => mei.allanystart.parser.RectParser.parse(n)
        case n: Elem if n.label == "script"           => mei.allanystart.parser.ScriptParser.parse(n)
        case n: Elem if n.label == "set"              => mei.allanystart.parser.SetParser.parse(n)
        case n: Elem if n.label == "style"            => mei.allanystart.parser.StyleParser.parse(n)
        case n: Elem if n.label == "svg"              => mei.allanystart.parser.SvgParser.parse(n)
        case n: Elem if n.label == "switch"           => mei.allanystart.parser.SwitchParser.parse(n)
        case n: Elem if n.label == "symbol"           => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "text"             => mei.allanystart.parser.TextParser.parse(n)
        case n: Elem if n.label == "title"            => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "use"              => mei.allanystart.parser.UseParser.parse(n)
        case n: Elem if n.label == "view"             => mei.allanystart.parser.ViewParser.parse(n)
      }.toList,
    )
  }
}
object MissingGlyphParser extends MeiModelDummyParser {
  def parse(node: Node): MissingGlyph = {
    MissingGlyph(
      node.child.collect {
        case n: Elem if n.label == "a"                => mei.allanystart.parser.AParser.parse(n)
        case n: Elem if n.label == "altGlyphDef"      => mei.allanystart.parser.AltGlyphDefParser.parse(n)
        case n: Elem if n.label == "animate"          => mei.allanystart.parser.AnimateParser.parse(n)
        case n: Elem if n.label == "animateColor"     => mei.allanystart.parser.AnimateColorParser.parse(n)
        case n: Elem if n.label == "animateMotion"    => mei.allanystart.parser.AnimateMotionParser.parse(n)
        case n: Elem if n.label == "animateTransform" => mei.allanystart.parser.AnimateTransformParser.parse(n)
        case n: Elem if n.label == "circle"           => mei.allanystart.parser.CircleParser.parse(n)
        case n: Elem if n.label == "clipPath"         => mei.allanystart.parser.ClipPathParser.parse(n)
        case n: Elem if n.label == "color-profile"    => mei.allanystart.parser.ColorProfileParser.parse(n)
        case n: Elem if n.label == "cursor"           => mei.allanystart.parser.CursorParser.parse(n)
        case n: Elem if n.label == "defs"             => mei.allanystart.parser.DefsParser.parse(n)
        case n: Elem if n.label == "desc"             => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "ellipse"          => mei.allanystart.parser.EllipseParser.parse(n)
        case n: Elem if n.label == "filter"           => mei.allanystart.parser.FilterParser.parse(n)
        case n: Elem if n.label == "font"             => mei.allanystart.parser.FontParser.parse(n)
        case n: Elem if n.label == "font-face"        => mei.allanystart.parser.FontFaceParser.parse(n)
        case n: Elem if n.label == "g"                => mei.allanystart.parser.GParser.parse(n)
        case n: Elem if n.label == "image"            => mei.allanystart.parser.ImageParser.parse(n)
        case n: Elem if n.label == "line"             => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "linearGradient"   => mei.allanystart.parser.LinearGradientParser.parse(n)
        case n: Elem if n.label == "marker"           => mei.allanystart.parser.MarkerParser.parse(n)
        case n: Elem if n.label == "mask"             => mei.allanystart.parser.MaskParser.parse(n)
        case n: Elem if n.label == "metadata"         => mei.allanystart.parser.MetadataParser.parse(n)
        case n: Elem if n.label == "path"             => mei.allanystart.parser.PathParser.parse(n)
        case n: Elem if n.label == "pattern"          => mei.allanystart.parser.PatternParser.parse(n)
        case n: Elem if n.label == "polygon"          => mei.allanystart.parser.PolygonParser.parse(n)
        case n: Elem if n.label == "polyline"         => mei.allanystart.parser.PolylineParser.parse(n)
        case n: Elem if n.label == "radialGradient"   => mei.allanystart.parser.RadialGradientParser.parse(n)
        case n: Elem if n.label == "rect"             => mei.allanystart.parser.RectParser.parse(n)
        case n: Elem if n.label == "script"           => mei.allanystart.parser.ScriptParser.parse(n)
        case n: Elem if n.label == "set"              => mei.allanystart.parser.SetParser.parse(n)
        case n: Elem if n.label == "style"            => mei.allanystart.parser.StyleParser.parse(n)
        case n: Elem if n.label == "svg"              => mei.allanystart.parser.SvgParser.parse(n)
        case n: Elem if n.label == "switch"           => mei.allanystart.parser.SwitchParser.parse(n)
        case n: Elem if n.label == "symbol"           => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "text"             => mei.allanystart.parser.TextParser.parse(n)
        case n: Elem if n.label == "title"            => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "use"              => mei.allanystart.parser.UseParser.parse(n)
        case n: Elem if n.label == "view"             => mei.allanystart.parser.ViewParser.parse(n)
      }.toList,
    )
  }
}
object HkernParser extends MeiModelDummyParser {
  def parse(node: Node): Hkern = {
    Hkern(
    )
  }
}
object VkernParser extends MeiModelDummyParser {
  def parse(node: Node): Vkern = {
    Vkern(
    )
  }
}
object FontFaceSrcParser extends MeiModelDummyParser {
  def parse(node: Node): FontFaceSrc = {
    FontFaceSrc(
      node.child.collect {
        case n: Elem if n.label == "font-face-name" => mei.allanystart.parser.FontFaceNameParser.parse(n)
        case n: Elem if n.label == "font-face-uri"  => mei.allanystart.parser.FontFaceUriParser.parse(n)
      }.toList,
    )
  }
}
object FontFaceUriParser extends MeiModelDummyParser {
  def parse(node: Node): FontFaceUri = {
    FontFaceUri(
      node.child.collect {
        case n: Elem if n.label == "font-face-format" => mei.allanystart.parser.FontFaceFormatParser.parse(n)
      }.toList,
    )
  }
}
object FontFaceFormatParser extends MeiModelDummyParser {
  def parse(node: Node): FontFaceFormat = {
    FontFaceFormat(
    )
  }
}
object FontFaceNameParser extends MeiModelDummyParser {
  def parse(node: Node): FontFaceName = {
    FontFaceName(
    )
  }
}
object DefinitionSrcParser extends MeiModelDummyParser {
  def parse(node: Node): DefinitionSrc = {
    DefinitionSrc(
    )
  }
}
object ForeignObjectParser extends MeiModelDummyParser {
  def parse(node: Node): ForeignObject = {
    ForeignObject(
      node.child.collect {
        case n if n.text.trim.nonEmpty => n.text
      }.toList,
    )
  }
}
object ArpegParser extends MeiModelDummyParser {
  def parse(node: Node): Arpeg = {
    Arpeg(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("order").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("enclose").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("lform").map(_.text),
      node.attribute("lwidth").map(_.text),
      node.attribute("lsegs").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("arrow").map(_.text),
      node.attribute("arrow.shape").map(_.text),
      node.attribute("arrow.size").map(_.text),
      node.attribute("arrow.color").map(_.text),
      node.attribute("arrow.fillcolor").map(_.text),
    )
  }
}
object AttaccaParser extends MeiModelDummyParser {
  def parse(node: Node): Attacca = {
    Attacca(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("tstamp2.ges").map(_.text),
      node.attribute("tstamp2.real").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("tstamp2").map(_.text),
      node.attribute("target").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("lform").map(_.text),
      node.attribute("lwidth").map(_.text),
      node.attribute("lsegs").map(_.text),
      node.attribute("lendsym").map(_.text),
      node.attribute("lendsym.size").map(_.text),
      node.attribute("lstartsym").map(_.text),
      node.attribute("lstartsym.size").map(_.text),
      node.attribute("extender").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("vgrp").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("startho").map(_.text),
      node.attribute("endho").map(_.text),
      node.attribute("startto").map(_.text),
      node.attribute("endto").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object BeamParser extends MeiModelDummyParser {
  def parse(node: Node): Beam = {
    Beam(
      node.child.collect {
        case n: Elem if n.label == "add"         => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "app"         => mei.allanystart.parser.AppParser.parse(n)
        case n: Elem if n.label == "bTrem"       => mei.allanystart.parser.BTremParser.parse(n)
        case n: Elem if n.label == "barLine"     => mei.allanystart.parser.BarLineParser.parse(n)
        case n: Elem if n.label == "beam"        => mei.allanystart.parser.BeamParser.parse(n)
        case n: Elem if n.label == "beatRpt"     => mei.allanystart.parser.BeatRptParser.parse(n)
        case n: Elem if n.label == "choice"      => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "chord"       => mei.allanystart.parser.ChordParser.parse(n)
        case n: Elem if n.label == "clef"        => mei.allanystart.parser.ClefParser.parse(n)
        case n: Elem if n.label == "clefGrp"     => mei.allanystart.parser.ClefGrpParser.parse(n)
        case n: Elem if n.label == "corr"        => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "custos"      => mei.allanystart.parser.CustosParser.parse(n)
        case n: Elem if n.label == "damage"      => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "del"         => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "fTrem"       => mei.allanystart.parser.FTremParser.parse(n)
        case n: Elem if n.label == "gap"         => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "graceGrp"    => mei.allanystart.parser.GraceGrpParser.parse(n)
        case n: Elem if n.label == "halfmRpt"    => mei.allanystart.parser.HalfmRptParser.parse(n)
        case n: Elem if n.label == "handShift"   => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "keySig"      => mei.allanystart.parser.KeySigParser.parse(n)
        case n: Elem if n.label == "meterSig"    => mei.allanystart.parser.MeterSigParser.parse(n)
        case n: Elem if n.label == "meterSigGrp" => mei.allanystart.parser.MeterSigGrpParser.parse(n)
        case n: Elem if n.label == "note"        => mei.allanystart.parser.NoteParser.parse(n)
        case n: Elem if n.label == "orig"        => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pad"         => mei.allanystart.parser.PadParser.parse(n)
        case n: Elem if n.label == "reg"         => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "rest"        => mei.allanystart.parser.RestParser.parse(n)
        case n: Elem if n.label == "restore"     => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sic"         => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "space"       => mei.allanystart.parser.SpaceParser.parse(n)
        case n: Elem if n.label == "subst"       => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"    => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "tabGrp"      => mei.allanystart.parser.TabGrpParser.parse(n)
        case n: Elem if n.label == "tuplet"      => mei.allanystart.parser.TupletParser.parse(n)
        case n: Elem if n.label == "unclear"     => mei.allanystart.parser.UnclearParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("beam.with").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("cue").map(_.text),
      node.attribute("form").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("slash").map(_.text),
      node.attribute("slope").map(_.text),
      node.attribute("visible").map(_.text),
    )
  }
}
object BeamSpanParser extends MeiModelDummyParser {
  def parse(node: Node): BeamSpan = {
    BeamSpan(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("beam.with").map(_.text),
      node.attribute("dur").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("tstamp2").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("cue").map(_.text),
      node.attribute("form").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("slash").map(_.text),
      node.attribute("slope").map(_.text),
      node.attribute("visible").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
      node.attribute("tstamp2.ges").map(_.text),
      node.attribute("tstamp2.real").map(_.text),
    )
  }
}
object BeatRptParser extends MeiModelDummyParser {
  def parse(node: Node): BeatRpt = {
    BeatRpt(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("beatdef").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("expand").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("slash").map(_.text).get,
      node.attribute("plist").map(_.text),
      node.attribute("evaluate").map(_.text),
    )
  }
}
object BendParser extends MeiModelDummyParser {
  def parse(node: Node): Bend = {
    Bend(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("dur").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("tstamp2").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("bezier").map(_.text),
      node.attribute("bulge").map(_.text),
      node.attribute("curvedir").map(_.text),
      node.attribute("lform").map(_.text),
      node.attribute("lwidth").map(_.text),
      node.attribute("lsegs").map(_.text),
      node.attribute("vgrp").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("startho").map(_.text),
      node.attribute("endho").map(_.text),
      node.attribute("startto").map(_.text),
      node.attribute("endto").map(_.text),
      node.attribute("startvo").map(_.text),
      node.attribute("endvo").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("x2").map(_.text),
      node.attribute("y2").map(_.text),
      node.attribute("tstamp2.ges").map(_.text),
      node.attribute("tstamp2.real").map(_.text),
      node.attribute("amount").map(_.text),
    )
  }
}
object BracketSpanParser extends MeiModelDummyParser {
  def parse(node: Node): BracketSpan = {
    BracketSpan(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("dur").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("tstamp2").map(_.text),
      node.attribute("func").map(_.text).get,
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("lform").map(_.text),
      node.attribute("lwidth").map(_.text),
      node.attribute("lsegs").map(_.text),
      node.attribute("lendsym").map(_.text),
      node.attribute("lendsym.size").map(_.text),
      node.attribute("lstartsym").map(_.text),
      node.attribute("lstartsym.size").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("startho").map(_.text),
      node.attribute("endho").map(_.text),
      node.attribute("startto").map(_.text),
      node.attribute("endto").map(_.text),
      node.attribute("startvo").map(_.text),
      node.attribute("endvo").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("x2").map(_.text),
      node.attribute("y2").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
      node.attribute("tstamp2.ges").map(_.text),
      node.attribute("tstamp2.real").map(_.text),
      node.attribute("facs").map(_.text),
    )
  }
}
object BreathParser extends MeiModelDummyParser {
  def parse(node: Node): Breath = {
    Breath(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("loc").map(_.text),
      node.attribute("ploc").map(_.text),
      node.attribute("oloc").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("vgrp").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
    )
  }
}
object BTremParser extends MeiModelDummyParser {
  def parse(node: Node): BTrem = {
    BTrem(
      node.child.collect {
        case n: Elem if n.label == "chord" => mei.allanystart.parser.ChordParser.parse(n)
        case n: Elem if n.label == "note"  => mei.allanystart.parser.NoteParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("dots").map(_.text),
      node.attribute("dur").map(_.text),
      node.attribute("num").map(_.text),
      node.attribute("form").map(_.text),
      node.attribute("num.place").map(_.text),
      node.attribute("num.visible").map(_.text),
      node.attribute("unitdur").map(_.text),
    )
  }
}
object FermataParser extends MeiModelDummyParser {
  def parse(node: Node): Fermata = {
    Fermata(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("enclose").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("vgrp").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("form").map(_.text),
      node.attribute("shape").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
    )
  }
}
object FTremParser extends MeiModelDummyParser {
  def parse(node: Node): FTrem = {
    FTrem(
      node.child.collect {
        case n: Elem if n.label == "chord" => mei.allanystart.parser.ChordParser.parse(n)
        case n: Elem if n.label == "clef"  => mei.allanystart.parser.ClefParser.parse(n)
        case n: Elem if n.label == "note"  => mei.allanystart.parser.NoteParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("dots").map(_.text),
      node.attribute("dur").map(_.text),
      node.attribute("form").map(_.text),
      node.attribute("beams").map(_.text),
      node.attribute("beams.float").map(_.text),
      node.attribute("float.gap").map(_.text),
      node.attribute("unitdur").map(_.text),
    )
  }
}
object GlissParser extends MeiModelDummyParser {
  def parse(node: Node): Gliss = {
    Gliss(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("dur").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("tstamp2").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("startho").map(_.text),
      node.attribute("endho").map(_.text),
      node.attribute("startto").map(_.text),
      node.attribute("endto").map(_.text),
      node.attribute("startvo").map(_.text),
      node.attribute("endvo").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("x2").map(_.text),
      node.attribute("y2").map(_.text),
      node.attribute("lform").map(_.text),
      node.attribute("lwidth").map(_.text),
      node.attribute("lsegs").map(_.text),
      node.attribute("lendsym").map(_.text),
      node.attribute("lendsym.size").map(_.text),
      node.attribute("lstartsym").map(_.text),
      node.attribute("lstartsym.size").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
      node.attribute("tstamp2.ges").map(_.text),
      node.attribute("tstamp2.real").map(_.text),
    )
  }
}
object GraceGrpParser extends MeiModelDummyParser {
  def parse(node: Node): GraceGrp = {
    GraceGrp(
      node.child.collect {
        case n: Elem if n.label == "add"         => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "app"         => mei.allanystart.parser.AppParser.parse(n)
        case n: Elem if n.label == "bTrem"       => mei.allanystart.parser.BTremParser.parse(n)
        case n: Elem if n.label == "barLine"     => mei.allanystart.parser.BarLineParser.parse(n)
        case n: Elem if n.label == "beam"        => mei.allanystart.parser.BeamParser.parse(n)
        case n: Elem if n.label == "beatRpt"     => mei.allanystart.parser.BeatRptParser.parse(n)
        case n: Elem if n.label == "choice"      => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "chord"       => mei.allanystart.parser.ChordParser.parse(n)
        case n: Elem if n.label == "clef"        => mei.allanystart.parser.ClefParser.parse(n)
        case n: Elem if n.label == "clefGrp"     => mei.allanystart.parser.ClefGrpParser.parse(n)
        case n: Elem if n.label == "corr"        => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "custos"      => mei.allanystart.parser.CustosParser.parse(n)
        case n: Elem if n.label == "damage"      => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "del"         => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "fTrem"       => mei.allanystart.parser.FTremParser.parse(n)
        case n: Elem if n.label == "gap"         => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "graceGrp"    => mei.allanystart.parser.GraceGrpParser.parse(n)
        case n: Elem if n.label == "halfmRpt"    => mei.allanystart.parser.HalfmRptParser.parse(n)
        case n: Elem if n.label == "handShift"   => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "keySig"      => mei.allanystart.parser.KeySigParser.parse(n)
        case n: Elem if n.label == "meterSig"    => mei.allanystart.parser.MeterSigParser.parse(n)
        case n: Elem if n.label == "meterSigGrp" => mei.allanystart.parser.MeterSigGrpParser.parse(n)
        case n: Elem if n.label == "note"        => mei.allanystart.parser.NoteParser.parse(n)
        case n: Elem if n.label == "orig"        => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pad"         => mei.allanystart.parser.PadParser.parse(n)
        case n: Elem if n.label == "reg"         => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "rest"        => mei.allanystart.parser.RestParser.parse(n)
        case n: Elem if n.label == "restore"     => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sic"         => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "space"       => mei.allanystart.parser.SpaceParser.parse(n)
        case n: Elem if n.label == "subst"       => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"    => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "tabGrp"      => mei.allanystart.parser.TabGrpParser.parse(n)
        case n: Elem if n.label == "tuplet"      => mei.allanystart.parser.TupletParser.parse(n)
        case n: Elem if n.label == "unclear"     => mei.allanystart.parser.UnclearParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("grace").map(_.text),
      node.attribute("grace.time").map(_.text),
      node.attribute("attach").map(_.text),
      node.attribute("color").map(_.text),
    )
  }
}
object HairpinParser extends MeiModelDummyParser {
  def parse(node: Node): Hairpin = {
    Hairpin(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("dur").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("tstamp2").map(_.text),
      node.attribute("form").map(_.text).get,
      node.attribute("niente").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("lform").map(_.text),
      node.attribute("lwidth").map(_.text),
      node.attribute("lsegs").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("vgrp").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("startho").map(_.text),
      node.attribute("endho").map(_.text),
      node.attribute("startto").map(_.text),
      node.attribute("endto").map(_.text),
      node.attribute("startvo").map(_.text),
      node.attribute("endvo").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("x2").map(_.text),
      node.attribute("y2").map(_.text),
      node.attribute("opening").map(_.text),
      node.attribute("closed").map(_.text),
      node.attribute("opening.vertical").map(_.text),
      node.attribute("angle.optimize").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
      node.attribute("val").map(_.text),
      node.attribute("val2").map(_.text),
      node.attribute("tstamp2.ges").map(_.text),
      node.attribute("tstamp2.real").map(_.text),
    )
  }
}
object HalfmRptParser extends MeiModelDummyParser {
  def parse(node: Node): HalfmRpt = {
    HalfmRpt(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("dur").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("expand").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
    )
  }
}
object HarpPedalParser extends MeiModelDummyParser {
  def parse(node: Node): HarpPedal = {
    HarpPedal(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("c").map(_.text),
      node.attribute("d").map(_.text),
      node.attribute("e").map(_.text),
      node.attribute("f").map(_.text),
      node.attribute("g").map(_.text),
      node.attribute("a").map(_.text),
      node.attribute("b").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("vgrp").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
    )
  }
}
object LvParser extends MeiModelDummyParser {
  def parse(node: Node): Lv = {
    Lv(
      node.child.collect {
        case n: Elem if n.label == "curve" => mei.allanystart.parser.CurveParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("tstamp2").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("bezier").map(_.text),
      node.attribute("bulge").map(_.text),
      node.attribute("curvedir").map(_.text),
      node.attribute("lform").map(_.text),
      node.attribute("lwidth").map(_.text),
      node.attribute("lsegs").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("startho").map(_.text),
      node.attribute("endho").map(_.text),
      node.attribute("startto").map(_.text),
      node.attribute("endto").map(_.text),
      node.attribute("startvo").map(_.text),
      node.attribute("endvo").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("x2").map(_.text),
      node.attribute("y2").map(_.text),
      node.attribute("tstamp2.ges").map(_.text),
      node.attribute("tstamp2.real").map(_.text),
    )
  }
}
object MeasureParser extends MeiModelDummyParser {
  def parse(node: Node): Measure = {
    Measure(
      node.child.collect {
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "app"          => mei.allanystart.parser.AppParser.parse(n)
        case n: Elem if n.label == "arpeg"        => mei.allanystart.parser.ArpegParser.parse(n)
        case n: Elem if n.label == "attacca"      => mei.allanystart.parser.AttaccaParser.parse(n)
        case n: Elem if n.label == "beamSpan"     => mei.allanystart.parser.BeamSpanParser.parse(n)
        case n: Elem if n.label == "bend"         => mei.allanystart.parser.BendParser.parse(n)
        case n: Elem if n.label == "bracketSpan"  => mei.allanystart.parser.BracketSpanParser.parse(n)
        case n: Elem if n.label == "breath"       => mei.allanystart.parser.BreathParser.parse(n)
        case n: Elem if n.label == "caesura"      => mei.allanystart.parser.CaesuraParser.parse(n)
        case n: Elem if n.label == "cb"           => mei.allanystart.parser.CbParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "colLayout"    => mei.allanystart.parser.ColLayoutParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "cpMark"       => mei.allanystart.parser.CpMarkParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "dir"          => mei.allanystart.parser.DirParser.parse(n)
        case n: Elem if n.label == "dynam"        => mei.allanystart.parser.DynamParser.parse(n)
        case n: Elem if n.label == "fermata"      => mei.allanystart.parser.FermataParser.parse(n)
        case n: Elem if n.label == "fing"         => mei.allanystart.parser.FingParser.parse(n)
        case n: Elem if n.label == "fingGrp"      => mei.allanystart.parser.FingGrpParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "gliss"        => mei.allanystart.parser.GlissParser.parse(n)
        case n: Elem if n.label == "hairpin"      => mei.allanystart.parser.HairpinParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "harm"         => mei.allanystart.parser.HarmParser.parse(n)
        case n: Elem if n.label == "harpPedal"    => mei.allanystart.parser.HarpPedalParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "lv"           => mei.allanystart.parser.LvParser.parse(n)
        case n: Elem if n.label == "mNum"         => mei.allanystart.parser.MNumParser.parse(n)
        case n: Elem if n.label == "metaMark"     => mei.allanystart.parser.MetaMarkParser.parse(n)
        case n: Elem if n.label == "midi"         => mei.allanystart.parser.MidiParser.parse(n)
        case n: Elem if n.label == "mordent"      => mei.allanystart.parser.MordentParser.parse(n)
        case n: Elem if n.label == "octave"       => mei.allanystart.parser.OctaveParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "ornam"        => mei.allanystart.parser.OrnamParser.parse(n)
        case n: Elem if n.label == "ossia"        => mei.allanystart.parser.OssiaParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "pedal"        => mei.allanystart.parser.PedalParser.parse(n)
        case n: Elem if n.label == "phrase"       => mei.allanystart.parser.PhraseParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "reh"          => mei.allanystart.parser.RehParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "repeatMark"   => mei.allanystart.parser.RepeatMarkParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sb"           => mei.allanystart.parser.SbParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "slur"         => mei.allanystart.parser.SlurParser.parse(n)
        case n: Elem if n.label == "sp"           => mei.allanystart.parser.SpParser.parse(n)
        case n: Elem if n.label == "staff"        => mei.allanystart.parser.StaffParser.parse(n)
        case n: Elem if n.label == "staffDef"     => mei.allanystart.parser.StaffDefParser.parse(n)
        case n: Elem if n.label == "stageDir"     => mei.allanystart.parser.StageDirParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "tempo"        => mei.allanystart.parser.TempoParser.parse(n)
        case n: Elem if n.label == "tie"          => mei.allanystart.parser.TieParser.parse(n)
        case n: Elem if n.label == "trill"        => mei.allanystart.parser.TrillParser.parse(n)
        case n: Elem if n.label == "tupletSpan"   => mei.allanystart.parser.TupletSpanParser.parse(n)
        case n: Elem if n.label == "turn"         => mei.allanystart.parser.TurnParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("xlink:actuate").map(_.text),
      node.attribute("xlink:role").map(_.text),
      node.attribute("xlink:show").map(_.text),
      node.attribute("target").map(_.text),
      node.attribute("targettype").map(_.text),
      node.attribute("join").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("metcon").map(_.text),
      node.attribute("control").map(_.text),
      node.attribute("left").map(_.text),
      node.attribute("right").map(_.text),
      node.attribute("bar.len").map(_.text),
      node.attribute("bar.method").map(_.text),
      node.attribute("bar.place").map(_.text),
      node.attribute("width").map(_.text),
      node.attribute("evaluate").map(_.text),
    )
  }
}
object MeterSigParser extends MeiModelDummyParser {
  def parse(node: Node): MeterSig = {
    MeterSig(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("count").map(_.text),
      node.attribute("sym").map(_.text),
      node.attribute("unit").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("enclose").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("visible").map(_.text),
      node.attribute("form").map(_.text),
    )
  }
}
object MeterSigGrpParser extends MeiModelDummyParser {
  def parse(node: Node): MeterSigGrp = {
    MeterSigGrp(
      node.child.collect {
        case n: Elem if n.label == "meterSig"    => mei.allanystart.parser.MeterSigParser.parse(n)
        case n: Elem if n.label == "meterSigGrp" => mei.allanystart.parser.MeterSigGrpParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("func").map(_.text).get,
      node.attribute("enclose").map(_.text),
      node.attribute("visible").map(_.text),
    )
  }
}
object MNumParser extends MeiModelDummyParser {
  def parse(node: Node): MNum = {
    MNum(
      node.child.collect {
        case n: Elem if n.label == "lb"    => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "rend"  => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "stack" => mei.allanystart.parser.StackParser.parse(n)
        case n if n.text.trim.nonEmpty     => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object MRestParser extends MeiModelDummyParser {
  def parse(node: Node): MRest = {
    MRest(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("cue").map(_.text),
      node.attribute("dur").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("cutout").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("loc").map(_.text),
      node.attribute("ploc").map(_.text),
      node.attribute("oloc").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
      node.attribute("fermata").map(_.text),
    )
  }
}
object MRptParser extends MeiModelDummyParser {
  def parse(node: Node): MRpt = {
    MRpt(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("num").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("expand").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("num.place").map(_.text),
      node.attribute("num.visible").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
    )
  }
}
object MRpt2Parser extends MeiModelDummyParser {
  def parse(node: Node): MRpt2 = {
    MRpt2(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("expand").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
    )
  }
}
object MSpaceParser extends MeiModelDummyParser {
  def parse(node: Node): MSpace = {
    MSpace(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("dur").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("cutout").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
      node.attribute("fermata").map(_.text),
    )
  }
}
object MultiRestParser extends MeiModelDummyParser {
  def parse(node: Node): MultiRest = {
    MultiRest(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("num").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("num.place").map(_.text),
      node.attribute("num.visible").map(_.text),
      node.attribute("loc").map(_.text),
      node.attribute("ploc").map(_.text),
      node.attribute("oloc").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("visible").map(_.text),
      node.attribute("width").map(_.text),
      node.attribute("block").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
    )
  }
}
object MultiRptParser extends MeiModelDummyParser {
  def parse(node: Node): MultiRpt = {
    MultiRpt(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("num").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("expand").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
    )
  }
}
object OctaveParser extends MeiModelDummyParser {
  def parse(node: Node): Octave = {
    Octave(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("dur").map(_.text),
      node.attribute("dis").map(_.text),
      node.attribute("dis.place").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("tstamp2").map(_.text),
      node.attribute("coll").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("lform").map(_.text),
      node.attribute("lwidth").map(_.text),
      node.attribute("lsegs").map(_.text),
      node.attribute("lendsym").map(_.text),
      node.attribute("lendsym.size").map(_.text),
      node.attribute("lstartsym").map(_.text),
      node.attribute("lstartsym.size").map(_.text),
      node.attribute("extender").map(_.text),
      node.attribute("vgrp").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("startho").map(_.text),
      node.attribute("endho").map(_.text),
      node.attribute("startto").map(_.text),
      node.attribute("endto").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
      node.attribute("tstamp2.ges").map(_.text),
      node.attribute("tstamp2.real").map(_.text),
    )
  }
}
object OLayerParser extends MeiModelDummyParser {
  def parse(node: Node): OLayer = {
    OLayer(
      node.child.collect {
        case n: Elem if n.label == "accid"        => mei.allanystart.parser.AccidParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "app"          => mei.allanystart.parser.AppParser.parse(n)
        case n: Elem if n.label == "artic"        => mei.allanystart.parser.ArticParser.parse(n)
        case n: Elem if n.label == "bTrem"        => mei.allanystart.parser.BTremParser.parse(n)
        case n: Elem if n.label == "barLine"      => mei.allanystart.parser.BarLineParser.parse(n)
        case n: Elem if n.label == "beam"         => mei.allanystart.parser.BeamParser.parse(n)
        case n: Elem if n.label == "beatRpt"      => mei.allanystart.parser.BeatRptParser.parse(n)
        case n: Elem if n.label == "cb"           => mei.allanystart.parser.CbParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "chord"        => mei.allanystart.parser.ChordParser.parse(n)
        case n: Elem if n.label == "clef"         => mei.allanystart.parser.ClefParser.parse(n)
        case n: Elem if n.label == "clefGrp"      => mei.allanystart.parser.ClefGrpParser.parse(n)
        case n: Elem if n.label == "colLayout"    => mei.allanystart.parser.ColLayoutParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "custos"       => mei.allanystart.parser.CustosParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "divLine"      => mei.allanystart.parser.DivLineParser.parse(n)
        case n: Elem if n.label == "dot"          => mei.allanystart.parser.DotParser.parse(n)
        case n: Elem if n.label == "fTrem"        => mei.allanystart.parser.FTremParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "graceGrp"     => mei.allanystart.parser.GraceGrpParser.parse(n)
        case n: Elem if n.label == "halfmRpt"     => mei.allanystart.parser.HalfmRptParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "keySig"       => mei.allanystart.parser.KeySigParser.parse(n)
        case n: Elem if n.label == "ligature"     => mei.allanystart.parser.LigatureParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "mRest"        => mei.allanystart.parser.MRestParser.parse(n)
        case n: Elem if n.label == "mRpt"         => mei.allanystart.parser.MRptParser.parse(n)
        case n: Elem if n.label == "mRpt2"        => mei.allanystart.parser.MRpt2Parser.parse(n)
        case n: Elem if n.label == "mSpace"       => mei.allanystart.parser.MSpaceParser.parse(n)
        case n: Elem if n.label == "mensur"       => mei.allanystart.parser.MensurParser.parse(n)
        case n: Elem if n.label == "meterSig"     => mei.allanystart.parser.MeterSigParser.parse(n)
        case n: Elem if n.label == "meterSigGrp"  => mei.allanystart.parser.MeterSigGrpParser.parse(n)
        case n: Elem if n.label == "midi"         => mei.allanystart.parser.MidiParser.parse(n)
        case n: Elem if n.label == "multiRest"    => mei.allanystart.parser.MultiRestParser.parse(n)
        case n: Elem if n.label == "multiRpt"     => mei.allanystart.parser.MultiRptParser.parse(n)
        case n: Elem if n.label == "neume"        => mei.allanystart.parser.NeumeParser.parse(n)
        case n: Elem if n.label == "note"         => mei.allanystart.parser.NoteParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pad"          => mei.allanystart.parser.PadParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "proport"      => mei.allanystart.parser.ProportParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "rest"         => mei.allanystart.parser.RestParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sb"           => mei.allanystart.parser.SbParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "space"        => mei.allanystart.parser.SpaceParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "syllable"     => mei.allanystart.parser.SyllableParser.parse(n)
        case n: Elem if n.label == "tabGrp"       => mei.allanystart.parser.TabGrpParser.parse(n)
        case n: Elem if n.label == "tuplet"       => mei.allanystart.parser.TupletParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("cue").map(_.text),
      node.attribute("metcon").map(_.text),
      node.attribute("def").map(_.text),
      node.attribute("visible").map(_.text),
    )
  }
}
object OssiaParser extends MeiModelDummyParser {
  def parse(node: Node): Ossia = {
    Ossia(
      node.child.collect {
        case n: Elem if n.label == "layer"  => mei.allanystart.parser.LayerParser.parse(n)
        case n: Elem if n.label == "oLayer" => mei.allanystart.parser.OLayerParser.parse(n)
        case n: Elem if n.label == "oStaff" => mei.allanystart.parser.OStaffParser.parse(n)
        case n: Elem if n.label == "staff"  => mei.allanystart.parser.StaffParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
    )
  }
}
object OStaffParser extends MeiModelDummyParser {
  def parse(node: Node): OStaff = {
    OStaff(
      node.child.collect {
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "app"          => mei.allanystart.parser.AppParser.parse(n)
        case n: Elem if n.label == "bend"         => mei.allanystart.parser.BendParser.parse(n)
        case n: Elem if n.label == "caesura"      => mei.allanystart.parser.CaesuraParser.parse(n)
        case n: Elem if n.label == "cb"           => mei.allanystart.parser.CbParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "colLayout"    => mei.allanystart.parser.ColLayoutParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "cpMark"       => mei.allanystart.parser.CpMarkParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "dir"          => mei.allanystart.parser.DirParser.parse(n)
        case n: Elem if n.label == "dynam"        => mei.allanystart.parser.DynamParser.parse(n)
        case n: Elem if n.label == "fing"         => mei.allanystart.parser.FingParser.parse(n)
        case n: Elem if n.label == "fingGrp"      => mei.allanystart.parser.FingGrpParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "gliss"        => mei.allanystart.parser.GlissParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "harm"         => mei.allanystart.parser.HarmParser.parse(n)
        case n: Elem if n.label == "layer"        => mei.allanystart.parser.LayerParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "metaMark"     => mei.allanystart.parser.MetaMarkParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "ornam"        => mei.allanystart.parser.OrnamParser.parse(n)
        case n: Elem if n.label == "ossia"        => mei.allanystart.parser.OssiaParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "phrase"       => mei.allanystart.parser.PhraseParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "repeatMark"   => mei.allanystart.parser.RepeatMarkParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sb"           => mei.allanystart.parser.SbParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "sp"           => mei.allanystart.parser.SpParser.parse(n)
        case n: Elem if n.label == "staffDef"     => mei.allanystart.parser.StaffDefParser.parse(n)
        case n: Elem if n.label == "stageDir"     => mei.allanystart.parser.StageDirParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "tempo"        => mei.allanystart.parser.TempoParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("metcon").map(_.text),
      node.attribute("def").map(_.text),
      node.attribute("visible").map(_.text),
    )
  }
}
object PedalParser extends MeiModelDummyParser {
  def parse(node: Node): Pedal = {
    Pedal(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("tstamp2").map(_.text),
      node.attribute("dir").map(_.text).get,
      node.attribute("func").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("lform").map(_.text),
      node.attribute("lwidth").map(_.text),
      node.attribute("lsegs").map(_.text),
      node.attribute("lendsym").map(_.text),
      node.attribute("lendsym.size").map(_.text),
      node.attribute("lstartsym").map(_.text),
      node.attribute("lstartsym.size").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("vgrp").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("form").map(_.text),
      node.attribute("tstamp2.ges").map(_.text),
      node.attribute("tstamp2.real").map(_.text),
    )
  }
}
object RehParser extends MeiModelDummyParser {
  def parse(node: Node): Reh = {
    Reh(
      node.child.collect {
        case n: Elem if n.label == "lb"    => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "rend"  => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "stack" => mei.allanystart.parser.StackParser.parse(n)
        case n if n.text.trim.nonEmpty     => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object RepeatMarkParser extends MeiModelDummyParser {
  def parse(node: Node): RepeatMark = {
    RepeatMark(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("dur").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("tstamp2").map(_.text),
      node.attribute("func").map(_.text).get,
      node.attribute("color").map(_.text),
      node.attribute("lform").map(_.text),
      node.attribute("lwidth").map(_.text),
      node.attribute("lsegs").map(_.text),
      node.attribute("lendsym").map(_.text),
      node.attribute("lendsym.size").map(_.text),
      node.attribute("lstartsym").map(_.text),
      node.attribute("lstartsym.size").map(_.text),
      node.attribute("extender").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("vgrp").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("startho").map(_.text),
      node.attribute("endho").map(_.text),
      node.attribute("startto").map(_.text),
      node.attribute("endto").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
      node.attribute("tstamp2.ges").map(_.text),
      node.attribute("tstamp2.real").map(_.text),
    )
  }
}
object SlurParser extends MeiModelDummyParser {
  def parse(node: Node): Slur = {
    Slur(
      node.child.collect {
        case n: Elem if n.label == "curve" => mei.allanystart.parser.CurveParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("dur").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("tstamp2").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("bezier").map(_.text),
      node.attribute("bulge").map(_.text),
      node.attribute("curvedir").map(_.text),
      node.attribute("lform").map(_.text),
      node.attribute("lwidth").map(_.text),
      node.attribute("lsegs").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("startho").map(_.text),
      node.attribute("endho").map(_.text),
      node.attribute("startto").map(_.text),
      node.attribute("endto").map(_.text),
      node.attribute("startvo").map(_.text),
      node.attribute("endvo").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("x2").map(_.text),
      node.attribute("y2").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
      node.attribute("tstamp2.ges").map(_.text),
      node.attribute("tstamp2.real").map(_.text),
      node.attribute("join").map(_.text),
    )
  }
}
object TieParser extends MeiModelDummyParser {
  def parse(node: Node): Tie = {
    Tie(
      node.child.collect {
        case n: Elem if n.label == "curve" => mei.allanystart.parser.CurveParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("tstamp2").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("bezier").map(_.text),
      node.attribute("bulge").map(_.text),
      node.attribute("curvedir").map(_.text),
      node.attribute("lform").map(_.text),
      node.attribute("lwidth").map(_.text),
      node.attribute("lsegs").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("startho").map(_.text),
      node.attribute("endho").map(_.text),
      node.attribute("startto").map(_.text),
      node.attribute("endto").map(_.text),
      node.attribute("startvo").map(_.text),
      node.attribute("endvo").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("x2").map(_.text),
      node.attribute("y2").map(_.text),
      node.attribute("tstamp2.ges").map(_.text),
      node.attribute("tstamp2.real").map(_.text),
    )
  }
}
object TupletParser extends MeiModelDummyParser {
  def parse(node: Node): Tuplet = {
    Tuplet(
      node.child.collect {
        case n: Elem if n.label == "add"         => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "app"         => mei.allanystart.parser.AppParser.parse(n)
        case n: Elem if n.label == "bTrem"       => mei.allanystart.parser.BTremParser.parse(n)
        case n: Elem if n.label == "barLine"     => mei.allanystart.parser.BarLineParser.parse(n)
        case n: Elem if n.label == "beam"        => mei.allanystart.parser.BeamParser.parse(n)
        case n: Elem if n.label == "beatRpt"     => mei.allanystart.parser.BeatRptParser.parse(n)
        case n: Elem if n.label == "choice"      => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "chord"       => mei.allanystart.parser.ChordParser.parse(n)
        case n: Elem if n.label == "clef"        => mei.allanystart.parser.ClefParser.parse(n)
        case n: Elem if n.label == "clefGrp"     => mei.allanystart.parser.ClefGrpParser.parse(n)
        case n: Elem if n.label == "corr"        => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "custos"      => mei.allanystart.parser.CustosParser.parse(n)
        case n: Elem if n.label == "damage"      => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "del"         => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "fTrem"       => mei.allanystart.parser.FTremParser.parse(n)
        case n: Elem if n.label == "gap"         => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "graceGrp"    => mei.allanystart.parser.GraceGrpParser.parse(n)
        case n: Elem if n.label == "halfmRpt"    => mei.allanystart.parser.HalfmRptParser.parse(n)
        case n: Elem if n.label == "handShift"   => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "keySig"      => mei.allanystart.parser.KeySigParser.parse(n)
        case n: Elem if n.label == "meterSig"    => mei.allanystart.parser.MeterSigParser.parse(n)
        case n: Elem if n.label == "meterSigGrp" => mei.allanystart.parser.MeterSigGrpParser.parse(n)
        case n: Elem if n.label == "note"        => mei.allanystart.parser.NoteParser.parse(n)
        case n: Elem if n.label == "orig"        => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pad"         => mei.allanystart.parser.PadParser.parse(n)
        case n: Elem if n.label == "reg"         => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "rest"        => mei.allanystart.parser.RestParser.parse(n)
        case n: Elem if n.label == "restore"     => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sic"         => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "space"       => mei.allanystart.parser.SpaceParser.parse(n)
        case n: Elem if n.label == "subst"       => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"    => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "tabGrp"      => mei.allanystart.parser.TabGrpParser.parse(n)
        case n: Elem if n.label == "tuplet"      => mei.allanystart.parser.TupletParser.parse(n)
        case n: Elem if n.label == "unclear"     => mei.allanystart.parser.UnclearParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("beam.with").map(_.text),
      node.attribute("dur").map(_.text),
      node.attribute("num").map(_.text),
      node.attribute("numbase").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("num.place").map(_.text),
      node.attribute("num.visible").map(_.text),
      node.attribute("bracket.place").map(_.text),
      node.attribute("bracket.visible").map(_.text),
      node.attribute("dur.visible").map(_.text),
      node.attribute("num.format").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
    )
  }
}
object TupletSpanParser extends MeiModelDummyParser {
  def parse(node: Node): TupletSpan = {
    TupletSpan(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("beam.with").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("dur").map(_.text),
      node.attribute("num").map(_.text),
      node.attribute("numbase").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("tstamp2").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("num.place").map(_.text),
      node.attribute("num.visible").map(_.text),
      node.attribute("bracket.place").map(_.text),
      node.attribute("bracket.visible").map(_.text),
      node.attribute("dur.visible").map(_.text),
      node.attribute("num.format").map(_.text),
      node.attribute("tstamp2.ges").map(_.text),
      node.attribute("tstamp2.real").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
    )
  }
}
object MordentParser extends MeiModelDummyParser {
  def parse(node: Node): Mordent = {
    Mordent(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("accidupper.ges").map(_.text),
      node.attribute("accidlower.ges").map(_.text),
      node.attribute("accidupper").map(_.text),
      node.attribute("accidlower").map(_.text),
      node.attribute("form").map(_.text),
      node.attribute("long").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("enclose").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("vgrp").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
    )
  }
}
object TrillParser extends MeiModelDummyParser {
  def parse(node: Node): Trill = {
    Trill(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
      node.attribute("tstamp2.ges").map(_.text),
      node.attribute("tstamp2.real").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("dur").map(_.text),
      node.attribute("accidupper.ges").map(_.text),
      node.attribute("accidlower.ges").map(_.text),
      node.attribute("accidupper").map(_.text),
      node.attribute("accidlower").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("tstamp2").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("enclose").map(_.text),
      node.attribute("lform").map(_.text),
      node.attribute("lwidth").map(_.text),
      node.attribute("lsegs").map(_.text),
      node.attribute("lendsym").map(_.text),
      node.attribute("lendsym.size").map(_.text),
      node.attribute("lstartsym").map(_.text),
      node.attribute("lstartsym.size").map(_.text),
      node.attribute("extender").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("vgrp").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("startho").map(_.text),
      node.attribute("endho").map(_.text),
      node.attribute("startto").map(_.text),
      node.attribute("endto").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object TurnParser extends MeiModelDummyParser {
  def parse(node: Node): Turn = {
    Turn(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("accidupper.ges").map(_.text),
      node.attribute("accidlower.ges").map(_.text),
      node.attribute("accidupper").map(_.text),
      node.attribute("accidlower").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("delayed").map(_.text),
      node.attribute("form").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("enclose").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("vgrp").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object MeiCorpusParser extends MeiModelDummyParser {
  def parse(node: Node): MeiCorpus = {
    MeiCorpus(
      node.child.collect {
        case n: Elem if n.label == "mei"     => mei.allanystart.parser.MeiParser.parse(n)
        case n: Elem if n.label == "meiHead" => mei.allanystart.parser.MeiHeadParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
    )
  }
}
object AppParser extends MeiModelDummyParser {
  def parse(node: Node): App = {
    App(
      node.child.collect {
        case n: Elem if n.label == "lem" => mei.allanystart.parser.LemParser.parse(n)
        case n: Elem if n.label == "rdg" => mei.allanystart.parser.RdgParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
    )
  }
}
object LemParser extends MeiModelDummyParser {
  def parse(node: Node): Lem = {
    Lem(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "accid"        => mei.allanystart.parser.AccidParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "app"          => mei.allanystart.parser.AppParser.parse(n)
        case n: Elem if n.label == "arpeg"        => mei.allanystart.parser.ArpegParser.parse(n)
        case n: Elem if n.label == "artic"        => mei.allanystart.parser.ArticParser.parse(n)
        case n: Elem if n.label == "attacca"      => mei.allanystart.parser.AttaccaParser.parse(n)
        case n: Elem if n.label == "bTrem"        => mei.allanystart.parser.BTremParser.parse(n)
        case n: Elem if n.label == "barLine"      => mei.allanystart.parser.BarLineParser.parse(n)
        case n: Elem if n.label == "beam"         => mei.allanystart.parser.BeamParser.parse(n)
        case n: Elem if n.label == "beamSpan"     => mei.allanystart.parser.BeamSpanParser.parse(n)
        case n: Elem if n.label == "beatRpt"      => mei.allanystart.parser.BeatRptParser.parse(n)
        case n: Elem if n.label == "bend"         => mei.allanystart.parser.BendParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblList"     => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "bracketSpan"  => mei.allanystart.parser.BracketSpanParser.parse(n)
        case n: Elem if n.label == "breath"       => mei.allanystart.parser.BreathParser.parse(n)
        case n: Elem if n.label == "caesura"      => mei.allanystart.parser.CaesuraParser.parse(n)
        case n: Elem if n.label == "castList"     => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "cb"           => mei.allanystart.parser.CbParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "chord"        => mei.allanystart.parser.ChordParser.parse(n)
        case n: Elem if n.label == "clef"         => mei.allanystart.parser.ClefParser.parse(n)
        case n: Elem if n.label == "clefGrp"      => mei.allanystart.parser.ClefGrpParser.parse(n)
        case n: Elem if n.label == "colLayout"    => mei.allanystart.parser.ColLayoutParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "cpMark"       => mei.allanystart.parser.CpMarkParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "custos"       => mei.allanystart.parser.CustosParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "dir"          => mei.allanystart.parser.DirParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "div"          => mei.allanystart.parser.DivParser.parse(n)
        case n: Elem if n.label == "divLine"      => mei.allanystart.parser.DivLineParser.parse(n)
        case n: Elem if n.label == "dot"          => mei.allanystart.parser.DotParser.parse(n)
        case n: Elem if n.label == "dynam"        => mei.allanystart.parser.DynamParser.parse(n)
        case n: Elem if n.label == "ending"       => mei.allanystart.parser.EndingParser.parse(n)
        case n: Elem if n.label == "episema"      => mei.allanystart.parser.EpisemaParser.parse(n)
        case n: Elem if n.label == "eventList"    => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "expansion"    => mei.allanystart.parser.ExpansionParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "f"            => mei.allanystart.parser.FParser.parse(n)
        case n: Elem if n.label == "fTrem"        => mei.allanystart.parser.FTremParser.parse(n)
        case n: Elem if n.label == "fermata"      => mei.allanystart.parser.FermataParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "fing"         => mei.allanystart.parser.FingParser.parse(n)
        case n: Elem if n.label == "fingGrp"      => mei.allanystart.parser.FingGrpParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "gliss"        => mei.allanystart.parser.GlissParser.parse(n)
        case n: Elem if n.label == "graceGrp"     => mei.allanystart.parser.GraceGrpParser.parse(n)
        case n: Elem if n.label == "hairpin"      => mei.allanystart.parser.HairpinParser.parse(n)
        case n: Elem if n.label == "halfmRpt"     => mei.allanystart.parser.HalfmRptParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "harm"         => mei.allanystart.parser.HarmParser.parse(n)
        case n: Elem if n.label == "harpPedal"    => mei.allanystart.parser.HarpPedalParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "hispanTick"   => mei.allanystart.parser.HispanTickParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "keySig"       => mei.allanystart.parser.KeySigParser.parse(n)
        case n: Elem if n.label == "layer"        => mei.allanystart.parser.LayerParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "lg"           => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "ligature"     => mei.allanystart.parser.LigatureParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "liquescent"   => mei.allanystart.parser.LiquescentParser.parse(n)
        case n: Elem if n.label == "list"         => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "lv"           => mei.allanystart.parser.LvParser.parse(n)
        case n: Elem if n.label == "mRest"        => mei.allanystart.parser.MRestParser.parse(n)
        case n: Elem if n.label == "mRpt"         => mei.allanystart.parser.MRptParser.parse(n)
        case n: Elem if n.label == "mRpt2"        => mei.allanystart.parser.MRpt2Parser.parse(n)
        case n: Elem if n.label == "mSpace"       => mei.allanystart.parser.MSpaceParser.parse(n)
        case n: Elem if n.label == "measure"      => mei.allanystart.parser.MeasureParser.parse(n)
        case n: Elem if n.label == "mensur"       => mei.allanystart.parser.MensurParser.parse(n)
        case n: Elem if n.label == "metaMark"     => mei.allanystart.parser.MetaMarkParser.parse(n)
        case n: Elem if n.label == "meterSig"     => mei.allanystart.parser.MeterSigParser.parse(n)
        case n: Elem if n.label == "meterSigGrp"  => mei.allanystart.parser.MeterSigGrpParser.parse(n)
        case n: Elem if n.label == "midi"         => mei.allanystart.parser.MidiParser.parse(n)
        case n: Elem if n.label == "mordent"      => mei.allanystart.parser.MordentParser.parse(n)
        case n: Elem if n.label == "multiRest"    => mei.allanystart.parser.MultiRestParser.parse(n)
        case n: Elem if n.label == "multiRpt"     => mei.allanystart.parser.MultiRptParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "nc"           => mei.allanystart.parser.NcParser.parse(n)
        case n: Elem if n.label == "ncGrp"        => mei.allanystart.parser.NcGrpParser.parse(n)
        case n: Elem if n.label == "neume"        => mei.allanystart.parser.NeumeParser.parse(n)
        case n: Elem if n.label == "note"         => mei.allanystart.parser.NoteParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "octave"       => mei.allanystart.parser.OctaveParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "oriscus"      => mei.allanystart.parser.OriscusParser.parse(n)
        case n: Elem if n.label == "ornam"        => mei.allanystart.parser.OrnamParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "pad"          => mei.allanystart.parser.PadParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "pedal"        => mei.allanystart.parser.PedalParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "phrase"       => mei.allanystart.parser.PhraseParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "proport"      => mei.allanystart.parser.ProportParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "quilisma"     => mei.allanystart.parser.QuilismaParser.parse(n)
        case n: Elem if n.label == "quote"        => mei.allanystart.parser.QuoteParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "refrain"      => mei.allanystart.parser.RefrainParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "reh"          => mei.allanystart.parser.RehParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repeatMark"   => mei.allanystart.parser.RepeatMarkParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "rest"         => mei.allanystart.parser.RestParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sb"           => mei.allanystart.parser.SbParser.parse(n)
        case n: Elem if n.label == "scoreDef"     => mei.allanystart.parser.ScoreDefParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "section"      => mei.allanystart.parser.SectionParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "signifLet"    => mei.allanystart.parser.SignifLetParser.parse(n)
        case n: Elem if n.label == "slur"         => mei.allanystart.parser.SlurParser.parse(n)
        case n: Elem if n.label == "sp"           => mei.allanystart.parser.SpParser.parse(n)
        case n: Elem if n.label == "space"        => mei.allanystart.parser.SpaceParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "staff"        => mei.allanystart.parser.StaffParser.parse(n)
        case n: Elem if n.label == "staffDef"     => mei.allanystart.parser.StaffDefParser.parse(n)
        case n: Elem if n.label == "staffGrp"     => mei.allanystart.parser.StaffGrpParser.parse(n)
        case n: Elem if n.label == "stageDir"     => mei.allanystart.parser.StageDirParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "strophicus"   => mei.allanystart.parser.StrophicusParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "syl"          => mei.allanystart.parser.SylParser.parse(n)
        case n: Elem if n.label == "syllable"     => mei.allanystart.parser.SyllableParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "tabGrp"       => mei.allanystart.parser.TabGrpParser.parse(n)
        case n: Elem if n.label == "table"        => mei.allanystart.parser.TableParser.parse(n)
        case n: Elem if n.label == "tempo"        => mei.allanystart.parser.TempoParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "tie"          => mei.allanystart.parser.TieParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "trill"        => mei.allanystart.parser.TrillParser.parse(n)
        case n: Elem if n.label == "tuplet"       => mei.allanystart.parser.TupletParser.parse(n)
        case n: Elem if n.label == "tupletSpan"   => mei.allanystart.parser.TupletSpanParser.parse(n)
        case n: Elem if n.label == "turn"         => mei.allanystart.parser.TurnParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "verse"        => mei.allanystart.parser.VerseParser.parse(n)
        case n: Elem if n.label == "volta"        => mei.allanystart.parser.VoltaParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("hand").map(_.text),
      node.attribute("seq").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cause").map(_.text),
      node.attribute("xlink:actuate").map(_.text),
      node.attribute("xlink:role").map(_.text),
      node.attribute("xlink:show").map(_.text),
      node.attribute("target").map(_.text),
      node.attribute("targettype").map(_.text),
      node.attribute("evaluate").map(_.text),
    )
  }
}
object RdgParser extends MeiModelDummyParser {
  def parse(node: Node): Rdg = {
    Rdg(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "accid"        => mei.allanystart.parser.AccidParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "app"          => mei.allanystart.parser.AppParser.parse(n)
        case n: Elem if n.label == "arpeg"        => mei.allanystart.parser.ArpegParser.parse(n)
        case n: Elem if n.label == "artic"        => mei.allanystart.parser.ArticParser.parse(n)
        case n: Elem if n.label == "attacca"      => mei.allanystart.parser.AttaccaParser.parse(n)
        case n: Elem if n.label == "bTrem"        => mei.allanystart.parser.BTremParser.parse(n)
        case n: Elem if n.label == "barLine"      => mei.allanystart.parser.BarLineParser.parse(n)
        case n: Elem if n.label == "beam"         => mei.allanystart.parser.BeamParser.parse(n)
        case n: Elem if n.label == "beamSpan"     => mei.allanystart.parser.BeamSpanParser.parse(n)
        case n: Elem if n.label == "beatRpt"      => mei.allanystart.parser.BeatRptParser.parse(n)
        case n: Elem if n.label == "bend"         => mei.allanystart.parser.BendParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblList"     => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "bracketSpan"  => mei.allanystart.parser.BracketSpanParser.parse(n)
        case n: Elem if n.label == "breath"       => mei.allanystart.parser.BreathParser.parse(n)
        case n: Elem if n.label == "caesura"      => mei.allanystart.parser.CaesuraParser.parse(n)
        case n: Elem if n.label == "castList"     => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "cb"           => mei.allanystart.parser.CbParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "chord"        => mei.allanystart.parser.ChordParser.parse(n)
        case n: Elem if n.label == "clef"         => mei.allanystart.parser.ClefParser.parse(n)
        case n: Elem if n.label == "clefGrp"      => mei.allanystart.parser.ClefGrpParser.parse(n)
        case n: Elem if n.label == "colLayout"    => mei.allanystart.parser.ColLayoutParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "cpMark"       => mei.allanystart.parser.CpMarkParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "custos"       => mei.allanystart.parser.CustosParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "dir"          => mei.allanystart.parser.DirParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "div"          => mei.allanystart.parser.DivParser.parse(n)
        case n: Elem if n.label == "divLine"      => mei.allanystart.parser.DivLineParser.parse(n)
        case n: Elem if n.label == "dot"          => mei.allanystart.parser.DotParser.parse(n)
        case n: Elem if n.label == "dynam"        => mei.allanystart.parser.DynamParser.parse(n)
        case n: Elem if n.label == "ending"       => mei.allanystart.parser.EndingParser.parse(n)
        case n: Elem if n.label == "episema"      => mei.allanystart.parser.EpisemaParser.parse(n)
        case n: Elem if n.label == "eventList"    => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "expansion"    => mei.allanystart.parser.ExpansionParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "f"            => mei.allanystart.parser.FParser.parse(n)
        case n: Elem if n.label == "fTrem"        => mei.allanystart.parser.FTremParser.parse(n)
        case n: Elem if n.label == "fermata"      => mei.allanystart.parser.FermataParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "fing"         => mei.allanystart.parser.FingParser.parse(n)
        case n: Elem if n.label == "fingGrp"      => mei.allanystart.parser.FingGrpParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "gliss"        => mei.allanystart.parser.GlissParser.parse(n)
        case n: Elem if n.label == "graceGrp"     => mei.allanystart.parser.GraceGrpParser.parse(n)
        case n: Elem if n.label == "hairpin"      => mei.allanystart.parser.HairpinParser.parse(n)
        case n: Elem if n.label == "halfmRpt"     => mei.allanystart.parser.HalfmRptParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "harm"         => mei.allanystart.parser.HarmParser.parse(n)
        case n: Elem if n.label == "harpPedal"    => mei.allanystart.parser.HarpPedalParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "hispanTick"   => mei.allanystart.parser.HispanTickParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "keySig"       => mei.allanystart.parser.KeySigParser.parse(n)
        case n: Elem if n.label == "layer"        => mei.allanystart.parser.LayerParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "lg"           => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "ligature"     => mei.allanystart.parser.LigatureParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "liquescent"   => mei.allanystart.parser.LiquescentParser.parse(n)
        case n: Elem if n.label == "list"         => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "lv"           => mei.allanystart.parser.LvParser.parse(n)
        case n: Elem if n.label == "mRest"        => mei.allanystart.parser.MRestParser.parse(n)
        case n: Elem if n.label == "mRpt"         => mei.allanystart.parser.MRptParser.parse(n)
        case n: Elem if n.label == "mRpt2"        => mei.allanystart.parser.MRpt2Parser.parse(n)
        case n: Elem if n.label == "mSpace"       => mei.allanystart.parser.MSpaceParser.parse(n)
        case n: Elem if n.label == "measure"      => mei.allanystart.parser.MeasureParser.parse(n)
        case n: Elem if n.label == "mensur"       => mei.allanystart.parser.MensurParser.parse(n)
        case n: Elem if n.label == "metaMark"     => mei.allanystart.parser.MetaMarkParser.parse(n)
        case n: Elem if n.label == "meterSig"     => mei.allanystart.parser.MeterSigParser.parse(n)
        case n: Elem if n.label == "meterSigGrp"  => mei.allanystart.parser.MeterSigGrpParser.parse(n)
        case n: Elem if n.label == "midi"         => mei.allanystart.parser.MidiParser.parse(n)
        case n: Elem if n.label == "mordent"      => mei.allanystart.parser.MordentParser.parse(n)
        case n: Elem if n.label == "multiRest"    => mei.allanystart.parser.MultiRestParser.parse(n)
        case n: Elem if n.label == "multiRpt"     => mei.allanystart.parser.MultiRptParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "nc"           => mei.allanystart.parser.NcParser.parse(n)
        case n: Elem if n.label == "ncGrp"        => mei.allanystart.parser.NcGrpParser.parse(n)
        case n: Elem if n.label == "neume"        => mei.allanystart.parser.NeumeParser.parse(n)
        case n: Elem if n.label == "note"         => mei.allanystart.parser.NoteParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "octave"       => mei.allanystart.parser.OctaveParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "oriscus"      => mei.allanystart.parser.OriscusParser.parse(n)
        case n: Elem if n.label == "ornam"        => mei.allanystart.parser.OrnamParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "pad"          => mei.allanystart.parser.PadParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "pedal"        => mei.allanystart.parser.PedalParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "phrase"       => mei.allanystart.parser.PhraseParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "proport"      => mei.allanystart.parser.ProportParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "quilisma"     => mei.allanystart.parser.QuilismaParser.parse(n)
        case n: Elem if n.label == "quote"        => mei.allanystart.parser.QuoteParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "refrain"      => mei.allanystart.parser.RefrainParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "reh"          => mei.allanystart.parser.RehParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repeatMark"   => mei.allanystart.parser.RepeatMarkParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "rest"         => mei.allanystart.parser.RestParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sb"           => mei.allanystart.parser.SbParser.parse(n)
        case n: Elem if n.label == "scoreDef"     => mei.allanystart.parser.ScoreDefParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "section"      => mei.allanystart.parser.SectionParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "signifLet"    => mei.allanystart.parser.SignifLetParser.parse(n)
        case n: Elem if n.label == "slur"         => mei.allanystart.parser.SlurParser.parse(n)
        case n: Elem if n.label == "sp"           => mei.allanystart.parser.SpParser.parse(n)
        case n: Elem if n.label == "space"        => mei.allanystart.parser.SpaceParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "staff"        => mei.allanystart.parser.StaffParser.parse(n)
        case n: Elem if n.label == "staffDef"     => mei.allanystart.parser.StaffDefParser.parse(n)
        case n: Elem if n.label == "staffGrp"     => mei.allanystart.parser.StaffGrpParser.parse(n)
        case n: Elem if n.label == "stageDir"     => mei.allanystart.parser.StageDirParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "strophicus"   => mei.allanystart.parser.StrophicusParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "syl"          => mei.allanystart.parser.SylParser.parse(n)
        case n: Elem if n.label == "syllable"     => mei.allanystart.parser.SyllableParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "tabGrp"       => mei.allanystart.parser.TabGrpParser.parse(n)
        case n: Elem if n.label == "table"        => mei.allanystart.parser.TableParser.parse(n)
        case n: Elem if n.label == "tempo"        => mei.allanystart.parser.TempoParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "tie"          => mei.allanystart.parser.TieParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "trill"        => mei.allanystart.parser.TrillParser.parse(n)
        case n: Elem if n.label == "tuplet"       => mei.allanystart.parser.TupletParser.parse(n)
        case n: Elem if n.label == "tupletSpan"   => mei.allanystart.parser.TupletSpanParser.parse(n)
        case n: Elem if n.label == "turn"         => mei.allanystart.parser.TurnParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "verse"        => mei.allanystart.parser.VerseParser.parse(n)
        case n: Elem if n.label == "volta"        => mei.allanystart.parser.VoltaParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("hand").map(_.text),
      node.attribute("seq").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cause").map(_.text),
      node.attribute("xlink:actuate").map(_.text),
      node.attribute("xlink:role").map(_.text),
      node.attribute("xlink:show").map(_.text),
      node.attribute("target").map(_.text),
      node.attribute("targettype").map(_.text),
      node.attribute("evaluate").map(_.text),
    )
  }
}
object SpParser extends MeiModelDummyParser {
  def parse(node: Node): Sp = {
    Sp(
      node.child.collect {
        case n: Elem if n.label == "annot"     => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "app"       => mei.allanystart.parser.AppParser.parse(n)
        case n: Elem if n.label == "cb"        => mei.allanystart.parser.CbParser.parse(n)
        case n: Elem if n.label == "colLayout" => mei.allanystart.parser.ColLayoutParser.parse(n)
        case n: Elem if n.label == "fig"       => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "l"         => mei.allanystart.parser.LParser.parse(n)
        case n: Elem if n.label == "lb"        => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "lg"        => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "list"      => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "p"         => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "pb"        => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "quote"     => mei.allanystart.parser.QuoteParser.parse(n)
        case n: Elem if n.label == "speaker"   => mei.allanystart.parser.SpeakerParser.parse(n)
        case n: Elem if n.label == "stageDir"  => mei.allanystart.parser.StageDirParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("tstamp2.ges").map(_.text),
      node.attribute("tstamp2.real").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("tstamp2").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("startho").map(_.text),
      node.attribute("endho").map(_.text),
      node.attribute("startto").map(_.text),
      node.attribute("endto").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object StageDirParser extends MeiModelDummyParser {
  def parse(node: Node): StageDir = {
    StageDir(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("tstamp2.ges").map(_.text),
      node.attribute("tstamp2.real").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("tstamp2").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("startho").map(_.text),
      node.attribute("endho").map(_.text),
      node.attribute("startto").map(_.text),
      node.attribute("endto").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object AbbrParser extends MeiModelDummyParser {
  def parse(node: Node): Abbr = {
    Abbr(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "accid"        => mei.allanystart.parser.AccidParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "arpeg"        => mei.allanystart.parser.ArpegParser.parse(n)
        case n: Elem if n.label == "artic"        => mei.allanystart.parser.ArticParser.parse(n)
        case n: Elem if n.label == "attacca"      => mei.allanystart.parser.AttaccaParser.parse(n)
        case n: Elem if n.label == "bTrem"        => mei.allanystart.parser.BTremParser.parse(n)
        case n: Elem if n.label == "barLine"      => mei.allanystart.parser.BarLineParser.parse(n)
        case n: Elem if n.label == "beam"         => mei.allanystart.parser.BeamParser.parse(n)
        case n: Elem if n.label == "beamSpan"     => mei.allanystart.parser.BeamSpanParser.parse(n)
        case n: Elem if n.label == "beatRpt"      => mei.allanystart.parser.BeatRptParser.parse(n)
        case n: Elem if n.label == "bend"         => mei.allanystart.parser.BendParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblList"     => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "bracketSpan"  => mei.allanystart.parser.BracketSpanParser.parse(n)
        case n: Elem if n.label == "breath"       => mei.allanystart.parser.BreathParser.parse(n)
        case n: Elem if n.label == "caesura"      => mei.allanystart.parser.CaesuraParser.parse(n)
        case n: Elem if n.label == "castList"     => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "cb"           => mei.allanystart.parser.CbParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "chord"        => mei.allanystart.parser.ChordParser.parse(n)
        case n: Elem if n.label == "clef"         => mei.allanystart.parser.ClefParser.parse(n)
        case n: Elem if n.label == "clefGrp"      => mei.allanystart.parser.ClefGrpParser.parse(n)
        case n: Elem if n.label == "colLayout"    => mei.allanystart.parser.ColLayoutParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "cpMark"       => mei.allanystart.parser.CpMarkParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "custos"       => mei.allanystart.parser.CustosParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "dir"          => mei.allanystart.parser.DirParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "div"          => mei.allanystart.parser.DivParser.parse(n)
        case n: Elem if n.label == "divLine"      => mei.allanystart.parser.DivLineParser.parse(n)
        case n: Elem if n.label == "dot"          => mei.allanystart.parser.DotParser.parse(n)
        case n: Elem if n.label == "dynam"        => mei.allanystart.parser.DynamParser.parse(n)
        case n: Elem if n.label == "ending"       => mei.allanystart.parser.EndingParser.parse(n)
        case n: Elem if n.label == "episema"      => mei.allanystart.parser.EpisemaParser.parse(n)
        case n: Elem if n.label == "eventList"    => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "f"            => mei.allanystart.parser.FParser.parse(n)
        case n: Elem if n.label == "fTrem"        => mei.allanystart.parser.FTremParser.parse(n)
        case n: Elem if n.label == "fermata"      => mei.allanystart.parser.FermataParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "fing"         => mei.allanystart.parser.FingParser.parse(n)
        case n: Elem if n.label == "fingGrp"      => mei.allanystart.parser.FingGrpParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "gliss"        => mei.allanystart.parser.GlissParser.parse(n)
        case n: Elem if n.label == "graceGrp"     => mei.allanystart.parser.GraceGrpParser.parse(n)
        case n: Elem if n.label == "hairpin"      => mei.allanystart.parser.HairpinParser.parse(n)
        case n: Elem if n.label == "halfmRpt"     => mei.allanystart.parser.HalfmRptParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "harm"         => mei.allanystart.parser.HarmParser.parse(n)
        case n: Elem if n.label == "harpPedal"    => mei.allanystart.parser.HarpPedalParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "hispanTick"   => mei.allanystart.parser.HispanTickParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "keySig"       => mei.allanystart.parser.KeySigParser.parse(n)
        case n: Elem if n.label == "layer"        => mei.allanystart.parser.LayerParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "lg"           => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "ligature"     => mei.allanystart.parser.LigatureParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "liquescent"   => mei.allanystart.parser.LiquescentParser.parse(n)
        case n: Elem if n.label == "list"         => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "lv"           => mei.allanystart.parser.LvParser.parse(n)
        case n: Elem if n.label == "mRest"        => mei.allanystart.parser.MRestParser.parse(n)
        case n: Elem if n.label == "mRpt"         => mei.allanystart.parser.MRptParser.parse(n)
        case n: Elem if n.label == "mRpt2"        => mei.allanystart.parser.MRpt2Parser.parse(n)
        case n: Elem if n.label == "mSpace"       => mei.allanystart.parser.MSpaceParser.parse(n)
        case n: Elem if n.label == "measure"      => mei.allanystart.parser.MeasureParser.parse(n)
        case n: Elem if n.label == "mensur"       => mei.allanystart.parser.MensurParser.parse(n)
        case n: Elem if n.label == "metaMark"     => mei.allanystart.parser.MetaMarkParser.parse(n)
        case n: Elem if n.label == "meterSig"     => mei.allanystart.parser.MeterSigParser.parse(n)
        case n: Elem if n.label == "meterSigGrp"  => mei.allanystart.parser.MeterSigGrpParser.parse(n)
        case n: Elem if n.label == "midi"         => mei.allanystart.parser.MidiParser.parse(n)
        case n: Elem if n.label == "mordent"      => mei.allanystart.parser.MordentParser.parse(n)
        case n: Elem if n.label == "multiRest"    => mei.allanystart.parser.MultiRestParser.parse(n)
        case n: Elem if n.label == "multiRpt"     => mei.allanystart.parser.MultiRptParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "nc"           => mei.allanystart.parser.NcParser.parse(n)
        case n: Elem if n.label == "ncGrp"        => mei.allanystart.parser.NcGrpParser.parse(n)
        case n: Elem if n.label == "neume"        => mei.allanystart.parser.NeumeParser.parse(n)
        case n: Elem if n.label == "note"         => mei.allanystart.parser.NoteParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "octave"       => mei.allanystart.parser.OctaveParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "oriscus"      => mei.allanystart.parser.OriscusParser.parse(n)
        case n: Elem if n.label == "ornam"        => mei.allanystart.parser.OrnamParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "pad"          => mei.allanystart.parser.PadParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "pedal"        => mei.allanystart.parser.PedalParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "phrase"       => mei.allanystart.parser.PhraseParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "proport"      => mei.allanystart.parser.ProportParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "quilisma"     => mei.allanystart.parser.QuilismaParser.parse(n)
        case n: Elem if n.label == "quote"        => mei.allanystart.parser.QuoteParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "refrain"      => mei.allanystart.parser.RefrainParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "reh"          => mei.allanystart.parser.RehParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repeatMark"   => mei.allanystart.parser.RepeatMarkParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "rest"         => mei.allanystart.parser.RestParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sb"           => mei.allanystart.parser.SbParser.parse(n)
        case n: Elem if n.label == "scoreDef"     => mei.allanystart.parser.ScoreDefParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "section"      => mei.allanystart.parser.SectionParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "signifLet"    => mei.allanystart.parser.SignifLetParser.parse(n)
        case n: Elem if n.label == "slur"         => mei.allanystart.parser.SlurParser.parse(n)
        case n: Elem if n.label == "sp"           => mei.allanystart.parser.SpParser.parse(n)
        case n: Elem if n.label == "space"        => mei.allanystart.parser.SpaceParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "staff"        => mei.allanystart.parser.StaffParser.parse(n)
        case n: Elem if n.label == "staffDef"     => mei.allanystart.parser.StaffDefParser.parse(n)
        case n: Elem if n.label == "staffGrp"     => mei.allanystart.parser.StaffGrpParser.parse(n)
        case n: Elem if n.label == "stageDir"     => mei.allanystart.parser.StageDirParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "strophicus"   => mei.allanystart.parser.StrophicusParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "syl"          => mei.allanystart.parser.SylParser.parse(n)
        case n: Elem if n.label == "syllable"     => mei.allanystart.parser.SyllableParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "tabGrp"       => mei.allanystart.parser.TabGrpParser.parse(n)
        case n: Elem if n.label == "table"        => mei.allanystart.parser.TableParser.parse(n)
        case n: Elem if n.label == "tempo"        => mei.allanystart.parser.TempoParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "tie"          => mei.allanystart.parser.TieParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "trill"        => mei.allanystart.parser.TrillParser.parse(n)
        case n: Elem if n.label == "tuplet"       => mei.allanystart.parser.TupletParser.parse(n)
        case n: Elem if n.label == "tupletSpan"   => mei.allanystart.parser.TupletSpanParser.parse(n)
        case n: Elem if n.label == "turn"         => mei.allanystart.parser.TurnParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "verse"        => mei.allanystart.parser.VerseParser.parse(n)
        case n: Elem if n.label == "volta"        => mei.allanystart.parser.VoltaParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("instant").map(_.text),
      node.attribute("state").map(_.text),
      node.attribute("hand").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("seq").map(_.text),
      node.attribute("expan").map(_.text),
    )
  }
}
object AddParser extends MeiModelDummyParser {
  def parse(node: Node): Add = {
    Add(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "accid"        => mei.allanystart.parser.AccidParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "arpeg"        => mei.allanystart.parser.ArpegParser.parse(n)
        case n: Elem if n.label == "artic"        => mei.allanystart.parser.ArticParser.parse(n)
        case n: Elem if n.label == "attacca"      => mei.allanystart.parser.AttaccaParser.parse(n)
        case n: Elem if n.label == "bTrem"        => mei.allanystart.parser.BTremParser.parse(n)
        case n: Elem if n.label == "barLine"      => mei.allanystart.parser.BarLineParser.parse(n)
        case n: Elem if n.label == "beam"         => mei.allanystart.parser.BeamParser.parse(n)
        case n: Elem if n.label == "beamSpan"     => mei.allanystart.parser.BeamSpanParser.parse(n)
        case n: Elem if n.label == "beatRpt"      => mei.allanystart.parser.BeatRptParser.parse(n)
        case n: Elem if n.label == "bend"         => mei.allanystart.parser.BendParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblList"     => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "bracketSpan"  => mei.allanystart.parser.BracketSpanParser.parse(n)
        case n: Elem if n.label == "breath"       => mei.allanystart.parser.BreathParser.parse(n)
        case n: Elem if n.label == "caesura"      => mei.allanystart.parser.CaesuraParser.parse(n)
        case n: Elem if n.label == "castList"     => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "cb"           => mei.allanystart.parser.CbParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "chord"        => mei.allanystart.parser.ChordParser.parse(n)
        case n: Elem if n.label == "clef"         => mei.allanystart.parser.ClefParser.parse(n)
        case n: Elem if n.label == "clefGrp"      => mei.allanystart.parser.ClefGrpParser.parse(n)
        case n: Elem if n.label == "colLayout"    => mei.allanystart.parser.ColLayoutParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "cpMark"       => mei.allanystart.parser.CpMarkParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "custos"       => mei.allanystart.parser.CustosParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "dir"          => mei.allanystart.parser.DirParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "div"          => mei.allanystart.parser.DivParser.parse(n)
        case n: Elem if n.label == "divLine"      => mei.allanystart.parser.DivLineParser.parse(n)
        case n: Elem if n.label == "dot"          => mei.allanystart.parser.DotParser.parse(n)
        case n: Elem if n.label == "dynam"        => mei.allanystart.parser.DynamParser.parse(n)
        case n: Elem if n.label == "ending"       => mei.allanystart.parser.EndingParser.parse(n)
        case n: Elem if n.label == "eventList"    => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "f"            => mei.allanystart.parser.FParser.parse(n)
        case n: Elem if n.label == "fTrem"        => mei.allanystart.parser.FTremParser.parse(n)
        case n: Elem if n.label == "fermata"      => mei.allanystart.parser.FermataParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "fing"         => mei.allanystart.parser.FingParser.parse(n)
        case n: Elem if n.label == "fingGrp"      => mei.allanystart.parser.FingGrpParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "gliss"        => mei.allanystart.parser.GlissParser.parse(n)
        case n: Elem if n.label == "graceGrp"     => mei.allanystart.parser.GraceGrpParser.parse(n)
        case n: Elem if n.label == "hairpin"      => mei.allanystart.parser.HairpinParser.parse(n)
        case n: Elem if n.label == "halfmRpt"     => mei.allanystart.parser.HalfmRptParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "harm"         => mei.allanystart.parser.HarmParser.parse(n)
        case n: Elem if n.label == "harpPedal"    => mei.allanystart.parser.HarpPedalParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "keyAccid"     => mei.allanystart.parser.KeyAccidParser.parse(n)
        case n: Elem if n.label == "keySig"       => mei.allanystart.parser.KeySigParser.parse(n)
        case n: Elem if n.label == "layer"        => mei.allanystart.parser.LayerParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "lg"           => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "ligature"     => mei.allanystart.parser.LigatureParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "list"         => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "lv"           => mei.allanystart.parser.LvParser.parse(n)
        case n: Elem if n.label == "mRest"        => mei.allanystart.parser.MRestParser.parse(n)
        case n: Elem if n.label == "mRpt"         => mei.allanystart.parser.MRptParser.parse(n)
        case n: Elem if n.label == "mRpt2"        => mei.allanystart.parser.MRpt2Parser.parse(n)
        case n: Elem if n.label == "mSpace"       => mei.allanystart.parser.MSpaceParser.parse(n)
        case n: Elem if n.label == "measure"      => mei.allanystart.parser.MeasureParser.parse(n)
        case n: Elem if n.label == "mensur"       => mei.allanystart.parser.MensurParser.parse(n)
        case n: Elem if n.label == "metaMark"     => mei.allanystart.parser.MetaMarkParser.parse(n)
        case n: Elem if n.label == "meterSig"     => mei.allanystart.parser.MeterSigParser.parse(n)
        case n: Elem if n.label == "meterSigGrp"  => mei.allanystart.parser.MeterSigGrpParser.parse(n)
        case n: Elem if n.label == "midi"         => mei.allanystart.parser.MidiParser.parse(n)
        case n: Elem if n.label == "mordent"      => mei.allanystart.parser.MordentParser.parse(n)
        case n: Elem if n.label == "multiRest"    => mei.allanystart.parser.MultiRestParser.parse(n)
        case n: Elem if n.label == "multiRpt"     => mei.allanystart.parser.MultiRptParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "neume"        => mei.allanystart.parser.NeumeParser.parse(n)
        case n: Elem if n.label == "note"         => mei.allanystart.parser.NoteParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "octave"       => mei.allanystart.parser.OctaveParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "ornam"        => mei.allanystart.parser.OrnamParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "pad"          => mei.allanystart.parser.PadParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "pedal"        => mei.allanystart.parser.PedalParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "phrase"       => mei.allanystart.parser.PhraseParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "proport"      => mei.allanystart.parser.ProportParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "quote"        => mei.allanystart.parser.QuoteParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "refrain"      => mei.allanystart.parser.RefrainParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "reh"          => mei.allanystart.parser.RehParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repeatMark"   => mei.allanystart.parser.RepeatMarkParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "rest"         => mei.allanystart.parser.RestParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sb"           => mei.allanystart.parser.SbParser.parse(n)
        case n: Elem if n.label == "scoreDef"     => mei.allanystart.parser.ScoreDefParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "section"      => mei.allanystart.parser.SectionParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "slur"         => mei.allanystart.parser.SlurParser.parse(n)
        case n: Elem if n.label == "sp"           => mei.allanystart.parser.SpParser.parse(n)
        case n: Elem if n.label == "space"        => mei.allanystart.parser.SpaceParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "staff"        => mei.allanystart.parser.StaffParser.parse(n)
        case n: Elem if n.label == "staffDef"     => mei.allanystart.parser.StaffDefParser.parse(n)
        case n: Elem if n.label == "staffGrp"     => mei.allanystart.parser.StaffGrpParser.parse(n)
        case n: Elem if n.label == "stageDir"     => mei.allanystart.parser.StageDirParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "syl"          => mei.allanystart.parser.SylParser.parse(n)
        case n: Elem if n.label == "syllable"     => mei.allanystart.parser.SyllableParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "tabGrp"       => mei.allanystart.parser.TabGrpParser.parse(n)
        case n: Elem if n.label == "table"        => mei.allanystart.parser.TableParser.parse(n)
        case n: Elem if n.label == "tempo"        => mei.allanystart.parser.TempoParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "tie"          => mei.allanystart.parser.TieParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "trill"        => mei.allanystart.parser.TrillParser.parse(n)
        case n: Elem if n.label == "tuplet"       => mei.allanystart.parser.TupletParser.parse(n)
        case n: Elem if n.label == "tupletSpan"   => mei.allanystart.parser.TupletSpanParser.parse(n)
        case n: Elem if n.label == "turn"         => mei.allanystart.parser.TurnParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "verse"        => mei.allanystart.parser.VerseParser.parse(n)
        case n: Elem if n.label == "volta"        => mei.allanystart.parser.VoltaParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("unit").map(_.text),
      node.attribute("atleast").map(_.text),
      node.attribute("atmost").map(_.text),
      node.attribute("min").map(_.text),
      node.attribute("max").map(_.text),
      node.attribute("confidence").map(_.text),
      node.attribute("extent").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("instant").map(_.text),
      node.attribute("state").map(_.text),
      node.attribute("hand").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("seq").map(_.text),
      node.attribute("place").map(_.text),
    )
  }
}
object ChoiceParser extends MeiModelDummyParser {
  def parse(node: Node): Choice = {
    Choice(
      node.child.collect {
        case n: Elem if n.label == "abbr"    => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "choice"  => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corr"    => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "expan"   => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "orig"    => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "reg"     => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "sic"     => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "subst"   => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "unclear" => mei.allanystart.parser.UnclearParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
    )
  }
}
object CorrParser extends MeiModelDummyParser {
  def parse(node: Node): Corr = {
    Corr(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "accid"        => mei.allanystart.parser.AccidParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "arpeg"        => mei.allanystart.parser.ArpegParser.parse(n)
        case n: Elem if n.label == "artic"        => mei.allanystart.parser.ArticParser.parse(n)
        case n: Elem if n.label == "attacca"      => mei.allanystart.parser.AttaccaParser.parse(n)
        case n: Elem if n.label == "bTrem"        => mei.allanystart.parser.BTremParser.parse(n)
        case n: Elem if n.label == "barLine"      => mei.allanystart.parser.BarLineParser.parse(n)
        case n: Elem if n.label == "beam"         => mei.allanystart.parser.BeamParser.parse(n)
        case n: Elem if n.label == "beamSpan"     => mei.allanystart.parser.BeamSpanParser.parse(n)
        case n: Elem if n.label == "beatRpt"      => mei.allanystart.parser.BeatRptParser.parse(n)
        case n: Elem if n.label == "bend"         => mei.allanystart.parser.BendParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblList"     => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "bracketSpan"  => mei.allanystart.parser.BracketSpanParser.parse(n)
        case n: Elem if n.label == "breath"       => mei.allanystart.parser.BreathParser.parse(n)
        case n: Elem if n.label == "caesura"      => mei.allanystart.parser.CaesuraParser.parse(n)
        case n: Elem if n.label == "castList"     => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "cb"           => mei.allanystart.parser.CbParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "chord"        => mei.allanystart.parser.ChordParser.parse(n)
        case n: Elem if n.label == "clef"         => mei.allanystart.parser.ClefParser.parse(n)
        case n: Elem if n.label == "clefGrp"      => mei.allanystart.parser.ClefGrpParser.parse(n)
        case n: Elem if n.label == "colLayout"    => mei.allanystart.parser.ColLayoutParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "cpMark"       => mei.allanystart.parser.CpMarkParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "custos"       => mei.allanystart.parser.CustosParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "dir"          => mei.allanystart.parser.DirParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "div"          => mei.allanystart.parser.DivParser.parse(n)
        case n: Elem if n.label == "divLine"      => mei.allanystart.parser.DivLineParser.parse(n)
        case n: Elem if n.label == "dot"          => mei.allanystart.parser.DotParser.parse(n)
        case n: Elem if n.label == "dynam"        => mei.allanystart.parser.DynamParser.parse(n)
        case n: Elem if n.label == "ending"       => mei.allanystart.parser.EndingParser.parse(n)
        case n: Elem if n.label == "episema"      => mei.allanystart.parser.EpisemaParser.parse(n)
        case n: Elem if n.label == "eventList"    => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "f"            => mei.allanystart.parser.FParser.parse(n)
        case n: Elem if n.label == "fTrem"        => mei.allanystart.parser.FTremParser.parse(n)
        case n: Elem if n.label == "fermata"      => mei.allanystart.parser.FermataParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "fing"         => mei.allanystart.parser.FingParser.parse(n)
        case n: Elem if n.label == "fingGrp"      => mei.allanystart.parser.FingGrpParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "gliss"        => mei.allanystart.parser.GlissParser.parse(n)
        case n: Elem if n.label == "graceGrp"     => mei.allanystart.parser.GraceGrpParser.parse(n)
        case n: Elem if n.label == "hairpin"      => mei.allanystart.parser.HairpinParser.parse(n)
        case n: Elem if n.label == "halfmRpt"     => mei.allanystart.parser.HalfmRptParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "harm"         => mei.allanystart.parser.HarmParser.parse(n)
        case n: Elem if n.label == "harpPedal"    => mei.allanystart.parser.HarpPedalParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "hispanTick"   => mei.allanystart.parser.HispanTickParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "keyAccid"     => mei.allanystart.parser.KeyAccidParser.parse(n)
        case n: Elem if n.label == "keySig"       => mei.allanystart.parser.KeySigParser.parse(n)
        case n: Elem if n.label == "layer"        => mei.allanystart.parser.LayerParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "lg"           => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "ligature"     => mei.allanystart.parser.LigatureParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "liquescent"   => mei.allanystart.parser.LiquescentParser.parse(n)
        case n: Elem if n.label == "list"         => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "lv"           => mei.allanystart.parser.LvParser.parse(n)
        case n: Elem if n.label == "mRest"        => mei.allanystart.parser.MRestParser.parse(n)
        case n: Elem if n.label == "mRpt"         => mei.allanystart.parser.MRptParser.parse(n)
        case n: Elem if n.label == "mRpt2"        => mei.allanystart.parser.MRpt2Parser.parse(n)
        case n: Elem if n.label == "mSpace"       => mei.allanystart.parser.MSpaceParser.parse(n)
        case n: Elem if n.label == "measure"      => mei.allanystart.parser.MeasureParser.parse(n)
        case n: Elem if n.label == "mensur"       => mei.allanystart.parser.MensurParser.parse(n)
        case n: Elem if n.label == "metaMark"     => mei.allanystart.parser.MetaMarkParser.parse(n)
        case n: Elem if n.label == "meterSig"     => mei.allanystart.parser.MeterSigParser.parse(n)
        case n: Elem if n.label == "meterSigGrp"  => mei.allanystart.parser.MeterSigGrpParser.parse(n)
        case n: Elem if n.label == "midi"         => mei.allanystart.parser.MidiParser.parse(n)
        case n: Elem if n.label == "mordent"      => mei.allanystart.parser.MordentParser.parse(n)
        case n: Elem if n.label == "multiRest"    => mei.allanystart.parser.MultiRestParser.parse(n)
        case n: Elem if n.label == "multiRpt"     => mei.allanystart.parser.MultiRptParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "nc"           => mei.allanystart.parser.NcParser.parse(n)
        case n: Elem if n.label == "ncGrp"        => mei.allanystart.parser.NcGrpParser.parse(n)
        case n: Elem if n.label == "neume"        => mei.allanystart.parser.NeumeParser.parse(n)
        case n: Elem if n.label == "note"         => mei.allanystart.parser.NoteParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "octave"       => mei.allanystart.parser.OctaveParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "oriscus"      => mei.allanystart.parser.OriscusParser.parse(n)
        case n: Elem if n.label == "ornam"        => mei.allanystart.parser.OrnamParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "pad"          => mei.allanystart.parser.PadParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "pedal"        => mei.allanystart.parser.PedalParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "phrase"       => mei.allanystart.parser.PhraseParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "proport"      => mei.allanystart.parser.ProportParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "quilisma"     => mei.allanystart.parser.QuilismaParser.parse(n)
        case n: Elem if n.label == "quote"        => mei.allanystart.parser.QuoteParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "refrain"      => mei.allanystart.parser.RefrainParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "reh"          => mei.allanystart.parser.RehParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repeatMark"   => mei.allanystart.parser.RepeatMarkParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "rest"         => mei.allanystart.parser.RestParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sb"           => mei.allanystart.parser.SbParser.parse(n)
        case n: Elem if n.label == "scoreDef"     => mei.allanystart.parser.ScoreDefParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "section"      => mei.allanystart.parser.SectionParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "signifLet"    => mei.allanystart.parser.SignifLetParser.parse(n)
        case n: Elem if n.label == "slur"         => mei.allanystart.parser.SlurParser.parse(n)
        case n: Elem if n.label == "sp"           => mei.allanystart.parser.SpParser.parse(n)
        case n: Elem if n.label == "space"        => mei.allanystart.parser.SpaceParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "staff"        => mei.allanystart.parser.StaffParser.parse(n)
        case n: Elem if n.label == "staffDef"     => mei.allanystart.parser.StaffDefParser.parse(n)
        case n: Elem if n.label == "staffGrp"     => mei.allanystart.parser.StaffGrpParser.parse(n)
        case n: Elem if n.label == "stageDir"     => mei.allanystart.parser.StageDirParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "strophicus"   => mei.allanystart.parser.StrophicusParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "syl"          => mei.allanystart.parser.SylParser.parse(n)
        case n: Elem if n.label == "syllable"     => mei.allanystart.parser.SyllableParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "tabGrp"       => mei.allanystart.parser.TabGrpParser.parse(n)
        case n: Elem if n.label == "table"        => mei.allanystart.parser.TableParser.parse(n)
        case n: Elem if n.label == "tempo"        => mei.allanystart.parser.TempoParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "tie"          => mei.allanystart.parser.TieParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "trill"        => mei.allanystart.parser.TrillParser.parse(n)
        case n: Elem if n.label == "tuplet"       => mei.allanystart.parser.TupletParser.parse(n)
        case n: Elem if n.label == "tupletSpan"   => mei.allanystart.parser.TupletSpanParser.parse(n)
        case n: Elem if n.label == "turn"         => mei.allanystart.parser.TurnParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "verse"        => mei.allanystart.parser.VerseParser.parse(n)
        case n: Elem if n.label == "volta"        => mei.allanystart.parser.VoltaParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("unit").map(_.text),
      node.attribute("atleast").map(_.text),
      node.attribute("atmost").map(_.text),
      node.attribute("min").map(_.text),
      node.attribute("max").map(_.text),
      node.attribute("confidence").map(_.text),
      node.attribute("extent").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("instant").map(_.text),
      node.attribute("state").map(_.text),
      node.attribute("hand").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("seq").map(_.text),
    )
  }
}
object CpMarkParser extends MeiModelDummyParser {
  def parse(node: Node): CpMark = {
    CpMark(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("origin.tstamp").map(_.text),
      node.attribute("origin.tstamp2").map(_.text),
      node.attribute("origin.staff").map(_.text),
      node.attribute("origin.layer").map(_.text),
      node.attribute("origin.startid").map(_.text),
      node.attribute("origin.endid").map(_.text),
      node.attribute("dis").map(_.text),
      node.attribute("dis.place").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("tstamp2").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("enclose").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("vgrp").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
      node.attribute("tstamp2.ges").map(_.text),
      node.attribute("tstamp2.real").map(_.text),
    )
  }
}
object DamageParser extends MeiModelDummyParser {
  def parse(node: Node): Damage = {
    Damage(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "accid"        => mei.allanystart.parser.AccidParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "arpeg"        => mei.allanystart.parser.ArpegParser.parse(n)
        case n: Elem if n.label == "artic"        => mei.allanystart.parser.ArticParser.parse(n)
        case n: Elem if n.label == "attacca"      => mei.allanystart.parser.AttaccaParser.parse(n)
        case n: Elem if n.label == "bTrem"        => mei.allanystart.parser.BTremParser.parse(n)
        case n: Elem if n.label == "barLine"      => mei.allanystart.parser.BarLineParser.parse(n)
        case n: Elem if n.label == "beam"         => mei.allanystart.parser.BeamParser.parse(n)
        case n: Elem if n.label == "beamSpan"     => mei.allanystart.parser.BeamSpanParser.parse(n)
        case n: Elem if n.label == "beatRpt"      => mei.allanystart.parser.BeatRptParser.parse(n)
        case n: Elem if n.label == "bend"         => mei.allanystart.parser.BendParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblList"     => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "bracketSpan"  => mei.allanystart.parser.BracketSpanParser.parse(n)
        case n: Elem if n.label == "breath"       => mei.allanystart.parser.BreathParser.parse(n)
        case n: Elem if n.label == "caesura"      => mei.allanystart.parser.CaesuraParser.parse(n)
        case n: Elem if n.label == "castList"     => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "cb"           => mei.allanystart.parser.CbParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "chord"        => mei.allanystart.parser.ChordParser.parse(n)
        case n: Elem if n.label == "clef"         => mei.allanystart.parser.ClefParser.parse(n)
        case n: Elem if n.label == "clefGrp"      => mei.allanystart.parser.ClefGrpParser.parse(n)
        case n: Elem if n.label == "colLayout"    => mei.allanystart.parser.ColLayoutParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "cpMark"       => mei.allanystart.parser.CpMarkParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "custos"       => mei.allanystart.parser.CustosParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "dir"          => mei.allanystart.parser.DirParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "div"          => mei.allanystart.parser.DivParser.parse(n)
        case n: Elem if n.label == "divLine"      => mei.allanystart.parser.DivLineParser.parse(n)
        case n: Elem if n.label == "dot"          => mei.allanystart.parser.DotParser.parse(n)
        case n: Elem if n.label == "dynam"        => mei.allanystart.parser.DynamParser.parse(n)
        case n: Elem if n.label == "ending"       => mei.allanystart.parser.EndingParser.parse(n)
        case n: Elem if n.label == "episema"      => mei.allanystart.parser.EpisemaParser.parse(n)
        case n: Elem if n.label == "eventList"    => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "f"            => mei.allanystart.parser.FParser.parse(n)
        case n: Elem if n.label == "fTrem"        => mei.allanystart.parser.FTremParser.parse(n)
        case n: Elem if n.label == "fermata"      => mei.allanystart.parser.FermataParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "fing"         => mei.allanystart.parser.FingParser.parse(n)
        case n: Elem if n.label == "fingGrp"      => mei.allanystart.parser.FingGrpParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "gliss"        => mei.allanystart.parser.GlissParser.parse(n)
        case n: Elem if n.label == "graceGrp"     => mei.allanystart.parser.GraceGrpParser.parse(n)
        case n: Elem if n.label == "hairpin"      => mei.allanystart.parser.HairpinParser.parse(n)
        case n: Elem if n.label == "halfmRpt"     => mei.allanystart.parser.HalfmRptParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "harm"         => mei.allanystart.parser.HarmParser.parse(n)
        case n: Elem if n.label == "harpPedal"    => mei.allanystart.parser.HarpPedalParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "hispanTick"   => mei.allanystart.parser.HispanTickParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "keyAccid"     => mei.allanystart.parser.KeyAccidParser.parse(n)
        case n: Elem if n.label == "keySig"       => mei.allanystart.parser.KeySigParser.parse(n)
        case n: Elem if n.label == "layer"        => mei.allanystart.parser.LayerParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "lg"           => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "ligature"     => mei.allanystart.parser.LigatureParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "liquescent"   => mei.allanystart.parser.LiquescentParser.parse(n)
        case n: Elem if n.label == "list"         => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "lv"           => mei.allanystart.parser.LvParser.parse(n)
        case n: Elem if n.label == "mRest"        => mei.allanystart.parser.MRestParser.parse(n)
        case n: Elem if n.label == "mRpt"         => mei.allanystart.parser.MRptParser.parse(n)
        case n: Elem if n.label == "mRpt2"        => mei.allanystart.parser.MRpt2Parser.parse(n)
        case n: Elem if n.label == "mSpace"       => mei.allanystart.parser.MSpaceParser.parse(n)
        case n: Elem if n.label == "measure"      => mei.allanystart.parser.MeasureParser.parse(n)
        case n: Elem if n.label == "mensur"       => mei.allanystart.parser.MensurParser.parse(n)
        case n: Elem if n.label == "metaMark"     => mei.allanystart.parser.MetaMarkParser.parse(n)
        case n: Elem if n.label == "meterSig"     => mei.allanystart.parser.MeterSigParser.parse(n)
        case n: Elem if n.label == "meterSigGrp"  => mei.allanystart.parser.MeterSigGrpParser.parse(n)
        case n: Elem if n.label == "midi"         => mei.allanystart.parser.MidiParser.parse(n)
        case n: Elem if n.label == "mordent"      => mei.allanystart.parser.MordentParser.parse(n)
        case n: Elem if n.label == "multiRest"    => mei.allanystart.parser.MultiRestParser.parse(n)
        case n: Elem if n.label == "multiRpt"     => mei.allanystart.parser.MultiRptParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "nc"           => mei.allanystart.parser.NcParser.parse(n)
        case n: Elem if n.label == "ncGrp"        => mei.allanystart.parser.NcGrpParser.parse(n)
        case n: Elem if n.label == "neume"        => mei.allanystart.parser.NeumeParser.parse(n)
        case n: Elem if n.label == "note"         => mei.allanystart.parser.NoteParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "octave"       => mei.allanystart.parser.OctaveParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "oriscus"      => mei.allanystart.parser.OriscusParser.parse(n)
        case n: Elem if n.label == "ornam"        => mei.allanystart.parser.OrnamParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "pad"          => mei.allanystart.parser.PadParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "pedal"        => mei.allanystart.parser.PedalParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "phrase"       => mei.allanystart.parser.PhraseParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "proport"      => mei.allanystart.parser.ProportParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "quilisma"     => mei.allanystart.parser.QuilismaParser.parse(n)
        case n: Elem if n.label == "quote"        => mei.allanystart.parser.QuoteParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "refrain"      => mei.allanystart.parser.RefrainParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "reh"          => mei.allanystart.parser.RehParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repeatMark"   => mei.allanystart.parser.RepeatMarkParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "rest"         => mei.allanystart.parser.RestParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sb"           => mei.allanystart.parser.SbParser.parse(n)
        case n: Elem if n.label == "scoreDef"     => mei.allanystart.parser.ScoreDefParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "section"      => mei.allanystart.parser.SectionParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "signifLet"    => mei.allanystart.parser.SignifLetParser.parse(n)
        case n: Elem if n.label == "slur"         => mei.allanystart.parser.SlurParser.parse(n)
        case n: Elem if n.label == "sp"           => mei.allanystart.parser.SpParser.parse(n)
        case n: Elem if n.label == "space"        => mei.allanystart.parser.SpaceParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "staff"        => mei.allanystart.parser.StaffParser.parse(n)
        case n: Elem if n.label == "staffDef"     => mei.allanystart.parser.StaffDefParser.parse(n)
        case n: Elem if n.label == "staffGrp"     => mei.allanystart.parser.StaffGrpParser.parse(n)
        case n: Elem if n.label == "stageDir"     => mei.allanystart.parser.StageDirParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "strophicus"   => mei.allanystart.parser.StrophicusParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "syl"          => mei.allanystart.parser.SylParser.parse(n)
        case n: Elem if n.label == "syllable"     => mei.allanystart.parser.SyllableParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "tabGrp"       => mei.allanystart.parser.TabGrpParser.parse(n)
        case n: Elem if n.label == "table"        => mei.allanystart.parser.TableParser.parse(n)
        case n: Elem if n.label == "tempo"        => mei.allanystart.parser.TempoParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "tie"          => mei.allanystart.parser.TieParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "trill"        => mei.allanystart.parser.TrillParser.parse(n)
        case n: Elem if n.label == "tuplet"       => mei.allanystart.parser.TupletParser.parse(n)
        case n: Elem if n.label == "tupletSpan"   => mei.allanystart.parser.TupletSpanParser.parse(n)
        case n: Elem if n.label == "turn"         => mei.allanystart.parser.TurnParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "verse"        => mei.allanystart.parser.VerseParser.parse(n)
        case n: Elem if n.label == "volta"        => mei.allanystart.parser.VoltaParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("agent").map(_.text),
      node.attribute("unit").map(_.text),
      node.attribute("atleast").map(_.text),
      node.attribute("atmost").map(_.text),
      node.attribute("min").map(_.text),
      node.attribute("max").map(_.text),
      node.attribute("confidence").map(_.text),
      node.attribute("extent").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("hand").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("degree").map(_.text),
    )
  }
}
object DelParser extends MeiModelDummyParser {
  def parse(node: Node): Del = {
    Del(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "accid"        => mei.allanystart.parser.AccidParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "arpeg"        => mei.allanystart.parser.ArpegParser.parse(n)
        case n: Elem if n.label == "artic"        => mei.allanystart.parser.ArticParser.parse(n)
        case n: Elem if n.label == "attacca"      => mei.allanystart.parser.AttaccaParser.parse(n)
        case n: Elem if n.label == "bTrem"        => mei.allanystart.parser.BTremParser.parse(n)
        case n: Elem if n.label == "barLine"      => mei.allanystart.parser.BarLineParser.parse(n)
        case n: Elem if n.label == "beam"         => mei.allanystart.parser.BeamParser.parse(n)
        case n: Elem if n.label == "beamSpan"     => mei.allanystart.parser.BeamSpanParser.parse(n)
        case n: Elem if n.label == "beatRpt"      => mei.allanystart.parser.BeatRptParser.parse(n)
        case n: Elem if n.label == "bend"         => mei.allanystart.parser.BendParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblList"     => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "bracketSpan"  => mei.allanystart.parser.BracketSpanParser.parse(n)
        case n: Elem if n.label == "breath"       => mei.allanystart.parser.BreathParser.parse(n)
        case n: Elem if n.label == "caesura"      => mei.allanystart.parser.CaesuraParser.parse(n)
        case n: Elem if n.label == "castList"     => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "cb"           => mei.allanystart.parser.CbParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "chord"        => mei.allanystart.parser.ChordParser.parse(n)
        case n: Elem if n.label == "clef"         => mei.allanystart.parser.ClefParser.parse(n)
        case n: Elem if n.label == "clefGrp"      => mei.allanystart.parser.ClefGrpParser.parse(n)
        case n: Elem if n.label == "colLayout"    => mei.allanystart.parser.ColLayoutParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "cpMark"       => mei.allanystart.parser.CpMarkParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "custos"       => mei.allanystart.parser.CustosParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "dir"          => mei.allanystart.parser.DirParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "div"          => mei.allanystart.parser.DivParser.parse(n)
        case n: Elem if n.label == "divLine"      => mei.allanystart.parser.DivLineParser.parse(n)
        case n: Elem if n.label == "dot"          => mei.allanystart.parser.DotParser.parse(n)
        case n: Elem if n.label == "dynam"        => mei.allanystart.parser.DynamParser.parse(n)
        case n: Elem if n.label == "ending"       => mei.allanystart.parser.EndingParser.parse(n)
        case n: Elem if n.label == "episema"      => mei.allanystart.parser.EpisemaParser.parse(n)
        case n: Elem if n.label == "eventList"    => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "f"            => mei.allanystart.parser.FParser.parse(n)
        case n: Elem if n.label == "fTrem"        => mei.allanystart.parser.FTremParser.parse(n)
        case n: Elem if n.label == "fermata"      => mei.allanystart.parser.FermataParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "fing"         => mei.allanystart.parser.FingParser.parse(n)
        case n: Elem if n.label == "fingGrp"      => mei.allanystart.parser.FingGrpParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "gliss"        => mei.allanystart.parser.GlissParser.parse(n)
        case n: Elem if n.label == "graceGrp"     => mei.allanystart.parser.GraceGrpParser.parse(n)
        case n: Elem if n.label == "hairpin"      => mei.allanystart.parser.HairpinParser.parse(n)
        case n: Elem if n.label == "halfmRpt"     => mei.allanystart.parser.HalfmRptParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "harm"         => mei.allanystart.parser.HarmParser.parse(n)
        case n: Elem if n.label == "harpPedal"    => mei.allanystart.parser.HarpPedalParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "hispanTick"   => mei.allanystart.parser.HispanTickParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "keyAccid"     => mei.allanystart.parser.KeyAccidParser.parse(n)
        case n: Elem if n.label == "keySig"       => mei.allanystart.parser.KeySigParser.parse(n)
        case n: Elem if n.label == "layer"        => mei.allanystart.parser.LayerParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "lg"           => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "ligature"     => mei.allanystart.parser.LigatureParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "liquescent"   => mei.allanystart.parser.LiquescentParser.parse(n)
        case n: Elem if n.label == "list"         => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "lv"           => mei.allanystart.parser.LvParser.parse(n)
        case n: Elem if n.label == "mRest"        => mei.allanystart.parser.MRestParser.parse(n)
        case n: Elem if n.label == "mRpt"         => mei.allanystart.parser.MRptParser.parse(n)
        case n: Elem if n.label == "mRpt2"        => mei.allanystart.parser.MRpt2Parser.parse(n)
        case n: Elem if n.label == "mSpace"       => mei.allanystart.parser.MSpaceParser.parse(n)
        case n: Elem if n.label == "measure"      => mei.allanystart.parser.MeasureParser.parse(n)
        case n: Elem if n.label == "mensur"       => mei.allanystart.parser.MensurParser.parse(n)
        case n: Elem if n.label == "metaMark"     => mei.allanystart.parser.MetaMarkParser.parse(n)
        case n: Elem if n.label == "meterSig"     => mei.allanystart.parser.MeterSigParser.parse(n)
        case n: Elem if n.label == "meterSigGrp"  => mei.allanystart.parser.MeterSigGrpParser.parse(n)
        case n: Elem if n.label == "midi"         => mei.allanystart.parser.MidiParser.parse(n)
        case n: Elem if n.label == "mordent"      => mei.allanystart.parser.MordentParser.parse(n)
        case n: Elem if n.label == "multiRest"    => mei.allanystart.parser.MultiRestParser.parse(n)
        case n: Elem if n.label == "multiRpt"     => mei.allanystart.parser.MultiRptParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "nc"           => mei.allanystart.parser.NcParser.parse(n)
        case n: Elem if n.label == "ncGrp"        => mei.allanystart.parser.NcGrpParser.parse(n)
        case n: Elem if n.label == "neume"        => mei.allanystart.parser.NeumeParser.parse(n)
        case n: Elem if n.label == "note"         => mei.allanystart.parser.NoteParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "octave"       => mei.allanystart.parser.OctaveParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "oriscus"      => mei.allanystart.parser.OriscusParser.parse(n)
        case n: Elem if n.label == "ornam"        => mei.allanystart.parser.OrnamParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "pad"          => mei.allanystart.parser.PadParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "pedal"        => mei.allanystart.parser.PedalParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "phrase"       => mei.allanystart.parser.PhraseParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "proport"      => mei.allanystart.parser.ProportParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "quilisma"     => mei.allanystart.parser.QuilismaParser.parse(n)
        case n: Elem if n.label == "quote"        => mei.allanystart.parser.QuoteParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "refrain"      => mei.allanystart.parser.RefrainParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "reh"          => mei.allanystart.parser.RehParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repeatMark"   => mei.allanystart.parser.RepeatMarkParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "rest"         => mei.allanystart.parser.RestParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sb"           => mei.allanystart.parser.SbParser.parse(n)
        case n: Elem if n.label == "scoreDef"     => mei.allanystart.parser.ScoreDefParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "section"      => mei.allanystart.parser.SectionParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "signifLet"    => mei.allanystart.parser.SignifLetParser.parse(n)
        case n: Elem if n.label == "slur"         => mei.allanystart.parser.SlurParser.parse(n)
        case n: Elem if n.label == "sp"           => mei.allanystart.parser.SpParser.parse(n)
        case n: Elem if n.label == "space"        => mei.allanystart.parser.SpaceParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "staff"        => mei.allanystart.parser.StaffParser.parse(n)
        case n: Elem if n.label == "staffDef"     => mei.allanystart.parser.StaffDefParser.parse(n)
        case n: Elem if n.label == "staffGrp"     => mei.allanystart.parser.StaffGrpParser.parse(n)
        case n: Elem if n.label == "stageDir"     => mei.allanystart.parser.StageDirParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "strophicus"   => mei.allanystart.parser.StrophicusParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "syl"          => mei.allanystart.parser.SylParser.parse(n)
        case n: Elem if n.label == "syllable"     => mei.allanystart.parser.SyllableParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "tabGrp"       => mei.allanystart.parser.TabGrpParser.parse(n)
        case n: Elem if n.label == "table"        => mei.allanystart.parser.TableParser.parse(n)
        case n: Elem if n.label == "tempo"        => mei.allanystart.parser.TempoParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "tie"          => mei.allanystart.parser.TieParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "trill"        => mei.allanystart.parser.TrillParser.parse(n)
        case n: Elem if n.label == "tuplet"       => mei.allanystart.parser.TupletParser.parse(n)
        case n: Elem if n.label == "tupletSpan"   => mei.allanystart.parser.TupletSpanParser.parse(n)
        case n: Elem if n.label == "turn"         => mei.allanystart.parser.TurnParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "verse"        => mei.allanystart.parser.VerseParser.parse(n)
        case n: Elem if n.label == "volta"        => mei.allanystart.parser.VoltaParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("unit").map(_.text),
      node.attribute("atleast").map(_.text),
      node.attribute("atmost").map(_.text),
      node.attribute("min").map(_.text),
      node.attribute("max").map(_.text),
      node.attribute("confidence").map(_.text),
      node.attribute("extent").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("altrend").map(_.text),
      node.attribute("rend").map(_.text),
      node.attribute("instant").map(_.text),
      node.attribute("state").map(_.text),
      node.attribute("hand").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("seq").map(_.text),
    )
  }
}
object ExpanParser extends MeiModelDummyParser {
  def parse(node: Node): Expan = {
    Expan(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "accid"        => mei.allanystart.parser.AccidParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "arpeg"        => mei.allanystart.parser.ArpegParser.parse(n)
        case n: Elem if n.label == "artic"        => mei.allanystart.parser.ArticParser.parse(n)
        case n: Elem if n.label == "attacca"      => mei.allanystart.parser.AttaccaParser.parse(n)
        case n: Elem if n.label == "bTrem"        => mei.allanystart.parser.BTremParser.parse(n)
        case n: Elem if n.label == "barLine"      => mei.allanystart.parser.BarLineParser.parse(n)
        case n: Elem if n.label == "beam"         => mei.allanystart.parser.BeamParser.parse(n)
        case n: Elem if n.label == "beamSpan"     => mei.allanystart.parser.BeamSpanParser.parse(n)
        case n: Elem if n.label == "beatRpt"      => mei.allanystart.parser.BeatRptParser.parse(n)
        case n: Elem if n.label == "bend"         => mei.allanystart.parser.BendParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblList"     => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "bracketSpan"  => mei.allanystart.parser.BracketSpanParser.parse(n)
        case n: Elem if n.label == "breath"       => mei.allanystart.parser.BreathParser.parse(n)
        case n: Elem if n.label == "caesura"      => mei.allanystart.parser.CaesuraParser.parse(n)
        case n: Elem if n.label == "castList"     => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "cb"           => mei.allanystart.parser.CbParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "chord"        => mei.allanystart.parser.ChordParser.parse(n)
        case n: Elem if n.label == "clef"         => mei.allanystart.parser.ClefParser.parse(n)
        case n: Elem if n.label == "clefGrp"      => mei.allanystart.parser.ClefGrpParser.parse(n)
        case n: Elem if n.label == "colLayout"    => mei.allanystart.parser.ColLayoutParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "cpMark"       => mei.allanystart.parser.CpMarkParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "custos"       => mei.allanystart.parser.CustosParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "dir"          => mei.allanystart.parser.DirParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "div"          => mei.allanystart.parser.DivParser.parse(n)
        case n: Elem if n.label == "divLine"      => mei.allanystart.parser.DivLineParser.parse(n)
        case n: Elem if n.label == "dot"          => mei.allanystart.parser.DotParser.parse(n)
        case n: Elem if n.label == "dynam"        => mei.allanystart.parser.DynamParser.parse(n)
        case n: Elem if n.label == "ending"       => mei.allanystart.parser.EndingParser.parse(n)
        case n: Elem if n.label == "episema"      => mei.allanystart.parser.EpisemaParser.parse(n)
        case n: Elem if n.label == "eventList"    => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "f"            => mei.allanystart.parser.FParser.parse(n)
        case n: Elem if n.label == "fTrem"        => mei.allanystart.parser.FTremParser.parse(n)
        case n: Elem if n.label == "fermata"      => mei.allanystart.parser.FermataParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "fing"         => mei.allanystart.parser.FingParser.parse(n)
        case n: Elem if n.label == "fingGrp"      => mei.allanystart.parser.FingGrpParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "gliss"        => mei.allanystart.parser.GlissParser.parse(n)
        case n: Elem if n.label == "graceGrp"     => mei.allanystart.parser.GraceGrpParser.parse(n)
        case n: Elem if n.label == "hairpin"      => mei.allanystart.parser.HairpinParser.parse(n)
        case n: Elem if n.label == "halfmRpt"     => mei.allanystart.parser.HalfmRptParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "harm"         => mei.allanystart.parser.HarmParser.parse(n)
        case n: Elem if n.label == "harpPedal"    => mei.allanystart.parser.HarpPedalParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "hispanTick"   => mei.allanystart.parser.HispanTickParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "keySig"       => mei.allanystart.parser.KeySigParser.parse(n)
        case n: Elem if n.label == "layer"        => mei.allanystart.parser.LayerParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "lg"           => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "ligature"     => mei.allanystart.parser.LigatureParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "liquescent"   => mei.allanystart.parser.LiquescentParser.parse(n)
        case n: Elem if n.label == "list"         => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "lv"           => mei.allanystart.parser.LvParser.parse(n)
        case n: Elem if n.label == "mRest"        => mei.allanystart.parser.MRestParser.parse(n)
        case n: Elem if n.label == "mRpt"         => mei.allanystart.parser.MRptParser.parse(n)
        case n: Elem if n.label == "mRpt2"        => mei.allanystart.parser.MRpt2Parser.parse(n)
        case n: Elem if n.label == "mSpace"       => mei.allanystart.parser.MSpaceParser.parse(n)
        case n: Elem if n.label == "measure"      => mei.allanystart.parser.MeasureParser.parse(n)
        case n: Elem if n.label == "mensur"       => mei.allanystart.parser.MensurParser.parse(n)
        case n: Elem if n.label == "metaMark"     => mei.allanystart.parser.MetaMarkParser.parse(n)
        case n: Elem if n.label == "meterSig"     => mei.allanystart.parser.MeterSigParser.parse(n)
        case n: Elem if n.label == "meterSigGrp"  => mei.allanystart.parser.MeterSigGrpParser.parse(n)
        case n: Elem if n.label == "midi"         => mei.allanystart.parser.MidiParser.parse(n)
        case n: Elem if n.label == "mordent"      => mei.allanystart.parser.MordentParser.parse(n)
        case n: Elem if n.label == "multiRest"    => mei.allanystart.parser.MultiRestParser.parse(n)
        case n: Elem if n.label == "multiRpt"     => mei.allanystart.parser.MultiRptParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "nc"           => mei.allanystart.parser.NcParser.parse(n)
        case n: Elem if n.label == "ncGrp"        => mei.allanystart.parser.NcGrpParser.parse(n)
        case n: Elem if n.label == "neume"        => mei.allanystart.parser.NeumeParser.parse(n)
        case n: Elem if n.label == "note"         => mei.allanystart.parser.NoteParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "octave"       => mei.allanystart.parser.OctaveParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "oriscus"      => mei.allanystart.parser.OriscusParser.parse(n)
        case n: Elem if n.label == "ornam"        => mei.allanystart.parser.OrnamParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "pad"          => mei.allanystart.parser.PadParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "pedal"        => mei.allanystart.parser.PedalParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "phrase"       => mei.allanystart.parser.PhraseParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "proport"      => mei.allanystart.parser.ProportParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "quilisma"     => mei.allanystart.parser.QuilismaParser.parse(n)
        case n: Elem if n.label == "quote"        => mei.allanystart.parser.QuoteParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "refrain"      => mei.allanystart.parser.RefrainParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "reh"          => mei.allanystart.parser.RehParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repeatMark"   => mei.allanystart.parser.RepeatMarkParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "rest"         => mei.allanystart.parser.RestParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sb"           => mei.allanystart.parser.SbParser.parse(n)
        case n: Elem if n.label == "scoreDef"     => mei.allanystart.parser.ScoreDefParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "section"      => mei.allanystart.parser.SectionParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "signifLet"    => mei.allanystart.parser.SignifLetParser.parse(n)
        case n: Elem if n.label == "slur"         => mei.allanystart.parser.SlurParser.parse(n)
        case n: Elem if n.label == "sp"           => mei.allanystart.parser.SpParser.parse(n)
        case n: Elem if n.label == "space"        => mei.allanystart.parser.SpaceParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "staff"        => mei.allanystart.parser.StaffParser.parse(n)
        case n: Elem if n.label == "staffDef"     => mei.allanystart.parser.StaffDefParser.parse(n)
        case n: Elem if n.label == "staffGrp"     => mei.allanystart.parser.StaffGrpParser.parse(n)
        case n: Elem if n.label == "stageDir"     => mei.allanystart.parser.StageDirParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "strophicus"   => mei.allanystart.parser.StrophicusParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "syl"          => mei.allanystart.parser.SylParser.parse(n)
        case n: Elem if n.label == "syllable"     => mei.allanystart.parser.SyllableParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "tabGrp"       => mei.allanystart.parser.TabGrpParser.parse(n)
        case n: Elem if n.label == "table"        => mei.allanystart.parser.TableParser.parse(n)
        case n: Elem if n.label == "tempo"        => mei.allanystart.parser.TempoParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "tie"          => mei.allanystart.parser.TieParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "trill"        => mei.allanystart.parser.TrillParser.parse(n)
        case n: Elem if n.label == "tuplet"       => mei.allanystart.parser.TupletParser.parse(n)
        case n: Elem if n.label == "tupletSpan"   => mei.allanystart.parser.TupletSpanParser.parse(n)
        case n: Elem if n.label == "turn"         => mei.allanystart.parser.TurnParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "verse"        => mei.allanystart.parser.VerseParser.parse(n)
        case n: Elem if n.label == "volta"        => mei.allanystart.parser.VoltaParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("unit").map(_.text),
      node.attribute("atleast").map(_.text),
      node.attribute("atmost").map(_.text),
      node.attribute("min").map(_.text),
      node.attribute("max").map(_.text),
      node.attribute("confidence").map(_.text),
      node.attribute("extent").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("instant").map(_.text),
      node.attribute("state").map(_.text),
      node.attribute("hand").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("seq").map(_.text),
      node.attribute("abbr").map(_.text),
    )
  }
}
object GapParser extends MeiModelDummyParser {
  def parse(node: Node): Gap = {
    Gap(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("unit").map(_.text),
      node.attribute("atleast").map(_.text),
      node.attribute("atmost").map(_.text),
      node.attribute("min").map(_.text),
      node.attribute("max").map(_.text),
      node.attribute("confidence").map(_.text),
      node.attribute("extent").map(_.text),
      node.attribute("hand").map(_.text),
      node.attribute("reason").map(_.text),
    )
  }
}
object HandShiftParser extends MeiModelDummyParser {
  def parse(node: Node): HandShift = {
    HandShift(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("medium").map(_.text),
      node.attribute("character").map(_.text),
      node.attribute("new").map(_.text),
      node.attribute("old").map(_.text),
    )
  }
}
object MetaMarkParser extends MeiModelDummyParser {
  def parse(node: Node): MetaMark = {
    MetaMark(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "section"      => mei.allanystart.parser.SectionParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("tstamp2").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("instant").map(_.text),
      node.attribute("state").map(_.text),
      node.attribute("hand").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("seq").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("vgrp").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
      node.attribute("tstamp2.ges").map(_.text),
      node.attribute("tstamp2.real").map(_.text),
      node.attribute("xlink:actuate").map(_.text),
      node.attribute("xlink:role").map(_.text),
      node.attribute("xlink:show").map(_.text),
      node.attribute("target").map(_.text),
      node.attribute("targettype").map(_.text),
      node.attribute("function").map(_.text),
    )
  }
}
object OrigParser extends MeiModelDummyParser {
  def parse(node: Node): Orig = {
    Orig(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "accid"        => mei.allanystart.parser.AccidParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "arpeg"        => mei.allanystart.parser.ArpegParser.parse(n)
        case n: Elem if n.label == "artic"        => mei.allanystart.parser.ArticParser.parse(n)
        case n: Elem if n.label == "attacca"      => mei.allanystart.parser.AttaccaParser.parse(n)
        case n: Elem if n.label == "bTrem"        => mei.allanystart.parser.BTremParser.parse(n)
        case n: Elem if n.label == "barLine"      => mei.allanystart.parser.BarLineParser.parse(n)
        case n: Elem if n.label == "beam"         => mei.allanystart.parser.BeamParser.parse(n)
        case n: Elem if n.label == "beamSpan"     => mei.allanystart.parser.BeamSpanParser.parse(n)
        case n: Elem if n.label == "beatRpt"      => mei.allanystart.parser.BeatRptParser.parse(n)
        case n: Elem if n.label == "bend"         => mei.allanystart.parser.BendParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblList"     => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "bracketSpan"  => mei.allanystart.parser.BracketSpanParser.parse(n)
        case n: Elem if n.label == "breath"       => mei.allanystart.parser.BreathParser.parse(n)
        case n: Elem if n.label == "caesura"      => mei.allanystart.parser.CaesuraParser.parse(n)
        case n: Elem if n.label == "castList"     => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "cb"           => mei.allanystart.parser.CbParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "chord"        => mei.allanystart.parser.ChordParser.parse(n)
        case n: Elem if n.label == "clef"         => mei.allanystart.parser.ClefParser.parse(n)
        case n: Elem if n.label == "clefGrp"      => mei.allanystart.parser.ClefGrpParser.parse(n)
        case n: Elem if n.label == "colLayout"    => mei.allanystart.parser.ColLayoutParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "cpMark"       => mei.allanystart.parser.CpMarkParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "custos"       => mei.allanystart.parser.CustosParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "dir"          => mei.allanystart.parser.DirParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "div"          => mei.allanystart.parser.DivParser.parse(n)
        case n: Elem if n.label == "divLine"      => mei.allanystart.parser.DivLineParser.parse(n)
        case n: Elem if n.label == "dot"          => mei.allanystart.parser.DotParser.parse(n)
        case n: Elem if n.label == "dynam"        => mei.allanystart.parser.DynamParser.parse(n)
        case n: Elem if n.label == "ending"       => mei.allanystart.parser.EndingParser.parse(n)
        case n: Elem if n.label == "episema"      => mei.allanystart.parser.EpisemaParser.parse(n)
        case n: Elem if n.label == "eventList"    => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "f"            => mei.allanystart.parser.FParser.parse(n)
        case n: Elem if n.label == "fTrem"        => mei.allanystart.parser.FTremParser.parse(n)
        case n: Elem if n.label == "fermata"      => mei.allanystart.parser.FermataParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "fing"         => mei.allanystart.parser.FingParser.parse(n)
        case n: Elem if n.label == "fingGrp"      => mei.allanystart.parser.FingGrpParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "gliss"        => mei.allanystart.parser.GlissParser.parse(n)
        case n: Elem if n.label == "graceGrp"     => mei.allanystart.parser.GraceGrpParser.parse(n)
        case n: Elem if n.label == "hairpin"      => mei.allanystart.parser.HairpinParser.parse(n)
        case n: Elem if n.label == "halfmRpt"     => mei.allanystart.parser.HalfmRptParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "harm"         => mei.allanystart.parser.HarmParser.parse(n)
        case n: Elem if n.label == "harpPedal"    => mei.allanystart.parser.HarpPedalParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "hispanTick"   => mei.allanystart.parser.HispanTickParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "keyAccid"     => mei.allanystart.parser.KeyAccidParser.parse(n)
        case n: Elem if n.label == "keySig"       => mei.allanystart.parser.KeySigParser.parse(n)
        case n: Elem if n.label == "layer"        => mei.allanystart.parser.LayerParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "lg"           => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "ligature"     => mei.allanystart.parser.LigatureParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "liquescent"   => mei.allanystart.parser.LiquescentParser.parse(n)
        case n: Elem if n.label == "list"         => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "lv"           => mei.allanystart.parser.LvParser.parse(n)
        case n: Elem if n.label == "mRest"        => mei.allanystart.parser.MRestParser.parse(n)
        case n: Elem if n.label == "mRpt"         => mei.allanystart.parser.MRptParser.parse(n)
        case n: Elem if n.label == "mRpt2"        => mei.allanystart.parser.MRpt2Parser.parse(n)
        case n: Elem if n.label == "mSpace"       => mei.allanystart.parser.MSpaceParser.parse(n)
        case n: Elem if n.label == "measure"      => mei.allanystart.parser.MeasureParser.parse(n)
        case n: Elem if n.label == "mensur"       => mei.allanystart.parser.MensurParser.parse(n)
        case n: Elem if n.label == "metaMark"     => mei.allanystart.parser.MetaMarkParser.parse(n)
        case n: Elem if n.label == "meterSig"     => mei.allanystart.parser.MeterSigParser.parse(n)
        case n: Elem if n.label == "meterSigGrp"  => mei.allanystart.parser.MeterSigGrpParser.parse(n)
        case n: Elem if n.label == "midi"         => mei.allanystart.parser.MidiParser.parse(n)
        case n: Elem if n.label == "mordent"      => mei.allanystart.parser.MordentParser.parse(n)
        case n: Elem if n.label == "multiRest"    => mei.allanystart.parser.MultiRestParser.parse(n)
        case n: Elem if n.label == "multiRpt"     => mei.allanystart.parser.MultiRptParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "nc"           => mei.allanystart.parser.NcParser.parse(n)
        case n: Elem if n.label == "ncGrp"        => mei.allanystart.parser.NcGrpParser.parse(n)
        case n: Elem if n.label == "neume"        => mei.allanystart.parser.NeumeParser.parse(n)
        case n: Elem if n.label == "note"         => mei.allanystart.parser.NoteParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "octave"       => mei.allanystart.parser.OctaveParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "oriscus"      => mei.allanystart.parser.OriscusParser.parse(n)
        case n: Elem if n.label == "ornam"        => mei.allanystart.parser.OrnamParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "pad"          => mei.allanystart.parser.PadParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "pedal"        => mei.allanystart.parser.PedalParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "phrase"       => mei.allanystart.parser.PhraseParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "proport"      => mei.allanystart.parser.ProportParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "quilisma"     => mei.allanystart.parser.QuilismaParser.parse(n)
        case n: Elem if n.label == "quote"        => mei.allanystart.parser.QuoteParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "refrain"      => mei.allanystart.parser.RefrainParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "reh"          => mei.allanystart.parser.RehParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repeatMark"   => mei.allanystart.parser.RepeatMarkParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "rest"         => mei.allanystart.parser.RestParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sb"           => mei.allanystart.parser.SbParser.parse(n)
        case n: Elem if n.label == "scoreDef"     => mei.allanystart.parser.ScoreDefParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "section"      => mei.allanystart.parser.SectionParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "signifLet"    => mei.allanystart.parser.SignifLetParser.parse(n)
        case n: Elem if n.label == "slur"         => mei.allanystart.parser.SlurParser.parse(n)
        case n: Elem if n.label == "sp"           => mei.allanystart.parser.SpParser.parse(n)
        case n: Elem if n.label == "space"        => mei.allanystart.parser.SpaceParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "staff"        => mei.allanystart.parser.StaffParser.parse(n)
        case n: Elem if n.label == "staffDef"     => mei.allanystart.parser.StaffDefParser.parse(n)
        case n: Elem if n.label == "staffGrp"     => mei.allanystart.parser.StaffGrpParser.parse(n)
        case n: Elem if n.label == "stageDir"     => mei.allanystart.parser.StageDirParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "strophicus"   => mei.allanystart.parser.StrophicusParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "syl"          => mei.allanystart.parser.SylParser.parse(n)
        case n: Elem if n.label == "syllable"     => mei.allanystart.parser.SyllableParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "tabGrp"       => mei.allanystart.parser.TabGrpParser.parse(n)
        case n: Elem if n.label == "table"        => mei.allanystart.parser.TableParser.parse(n)
        case n: Elem if n.label == "tempo"        => mei.allanystart.parser.TempoParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "tie"          => mei.allanystart.parser.TieParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "trill"        => mei.allanystart.parser.TrillParser.parse(n)
        case n: Elem if n.label == "tuplet"       => mei.allanystart.parser.TupletParser.parse(n)
        case n: Elem if n.label == "tupletSpan"   => mei.allanystart.parser.TupletSpanParser.parse(n)
        case n: Elem if n.label == "turn"         => mei.allanystart.parser.TurnParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "verse"        => mei.allanystart.parser.VerseParser.parse(n)
        case n: Elem if n.label == "volta"        => mei.allanystart.parser.VoltaParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("unit").map(_.text),
      node.attribute("atleast").map(_.text),
      node.attribute("atmost").map(_.text),
      node.attribute("min").map(_.text),
      node.attribute("max").map(_.text),
      node.attribute("confidence").map(_.text),
      node.attribute("extent").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object RegParser extends MeiModelDummyParser {
  def parse(node: Node): Reg = {
    Reg(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "accid"        => mei.allanystart.parser.AccidParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "arpeg"        => mei.allanystart.parser.ArpegParser.parse(n)
        case n: Elem if n.label == "artic"        => mei.allanystart.parser.ArticParser.parse(n)
        case n: Elem if n.label == "attacca"      => mei.allanystart.parser.AttaccaParser.parse(n)
        case n: Elem if n.label == "bTrem"        => mei.allanystart.parser.BTremParser.parse(n)
        case n: Elem if n.label == "barLine"      => mei.allanystart.parser.BarLineParser.parse(n)
        case n: Elem if n.label == "beam"         => mei.allanystart.parser.BeamParser.parse(n)
        case n: Elem if n.label == "beamSpan"     => mei.allanystart.parser.BeamSpanParser.parse(n)
        case n: Elem if n.label == "beatRpt"      => mei.allanystart.parser.BeatRptParser.parse(n)
        case n: Elem if n.label == "bend"         => mei.allanystart.parser.BendParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblList"     => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "bracketSpan"  => mei.allanystart.parser.BracketSpanParser.parse(n)
        case n: Elem if n.label == "breath"       => mei.allanystart.parser.BreathParser.parse(n)
        case n: Elem if n.label == "caesura"      => mei.allanystart.parser.CaesuraParser.parse(n)
        case n: Elem if n.label == "castList"     => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "cb"           => mei.allanystart.parser.CbParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "chord"        => mei.allanystart.parser.ChordParser.parse(n)
        case n: Elem if n.label == "clef"         => mei.allanystart.parser.ClefParser.parse(n)
        case n: Elem if n.label == "clefGrp"      => mei.allanystart.parser.ClefGrpParser.parse(n)
        case n: Elem if n.label == "colLayout"    => mei.allanystart.parser.ColLayoutParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "cpMark"       => mei.allanystart.parser.CpMarkParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "custos"       => mei.allanystart.parser.CustosParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "dir"          => mei.allanystart.parser.DirParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "div"          => mei.allanystart.parser.DivParser.parse(n)
        case n: Elem if n.label == "divLine"      => mei.allanystart.parser.DivLineParser.parse(n)
        case n: Elem if n.label == "dot"          => mei.allanystart.parser.DotParser.parse(n)
        case n: Elem if n.label == "dynam"        => mei.allanystart.parser.DynamParser.parse(n)
        case n: Elem if n.label == "ending"       => mei.allanystart.parser.EndingParser.parse(n)
        case n: Elem if n.label == "episema"      => mei.allanystart.parser.EpisemaParser.parse(n)
        case n: Elem if n.label == "eventList"    => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "f"            => mei.allanystart.parser.FParser.parse(n)
        case n: Elem if n.label == "fTrem"        => mei.allanystart.parser.FTremParser.parse(n)
        case n: Elem if n.label == "fermata"      => mei.allanystart.parser.FermataParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "fing"         => mei.allanystart.parser.FingParser.parse(n)
        case n: Elem if n.label == "fingGrp"      => mei.allanystart.parser.FingGrpParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "gliss"        => mei.allanystart.parser.GlissParser.parse(n)
        case n: Elem if n.label == "graceGrp"     => mei.allanystart.parser.GraceGrpParser.parse(n)
        case n: Elem if n.label == "hairpin"      => mei.allanystart.parser.HairpinParser.parse(n)
        case n: Elem if n.label == "halfmRpt"     => mei.allanystart.parser.HalfmRptParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "harm"         => mei.allanystart.parser.HarmParser.parse(n)
        case n: Elem if n.label == "harpPedal"    => mei.allanystart.parser.HarpPedalParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "hispanTick"   => mei.allanystart.parser.HispanTickParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "keyAccid"     => mei.allanystart.parser.KeyAccidParser.parse(n)
        case n: Elem if n.label == "keySig"       => mei.allanystart.parser.KeySigParser.parse(n)
        case n: Elem if n.label == "layer"        => mei.allanystart.parser.LayerParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "lg"           => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "ligature"     => mei.allanystart.parser.LigatureParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "liquescent"   => mei.allanystart.parser.LiquescentParser.parse(n)
        case n: Elem if n.label == "list"         => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "lv"           => mei.allanystart.parser.LvParser.parse(n)
        case n: Elem if n.label == "mRest"        => mei.allanystart.parser.MRestParser.parse(n)
        case n: Elem if n.label == "mRpt"         => mei.allanystart.parser.MRptParser.parse(n)
        case n: Elem if n.label == "mRpt2"        => mei.allanystart.parser.MRpt2Parser.parse(n)
        case n: Elem if n.label == "mSpace"       => mei.allanystart.parser.MSpaceParser.parse(n)
        case n: Elem if n.label == "measure"      => mei.allanystart.parser.MeasureParser.parse(n)
        case n: Elem if n.label == "mensur"       => mei.allanystart.parser.MensurParser.parse(n)
        case n: Elem if n.label == "metaMark"     => mei.allanystart.parser.MetaMarkParser.parse(n)
        case n: Elem if n.label == "meterSig"     => mei.allanystart.parser.MeterSigParser.parse(n)
        case n: Elem if n.label == "meterSigGrp"  => mei.allanystart.parser.MeterSigGrpParser.parse(n)
        case n: Elem if n.label == "midi"         => mei.allanystart.parser.MidiParser.parse(n)
        case n: Elem if n.label == "mordent"      => mei.allanystart.parser.MordentParser.parse(n)
        case n: Elem if n.label == "multiRest"    => mei.allanystart.parser.MultiRestParser.parse(n)
        case n: Elem if n.label == "multiRpt"     => mei.allanystart.parser.MultiRptParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "nc"           => mei.allanystart.parser.NcParser.parse(n)
        case n: Elem if n.label == "ncGrp"        => mei.allanystart.parser.NcGrpParser.parse(n)
        case n: Elem if n.label == "neume"        => mei.allanystart.parser.NeumeParser.parse(n)
        case n: Elem if n.label == "note"         => mei.allanystart.parser.NoteParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "octave"       => mei.allanystart.parser.OctaveParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "oriscus"      => mei.allanystart.parser.OriscusParser.parse(n)
        case n: Elem if n.label == "ornam"        => mei.allanystart.parser.OrnamParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "pad"          => mei.allanystart.parser.PadParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "pedal"        => mei.allanystart.parser.PedalParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "phrase"       => mei.allanystart.parser.PhraseParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "proport"      => mei.allanystart.parser.ProportParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "quilisma"     => mei.allanystart.parser.QuilismaParser.parse(n)
        case n: Elem if n.label == "quote"        => mei.allanystart.parser.QuoteParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "refrain"      => mei.allanystart.parser.RefrainParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "reh"          => mei.allanystart.parser.RehParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repeatMark"   => mei.allanystart.parser.RepeatMarkParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "rest"         => mei.allanystart.parser.RestParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sb"           => mei.allanystart.parser.SbParser.parse(n)
        case n: Elem if n.label == "scoreDef"     => mei.allanystart.parser.ScoreDefParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "section"      => mei.allanystart.parser.SectionParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "signifLet"    => mei.allanystart.parser.SignifLetParser.parse(n)
        case n: Elem if n.label == "slur"         => mei.allanystart.parser.SlurParser.parse(n)
        case n: Elem if n.label == "sp"           => mei.allanystart.parser.SpParser.parse(n)
        case n: Elem if n.label == "space"        => mei.allanystart.parser.SpaceParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "staff"        => mei.allanystart.parser.StaffParser.parse(n)
        case n: Elem if n.label == "staffDef"     => mei.allanystart.parser.StaffDefParser.parse(n)
        case n: Elem if n.label == "staffGrp"     => mei.allanystart.parser.StaffGrpParser.parse(n)
        case n: Elem if n.label == "stageDir"     => mei.allanystart.parser.StageDirParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "strophicus"   => mei.allanystart.parser.StrophicusParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "syl"          => mei.allanystart.parser.SylParser.parse(n)
        case n: Elem if n.label == "syllable"     => mei.allanystart.parser.SyllableParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "tabGrp"       => mei.allanystart.parser.TabGrpParser.parse(n)
        case n: Elem if n.label == "table"        => mei.allanystart.parser.TableParser.parse(n)
        case n: Elem if n.label == "tempo"        => mei.allanystart.parser.TempoParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "tie"          => mei.allanystart.parser.TieParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "trill"        => mei.allanystart.parser.TrillParser.parse(n)
        case n: Elem if n.label == "tuplet"       => mei.allanystart.parser.TupletParser.parse(n)
        case n: Elem if n.label == "tupletSpan"   => mei.allanystart.parser.TupletSpanParser.parse(n)
        case n: Elem if n.label == "turn"         => mei.allanystart.parser.TurnParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "verse"        => mei.allanystart.parser.VerseParser.parse(n)
        case n: Elem if n.label == "volta"        => mei.allanystart.parser.VoltaParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("unit").map(_.text),
      node.attribute("atleast").map(_.text),
      node.attribute("atmost").map(_.text),
      node.attribute("min").map(_.text),
      node.attribute("max").map(_.text),
      node.attribute("confidence").map(_.text),
      node.attribute("extent").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object RestoreParser extends MeiModelDummyParser {
  def parse(node: Node): Restore = {
    Restore(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "accid"        => mei.allanystart.parser.AccidParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "arpeg"        => mei.allanystart.parser.ArpegParser.parse(n)
        case n: Elem if n.label == "artic"        => mei.allanystart.parser.ArticParser.parse(n)
        case n: Elem if n.label == "attacca"      => mei.allanystart.parser.AttaccaParser.parse(n)
        case n: Elem if n.label == "bTrem"        => mei.allanystart.parser.BTremParser.parse(n)
        case n: Elem if n.label == "barLine"      => mei.allanystart.parser.BarLineParser.parse(n)
        case n: Elem if n.label == "beam"         => mei.allanystart.parser.BeamParser.parse(n)
        case n: Elem if n.label == "beamSpan"     => mei.allanystart.parser.BeamSpanParser.parse(n)
        case n: Elem if n.label == "beatRpt"      => mei.allanystart.parser.BeatRptParser.parse(n)
        case n: Elem if n.label == "bend"         => mei.allanystart.parser.BendParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblList"     => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "bracketSpan"  => mei.allanystart.parser.BracketSpanParser.parse(n)
        case n: Elem if n.label == "breath"       => mei.allanystart.parser.BreathParser.parse(n)
        case n: Elem if n.label == "caesura"      => mei.allanystart.parser.CaesuraParser.parse(n)
        case n: Elem if n.label == "castList"     => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "cb"           => mei.allanystart.parser.CbParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "chord"        => mei.allanystart.parser.ChordParser.parse(n)
        case n: Elem if n.label == "clef"         => mei.allanystart.parser.ClefParser.parse(n)
        case n: Elem if n.label == "clefGrp"      => mei.allanystart.parser.ClefGrpParser.parse(n)
        case n: Elem if n.label == "colLayout"    => mei.allanystart.parser.ColLayoutParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "cpMark"       => mei.allanystart.parser.CpMarkParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "custos"       => mei.allanystart.parser.CustosParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "dir"          => mei.allanystart.parser.DirParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "div"          => mei.allanystart.parser.DivParser.parse(n)
        case n: Elem if n.label == "divLine"      => mei.allanystart.parser.DivLineParser.parse(n)
        case n: Elem if n.label == "dot"          => mei.allanystart.parser.DotParser.parse(n)
        case n: Elem if n.label == "dynam"        => mei.allanystart.parser.DynamParser.parse(n)
        case n: Elem if n.label == "ending"       => mei.allanystart.parser.EndingParser.parse(n)
        case n: Elem if n.label == "episema"      => mei.allanystart.parser.EpisemaParser.parse(n)
        case n: Elem if n.label == "eventList"    => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "f"            => mei.allanystart.parser.FParser.parse(n)
        case n: Elem if n.label == "fTrem"        => mei.allanystart.parser.FTremParser.parse(n)
        case n: Elem if n.label == "fermata"      => mei.allanystart.parser.FermataParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "fing"         => mei.allanystart.parser.FingParser.parse(n)
        case n: Elem if n.label == "fingGrp"      => mei.allanystart.parser.FingGrpParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "gliss"        => mei.allanystart.parser.GlissParser.parse(n)
        case n: Elem if n.label == "graceGrp"     => mei.allanystart.parser.GraceGrpParser.parse(n)
        case n: Elem if n.label == "hairpin"      => mei.allanystart.parser.HairpinParser.parse(n)
        case n: Elem if n.label == "halfmRpt"     => mei.allanystart.parser.HalfmRptParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "harm"         => mei.allanystart.parser.HarmParser.parse(n)
        case n: Elem if n.label == "harpPedal"    => mei.allanystart.parser.HarpPedalParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "hispanTick"   => mei.allanystart.parser.HispanTickParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "keyAccid"     => mei.allanystart.parser.KeyAccidParser.parse(n)
        case n: Elem if n.label == "keySig"       => mei.allanystart.parser.KeySigParser.parse(n)
        case n: Elem if n.label == "layer"        => mei.allanystart.parser.LayerParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "lg"           => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "ligature"     => mei.allanystart.parser.LigatureParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "liquescent"   => mei.allanystart.parser.LiquescentParser.parse(n)
        case n: Elem if n.label == "list"         => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "lv"           => mei.allanystart.parser.LvParser.parse(n)
        case n: Elem if n.label == "mRest"        => mei.allanystart.parser.MRestParser.parse(n)
        case n: Elem if n.label == "mRpt"         => mei.allanystart.parser.MRptParser.parse(n)
        case n: Elem if n.label == "mRpt2"        => mei.allanystart.parser.MRpt2Parser.parse(n)
        case n: Elem if n.label == "mSpace"       => mei.allanystart.parser.MSpaceParser.parse(n)
        case n: Elem if n.label == "measure"      => mei.allanystart.parser.MeasureParser.parse(n)
        case n: Elem if n.label == "mensur"       => mei.allanystart.parser.MensurParser.parse(n)
        case n: Elem if n.label == "metaMark"     => mei.allanystart.parser.MetaMarkParser.parse(n)
        case n: Elem if n.label == "meterSig"     => mei.allanystart.parser.MeterSigParser.parse(n)
        case n: Elem if n.label == "meterSigGrp"  => mei.allanystart.parser.MeterSigGrpParser.parse(n)
        case n: Elem if n.label == "midi"         => mei.allanystart.parser.MidiParser.parse(n)
        case n: Elem if n.label == "mordent"      => mei.allanystart.parser.MordentParser.parse(n)
        case n: Elem if n.label == "multiRest"    => mei.allanystart.parser.MultiRestParser.parse(n)
        case n: Elem if n.label == "multiRpt"     => mei.allanystart.parser.MultiRptParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "nc"           => mei.allanystart.parser.NcParser.parse(n)
        case n: Elem if n.label == "ncGrp"        => mei.allanystart.parser.NcGrpParser.parse(n)
        case n: Elem if n.label == "neume"        => mei.allanystart.parser.NeumeParser.parse(n)
        case n: Elem if n.label == "note"         => mei.allanystart.parser.NoteParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "octave"       => mei.allanystart.parser.OctaveParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "oriscus"      => mei.allanystart.parser.OriscusParser.parse(n)
        case n: Elem if n.label == "ornam"        => mei.allanystart.parser.OrnamParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "pad"          => mei.allanystart.parser.PadParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "pedal"        => mei.allanystart.parser.PedalParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "phrase"       => mei.allanystart.parser.PhraseParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "proport"      => mei.allanystart.parser.ProportParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "quilisma"     => mei.allanystart.parser.QuilismaParser.parse(n)
        case n: Elem if n.label == "quote"        => mei.allanystart.parser.QuoteParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "refrain"      => mei.allanystart.parser.RefrainParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "reh"          => mei.allanystart.parser.RehParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repeatMark"   => mei.allanystart.parser.RepeatMarkParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "rest"         => mei.allanystart.parser.RestParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sb"           => mei.allanystart.parser.SbParser.parse(n)
        case n: Elem if n.label == "scoreDef"     => mei.allanystart.parser.ScoreDefParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "section"      => mei.allanystart.parser.SectionParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "signifLet"    => mei.allanystart.parser.SignifLetParser.parse(n)
        case n: Elem if n.label == "slur"         => mei.allanystart.parser.SlurParser.parse(n)
        case n: Elem if n.label == "sp"           => mei.allanystart.parser.SpParser.parse(n)
        case n: Elem if n.label == "space"        => mei.allanystart.parser.SpaceParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "staff"        => mei.allanystart.parser.StaffParser.parse(n)
        case n: Elem if n.label == "staffDef"     => mei.allanystart.parser.StaffDefParser.parse(n)
        case n: Elem if n.label == "staffGrp"     => mei.allanystart.parser.StaffGrpParser.parse(n)
        case n: Elem if n.label == "stageDir"     => mei.allanystart.parser.StageDirParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "strophicus"   => mei.allanystart.parser.StrophicusParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "syl"          => mei.allanystart.parser.SylParser.parse(n)
        case n: Elem if n.label == "syllable"     => mei.allanystart.parser.SyllableParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "tabGrp"       => mei.allanystart.parser.TabGrpParser.parse(n)
        case n: Elem if n.label == "table"        => mei.allanystart.parser.TableParser.parse(n)
        case n: Elem if n.label == "tempo"        => mei.allanystart.parser.TempoParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "tie"          => mei.allanystart.parser.TieParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "trill"        => mei.allanystart.parser.TrillParser.parse(n)
        case n: Elem if n.label == "tuplet"       => mei.allanystart.parser.TupletParser.parse(n)
        case n: Elem if n.label == "tupletSpan"   => mei.allanystart.parser.TupletSpanParser.parse(n)
        case n: Elem if n.label == "turn"         => mei.allanystart.parser.TurnParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "verse"        => mei.allanystart.parser.VerseParser.parse(n)
        case n: Elem if n.label == "volta"        => mei.allanystart.parser.VoltaParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("unit").map(_.text),
      node.attribute("atleast").map(_.text),
      node.attribute("atmost").map(_.text),
      node.attribute("min").map(_.text),
      node.attribute("max").map(_.text),
      node.attribute("confidence").map(_.text),
      node.attribute("extent").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("instant").map(_.text),
      node.attribute("state").map(_.text),
      node.attribute("hand").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("seq").map(_.text),
      node.attribute("desc").map(_.text),
    )
  }
}
object SicParser extends MeiModelDummyParser {
  def parse(node: Node): Sic = {
    Sic(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "accid"        => mei.allanystart.parser.AccidParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "arpeg"        => mei.allanystart.parser.ArpegParser.parse(n)
        case n: Elem if n.label == "artic"        => mei.allanystart.parser.ArticParser.parse(n)
        case n: Elem if n.label == "attacca"      => mei.allanystart.parser.AttaccaParser.parse(n)
        case n: Elem if n.label == "bTrem"        => mei.allanystart.parser.BTremParser.parse(n)
        case n: Elem if n.label == "barLine"      => mei.allanystart.parser.BarLineParser.parse(n)
        case n: Elem if n.label == "beam"         => mei.allanystart.parser.BeamParser.parse(n)
        case n: Elem if n.label == "beamSpan"     => mei.allanystart.parser.BeamSpanParser.parse(n)
        case n: Elem if n.label == "beatRpt"      => mei.allanystart.parser.BeatRptParser.parse(n)
        case n: Elem if n.label == "bend"         => mei.allanystart.parser.BendParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblList"     => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "bracketSpan"  => mei.allanystart.parser.BracketSpanParser.parse(n)
        case n: Elem if n.label == "breath"       => mei.allanystart.parser.BreathParser.parse(n)
        case n: Elem if n.label == "caesura"      => mei.allanystart.parser.CaesuraParser.parse(n)
        case n: Elem if n.label == "castList"     => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "cb"           => mei.allanystart.parser.CbParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "chord"        => mei.allanystart.parser.ChordParser.parse(n)
        case n: Elem if n.label == "clef"         => mei.allanystart.parser.ClefParser.parse(n)
        case n: Elem if n.label == "clefGrp"      => mei.allanystart.parser.ClefGrpParser.parse(n)
        case n: Elem if n.label == "colLayout"    => mei.allanystart.parser.ColLayoutParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "cpMark"       => mei.allanystart.parser.CpMarkParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "custos"       => mei.allanystart.parser.CustosParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "dir"          => mei.allanystart.parser.DirParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "div"          => mei.allanystart.parser.DivParser.parse(n)
        case n: Elem if n.label == "divLine"      => mei.allanystart.parser.DivLineParser.parse(n)
        case n: Elem if n.label == "dot"          => mei.allanystart.parser.DotParser.parse(n)
        case n: Elem if n.label == "dynam"        => mei.allanystart.parser.DynamParser.parse(n)
        case n: Elem if n.label == "ending"       => mei.allanystart.parser.EndingParser.parse(n)
        case n: Elem if n.label == "episema"      => mei.allanystart.parser.EpisemaParser.parse(n)
        case n: Elem if n.label == "eventList"    => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "f"            => mei.allanystart.parser.FParser.parse(n)
        case n: Elem if n.label == "fTrem"        => mei.allanystart.parser.FTremParser.parse(n)
        case n: Elem if n.label == "fermata"      => mei.allanystart.parser.FermataParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "fing"         => mei.allanystart.parser.FingParser.parse(n)
        case n: Elem if n.label == "fingGrp"      => mei.allanystart.parser.FingGrpParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "gliss"        => mei.allanystart.parser.GlissParser.parse(n)
        case n: Elem if n.label == "graceGrp"     => mei.allanystart.parser.GraceGrpParser.parse(n)
        case n: Elem if n.label == "hairpin"      => mei.allanystart.parser.HairpinParser.parse(n)
        case n: Elem if n.label == "halfmRpt"     => mei.allanystart.parser.HalfmRptParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "harm"         => mei.allanystart.parser.HarmParser.parse(n)
        case n: Elem if n.label == "harpPedal"    => mei.allanystart.parser.HarpPedalParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "hispanTick"   => mei.allanystart.parser.HispanTickParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "keyAccid"     => mei.allanystart.parser.KeyAccidParser.parse(n)
        case n: Elem if n.label == "keySig"       => mei.allanystart.parser.KeySigParser.parse(n)
        case n: Elem if n.label == "layer"        => mei.allanystart.parser.LayerParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "lg"           => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "ligature"     => mei.allanystart.parser.LigatureParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "liquescent"   => mei.allanystart.parser.LiquescentParser.parse(n)
        case n: Elem if n.label == "list"         => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "lv"           => mei.allanystart.parser.LvParser.parse(n)
        case n: Elem if n.label == "mRest"        => mei.allanystart.parser.MRestParser.parse(n)
        case n: Elem if n.label == "mRpt"         => mei.allanystart.parser.MRptParser.parse(n)
        case n: Elem if n.label == "mRpt2"        => mei.allanystart.parser.MRpt2Parser.parse(n)
        case n: Elem if n.label == "mSpace"       => mei.allanystart.parser.MSpaceParser.parse(n)
        case n: Elem if n.label == "measure"      => mei.allanystart.parser.MeasureParser.parse(n)
        case n: Elem if n.label == "mensur"       => mei.allanystart.parser.MensurParser.parse(n)
        case n: Elem if n.label == "metaMark"     => mei.allanystart.parser.MetaMarkParser.parse(n)
        case n: Elem if n.label == "meterSig"     => mei.allanystart.parser.MeterSigParser.parse(n)
        case n: Elem if n.label == "meterSigGrp"  => mei.allanystart.parser.MeterSigGrpParser.parse(n)
        case n: Elem if n.label == "midi"         => mei.allanystart.parser.MidiParser.parse(n)
        case n: Elem if n.label == "mordent"      => mei.allanystart.parser.MordentParser.parse(n)
        case n: Elem if n.label == "multiRest"    => mei.allanystart.parser.MultiRestParser.parse(n)
        case n: Elem if n.label == "multiRpt"     => mei.allanystart.parser.MultiRptParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "nc"           => mei.allanystart.parser.NcParser.parse(n)
        case n: Elem if n.label == "ncGrp"        => mei.allanystart.parser.NcGrpParser.parse(n)
        case n: Elem if n.label == "neume"        => mei.allanystart.parser.NeumeParser.parse(n)
        case n: Elem if n.label == "note"         => mei.allanystart.parser.NoteParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "octave"       => mei.allanystart.parser.OctaveParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "oriscus"      => mei.allanystart.parser.OriscusParser.parse(n)
        case n: Elem if n.label == "ornam"        => mei.allanystart.parser.OrnamParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "pad"          => mei.allanystart.parser.PadParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "pedal"        => mei.allanystart.parser.PedalParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "phrase"       => mei.allanystart.parser.PhraseParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "proport"      => mei.allanystart.parser.ProportParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "quilisma"     => mei.allanystart.parser.QuilismaParser.parse(n)
        case n: Elem if n.label == "quote"        => mei.allanystart.parser.QuoteParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "refrain"      => mei.allanystart.parser.RefrainParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "reh"          => mei.allanystart.parser.RehParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repeatMark"   => mei.allanystart.parser.RepeatMarkParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "rest"         => mei.allanystart.parser.RestParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sb"           => mei.allanystart.parser.SbParser.parse(n)
        case n: Elem if n.label == "scoreDef"     => mei.allanystart.parser.ScoreDefParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "section"      => mei.allanystart.parser.SectionParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "signifLet"    => mei.allanystart.parser.SignifLetParser.parse(n)
        case n: Elem if n.label == "slur"         => mei.allanystart.parser.SlurParser.parse(n)
        case n: Elem if n.label == "sp"           => mei.allanystart.parser.SpParser.parse(n)
        case n: Elem if n.label == "space"        => mei.allanystart.parser.SpaceParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "staff"        => mei.allanystart.parser.StaffParser.parse(n)
        case n: Elem if n.label == "staffDef"     => mei.allanystart.parser.StaffDefParser.parse(n)
        case n: Elem if n.label == "staffGrp"     => mei.allanystart.parser.StaffGrpParser.parse(n)
        case n: Elem if n.label == "stageDir"     => mei.allanystart.parser.StageDirParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "strophicus"   => mei.allanystart.parser.StrophicusParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "syl"          => mei.allanystart.parser.SylParser.parse(n)
        case n: Elem if n.label == "syllable"     => mei.allanystart.parser.SyllableParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "tabGrp"       => mei.allanystart.parser.TabGrpParser.parse(n)
        case n: Elem if n.label == "table"        => mei.allanystart.parser.TableParser.parse(n)
        case n: Elem if n.label == "tempo"        => mei.allanystart.parser.TempoParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "tie"          => mei.allanystart.parser.TieParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "trill"        => mei.allanystart.parser.TrillParser.parse(n)
        case n: Elem if n.label == "tuplet"       => mei.allanystart.parser.TupletParser.parse(n)
        case n: Elem if n.label == "tupletSpan"   => mei.allanystart.parser.TupletSpanParser.parse(n)
        case n: Elem if n.label == "turn"         => mei.allanystart.parser.TurnParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "verse"        => mei.allanystart.parser.VerseParser.parse(n)
        case n: Elem if n.label == "volta"        => mei.allanystart.parser.VoltaParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("unit").map(_.text),
      node.attribute("atleast").map(_.text),
      node.attribute("atmost").map(_.text),
      node.attribute("min").map(_.text),
      node.attribute("max").map(_.text),
      node.attribute("confidence").map(_.text),
      node.attribute("extent").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object SubstParser extends MeiModelDummyParser {
  def parse(node: Node): Subst = {
    Subst(
      node.child.collect {
        case n: Elem if n.label == "add"       => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "corr"      => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "damage"    => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "del"       => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "gap"       => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "handShift" => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "orig"      => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "reg"       => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "restore"   => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sic"       => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "supplied"  => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "unclear"   => mei.allanystart.parser.UnclearParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("instant").map(_.text),
      node.attribute("state").map(_.text),
      node.attribute("hand").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("seq").map(_.text),
    )
  }
}
object SuppliedParser extends MeiModelDummyParser {
  def parse(node: Node): Supplied = {
    Supplied(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "accid"        => mei.allanystart.parser.AccidParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "arpeg"        => mei.allanystart.parser.ArpegParser.parse(n)
        case n: Elem if n.label == "artic"        => mei.allanystart.parser.ArticParser.parse(n)
        case n: Elem if n.label == "attacca"      => mei.allanystart.parser.AttaccaParser.parse(n)
        case n: Elem if n.label == "bTrem"        => mei.allanystart.parser.BTremParser.parse(n)
        case n: Elem if n.label == "barLine"      => mei.allanystart.parser.BarLineParser.parse(n)
        case n: Elem if n.label == "beam"         => mei.allanystart.parser.BeamParser.parse(n)
        case n: Elem if n.label == "beamSpan"     => mei.allanystart.parser.BeamSpanParser.parse(n)
        case n: Elem if n.label == "beatRpt"      => mei.allanystart.parser.BeatRptParser.parse(n)
        case n: Elem if n.label == "bend"         => mei.allanystart.parser.BendParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblList"     => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "bracketSpan"  => mei.allanystart.parser.BracketSpanParser.parse(n)
        case n: Elem if n.label == "breath"       => mei.allanystart.parser.BreathParser.parse(n)
        case n: Elem if n.label == "caesura"      => mei.allanystart.parser.CaesuraParser.parse(n)
        case n: Elem if n.label == "castList"     => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "cb"           => mei.allanystart.parser.CbParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "chord"        => mei.allanystart.parser.ChordParser.parse(n)
        case n: Elem if n.label == "clef"         => mei.allanystart.parser.ClefParser.parse(n)
        case n: Elem if n.label == "clefGrp"      => mei.allanystart.parser.ClefGrpParser.parse(n)
        case n: Elem if n.label == "colLayout"    => mei.allanystart.parser.ColLayoutParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "cpMark"       => mei.allanystart.parser.CpMarkParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "custos"       => mei.allanystart.parser.CustosParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "dir"          => mei.allanystart.parser.DirParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "div"          => mei.allanystart.parser.DivParser.parse(n)
        case n: Elem if n.label == "divLine"      => mei.allanystart.parser.DivLineParser.parse(n)
        case n: Elem if n.label == "dot"          => mei.allanystart.parser.DotParser.parse(n)
        case n: Elem if n.label == "dynam"        => mei.allanystart.parser.DynamParser.parse(n)
        case n: Elem if n.label == "ending"       => mei.allanystart.parser.EndingParser.parse(n)
        case n: Elem if n.label == "episema"      => mei.allanystart.parser.EpisemaParser.parse(n)
        case n: Elem if n.label == "eventList"    => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "f"            => mei.allanystart.parser.FParser.parse(n)
        case n: Elem if n.label == "fTrem"        => mei.allanystart.parser.FTremParser.parse(n)
        case n: Elem if n.label == "fermata"      => mei.allanystart.parser.FermataParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "fing"         => mei.allanystart.parser.FingParser.parse(n)
        case n: Elem if n.label == "fingGrp"      => mei.allanystart.parser.FingGrpParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "gliss"        => mei.allanystart.parser.GlissParser.parse(n)
        case n: Elem if n.label == "graceGrp"     => mei.allanystart.parser.GraceGrpParser.parse(n)
        case n: Elem if n.label == "hairpin"      => mei.allanystart.parser.HairpinParser.parse(n)
        case n: Elem if n.label == "halfmRpt"     => mei.allanystart.parser.HalfmRptParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "harm"         => mei.allanystart.parser.HarmParser.parse(n)
        case n: Elem if n.label == "harpPedal"    => mei.allanystart.parser.HarpPedalParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "hispanTick"   => mei.allanystart.parser.HispanTickParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "keyAccid"     => mei.allanystart.parser.KeyAccidParser.parse(n)
        case n: Elem if n.label == "keySig"       => mei.allanystart.parser.KeySigParser.parse(n)
        case n: Elem if n.label == "layer"        => mei.allanystart.parser.LayerParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "lg"           => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "ligature"     => mei.allanystart.parser.LigatureParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "liquescent"   => mei.allanystart.parser.LiquescentParser.parse(n)
        case n: Elem if n.label == "list"         => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "lv"           => mei.allanystart.parser.LvParser.parse(n)
        case n: Elem if n.label == "mRest"        => mei.allanystart.parser.MRestParser.parse(n)
        case n: Elem if n.label == "mRpt"         => mei.allanystart.parser.MRptParser.parse(n)
        case n: Elem if n.label == "mRpt2"        => mei.allanystart.parser.MRpt2Parser.parse(n)
        case n: Elem if n.label == "mSpace"       => mei.allanystart.parser.MSpaceParser.parse(n)
        case n: Elem if n.label == "measure"      => mei.allanystart.parser.MeasureParser.parse(n)
        case n: Elem if n.label == "mensur"       => mei.allanystart.parser.MensurParser.parse(n)
        case n: Elem if n.label == "metaMark"     => mei.allanystart.parser.MetaMarkParser.parse(n)
        case n: Elem if n.label == "meterSig"     => mei.allanystart.parser.MeterSigParser.parse(n)
        case n: Elem if n.label == "meterSigGrp"  => mei.allanystart.parser.MeterSigGrpParser.parse(n)
        case n: Elem if n.label == "midi"         => mei.allanystart.parser.MidiParser.parse(n)
        case n: Elem if n.label == "mordent"      => mei.allanystart.parser.MordentParser.parse(n)
        case n: Elem if n.label == "multiRest"    => mei.allanystart.parser.MultiRestParser.parse(n)
        case n: Elem if n.label == "multiRpt"     => mei.allanystart.parser.MultiRptParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "nc"           => mei.allanystart.parser.NcParser.parse(n)
        case n: Elem if n.label == "ncGrp"        => mei.allanystart.parser.NcGrpParser.parse(n)
        case n: Elem if n.label == "neume"        => mei.allanystart.parser.NeumeParser.parse(n)
        case n: Elem if n.label == "note"         => mei.allanystart.parser.NoteParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "octave"       => mei.allanystart.parser.OctaveParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "oriscus"      => mei.allanystart.parser.OriscusParser.parse(n)
        case n: Elem if n.label == "ornam"        => mei.allanystart.parser.OrnamParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "pad"          => mei.allanystart.parser.PadParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "pedal"        => mei.allanystart.parser.PedalParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "phrase"       => mei.allanystart.parser.PhraseParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "proport"      => mei.allanystart.parser.ProportParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "quilisma"     => mei.allanystart.parser.QuilismaParser.parse(n)
        case n: Elem if n.label == "quote"        => mei.allanystart.parser.QuoteParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "refrain"      => mei.allanystart.parser.RefrainParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "reh"          => mei.allanystart.parser.RehParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repeatMark"   => mei.allanystart.parser.RepeatMarkParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "rest"         => mei.allanystart.parser.RestParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sb"           => mei.allanystart.parser.SbParser.parse(n)
        case n: Elem if n.label == "scoreDef"     => mei.allanystart.parser.ScoreDefParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "section"      => mei.allanystart.parser.SectionParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "signifLet"    => mei.allanystart.parser.SignifLetParser.parse(n)
        case n: Elem if n.label == "slur"         => mei.allanystart.parser.SlurParser.parse(n)
        case n: Elem if n.label == "sp"           => mei.allanystart.parser.SpParser.parse(n)
        case n: Elem if n.label == "space"        => mei.allanystart.parser.SpaceParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "staff"        => mei.allanystart.parser.StaffParser.parse(n)
        case n: Elem if n.label == "staffDef"     => mei.allanystart.parser.StaffDefParser.parse(n)
        case n: Elem if n.label == "staffGrp"     => mei.allanystart.parser.StaffGrpParser.parse(n)
        case n: Elem if n.label == "stageDir"     => mei.allanystart.parser.StageDirParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "strophicus"   => mei.allanystart.parser.StrophicusParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "syl"          => mei.allanystart.parser.SylParser.parse(n)
        case n: Elem if n.label == "syllable"     => mei.allanystart.parser.SyllableParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "tabGrp"       => mei.allanystart.parser.TabGrpParser.parse(n)
        case n: Elem if n.label == "table"        => mei.allanystart.parser.TableParser.parse(n)
        case n: Elem if n.label == "tempo"        => mei.allanystart.parser.TempoParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "tie"          => mei.allanystart.parser.TieParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "trill"        => mei.allanystart.parser.TrillParser.parse(n)
        case n: Elem if n.label == "tuplet"       => mei.allanystart.parser.TupletParser.parse(n)
        case n: Elem if n.label == "tupletSpan"   => mei.allanystart.parser.TupletSpanParser.parse(n)
        case n: Elem if n.label == "turn"         => mei.allanystart.parser.TurnParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "verse"        => mei.allanystart.parser.VerseParser.parse(n)
        case n: Elem if n.label == "volta"        => mei.allanystart.parser.VoltaParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("agent").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("unit").map(_.text),
      node.attribute("atleast").map(_.text),
      node.attribute("atmost").map(_.text),
      node.attribute("min").map(_.text),
      node.attribute("max").map(_.text),
      node.attribute("confidence").map(_.text),
      node.attribute("extent").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("reason").map(_.text),
    )
  }
}
object UnclearParser extends MeiModelDummyParser {
  def parse(node: Node): Unclear = {
    Unclear(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "accid"        => mei.allanystart.parser.AccidParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "arpeg"        => mei.allanystart.parser.ArpegParser.parse(n)
        case n: Elem if n.label == "artic"        => mei.allanystart.parser.ArticParser.parse(n)
        case n: Elem if n.label == "attacca"      => mei.allanystart.parser.AttaccaParser.parse(n)
        case n: Elem if n.label == "bTrem"        => mei.allanystart.parser.BTremParser.parse(n)
        case n: Elem if n.label == "barLine"      => mei.allanystart.parser.BarLineParser.parse(n)
        case n: Elem if n.label == "beam"         => mei.allanystart.parser.BeamParser.parse(n)
        case n: Elem if n.label == "beamSpan"     => mei.allanystart.parser.BeamSpanParser.parse(n)
        case n: Elem if n.label == "beatRpt"      => mei.allanystart.parser.BeatRptParser.parse(n)
        case n: Elem if n.label == "bend"         => mei.allanystart.parser.BendParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblList"     => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "bracketSpan"  => mei.allanystart.parser.BracketSpanParser.parse(n)
        case n: Elem if n.label == "breath"       => mei.allanystart.parser.BreathParser.parse(n)
        case n: Elem if n.label == "caesura"      => mei.allanystart.parser.CaesuraParser.parse(n)
        case n: Elem if n.label == "castList"     => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "cb"           => mei.allanystart.parser.CbParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "chord"        => mei.allanystart.parser.ChordParser.parse(n)
        case n: Elem if n.label == "clef"         => mei.allanystart.parser.ClefParser.parse(n)
        case n: Elem if n.label == "clefGrp"      => mei.allanystart.parser.ClefGrpParser.parse(n)
        case n: Elem if n.label == "colLayout"    => mei.allanystart.parser.ColLayoutParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "cpMark"       => mei.allanystart.parser.CpMarkParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "custos"       => mei.allanystart.parser.CustosParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "dir"          => mei.allanystart.parser.DirParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "div"          => mei.allanystart.parser.DivParser.parse(n)
        case n: Elem if n.label == "divLine"      => mei.allanystart.parser.DivLineParser.parse(n)
        case n: Elem if n.label == "dot"          => mei.allanystart.parser.DotParser.parse(n)
        case n: Elem if n.label == "dynam"        => mei.allanystart.parser.DynamParser.parse(n)
        case n: Elem if n.label == "ending"       => mei.allanystart.parser.EndingParser.parse(n)
        case n: Elem if n.label == "episema"      => mei.allanystart.parser.EpisemaParser.parse(n)
        case n: Elem if n.label == "eventList"    => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "f"            => mei.allanystart.parser.FParser.parse(n)
        case n: Elem if n.label == "fTrem"        => mei.allanystart.parser.FTremParser.parse(n)
        case n: Elem if n.label == "fermata"      => mei.allanystart.parser.FermataParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "fing"         => mei.allanystart.parser.FingParser.parse(n)
        case n: Elem if n.label == "fingGrp"      => mei.allanystart.parser.FingGrpParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "gliss"        => mei.allanystart.parser.GlissParser.parse(n)
        case n: Elem if n.label == "graceGrp"     => mei.allanystart.parser.GraceGrpParser.parse(n)
        case n: Elem if n.label == "hairpin"      => mei.allanystart.parser.HairpinParser.parse(n)
        case n: Elem if n.label == "halfmRpt"     => mei.allanystart.parser.HalfmRptParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "harm"         => mei.allanystart.parser.HarmParser.parse(n)
        case n: Elem if n.label == "harpPedal"    => mei.allanystart.parser.HarpPedalParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "hispanTick"   => mei.allanystart.parser.HispanTickParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "keyAccid"     => mei.allanystart.parser.KeyAccidParser.parse(n)
        case n: Elem if n.label == "keySig"       => mei.allanystart.parser.KeySigParser.parse(n)
        case n: Elem if n.label == "layer"        => mei.allanystart.parser.LayerParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "lg"           => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "ligature"     => mei.allanystart.parser.LigatureParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "liquescent"   => mei.allanystart.parser.LiquescentParser.parse(n)
        case n: Elem if n.label == "list"         => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "lv"           => mei.allanystart.parser.LvParser.parse(n)
        case n: Elem if n.label == "mRest"        => mei.allanystart.parser.MRestParser.parse(n)
        case n: Elem if n.label == "mRpt"         => mei.allanystart.parser.MRptParser.parse(n)
        case n: Elem if n.label == "mRpt2"        => mei.allanystart.parser.MRpt2Parser.parse(n)
        case n: Elem if n.label == "mSpace"       => mei.allanystart.parser.MSpaceParser.parse(n)
        case n: Elem if n.label == "measure"      => mei.allanystart.parser.MeasureParser.parse(n)
        case n: Elem if n.label == "mensur"       => mei.allanystart.parser.MensurParser.parse(n)
        case n: Elem if n.label == "metaMark"     => mei.allanystart.parser.MetaMarkParser.parse(n)
        case n: Elem if n.label == "meterSig"     => mei.allanystart.parser.MeterSigParser.parse(n)
        case n: Elem if n.label == "meterSigGrp"  => mei.allanystart.parser.MeterSigGrpParser.parse(n)
        case n: Elem if n.label == "midi"         => mei.allanystart.parser.MidiParser.parse(n)
        case n: Elem if n.label == "mordent"      => mei.allanystart.parser.MordentParser.parse(n)
        case n: Elem if n.label == "multiRest"    => mei.allanystart.parser.MultiRestParser.parse(n)
        case n: Elem if n.label == "multiRpt"     => mei.allanystart.parser.MultiRptParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "nc"           => mei.allanystart.parser.NcParser.parse(n)
        case n: Elem if n.label == "ncGrp"        => mei.allanystart.parser.NcGrpParser.parse(n)
        case n: Elem if n.label == "neume"        => mei.allanystart.parser.NeumeParser.parse(n)
        case n: Elem if n.label == "note"         => mei.allanystart.parser.NoteParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "octave"       => mei.allanystart.parser.OctaveParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "oriscus"      => mei.allanystart.parser.OriscusParser.parse(n)
        case n: Elem if n.label == "ornam"        => mei.allanystart.parser.OrnamParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "pad"          => mei.allanystart.parser.PadParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "pedal"        => mei.allanystart.parser.PedalParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "phrase"       => mei.allanystart.parser.PhraseParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "proport"      => mei.allanystart.parser.ProportParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "quilisma"     => mei.allanystart.parser.QuilismaParser.parse(n)
        case n: Elem if n.label == "quote"        => mei.allanystart.parser.QuoteParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "refrain"      => mei.allanystart.parser.RefrainParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "reh"          => mei.allanystart.parser.RehParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repeatMark"   => mei.allanystart.parser.RepeatMarkParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "rest"         => mei.allanystart.parser.RestParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sb"           => mei.allanystart.parser.SbParser.parse(n)
        case n: Elem if n.label == "scoreDef"     => mei.allanystart.parser.ScoreDefParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "section"      => mei.allanystart.parser.SectionParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "signifLet"    => mei.allanystart.parser.SignifLetParser.parse(n)
        case n: Elem if n.label == "slur"         => mei.allanystart.parser.SlurParser.parse(n)
        case n: Elem if n.label == "sp"           => mei.allanystart.parser.SpParser.parse(n)
        case n: Elem if n.label == "space"        => mei.allanystart.parser.SpaceParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "staff"        => mei.allanystart.parser.StaffParser.parse(n)
        case n: Elem if n.label == "staffDef"     => mei.allanystart.parser.StaffDefParser.parse(n)
        case n: Elem if n.label == "staffGrp"     => mei.allanystart.parser.StaffGrpParser.parse(n)
        case n: Elem if n.label == "stageDir"     => mei.allanystart.parser.StageDirParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "strophicus"   => mei.allanystart.parser.StrophicusParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "syl"          => mei.allanystart.parser.SylParser.parse(n)
        case n: Elem if n.label == "syllable"     => mei.allanystart.parser.SyllableParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "tabGrp"       => mei.allanystart.parser.TabGrpParser.parse(n)
        case n: Elem if n.label == "table"        => mei.allanystart.parser.TableParser.parse(n)
        case n: Elem if n.label == "tempo"        => mei.allanystart.parser.TempoParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "tie"          => mei.allanystart.parser.TieParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "trill"        => mei.allanystart.parser.TrillParser.parse(n)
        case n: Elem if n.label == "tuplet"       => mei.allanystart.parser.TupletParser.parse(n)
        case n: Elem if n.label == "tupletSpan"   => mei.allanystart.parser.TupletSpanParser.parse(n)
        case n: Elem if n.label == "turn"         => mei.allanystart.parser.TurnParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "verse"        => mei.allanystart.parser.VerseParser.parse(n)
        case n: Elem if n.label == "volta"        => mei.allanystart.parser.VoltaParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("agent").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("unit").map(_.text),
      node.attribute("atleast").map(_.text),
      node.attribute("atmost").map(_.text),
      node.attribute("min").map(_.text),
      node.attribute("max").map(_.text),
      node.attribute("confidence").map(_.text),
      node.attribute("extent").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("hand").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("reason").map(_.text),
    )
  }
}
object FacsimileParser extends MeiModelDummyParser {
  def parse(node: Node): Facsimile = {
    Facsimile(
      node.child.collect {
        case n: Elem if n.label == "graphic" => mei.allanystart.parser.GraphicParser.parse(n)
        case n: Elem if n.label == "surface" => mei.allanystart.parser.SurfaceParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("decls").map(_.text),
    )
  }
}
object SurfaceParser extends MeiModelDummyParser {
  def parse(node: Node): Surface = {
    Surface(
      node.child.collect {
        case n: Elem if n.label == "figDesc" => mei.allanystart.parser.FigDescParser.parse(n)
        case n: Elem if n.label == "graphic" => mei.allanystart.parser.GraphicParser.parse(n)
        case n: Elem if n.label == "zone"    => mei.allanystart.parser.ZoneParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("ulx").map(_.text),
      node.attribute("uly").map(_.text),
      node.attribute("lrx").map(_.text),
      node.attribute("lry").map(_.text),
      node.attribute("rotate").map(_.text),
      node.attribute("data").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("startid").map(_.text),
    )
  }
}
object ZoneParser extends MeiModelDummyParser {
  def parse(node: Node): Zone = {
    Zone(
      node.child.collect {
        case n: Elem if n.label == "figDesc" => mei.allanystart.parser.FigDescParser.parse(n)
        case n: Elem if n.label == "graphic" => mei.allanystart.parser.GraphicParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("ulx").map(_.text),
      node.attribute("uly").map(_.text),
      node.attribute("lrx").map(_.text),
      node.attribute("lry").map(_.text),
      node.attribute("rotate").map(_.text),
      node.attribute("data").map(_.text),
    )
  }
}
object FigParser extends MeiModelDummyParser {
  def parse(node: Node): Fig = {
    Fig(
      node.child.collect {
        case n: Elem if n.label == "caption" => mei.allanystart.parser.CaptionParser.parse(n)
        case n: Elem if n.label == "figDesc" => mei.allanystart.parser.FigDescParser.parse(n)
        case n: Elem if n.label == "graphic" => mei.allanystart.parser.GraphicParser.parse(n)
        case n: Elem if n.label == "score"   => mei.allanystart.parser.ScoreParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("halign").map(_.text),
      node.attribute("valign").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object FigDescParser extends MeiModelDummyParser {
  def parse(node: Node): FigDesc = {
    FigDesc(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblList"     => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "castList"     => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "eventList"    => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "lg"           => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "list"         => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "quote"        => mei.allanystart.parser.QuoteParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "table"        => mei.allanystart.parser.TableParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object GraphicParser extends MeiModelDummyParser {
  def parse(node: Node): Graphic = {
    Graphic(
      node.child.collect {
        case n: Elem if n.label == "zone" => mei.allanystart.parser.ZoneParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("ulx").map(_.text),
      node.attribute("uly").map(_.text),
      node.attribute("height").map(_.text),
      node.attribute("width").map(_.text),
      node.attribute("mimetype").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("xlink:actuate").map(_.text),
      node.attribute("xlink:role").map(_.text),
      node.attribute("xlink:show").map(_.text),
      node.attribute("target").map(_.text),
      node.attribute("targettype").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
    )
  }
}
object TableParser extends MeiModelDummyParser {
  def parse(node: Node): Table = {
    Table(
      node.child.collect {
        case n: Elem if n.label == "caption" => mei.allanystart.parser.CaptionParser.parse(n)
        case n: Elem if n.label == "tr"      => mei.allanystart.parser.TrParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object TdParser extends MeiModelDummyParser {
  def parse(node: Node): Td = {
    Td(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblList"     => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "castList"     => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "eventList"    => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "lg"           => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "list"         => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "quote"        => mei.allanystart.parser.QuoteParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "table"        => mei.allanystart.parser.TableParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("colspan").map(_.text),
      node.attribute("rowspan").map(_.text),
    )
  }
}
object ThParser extends MeiModelDummyParser {
  def parse(node: Node): Th = {
    Th(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblList"     => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "castList"     => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "eventList"    => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "lg"           => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "list"         => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "quote"        => mei.allanystart.parser.QuoteParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "table"        => mei.allanystart.parser.TableParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("colspan").map(_.text),
      node.attribute("rowspan").map(_.text),
    )
  }
}
object TrParser extends MeiModelDummyParser {
  def parse(node: Node): Tr = {
    Tr(
      node.child.collect {
        case n: Elem if n.label == "td" => mei.allanystart.parser.TdParser.parse(n)
        case n: Elem if n.label == "th" => mei.allanystart.parser.ThParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object FingParser extends MeiModelDummyParser {
  def parse(node: Node): Fing = {
    Fing(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
      node.attribute("tstamp2.ges").map(_.text),
      node.attribute("tstamp2.real").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("dur").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("tstamp2").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("lform").map(_.text),
      node.attribute("lwidth").map(_.text),
      node.attribute("lsegs").map(_.text),
      node.attribute("lendsym").map(_.text),
      node.attribute("lendsym.size").map(_.text),
      node.attribute("lstartsym").map(_.text),
      node.attribute("lstartsym.size").map(_.text),
      node.attribute("extender").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("vgrp").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object FingGrpParser extends MeiModelDummyParser {
  def parse(node: Node): FingGrp = {
    FingGrp(
      node.child.collect {
        case n: Elem if n.label == "add"       => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "choice"    => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corr"      => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "damage"    => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "del"       => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "fing"      => mei.allanystart.parser.FingParser.parse(n)
        case n: Elem if n.label == "fingGrp"   => mei.allanystart.parser.FingGrpParser.parse(n)
        case n: Elem if n.label == "gap"       => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "handShift" => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "orig"      => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "reg"       => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "restore"   => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sic"       => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "subst"     => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"  => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "unclear"   => mei.allanystart.parser.UnclearParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
      node.attribute("tstamp2.ges").map(_.text),
      node.attribute("tstamp2.real").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("dur").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("tstamp2").map(_.text),
      node.attribute("form").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("lform").map(_.text),
      node.attribute("lwidth").map(_.text),
      node.attribute("lsegs").map(_.text),
      node.attribute("lendsym").map(_.text),
      node.attribute("lendsym.size").map(_.text),
      node.attribute("lstartsym").map(_.text),
      node.attribute("lstartsym.size").map(_.text),
      node.attribute("extender").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("vgrp").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("orient").map(_.text),
    )
  }
}
object ExpressionParser extends MeiModelDummyParser {
  def parse(node: Node): Expression = {
    Expression(
      node.child.collect {
        case n: Elem if n.label == "arranger"       => mei.allanystart.parser.ArrangerParser.parse(n)
        case n: Elem if n.label == "author"         => mei.allanystart.parser.AuthorParser.parse(n)
        case n: Elem if n.label == "biblList"       => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "classification" => mei.allanystart.parser.ClassificationParser.parse(n)
        case n: Elem if n.label == "componentList"  => mei.allanystart.parser.ComponentListParser.parse(n)
        case n: Elem if n.label == "composer"       => mei.allanystart.parser.ComposerParser.parse(n)
        case n: Elem if n.label == "contents"       => mei.allanystart.parser.ContentsParser.parse(n)
        case n: Elem if n.label == "context"        => mei.allanystart.parser.ContextParser.parse(n)
        case n: Elem if n.label == "contributor"    => mei.allanystart.parser.ContributorParser.parse(n)
        case n: Elem if n.label == "creation"       => mei.allanystart.parser.CreationParser.parse(n)
        case n: Elem if n.label == "editor"         => mei.allanystart.parser.EditorParser.parse(n)
        case n: Elem if n.label == "extMeta"        => mei.allanystart.parser.ExtMetaParser.parse(n)
        case n: Elem if n.label == "extent"         => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "funder"         => mei.allanystart.parser.FunderParser.parse(n)
        case n: Elem if n.label == "head"           => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "history"        => mei.allanystart.parser.HistoryParser.parse(n)
        case n: Elem if n.label == "identifier"     => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "incip"          => mei.allanystart.parser.IncipParser.parse(n)
        case n: Elem if n.label == "key"            => mei.allanystart.parser.KeyParser.parse(n)
        case n: Elem if n.label == "langUsage"      => mei.allanystart.parser.LangUsageParser.parse(n)
        case n: Elem if n.label == "librettist"     => mei.allanystart.parser.LibrettistParser.parse(n)
        case n: Elem if n.label == "lyricist"       => mei.allanystart.parser.LyricistParser.parse(n)
        case n: Elem if n.label == "mensuration"    => mei.allanystart.parser.MensurationParser.parse(n)
        case n: Elem if n.label == "meter"          => mei.allanystart.parser.MeterParser.parse(n)
        case n: Elem if n.label == "notesStmt"      => mei.allanystart.parser.NotesStmtParser.parse(n)
        case n: Elem if n.label == "otherChar"      => mei.allanystart.parser.OtherCharParser.parse(n)
        case n: Elem if n.label == "perfDuration"   => mei.allanystart.parser.PerfDurationParser.parse(n)
        case n: Elem if n.label == "perfMedium"     => mei.allanystart.parser.PerfMediumParser.parse(n)
        case n: Elem if n.label == "relationList"   => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "scoreFormat"    => mei.allanystart.parser.ScoreFormatParser.parse(n)
        case n: Elem if n.label == "sponsor"        => mei.allanystart.parser.SponsorParser.parse(n)
        case n: Elem if n.label == "tempo"          => mei.allanystart.parser.TempoParser.parse(n)
        case n: Elem if n.label == "title"          => mei.allanystart.parser.TitleParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("data").map(_.text),
    )
  }
}
object ExpressionListParser extends MeiModelDummyParser {
  def parse(node: Node): ExpressionList = {
    ExpressionList(
      node.child.collect {
        case n: Elem if n.label == "expression" => mei.allanystart.parser.ExpressionParser.parse(n)
        case n: Elem if n.label == "head"       => mei.allanystart.parser.HeadParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
    )
  }
}
object ItemParser extends MeiModelDummyParser {
  def parse(node: Node): Item = {
    Item(
      node.child.collect {
        case n: Elem if n.label == "availability"   => mei.allanystart.parser.AvailabilityParser.parse(n)
        case n: Elem if n.label == "classification" => mei.allanystart.parser.ClassificationParser.parse(n)
        case n: Elem if n.label == "componentList"  => mei.allanystart.parser.ComponentListParser.parse(n)
        case n: Elem if n.label == "extMeta"        => mei.allanystart.parser.ExtMetaParser.parse(n)
        case n: Elem if n.label == "head"           => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "history"        => mei.allanystart.parser.HistoryParser.parse(n)
        case n: Elem if n.label == "identifier"     => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "notesStmt"      => mei.allanystart.parser.NotesStmtParser.parse(n)
        case n: Elem if n.label == "physDesc"       => mei.allanystart.parser.PhysDescParser.parse(n)
        case n: Elem if n.label == "physLoc"        => mei.allanystart.parser.PhysLocParser.parse(n)
        case n: Elem if n.label == "relationList"   => mei.allanystart.parser.RelationListParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("data").map(_.text),
      node.attribute("xlink:actuate").map(_.text),
      node.attribute("xlink:role").map(_.text),
      node.attribute("xlink:show").map(_.text),
      node.attribute("target").map(_.text),
      node.attribute("targettype").map(_.text),
      node.attribute("evaluate").map(_.text),
    )
  }
}
object ItemListParser extends MeiModelDummyParser {
  def parse(node: Node): ItemList = {
    ItemList(
      node.child.collect {
        case n: Elem if n.label == "head" => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "item" => mei.allanystart.parser.ItemParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
    )
  }
}
object ManifestationParser extends MeiModelDummyParser {
  def parse(node: Node): Manifestation = {
    Manifestation(
      node.child.collect {
        case n: Elem if n.label == "availability"   => mei.allanystart.parser.AvailabilityParser.parse(n)
        case n: Elem if n.label == "biblList"       => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "classification" => mei.allanystart.parser.ClassificationParser.parse(n)
        case n: Elem if n.label == "componentList"  => mei.allanystart.parser.ComponentListParser.parse(n)
        case n: Elem if n.label == "contents"       => mei.allanystart.parser.ContentsParser.parse(n)
        case n: Elem if n.label == "creation"       => mei.allanystart.parser.CreationParser.parse(n)
        case n: Elem if n.label == "editionStmt"    => mei.allanystart.parser.EditionStmtParser.parse(n)
        case n: Elem if n.label == "extMeta"        => mei.allanystart.parser.ExtMetaParser.parse(n)
        case n: Elem if n.label == "head"           => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "history"        => mei.allanystart.parser.HistoryParser.parse(n)
        case n: Elem if n.label == "identifier"     => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "itemList"       => mei.allanystart.parser.ItemListParser.parse(n)
        case n: Elem if n.label == "langUsage"      => mei.allanystart.parser.LangUsageParser.parse(n)
        case n: Elem if n.label == "locus"          => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"       => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "notesStmt"      => mei.allanystart.parser.NotesStmtParser.parse(n)
        case n: Elem if n.label == "physDesc"       => mei.allanystart.parser.PhysDescParser.parse(n)
        case n: Elem if n.label == "physLoc"        => mei.allanystart.parser.PhysLocParser.parse(n)
        case n: Elem if n.label == "pubStmt"        => mei.allanystart.parser.PubStmtParser.parse(n)
        case n: Elem if n.label == "relationList"   => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "seriesStmt"     => mei.allanystart.parser.SeriesStmtParser.parse(n)
        case n: Elem if n.label == "titleStmt"      => mei.allanystart.parser.TitleStmtParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("comptype").map(_.text),
      node.attribute("data").map(_.text),
      node.attribute("xlink:actuate").map(_.text),
      node.attribute("xlink:role").map(_.text),
      node.attribute("xlink:show").map(_.text),
      node.attribute("target").map(_.text),
      node.attribute("targettype").map(_.text),
      node.attribute("recordtype").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("singleton").map(_.text),
    )
  }
}
object ManifestationListParser extends MeiModelDummyParser {
  def parse(node: Node): ManifestationList = {
    ManifestationList(
      node.child.collect {
        case n: Elem if n.label == "head"          => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "manifestation" => mei.allanystart.parser.ManifestationParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
    )
  }
}
object GenDescParser extends MeiModelDummyParser {
  def parse(node: Node): GenDesc = {
    GenDesc(
      node.child.collect {
        case n: Elem if n.label == "genDesc"  => mei.allanystart.parser.GenDescParser.parse(n)
        case n: Elem if n.label == "genState" => mei.allanystart.parser.GenStateParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("ordered").map(_.text),
    )
  }
}
object GenStateParser extends MeiModelDummyParser {
  def parse(node: Node): GenState = {
    GenState(
      node.child.collect {
        case n: Elem if n.label == "date"     => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "desc"     => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "respStmt" => mei.allanystart.parser.RespStmtParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("data").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("xlink:actuate").map(_.text),
      node.attribute("xlink:role").map(_.text),
      node.attribute("xlink:show").map(_.text),
      node.attribute("target").map(_.text),
      node.attribute("targettype").map(_.text),
    )
  }
}
object ChordDefParser extends MeiModelDummyParser {
  def parse(node: Node): ChordDef = {
    ChordDef(
      node.child.collect {
        case n: Elem if n.label == "barre"       => mei.allanystart.parser.BarreParser.parse(n)
        case n: Elem if n.label == "chordMember" => mei.allanystart.parser.ChordMemberParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("tab.pos").map(_.text),
      node.attribute("tab.strings").map(_.text),
      node.attribute("tab.courses").map(_.text),
    )
  }
}
object ChordMemberParser extends MeiModelDummyParser {
  def parse(node: Node): ChordMember = {
    ChordMember(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("inth").map(_.text),
      node.attribute("accid.ges").map(_.text),
      node.attribute("pname").map(_.text),
      node.attribute("oct").map(_.text),
      node.attribute("tab.fing").map(_.text),
      node.attribute("tab.fret").map(_.text),
      node.attribute("tab.line").map(_.text),
      node.attribute("tab.string").map(_.text),
      node.attribute("tab.course").map(_.text),
    )
  }
}
object ChordTableParser extends MeiModelDummyParser {
  def parse(node: Node): ChordTable = {
    ChordTable(
      node.child.collect {
        case n: Elem if n.label == "chordDef" => mei.allanystart.parser.ChordDefParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
    )
  }
}
object FParser extends MeiModelDummyParser {
  def parse(node: Node): F = {
    F(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
      node.attribute("tstamp2.ges").map(_.text),
      node.attribute("tstamp2.real").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("dur").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("tstamp2").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("lform").map(_.text),
      node.attribute("lwidth").map(_.text),
      node.attribute("lsegs").map(_.text),
      node.attribute("lendsym").map(_.text),
      node.attribute("lendsym.size").map(_.text),
      node.attribute("lstartsym").map(_.text),
      node.attribute("lstartsym.size").map(_.text),
      node.attribute("extender").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object FbParser extends MeiModelDummyParser {
  def parse(node: Node): Fb = {
    Fb(
      node.child.collect {
        case n: Elem if n.label == "add"       => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "choice"    => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corr"      => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "damage"    => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "del"       => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "f"         => mei.allanystart.parser.FParser.parse(n)
        case n: Elem if n.label == "gap"       => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "handShift" => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "orig"      => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "reg"       => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "restore"   => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sic"       => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "subst"     => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"  => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "unclear"   => mei.allanystart.parser.UnclearParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
    )
  }
}
object HarmParser extends MeiModelDummyParser {
  def parse(node: Node): Harm = {
    Harm(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fb"           => mei.allanystart.parser.FbParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("inth").map(_.text),
      node.attribute("form").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
      node.attribute("tstamp2.ges").map(_.text),
      node.attribute("tstamp2.real").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("dur").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("tstamp2").map(_.text),
      node.attribute("chordref").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("lform").map(_.text),
      node.attribute("lwidth").map(_.text),
      node.attribute("lsegs").map(_.text),
      node.attribute("lendsym").map(_.text),
      node.attribute("lendsym.size").map(_.text),
      node.attribute("lstartsym").map(_.text),
      node.attribute("lstartsym.size").map(_.text),
      node.attribute("extender").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("vgrp").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("startho").map(_.text),
      node.attribute("endho").map(_.text),
      node.attribute("startto").map(_.text),
      node.attribute("endto").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("rendgrid").map(_.text),
    )
  }
}
object AccessRestrictParser extends MeiModelDummyParser {
  def parse(node: Node): AccessRestrict = {
    AccessRestrict(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object AcquisitionParser extends MeiModelDummyParser {
  def parse(node: Node): Acquisition = {
    Acquisition(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "eventList"    => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object AltIdParser extends MeiModelDummyParser {
  def parse(node: Node): AltId = {
    AltId(
      node.child.collect {
        case n: Elem if n.label == "lb"    => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "rend"  => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "stack" => mei.allanystart.parser.StackParser.parse(n)
        case n if n.text.trim.nonEmpty     => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
    )
  }
}
object AppInfoParser extends MeiModelDummyParser {
  def parse(node: Node): AppInfo = {
    AppInfo(
      node.child.collect {
        case n: Elem if n.label == "application" => mei.allanystart.parser.ApplicationParser.parse(n)
        case n: Elem if n.label == "head"        => mei.allanystart.parser.HeadParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
    )
  }
}
object ApplicationParser extends MeiModelDummyParser {
  def parse(node: Node): Application = {
    Application(
      node.child.collect {
        case n: Elem if n.label == "name" => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "p"    => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "ptr"  => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "ref"  => mei.allanystart.parser.RefParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("version").map(_.text),
    )
  }
}
object AttUsageParser extends MeiModelDummyParser {
  def parse(node: Node): AttUsage = {
    AttUsage(
      node.child.collect {
        case n: Elem if n.label == "desc" => mei.allanystart.parser.DescParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("name").map(_.text).get,
      node.attribute("context").map(_.text),
    )
  }
}
object AudienceParser extends MeiModelDummyParser {
  def parse(node: Node): Audience = {
    Audience(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object AvailabilityParser extends MeiModelDummyParser {
  def parse(node: Node): Availability = {
    Availability(
      node.child.collect {
        case n: Elem if n.label == "accessRestrict" => mei.allanystart.parser.AccessRestrictParser.parse(n)
        case n: Elem if n.label == "address"        => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "date"           => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "distributor"    => mei.allanystart.parser.DistributorParser.parse(n)
        case n: Elem if n.label == "head"           => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "identifier"     => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "price"          => mei.allanystart.parser.PriceParser.parse(n)
        case n: Elem if n.label == "sysReq"         => mei.allanystart.parser.SysReqParser.parse(n)
        case n: Elem if n.label == "useRestrict"    => mei.allanystart.parser.UseRestrictParser.parse(n)
        case n if n.text.trim.nonEmpty              => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("data").map(_.text),
    )
  }
}
object BifoliumParser extends MeiModelDummyParser {
  def parse(node: Node): Bifolium = {
    Bifolium(
      node.child.collect {
        case n: Elem if n.label == "add"      => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "bifolium" => mei.allanystart.parser.BifoliumParser.parse(n)
        case n: Elem if n.label == "cutout"   => mei.allanystart.parser.CutoutParser.parse(n)
        case n: Elem if n.label == "damage"   => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "del"      => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "folium"   => mei.allanystart.parser.FoliumParser.parse(n)
        case n: Elem if n.label == "gap"      => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "patch"    => mei.allanystart.parser.PatchParser.parse(n)
        case n: Elem if n.label == "restore"  => mei.allanystart.parser.RestoreParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("height").map(_.text),
      node.attribute("width").map(_.text),
      node.attribute("unit").map(_.text),
      node.attribute("outer.recto").map(_.text),
      node.attribute("inner.verso").map(_.text),
      node.attribute("inner.recto").map(_.text),
      node.attribute("outer.verso").map(_.text),
    )
  }
}
object BylineParser extends MeiModelDummyParser {
  def parse(node: Node): Byline = {
    Byline(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "arranger"     => mei.allanystart.parser.ArrangerParser.parse(n)
        case n: Elem if n.label == "author"       => mei.allanystart.parser.AuthorParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "composer"     => mei.allanystart.parser.ComposerParser.parse(n)
        case n: Elem if n.label == "contributor"  => mei.allanystart.parser.ContributorParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "editor"       => mei.allanystart.parser.EditorParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "funder"       => mei.allanystart.parser.FunderParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "librettist"   => mei.allanystart.parser.LibrettistParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "lyricist"     => mei.allanystart.parser.LyricistParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "sponsor"      => mei.allanystart.parser.SponsorParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object CaptureModeParser extends MeiModelDummyParser {
  def parse(node: Node): CaptureMode = {
    CaptureMode(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object CarrierFormParser extends MeiModelDummyParser {
  def parse(node: Node): CarrierForm = {
    CarrierForm(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object CategoryParser extends MeiModelDummyParser {
  def parse(node: Node): Category = {
    Category(
      node.child.collect {
        case n: Elem if n.label == "altId"    => mei.allanystart.parser.AltIdParser.parse(n)
        case n: Elem if n.label == "catRel"   => mei.allanystart.parser.CatRelParser.parse(n)
        case n: Elem if n.label == "category" => mei.allanystart.parser.CategoryParser.parse(n)
        case n: Elem if n.label == "desc"     => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "label"    => mei.allanystart.parser.LabelParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("data").map(_.text),
    )
  }
}
object CatRelParser extends MeiModelDummyParser {
  def parse(node: Node): CatRel = {
    CatRel(
      node.child.collect {
        case n: Elem if n.label == "desc"  => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "label" => mei.allanystart.parser.LabelParser.parse(n)
      }.toList,
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("type").map(_.text).get,
    )
  }
}
object ChangeParser extends MeiModelDummyParser {
  def parse(node: Node): Change = {
    Change(
      node.child.collect {
        case n: Elem if n.label == "changeDesc" => mei.allanystart.parser.ChangeDescParser.parse(n)
        case n: Elem if n.label == "date"       => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "respStmt"   => mei.allanystart.parser.RespStmtParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
    )
  }
}
object ChangeDescParser extends MeiModelDummyParser {
  def parse(node: Node): ChangeDesc = {
    ChangeDesc(
      node.child.collect {
        case n: Elem if n.label == "p" => mei.allanystart.parser.PParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object ClassDeclsParser extends MeiModelDummyParser {
  def parse(node: Node): ClassDecls = {
    ClassDecls(
      node.child.collect {
        case n: Elem if n.label == "head"     => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "taxonomy" => mei.allanystart.parser.TaxonomyParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
    )
  }
}
object ClassificationParser extends MeiModelDummyParser {
  def parse(node: Node): Classification = {
    Classification(
      node.child.collect {
        case n: Elem if n.label == "head"     => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "termList" => mei.allanystart.parser.TermListParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("data").map(_.text),
    )
  }
}
object ComponentListParser extends MeiModelDummyParser {
  def parse(node: Node): ComponentList = {
    ComponentList(
      node.child.collect {
        case n: Elem if n.label == "expression"    => mei.allanystart.parser.ExpressionParser.parse(n)
        case n: Elem if n.label == "head"          => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "item"          => mei.allanystart.parser.ItemParser.parse(n)
        case n: Elem if n.label == "manifestation" => mei.allanystart.parser.ManifestationParser.parse(n)
        case n: Elem if n.label == "work"          => mei.allanystart.parser.WorkParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
    )
  }
}
object ConditionParser extends MeiModelDummyParser {
  def parse(node: Node): Condition = {
    Condition(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object ContentItemParser extends MeiModelDummyParser {
  def parse(node: Node): ContentItem = {
    ContentItem(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object ContentsParser extends MeiModelDummyParser {
  def parse(node: Node): Contents = {
    Contents(
      node.child.collect {
        case n: Elem if n.label == "contentItem" => mei.allanystart.parser.ContentItemParser.parse(n)
        case n: Elem if n.label == "head"        => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "label"       => mei.allanystart.parser.LabelParser.parse(n)
        case n: Elem if n.label == "p"           => mei.allanystart.parser.PParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xlink:actuate").map(_.text),
      node.attribute("xlink:role").map(_.text),
      node.attribute("xlink:show").map(_.text),
      node.attribute("target").map(_.text),
      node.attribute("targettype").map(_.text),
    )
  }
}
object ContextParser extends MeiModelDummyParser {
  def parse(node: Node): Context = {
    Context(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object CorrectionParser extends MeiModelDummyParser {
  def parse(node: Node): Correction = {
    Correction(
      node.child.collect {
        case n: Elem if n.label == "head" => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "p"    => mei.allanystart.parser.PParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("data").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("method").map(_.text),
      node.attribute("corrlevel").map(_.text),
    )
  }
}
object CutoutParser extends MeiModelDummyParser {
  def parse(node: Node): Cutout = {
    Cutout(
      node.child.collect {
        case n: Elem if n.label == "bifolium" => mei.allanystart.parser.BifoliumParser.parse(n)
        case n: Elem if n.label == "folium"   => mei.allanystart.parser.FoliumParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("height").map(_.text),
      node.attribute("width").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("unit").map(_.text),
      node.attribute("instant").map(_.text),
      node.attribute("state").map(_.text),
      node.attribute("hand").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("seq").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("removed.from").map(_.text).get,
      node.attribute("removed.by").map(_.text),
    )
  }
}
object DedicationParser extends MeiModelDummyParser {
  def parse(node: Node): Dedication = {
    Dedication(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblList"     => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "castList"     => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "eventList"    => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "lg"           => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "list"         => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "quote"        => mei.allanystart.parser.QuoteParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "table"        => mei.allanystart.parser.TableParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object DomainsDeclParser extends MeiModelDummyParser {
  def parse(node: Node): DomainsDecl = {
    DomainsDecl(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("anl").map(_.text).get,
      node.attribute("ges").map(_.text).get,
      node.attribute("vis").map(_.text).get,
    )
  }
}
object EditionStmtParser extends MeiModelDummyParser {
  def parse(node: Node): EditionStmt = {
    EditionStmt(
      node.child.collect {
        case n: Elem if n.label == "arranger"    => mei.allanystart.parser.ArrangerParser.parse(n)
        case n: Elem if n.label == "author"      => mei.allanystart.parser.AuthorParser.parse(n)
        case n: Elem if n.label == "composer"    => mei.allanystart.parser.ComposerParser.parse(n)
        case n: Elem if n.label == "contributor" => mei.allanystart.parser.ContributorParser.parse(n)
        case n: Elem if n.label == "edition"     => mei.allanystart.parser.EditionParser.parse(n)
        case n: Elem if n.label == "editor"      => mei.allanystart.parser.EditorParser.parse(n)
        case n: Elem if n.label == "funder"      => mei.allanystart.parser.FunderParser.parse(n)
        case n: Elem if n.label == "head"        => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "librettist"  => mei.allanystart.parser.LibrettistParser.parse(n)
        case n: Elem if n.label == "lyricist"    => mei.allanystart.parser.LyricistParser.parse(n)
        case n: Elem if n.label == "respStmt"    => mei.allanystart.parser.RespStmtParser.parse(n)
        case n: Elem if n.label == "sponsor"     => mei.allanystart.parser.SponsorParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object EditorialDeclParser extends MeiModelDummyParser {
  def parse(node: Node): EditorialDecl = {
    EditorialDecl(
      node.child.collect {
        case n: Elem if n.label == "correction"     => mei.allanystart.parser.CorrectionParser.parse(n)
        case n: Elem if n.label == "head"           => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "interpretation" => mei.allanystart.parser.InterpretationParser.parse(n)
        case n: Elem if n.label == "normalization"  => mei.allanystart.parser.NormalizationParser.parse(n)
        case n: Elem if n.label == "p"              => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "segmentation"   => mei.allanystart.parser.SegmentationParser.parse(n)
        case n: Elem if n.label == "stdVals"        => mei.allanystart.parser.StdValsParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("data").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object EncodingDescParser extends MeiModelDummyParser {
  def parse(node: Node): EncodingDesc = {
    EncodingDesc(
      node.child.collect {
        case n: Elem if n.label == "appInfo"       => mei.allanystart.parser.AppInfoParser.parse(n)
        case n: Elem if n.label == "classDecls"    => mei.allanystart.parser.ClassDeclsParser.parse(n)
        case n: Elem if n.label == "domainsDecl"   => mei.allanystart.parser.DomainsDeclParser.parse(n)
        case n: Elem if n.label == "editorialDecl" => mei.allanystart.parser.EditorialDeclParser.parse(n)
        case n: Elem if n.label == "head"          => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "projectDesc"   => mei.allanystart.parser.ProjectDescParser.parse(n)
        case n: Elem if n.label == "samplingDecl"  => mei.allanystart.parser.SamplingDeclParser.parse(n)
        case n: Elem if n.label == "tagsDecl"      => mei.allanystart.parser.TagsDeclParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
    )
  }
}
object ExhibHistParser extends MeiModelDummyParser {
  def parse(node: Node): ExhibHist = {
    ExhibHist(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "eventList"    => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object ExtMetaParser extends MeiModelDummyParser {
  def parse(node: Node): ExtMeta = {
    ExtMeta(
      node.child.collect {
        case n if n.text.trim.nonEmpty => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:space").map(_.text),
    )
  }
}
object FileCharParser extends MeiModelDummyParser {
  def parse(node: Node): FileChar = {
    FileChar(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object FileDescParser extends MeiModelDummyParser {
  def parse(node: Node): FileDesc = {
    FileDesc(
      node.child.collect {
        case n: Elem if n.label == "editionStmt" => mei.allanystart.parser.EditionStmtParser.parse(n)
        case n: Elem if n.label == "extent"      => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "notesStmt"   => mei.allanystart.parser.NotesStmtParser.parse(n)
        case n: Elem if n.label == "pubStmt"     => mei.allanystart.parser.PubStmtParser.parse(n)
        case n: Elem if n.label == "seriesStmt"  => mei.allanystart.parser.SeriesStmtParser.parse(n)
        case n: Elem if n.label == "sourceDesc"  => mei.allanystart.parser.SourceDescParser.parse(n)
        case n: Elem if n.label == "titleStmt"   => mei.allanystart.parser.TitleStmtParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
    )
  }
}
object FoliaDescParser extends MeiModelDummyParser {
  def parse(node: Node): FoliaDesc = {
    FoliaDesc(
      node.child.collect {
        case n: Elem if n.label == "add"      => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "bifolium" => mei.allanystart.parser.BifoliumParser.parse(n)
        case n: Elem if n.label == "damage"   => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "del"      => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "folium"   => mei.allanystart.parser.FoliumParser.parse(n)
        case n: Elem if n.label == "gap"      => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "restore"  => mei.allanystart.parser.RestoreParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
    )
  }
}
object FoliumParser extends MeiModelDummyParser {
  def parse(node: Node): Folium = {
    Folium(
      node.child.collect {
        case n: Elem if n.label == "cutout" => mei.allanystart.parser.CutoutParser.parse(n)
        case n: Elem if n.label == "patch"  => mei.allanystart.parser.PatchParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("height").map(_.text),
      node.attribute("width").map(_.text),
      node.attribute("unit").map(_.text),
      node.attribute("recto").map(_.text),
      node.attribute("verso").map(_.text),
    )
  }
}
object HandParser extends MeiModelDummyParser {
  def parse(node: Node): Hand = {
    Hand(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("medium").map(_.text),
      node.attribute("initial").map(_.text),
    )
  }
}
object HandListParser extends MeiModelDummyParser {
  def parse(node: Node): HandList = {
    HandList(
      node.child.collect {
        case n: Elem if n.label == "hand"  => mei.allanystart.parser.HandParser.parse(n)
        case n: Elem if n.label == "head"  => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "label" => mei.allanystart.parser.LabelParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
    )
  }
}
object HistoryParser extends MeiModelDummyParser {
  def parse(node: Node): History = {
    History(
      node.child.collect {
        case n: Elem if n.label == "acquisition" => mei.allanystart.parser.AcquisitionParser.parse(n)
        case n: Elem if n.label == "biblList"    => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "castList"    => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "div"         => mei.allanystart.parser.DivParser.parse(n)
        case n: Elem if n.label == "eventList"   => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "exhibHist"   => mei.allanystart.parser.ExhibHistParser.parse(n)
        case n: Elem if n.label == "head"        => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "lg"          => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "list"        => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "p"           => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "provenance"  => mei.allanystart.parser.ProvenanceParser.parse(n)
        case n: Elem if n.label == "quote"       => mei.allanystart.parser.QuoteParser.parse(n)
        case n: Elem if n.label == "table"       => mei.allanystart.parser.TableParser.parse(n)
        case n: Elem if n.label == "treatHist"   => mei.allanystart.parser.TreatHistParser.parse(n)
        case n: Elem if n.label == "treatSched"  => mei.allanystart.parser.TreatSchedParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
    )
  }
}
object IncipCodeParser extends MeiModelDummyParser {
  def parse(node: Node): IncipCode = {
    IncipCode(
      node.child.collect {
        case n if n.text.trim.nonEmpty => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("mimetype").map(_.text),
      node.attribute("xlink:actuate").map(_.text),
      node.attribute("xlink:role").map(_.text),
      node.attribute("xlink:show").map(_.text),
      node.attribute("target").map(_.text),
      node.attribute("targettype").map(_.text),
      node.attribute("xml:space").map(_.text),
      node.attribute("form").map(_.text),
    )
  }
}
object IncipTextParser extends MeiModelDummyParser {
  def parse(node: Node): IncipText = {
    IncipText(
      node.child.collect {
        case n: Elem if n.label == "head" => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "lg"   => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "p"    => mei.allanystart.parser.PParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("xlink:actuate").map(_.text),
      node.attribute("xlink:role").map(_.text),
      node.attribute("xlink:show").map(_.text),
      node.attribute("target").map(_.text),
      node.attribute("targettype").map(_.text),
      node.attribute("mimetype").map(_.text),
    )
  }
}
object InscriptionParser extends MeiModelDummyParser {
  def parse(node: Node): Inscription = {
    Inscription(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object InterpretationParser extends MeiModelDummyParser {
  def parse(node: Node): Interpretation = {
    Interpretation(
      node.child.collect {
        case n: Elem if n.label == "head" => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "p"    => mei.allanystart.parser.PParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("data").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object KeyParser extends MeiModelDummyParser {
  def parse(node: Node): Key = {
    Key(
      node.child.collect {
        case n if n.text.trim.nonEmpty => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("accid").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("mode").map(_.text),
      node.attribute("pname").map(_.text),
    )
  }
}
object LanguageParser extends MeiModelDummyParser {
  def parse(node: Node): Language = {
    Language(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object LangUsageParser extends MeiModelDummyParser {
  def parse(node: Node): LangUsage = {
    LangUsage(
      node.child.collect {
        case n: Elem if n.label == "head"     => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "language" => mei.allanystart.parser.LanguageParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("data").map(_.text),
    )
  }
}
object MeiHeadParser extends MeiModelDummyParser {
  def parse(node: Node): MeiHead = {
    MeiHead(
      node.child.collect {
        case n: Elem if n.label == "altId"             => mei.allanystart.parser.AltIdParser.parse(n)
        case n: Elem if n.label == "encodingDesc"      => mei.allanystart.parser.EncodingDescParser.parse(n)
        case n: Elem if n.label == "extMeta"           => mei.allanystart.parser.ExtMetaParser.parse(n)
        case n: Elem if n.label == "fileDesc"          => mei.allanystart.parser.FileDescParser.parse(n)
        case n: Elem if n.label == "manifestationList" => mei.allanystart.parser.ManifestationListParser.parse(n)
        case n: Elem if n.label == "revisionDesc"      => mei.allanystart.parser.RevisionDescParser.parse(n)
        case n: Elem if n.label == "workList"          => mei.allanystart.parser.WorkListParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("type").map(_.text),
    )
  }
}
object MensurationParser extends MeiModelDummyParser {
  def parse(node: Node): Mensuration = {
    Mensuration(
      node.child.collect {
        case n if n.text.trim.nonEmpty => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("num").map(_.text),
      node.attribute("numbase").map(_.text),
      node.attribute("modusmaior").map(_.text),
      node.attribute("modusminor").map(_.text),
      node.attribute("prolatio").map(_.text),
      node.attribute("tempus").map(_.text),
      node.attribute("divisio").map(_.text),
      node.attribute("level").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("loc").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("slash").map(_.text),
      node.attribute("dot").map(_.text),
      node.attribute("form").map(_.text),
      node.attribute("orient").map(_.text),
      node.attribute("sign").map(_.text),
    )
  }
}
object MeterParser extends MeiModelDummyParser {
  def parse(node: Node): Meter = {
    Meter(
      node.child.collect {
        case n if n.text.trim.nonEmpty => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("count").map(_.text),
      node.attribute("sym").map(_.text),
      node.attribute("unit").map(_.text),
    )
  }
}
object NamespaceParser extends MeiModelDummyParser {
  def parse(node: Node): Namespace = {
    Namespace(
      node.child.collect {
        case n: Elem if n.label == "attUsage" => mei.allanystart.parser.AttUsageParser.parse(n)
        case n: Elem if n.label == "desc"     => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "tagUsage" => mei.allanystart.parser.TagUsageParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("name").map(_.text).get,
      node.attribute("prefix").map(_.text),
    )
  }
}
object NormalizationParser extends MeiModelDummyParser {
  def parse(node: Node): Normalization = {
    Normalization(
      node.child.collect {
        case n: Elem if n.label == "head" => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "p"    => mei.allanystart.parser.PParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("data").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("method").map(_.text),
    )
  }
}
object NotesStmtParser extends MeiModelDummyParser {
  def parse(node: Node): NotesStmt = {
    NotesStmt(
      node.child.collect {
        case n: Elem if n.label == "annot" => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "head"  => mei.allanystart.parser.HeadParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
    )
  }
}
object OtherCharParser extends MeiModelDummyParser {
  def parse(node: Node): OtherChar = {
    OtherChar(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object PatchParser extends MeiModelDummyParser {
  def parse(node: Node): Patch = {
    Patch(
      node.child.collect {
        case n: Elem if n.label == "bifolium" => mei.allanystart.parser.BifoliumParser.parse(n)
        case n: Elem if n.label == "folium"   => mei.allanystart.parser.FoliumParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("unit").map(_.text),
      node.attribute("instant").map(_.text),
      node.attribute("state").map(_.text),
      node.attribute("hand").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("seq").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("attached.to").map(_.text).get,
      node.attribute("attached.by").map(_.text),
    )
  }
}
object PerfDurationParser extends MeiModelDummyParser {
  def parse(node: Node): PerfDuration = {
    PerfDuration(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("isodur").map(_.text),
    )
  }
}
object PerfMediumParser extends MeiModelDummyParser {
  def parse(node: Node): PerfMedium = {
    PerfMedium(
      node.child.collect {
        case n: Elem if n.label == "annot"       => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "castList"    => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "head"        => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "perfResList" => mei.allanystart.parser.PerfResListParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("analog").map(_.text),
    )
  }
}
object PerfResParser extends MeiModelDummyParser {
  def parse(node: Node): PerfRes = {
    PerfRes(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "perfRes"      => mei.allanystart.parser.PerfResParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("adlib").map(_.text),
      node.attribute("count").map(_.text),
      node.attribute("trans.diat").map(_.text),
      node.attribute("trans.semi").map(_.text),
      node.attribute("solo").map(_.text),
    )
  }
}
object PerfResListParser extends MeiModelDummyParser {
  def parse(node: Node): PerfResList = {
    PerfResList(
      node.child.collect {
        case n: Elem if n.label == "annot"       => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "head"        => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "perfRes"     => mei.allanystart.parser.PerfResParser.parse(n)
        case n: Elem if n.label == "perfResList" => mei.allanystart.parser.PerfResListParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("adlib").map(_.text),
      node.attribute("count").map(_.text),
    )
  }
}
object PhysDescParser extends MeiModelDummyParser {
  def parse(node: Node): PhysDesc = {
    PhysDesc(
      node.child.collect {
        case n: Elem if n.label == "accMat"       => mei.allanystart.parser.AccMatParser.parse(n)
        case n: Elem if n.label == "addDesc"      => mei.allanystart.parser.AddDescParser.parse(n)
        case n: Elem if n.label == "bindingDesc"  => mei.allanystart.parser.BindingDescParser.parse(n)
        case n: Elem if n.label == "captureMode"  => mei.allanystart.parser.CaptureModeParser.parse(n)
        case n: Elem if n.label == "carrierForm"  => mei.allanystart.parser.CarrierFormParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "colophon"     => mei.allanystart.parser.ColophonParser.parse(n)
        case n: Elem if n.label == "condition"    => mei.allanystart.parser.ConditionParser.parse(n)
        case n: Elem if n.label == "decoDesc"     => mei.allanystart.parser.DecoDescParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "explicit"     => mei.allanystart.parser.ExplicitParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fileChar"     => mei.allanystart.parser.FileCharParser.parse(n)
        case n: Elem if n.label == "foliaDesc"    => mei.allanystart.parser.FoliaDescParser.parse(n)
        case n: Elem if n.label == "handList"     => mei.allanystart.parser.HandListParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "history"      => mei.allanystart.parser.HistoryParser.parse(n)
        case n: Elem if n.label == "incip"        => mei.allanystart.parser.IncipParser.parse(n)
        case n: Elem if n.label == "inscription"  => mei.allanystart.parser.InscriptionParser.parse(n)
        case n: Elem if n.label == "layoutDesc"   => mei.allanystart.parser.LayoutDescParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "perfDuration" => mei.allanystart.parser.PerfDurationParser.parse(n)
        case n: Elem if n.label == "physMedium"   => mei.allanystart.parser.PhysMediumParser.parse(n)
        case n: Elem if n.label == "plateNum"     => mei.allanystart.parser.PlateNumParser.parse(n)
        case n: Elem if n.label == "playingSpeed" => mei.allanystart.parser.PlayingSpeedParser.parse(n)
        case n: Elem if n.label == "rubric"       => mei.allanystart.parser.RubricParser.parse(n)
        case n: Elem if n.label == "scoreFormat"  => mei.allanystart.parser.ScoreFormatParser.parse(n)
        case n: Elem if n.label == "scriptDesc"   => mei.allanystart.parser.ScriptDescParser.parse(n)
        case n: Elem if n.label == "sealDesc"     => mei.allanystart.parser.SealDescParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "soundChan"    => mei.allanystart.parser.SoundChanParser.parse(n)
        case n: Elem if n.label == "specRepro"    => mei.allanystart.parser.SpecReproParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "supportDesc"  => mei.allanystart.parser.SupportDescParser.parse(n)
        case n: Elem if n.label == "titlePage"    => mei.allanystart.parser.TitlePageParser.parse(n)
        case n: Elem if n.label == "trackConfig"  => mei.allanystart.parser.TrackConfigParser.parse(n)
        case n: Elem if n.label == "typeDesc"     => mei.allanystart.parser.TypeDescParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
    )
  }
}
object PhysMediumParser extends MeiModelDummyParser {
  def parse(node: Node): PhysMedium = {
    PhysMedium(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object PlateNumParser extends MeiModelDummyParser {
  def parse(node: Node): PlateNum = {
    PlateNum(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object PlayingSpeedParser extends MeiModelDummyParser {
  def parse(node: Node): PlayingSpeed = {
    PlayingSpeed(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object PriceParser extends MeiModelDummyParser {
  def parse(node: Node): Price = {
    Price(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("amount").map(_.text),
      node.attribute("currency").map(_.text),
    )
  }
}
object ProjectDescParser extends MeiModelDummyParser {
  def parse(node: Node): ProjectDesc = {
    ProjectDesc(
      node.child.collect {
        case n: Elem if n.label == "head" => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "p"    => mei.allanystart.parser.PParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("data").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object ProvenanceParser extends MeiModelDummyParser {
  def parse(node: Node): Provenance = {
    Provenance(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "eventList"    => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object PubStmtParser extends MeiModelDummyParser {
  def parse(node: Node): PubStmt = {
    PubStmt(
      node.child.collect {
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "availability" => mei.allanystart.parser.AvailabilityParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "distributor"  => mei.allanystart.parser.DistributorParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "pubPlace"     => mei.allanystart.parser.PubPlaceParser.parse(n)
        case n: Elem if n.label == "publisher"    => mei.allanystart.parser.PublisherParser.parse(n)
        case n: Elem if n.label == "respStmt"     => mei.allanystart.parser.RespStmtParser.parse(n)
        case n: Elem if n.label == "unpub"        => mei.allanystart.parser.UnpubParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
    )
  }
}
object RevisionDescParser extends MeiModelDummyParser {
  def parse(node: Node): RevisionDesc = {
    RevisionDesc(
      node.child.collect {
        case n: Elem if n.label == "change" => mei.allanystart.parser.ChangeParser.parse(n)
        case n: Elem if n.label == "head"   => mei.allanystart.parser.HeadParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
    )
  }
}
object SamplingDeclParser extends MeiModelDummyParser {
  def parse(node: Node): SamplingDecl = {
    SamplingDecl(
      node.child.collect {
        case n: Elem if n.label == "head" => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "p"    => mei.allanystart.parser.PParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("data").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object ScoreFormatParser extends MeiModelDummyParser {
  def parse(node: Node): ScoreFormat = {
    ScoreFormat(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object SegmentationParser extends MeiModelDummyParser {
  def parse(node: Node): Segmentation = {
    Segmentation(
      node.child.collect {
        case n: Elem if n.label == "head" => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "p"    => mei.allanystart.parser.PParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("data").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object SeriesStmtParser extends MeiModelDummyParser {
  def parse(node: Node): SeriesStmt = {
    SeriesStmt(
      node.child.collect {
        case n: Elem if n.label == "arranger"    => mei.allanystart.parser.ArrangerParser.parse(n)
        case n: Elem if n.label == "author"      => mei.allanystart.parser.AuthorParser.parse(n)
        case n: Elem if n.label == "biblScope"   => mei.allanystart.parser.BiblScopeParser.parse(n)
        case n: Elem if n.label == "composer"    => mei.allanystart.parser.ComposerParser.parse(n)
        case n: Elem if n.label == "contents"    => mei.allanystart.parser.ContentsParser.parse(n)
        case n: Elem if n.label == "contributor" => mei.allanystart.parser.ContributorParser.parse(n)
        case n: Elem if n.label == "editor"      => mei.allanystart.parser.EditorParser.parse(n)
        case n: Elem if n.label == "funder"      => mei.allanystart.parser.FunderParser.parse(n)
        case n: Elem if n.label == "head"        => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "identifier"  => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "librettist"  => mei.allanystart.parser.LibrettistParser.parse(n)
        case n: Elem if n.label == "lyricist"    => mei.allanystart.parser.LyricistParser.parse(n)
        case n: Elem if n.label == "respStmt"    => mei.allanystart.parser.RespStmtParser.parse(n)
        case n: Elem if n.label == "seriesStmt"  => mei.allanystart.parser.SeriesStmtParser.parse(n)
        case n: Elem if n.label == "sponsor"     => mei.allanystart.parser.SponsorParser.parse(n)
        case n: Elem if n.label == "title"       => mei.allanystart.parser.TitleParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
    )
  }
}
object SoundChanParser extends MeiModelDummyParser {
  def parse(node: Node): SoundChan = {
    SoundChan(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("num").map(_.text),
    )
  }
}
object SourceParser extends MeiModelDummyParser {
  def parse(node: Node): Source = {
    Source(
      node.child.collect {
        case n: Elem if n.label == "bibl"       => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct" => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "head"       => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "locus"      => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"   => mei.allanystart.parser.LocusGrpParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("comptype").map(_.text),
      node.attribute("data").map(_.text),
      node.attribute("xlink:actuate").map(_.text),
      node.attribute("xlink:role").map(_.text),
      node.attribute("xlink:show").map(_.text),
      node.attribute("target").map(_.text),
      node.attribute("targettype").map(_.text),
      node.attribute("recordtype").map(_.text),
      node.attribute("evaluate").map(_.text),
    )
  }
}
object SourceDescParser extends MeiModelDummyParser {
  def parse(node: Node): SourceDesc = {
    SourceDesc(
      node.child.collect {
        case n: Elem if n.label == "head"   => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "source" => mei.allanystart.parser.SourceParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
    )
  }
}
object SpecReproParser extends MeiModelDummyParser {
  def parse(node: Node): SpecRepro = {
    SpecRepro(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object StdValsParser extends MeiModelDummyParser {
  def parse(node: Node): StdVals = {
    StdVals(
      node.child.collect {
        case n: Elem if n.label == "head" => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "p"    => mei.allanystart.parser.PParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("data").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object SysReqParser extends MeiModelDummyParser {
  def parse(node: Node): SysReq = {
    SysReq(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object TagsDeclParser extends MeiModelDummyParser {
  def parse(node: Node): TagsDecl = {
    TagsDecl(
      node.child.collect {
        case n: Elem if n.label == "desc"      => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "head"      => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "namespace" => mei.allanystart.parser.NamespaceParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
    )
  }
}
object TagUsageParser extends MeiModelDummyParser {
  def parse(node: Node): TagUsage = {
    TagUsage(
      node.child.collect {
        case n: Elem if n.label == "attUsage" => mei.allanystart.parser.AttUsageParser.parse(n)
        case n: Elem if n.label == "desc"     => mei.allanystart.parser.DescParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("name").map(_.text).get,
      node.attribute("context").map(_.text),
      node.attribute("occurs").map(_.text),
      node.attribute("withid").map(_.text),
    )
  }
}
object TaxonomyParser extends MeiModelDummyParser {
  def parse(node: Node): Taxonomy = {
    Taxonomy(
      node.child.collect {
        case n: Elem if n.label == "bibl"       => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct" => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "category"   => mei.allanystart.parser.CategoryParser.parse(n)
        case n: Elem if n.label == "desc"       => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "head"       => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "taxonomy"   => mei.allanystart.parser.TaxonomyParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
    )
  }
}
object TermListParser extends MeiModelDummyParser {
  def parse(node: Node): TermList = {
    TermList(
      node.child.collect {
        case n: Elem if n.label == "head"  => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "label" => mei.allanystart.parser.LabelParser.parse(n)
        case n: Elem if n.label == "term"  => mei.allanystart.parser.TermParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xlink:actuate").map(_.text),
      node.attribute("xlink:role").map(_.text),
      node.attribute("xlink:show").map(_.text),
      node.attribute("target").map(_.text),
      node.attribute("targettype").map(_.text),
    )
  }
}
object TitleStmtParser extends MeiModelDummyParser {
  def parse(node: Node): TitleStmt = {
    TitleStmt(
      node.child.collect {
        case n: Elem if n.label == "arranger"    => mei.allanystart.parser.ArrangerParser.parse(n)
        case n: Elem if n.label == "author"      => mei.allanystart.parser.AuthorParser.parse(n)
        case n: Elem if n.label == "composer"    => mei.allanystart.parser.ComposerParser.parse(n)
        case n: Elem if n.label == "contributor" => mei.allanystart.parser.ContributorParser.parse(n)
        case n: Elem if n.label == "editor"      => mei.allanystart.parser.EditorParser.parse(n)
        case n: Elem if n.label == "funder"      => mei.allanystart.parser.FunderParser.parse(n)
        case n: Elem if n.label == "head"        => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "librettist"  => mei.allanystart.parser.LibrettistParser.parse(n)
        case n: Elem if n.label == "lyricist"    => mei.allanystart.parser.LyricistParser.parse(n)
        case n: Elem if n.label == "respStmt"    => mei.allanystart.parser.RespStmtParser.parse(n)
        case n: Elem if n.label == "sponsor"     => mei.allanystart.parser.SponsorParser.parse(n)
        case n: Elem if n.label == "title"       => mei.allanystart.parser.TitleParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
    )
  }
}
object TrackConfigParser extends MeiModelDummyParser {
  def parse(node: Node): TrackConfig = {
    TrackConfig(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("num").map(_.text),
    )
  }
}
object TreatHistParser extends MeiModelDummyParser {
  def parse(node: Node): TreatHist = {
    TreatHist(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "eventList"    => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object TreatSchedParser extends MeiModelDummyParser {
  def parse(node: Node): TreatSched = {
    TreatSched(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "eventList"    => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object UnpubParser extends MeiModelDummyParser {
  def parse(node: Node): Unpub = {
    Unpub(
      node.child.collect {
        case n if n.text.trim.nonEmpty => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object UseRestrictParser extends MeiModelDummyParser {
  def parse(node: Node): UseRestrict = {
    UseRestrict(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object WatermarkParser extends MeiModelDummyParser {
  def parse(node: Node): Watermark = {
    Watermark(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object WorkParser extends MeiModelDummyParser {
  def parse(node: Node): Work = {
    Work(
      node.child.collect {
        case n: Elem if n.label == "arranger"       => mei.allanystart.parser.ArrangerParser.parse(n)
        case n: Elem if n.label == "audience"       => mei.allanystart.parser.AudienceParser.parse(n)
        case n: Elem if n.label == "author"         => mei.allanystart.parser.AuthorParser.parse(n)
        case n: Elem if n.label == "biblList"       => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "classification" => mei.allanystart.parser.ClassificationParser.parse(n)
        case n: Elem if n.label == "componentList"  => mei.allanystart.parser.ComponentListParser.parse(n)
        case n: Elem if n.label == "composer"       => mei.allanystart.parser.ComposerParser.parse(n)
        case n: Elem if n.label == "contents"       => mei.allanystart.parser.ContentsParser.parse(n)
        case n: Elem if n.label == "context"        => mei.allanystart.parser.ContextParser.parse(n)
        case n: Elem if n.label == "contributor"    => mei.allanystart.parser.ContributorParser.parse(n)
        case n: Elem if n.label == "creation"       => mei.allanystart.parser.CreationParser.parse(n)
        case n: Elem if n.label == "editor"         => mei.allanystart.parser.EditorParser.parse(n)
        case n: Elem if n.label == "expressionList" => mei.allanystart.parser.ExpressionListParser.parse(n)
        case n: Elem if n.label == "extMeta"        => mei.allanystart.parser.ExtMetaParser.parse(n)
        case n: Elem if n.label == "funder"         => mei.allanystart.parser.FunderParser.parse(n)
        case n: Elem if n.label == "head"           => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "history"        => mei.allanystart.parser.HistoryParser.parse(n)
        case n: Elem if n.label == "identifier"     => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "incip"          => mei.allanystart.parser.IncipParser.parse(n)
        case n: Elem if n.label == "key"            => mei.allanystart.parser.KeyParser.parse(n)
        case n: Elem if n.label == "langUsage"      => mei.allanystart.parser.LangUsageParser.parse(n)
        case n: Elem if n.label == "librettist"     => mei.allanystart.parser.LibrettistParser.parse(n)
        case n: Elem if n.label == "lyricist"       => mei.allanystart.parser.LyricistParser.parse(n)
        case n: Elem if n.label == "mensuration"    => mei.allanystart.parser.MensurationParser.parse(n)
        case n: Elem if n.label == "meter"          => mei.allanystart.parser.MeterParser.parse(n)
        case n: Elem if n.label == "notesStmt"      => mei.allanystart.parser.NotesStmtParser.parse(n)
        case n: Elem if n.label == "otherChar"      => mei.allanystart.parser.OtherCharParser.parse(n)
        case n: Elem if n.label == "perfDuration"   => mei.allanystart.parser.PerfDurationParser.parse(n)
        case n: Elem if n.label == "perfMedium"     => mei.allanystart.parser.PerfMediumParser.parse(n)
        case n: Elem if n.label == "relationList"   => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "sponsor"        => mei.allanystart.parser.SponsorParser.parse(n)
        case n: Elem if n.label == "tempo"          => mei.allanystart.parser.TempoParser.parse(n)
        case n: Elem if n.label == "title"          => mei.allanystart.parser.TitleParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("data").map(_.text),
      node.attribute("xlink:actuate").map(_.text),
      node.attribute("xlink:role").map(_.text),
      node.attribute("xlink:show").map(_.text),
      node.attribute("target").map(_.text),
      node.attribute("targettype").map(_.text),
      node.attribute("evaluate").map(_.text),
    )
  }
}
object WorkListParser extends MeiModelDummyParser {
  def parse(node: Node): WorkList = {
    WorkList(
      node.child.collect {
        case n: Elem if n.label == "head" => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "work" => mei.allanystart.parser.WorkParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
    )
  }
}
object RefrainParser extends MeiModelDummyParser {
  def parse(node: Node): Refrain = {
    Refrain(
      node.child.collect {
        case n: Elem if n.label == "app"    => mei.allanystart.parser.AppParser.parse(n)
        case n: Elem if n.label == "choice" => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "dir"    => mei.allanystart.parser.DirParser.parse(n)
        case n: Elem if n.label == "dynam"  => mei.allanystart.parser.DynamParser.parse(n)
        case n: Elem if n.label == "lb"     => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "space"  => mei.allanystart.parser.SpaceParser.parse(n)
        case n: Elem if n.label == "subst"  => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "syl"    => mei.allanystart.parser.SylParser.parse(n)
        case n: Elem if n.label == "tempo"  => mei.allanystart.parser.TempoParser.parse(n)
        case n: Elem if n.label == "volta"  => mei.allanystart.parser.VoltaParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("voltasym").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object VerseParser extends MeiModelDummyParser {
  def parse(node: Node): Verse = {
    Verse(
      node.child.collect {
        case n: Elem if n.label == "app"       => mei.allanystart.parser.AppParser.parse(n)
        case n: Elem if n.label == "choice"    => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "dir"       => mei.allanystart.parser.DirParser.parse(n)
        case n: Elem if n.label == "dynam"     => mei.allanystart.parser.DynamParser.parse(n)
        case n: Elem if n.label == "label"     => mei.allanystart.parser.LabelParser.parse(n)
        case n: Elem if n.label == "labelAbbr" => mei.allanystart.parser.LabelAbbrParser.parse(n)
        case n: Elem if n.label == "lb"        => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "space"     => mei.allanystart.parser.SpaceParser.parse(n)
        case n: Elem if n.label == "subst"     => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "syl"       => mei.allanystart.parser.SylParser.parse(n)
        case n: Elem if n.label == "tempo"     => mei.allanystart.parser.TempoParser.parse(n)
        case n: Elem if n.label == "volta"     => mei.allanystart.parser.VoltaParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("voltasym").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object VoltaParser extends MeiModelDummyParser {
  def parse(node: Node): Volta = {
    Volta(
      node.child.collect {
        case n: Elem if n.label == "app"    => mei.allanystart.parser.AppParser.parse(n)
        case n: Elem if n.label == "choice" => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "dir"    => mei.allanystart.parser.DirParser.parse(n)
        case n: Elem if n.label == "dynam"  => mei.allanystart.parser.DynamParser.parse(n)
        case n: Elem if n.label == "lb"     => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "space"  => mei.allanystart.parser.SpaceParser.parse(n)
        case n: Elem if n.label == "subst"  => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "syl"    => mei.allanystart.parser.SylParser.parse(n)
        case n: Elem if n.label == "tempo"  => mei.allanystart.parser.TempoParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object LigatureParser extends MeiModelDummyParser {
  def parse(node: Node): Ligature = {
    Ligature(
      node.child.collect {
        case n: Elem if n.label == "add"         => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "app"         => mei.allanystart.parser.AppParser.parse(n)
        case n: Elem if n.label == "barLine"     => mei.allanystart.parser.BarLineParser.parse(n)
        case n: Elem if n.label == "choice"      => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "chord"       => mei.allanystart.parser.ChordParser.parse(n)
        case n: Elem if n.label == "clef"        => mei.allanystart.parser.ClefParser.parse(n)
        case n: Elem if n.label == "clefGrp"     => mei.allanystart.parser.ClefGrpParser.parse(n)
        case n: Elem if n.label == "corr"        => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "custos"      => mei.allanystart.parser.CustosParser.parse(n)
        case n: Elem if n.label == "damage"      => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "del"         => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "divLine"     => mei.allanystart.parser.DivLineParser.parse(n)
        case n: Elem if n.label == "dot"         => mei.allanystart.parser.DotParser.parse(n)
        case n: Elem if n.label == "gap"         => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "handShift"   => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "keySig"      => mei.allanystart.parser.KeySigParser.parse(n)
        case n: Elem if n.label == "ligature"    => mei.allanystart.parser.LigatureParser.parse(n)
        case n: Elem if n.label == "mensur"      => mei.allanystart.parser.MensurParser.parse(n)
        case n: Elem if n.label == "meterSig"    => mei.allanystart.parser.MeterSigParser.parse(n)
        case n: Elem if n.label == "meterSigGrp" => mei.allanystart.parser.MeterSigGrpParser.parse(n)
        case n: Elem if n.label == "neume"       => mei.allanystart.parser.NeumeParser.parse(n)
        case n: Elem if n.label == "note"        => mei.allanystart.parser.NoteParser.parse(n)
        case n: Elem if n.label == "orig"        => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pad"         => mei.allanystart.parser.PadParser.parse(n)
        case n: Elem if n.label == "proport"     => mei.allanystart.parser.ProportParser.parse(n)
        case n: Elem if n.label == "reg"         => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "rest"        => mei.allanystart.parser.RestParser.parse(n)
        case n: Elem if n.label == "restore"     => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sic"         => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "space"       => mei.allanystart.parser.SpaceParser.parse(n)
        case n: Elem if n.label == "subst"       => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"    => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "tabGrp"      => mei.allanystart.parser.TabGrpParser.parse(n)
        case n: Elem if n.label == "unclear"     => mei.allanystart.parser.UnclearParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("form").map(_.text),
    )
  }
}
object MensurParser extends MeiModelDummyParser {
  def parse(node: Node): Mensur = {
    Mensur(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("num").map(_.text),
      node.attribute("numbase").map(_.text),
      node.attribute("modusmaior").map(_.text),
      node.attribute("modusminor").map(_.text),
      node.attribute("prolatio").map(_.text),
      node.attribute("tempus").map(_.text),
      node.attribute("divisio").map(_.text),
      node.attribute("level").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("loc").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("slash").map(_.text),
      node.attribute("dot").map(_.text),
      node.attribute("form").map(_.text),
      node.attribute("orient").map(_.text),
      node.attribute("sign").map(_.text),
    )
  }
}
object PlicaParser extends MeiModelDummyParser {
  def parse(node: Node): Plica = {
    Plica(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("dir").map(_.text),
      node.attribute("len").map(_.text),
    )
  }
}
object ProportParser extends MeiModelDummyParser {
  def parse(node: Node): Proport = {
    Proport(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("num").map(_.text),
      node.attribute("numbase").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("loc").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
    )
  }
}
object StemParser extends MeiModelDummyParser {
  def parse(node: Node): Stem = {
    Stem(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("visible").map(_.text),
      node.attribute("pos").map(_.text),
      node.attribute("len").map(_.text),
      node.attribute("form").map(_.text),
      node.attribute("dir").map(_.text),
      node.attribute("flag.pos").map(_.text),
      node.attribute("flag.form").map(_.text),
    )
  }
}
object CcParser extends MeiModelDummyParser {
  def parse(node: Node): Cc = {
    Cc(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("num").map(_.text).get,
      node.attribute("val").map(_.text),
    )
  }
}
object ChanParser extends MeiModelDummyParser {
  def parse(node: Node): Chan = {
    Chan(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("num").map(_.text).get,
    )
  }
}
object ChanPrParser extends MeiModelDummyParser {
  def parse(node: Node): ChanPr = {
    ChanPr(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("num").map(_.text).get,
    )
  }
}
object CueParser extends MeiModelDummyParser {
  def parse(node: Node): Cue = {
    Cue(
      node.child.collect {
        case n if n.text.trim.nonEmpty => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
    )
  }
}
object HexParser extends MeiModelDummyParser {
  def parse(node: Node): Hex = {
    Hex(
      node.child.collect {
        case n if n.text.trim.nonEmpty => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
    )
  }
}
object InstrDefParser extends MeiModelDummyParser {
  def parse(node: Node): InstrDef = {
    InstrDef(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("midi.channel").map(_.text),
      node.attribute("midi.duty").map(_.text),
      node.attribute("midi.port").map(_.text),
      node.attribute("midi.track").map(_.text),
      node.attribute("midi.instrnum").map(_.text),
      node.attribute("midi.instrname").map(_.text),
      node.attribute("midi.pan").map(_.text),
      node.attribute("midi.patchname").map(_.text),
      node.attribute("midi.patchnum").map(_.text),
      node.attribute("midi.volume").map(_.text),
      node.attribute("azimuth").map(_.text),
      node.attribute("elevation").map(_.text),
    )
  }
}
object InstrGrpParser extends MeiModelDummyParser {
  def parse(node: Node): InstrGrp = {
    InstrGrp(
      node.child.collect {
        case n: Elem if n.label == "instrDef" => mei.allanystart.parser.InstrDefParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
    )
  }
}
object MetaTextParser extends MeiModelDummyParser {
  def parse(node: Node): MetaText = {
    MetaText(
      node.child.collect {
        case n if n.text.trim.nonEmpty => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
    )
  }
}
object MidiParser extends MeiModelDummyParser {
  def parse(node: Node): Midi = {
    Midi(
      node.child.collect {
        case n: Elem if n.label == "cc"       => mei.allanystart.parser.CcParser.parse(n)
        case n: Elem if n.label == "chan"     => mei.allanystart.parser.ChanParser.parse(n)
        case n: Elem if n.label == "chanPr"   => mei.allanystart.parser.ChanPrParser.parse(n)
        case n: Elem if n.label == "cue"      => mei.allanystart.parser.CueParser.parse(n)
        case n: Elem if n.label == "hex"      => mei.allanystart.parser.HexParser.parse(n)
        case n: Elem if n.label == "marker"   => mei.allanystart.parser.MarkerParser.parse(n)
        case n: Elem if n.label == "metaText" => mei.allanystart.parser.MetaTextParser.parse(n)
        case n: Elem if n.label == "noteOff"  => mei.allanystart.parser.NoteOffParser.parse(n)
        case n: Elem if n.label == "noteOn"   => mei.allanystart.parser.NoteOnParser.parse(n)
        case n: Elem if n.label == "port"     => mei.allanystart.parser.PortParser.parse(n)
        case n: Elem if n.label == "prog"     => mei.allanystart.parser.ProgParser.parse(n)
        case n: Elem if n.label == "seqNum"   => mei.allanystart.parser.SeqNumParser.parse(n)
        case n: Elem if n.label == "trkName"  => mei.allanystart.parser.TrkNameParser.parse(n)
        case n: Elem if n.label == "vel"      => mei.allanystart.parser.VelParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("staff").map(_.text),
    )
  }
}
object NoteOffParser extends MeiModelDummyParser {
  def parse(node: Node): NoteOff = {
    NoteOff(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("num").map(_.text).get,
    )
  }
}
object NoteOnParser extends MeiModelDummyParser {
  def parse(node: Node): NoteOn = {
    NoteOn(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("num").map(_.text).get,
    )
  }
}
object PortParser extends MeiModelDummyParser {
  def parse(node: Node): Port = {
    Port(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("num").map(_.text).get,
    )
  }
}
object ProgParser extends MeiModelDummyParser {
  def parse(node: Node): Prog = {
    Prog(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("num").map(_.text).get,
    )
  }
}
object SeqNumParser extends MeiModelDummyParser {
  def parse(node: Node): SeqNum = {
    SeqNum(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("num").map(_.text).get,
    )
  }
}
object TrkNameParser extends MeiModelDummyParser {
  def parse(node: Node): TrkName = {
    TrkName(
      node.child.collect {
        case n if n.text.trim.nonEmpty => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
    )
  }
}
object VelParser extends MeiModelDummyParser {
  def parse(node: Node): Vel = {
    Vel(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("num").map(_.text).get,
      node.attribute("form").map(_.text).get,
    )
  }
}
object AccMatParser extends MeiModelDummyParser {
  def parse(node: Node): AccMat = {
    AccMat(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object AddDescParser extends MeiModelDummyParser {
  def parse(node: Node): AddDesc = {
    AddDesc(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object BindingParser extends MeiModelDummyParser {
  def parse(node: Node): Binding = {
    Binding(
      node.child.collect {
        case n: Elem if n.label == "condition"  => mei.allanystart.parser.ConditionParser.parse(n)
        case n: Elem if n.label == "decoNote"   => mei.allanystart.parser.DecoNoteParser.parse(n)
        case n: Elem if n.label == "dimensions" => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "head"       => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "p"          => mei.allanystart.parser.PParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("contemporary").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object BindingDescParser extends MeiModelDummyParser {
  def parse(node: Node): BindingDesc = {
    BindingDesc(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "binding"      => mei.allanystart.parser.BindingParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "condition"    => mei.allanystart.parser.ConditionParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "decoNote"     => mei.allanystart.parser.DecoNoteParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object CatchwordsParser extends MeiModelDummyParser {
  def parse(node: Node): Catchwords = {
    Catchwords(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object CollationParser extends MeiModelDummyParser {
  def parse(node: Node): Collation = {
    Collation(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object ColophonParser extends MeiModelDummyParser {
  def parse(node: Node): Colophon = {
    Colophon(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object DecoDescParser extends MeiModelDummyParser {
  def parse(node: Node): DecoDesc = {
    DecoDesc(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "condition"    => mei.allanystart.parser.ConditionParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "decoNote"     => mei.allanystart.parser.DecoNoteParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object DecoNoteParser extends MeiModelDummyParser {
  def parse(node: Node): DecoNote = {
    DecoNote(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "condition"    => mei.allanystart.parser.ConditionParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "decoNote"     => mei.allanystart.parser.DecoNoteParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object ExplicitParser extends MeiModelDummyParser {
  def parse(node: Node): Explicit = {
    Explicit(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object FoliationParser extends MeiModelDummyParser {
  def parse(node: Node): Foliation = {
    Foliation(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object HeraldryParser extends MeiModelDummyParser {
  def parse(node: Node): Heraldry = {
    Heraldry(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object LayoutParser extends MeiModelDummyParser {
  def parse(node: Node): Layout = {
    Layout(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("cols").map(_.text),
      node.attribute("ruledlines").map(_.text),
      node.attribute("writtenlines").map(_.text),
      node.attribute("ruledstaves").map(_.text),
      node.attribute("writtenstaves").map(_.text),
    )
  }
}
object LayoutDescParser extends MeiModelDummyParser {
  def parse(node: Node): LayoutDesc = {
    LayoutDesc(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "layout"       => mei.allanystart.parser.LayoutParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object LocusParser extends MeiModelDummyParser {
  def parse(node: Node): Locus = {
    Locus(
      node.child.collect {
        case n: Elem if n.label == "locus"  => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "rend"   => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "symbol" => mei.allanystart.parser.SymbolParser.parse(n)
        case n if n.text.trim.nonEmpty      => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("scheme").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("from").map(_.text),
      node.attribute("to").map(_.text),
    )
  }
}
object LocusGrpParser extends MeiModelDummyParser {
  def parse(node: Node): LocusGrp = {
    LocusGrp(
      node.child.collect {
        case n: Elem if n.label == "locus" => mei.allanystart.parser.LocusParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("scheme").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object RubricParser extends MeiModelDummyParser {
  def parse(node: Node): Rubric = {
    Rubric(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("func").map(_.text),
    )
  }
}
object ScriptDescParser extends MeiModelDummyParser {
  def parse(node: Node): ScriptDesc = {
    ScriptDesc(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "scriptNote"   => mei.allanystart.parser.ScriptNoteParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object ScriptNoteParser extends MeiModelDummyParser {
  def parse(node: Node): ScriptNote = {
    ScriptNote(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object SealParser extends MeiModelDummyParser {
  def parse(node: Node): Seal = {
    Seal(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "condition"    => mei.allanystart.parser.ConditionParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "decoNote"     => mei.allanystart.parser.DecoNoteParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("contemporary").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object SealDescParser extends MeiModelDummyParser {
  def parse(node: Node): SealDesc = {
    SealDesc(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "condition"    => mei.allanystart.parser.ConditionParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "decoNote"     => mei.allanystart.parser.DecoNoteParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "seal"         => mei.allanystart.parser.SealParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object SecFolioParser extends MeiModelDummyParser {
  def parse(node: Node): SecFolio = {
    SecFolio(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object SignaturesParser extends MeiModelDummyParser {
  def parse(node: Node): Signatures = {
    Signatures(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object StampParser extends MeiModelDummyParser {
  def parse(node: Node): Stamp = {
    Stamp(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object SupportParser extends MeiModelDummyParser {
  def parse(node: Node): Support = {
    Support(
      node.child.collect {
        case n: Elem if n.label == "condition"  => mei.allanystart.parser.ConditionParser.parse(n)
        case n: Elem if n.label == "decoNote"   => mei.allanystart.parser.DecoNoteParser.parse(n)
        case n: Elem if n.label == "dimensions" => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "head"       => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "p"          => mei.allanystart.parser.PParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object SupportDescParser extends MeiModelDummyParser {
  def parse(node: Node): SupportDesc = {
    SupportDesc(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "collation"    => mei.allanystart.parser.CollationParser.parse(n)
        case n: Elem if n.label == "condition"    => mei.allanystart.parser.ConditionParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "foliation"    => mei.allanystart.parser.FoliationParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "support"      => mei.allanystart.parser.SupportParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("material").map(_.text),
    )
  }
}
object TypeDescParser extends MeiModelDummyParser {
  def parse(node: Node): TypeDesc = {
    TypeDesc(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "typeNote"     => mei.allanystart.parser.TypeNoteParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object TypeNoteParser extends MeiModelDummyParser {
  def parse(node: Node): TypeNote = {
    TypeNote(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object AddNameParser extends MeiModelDummyParser {
  def parse(node: Node): AddName = {
    AddName(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("nonfiling").map(_.text),
      node.attribute("nymref").map(_.text),
      node.attribute("role").map(_.text),
    )
  }
}
object BlocParser extends MeiModelDummyParser {
  def parse(node: Node): Bloc = {
    Bloc(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("nonfiling").map(_.text),
      node.attribute("nymref").map(_.text),
      node.attribute("role").map(_.text),
    )
  }
}
object CorpNameParser extends MeiModelDummyParser {
  def parse(node: Node): CorpName = {
    CorpName(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("nonfiling").map(_.text),
      node.attribute("nymref").map(_.text),
      node.attribute("role").map(_.text),
    )
  }
}
object CountryParser extends MeiModelDummyParser {
  def parse(node: Node): Country = {
    Country(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("nonfiling").map(_.text),
      node.attribute("nymref").map(_.text),
      node.attribute("role").map(_.text),
    )
  }
}
object DistrictParser extends MeiModelDummyParser {
  def parse(node: Node): District = {
    District(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("nonfiling").map(_.text),
      node.attribute("nymref").map(_.text),
      node.attribute("role").map(_.text),
    )
  }
}
object FamNameParser extends MeiModelDummyParser {
  def parse(node: Node): FamName = {
    FamName(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "nameLink"     => mei.allanystart.parser.NameLinkParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("nonfiling").map(_.text),
      node.attribute("nymref").map(_.text),
      node.attribute("role").map(_.text),
    )
  }
}
object ForeNameParser extends MeiModelDummyParser {
  def parse(node: Node): ForeName = {
    ForeName(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("nonfiling").map(_.text),
      node.attribute("nymref").map(_.text),
      node.attribute("role").map(_.text),
    )
  }
}
object GenNameParser extends MeiModelDummyParser {
  def parse(node: Node): GenName = {
    GenName(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("nonfiling").map(_.text),
      node.attribute("nymref").map(_.text),
      node.attribute("role").map(_.text),
    )
  }
}
object GeogFeatParser extends MeiModelDummyParser {
  def parse(node: Node): GeogFeat = {
    GeogFeat(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("nonfiling").map(_.text),
      node.attribute("nymref").map(_.text),
      node.attribute("role").map(_.text),
    )
  }
}
object GeogNameParser extends MeiModelDummyParser {
  def parse(node: Node): GeogName = {
    GeogName(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("nonfiling").map(_.text),
      node.attribute("nymref").map(_.text),
      node.attribute("role").map(_.text),
    )
  }
}
object NameLinkParser extends MeiModelDummyParser {
  def parse(node: Node): NameLink = {
    NameLink(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("nonfiling").map(_.text),
      node.attribute("nymref").map(_.text),
      node.attribute("role").map(_.text),
    )
  }
}
object PeriodNameParser extends MeiModelDummyParser {
  def parse(node: Node): PeriodName = {
    PeriodName(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("nonfiling").map(_.text),
      node.attribute("nymref").map(_.text),
      node.attribute("role").map(_.text),
    )
  }
}
object PersNameParser extends MeiModelDummyParser {
  def parse(node: Node): PersName = {
    PersName(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "addName"      => mei.allanystart.parser.AddNameParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "famName"      => mei.allanystart.parser.FamNameParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "foreName"     => mei.allanystart.parser.ForeNameParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "genName"      => mei.allanystart.parser.GenNameParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "nameLink"     => mei.allanystart.parser.NameLinkParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "roleName"     => mei.allanystart.parser.RoleNameParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("nonfiling").map(_.text),
      node.attribute("nymref").map(_.text),
      node.attribute("role").map(_.text),
    )
  }
}
object PostBoxParser extends MeiModelDummyParser {
  def parse(node: Node): PostBox = {
    PostBox(
      node.child.collect {
        case n: Elem if n.label == "add"       => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "choice"    => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corr"      => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "damage"    => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "del"       => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "gap"       => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "handShift" => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "orig"      => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "reg"       => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "restore"   => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sic"       => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "subst"     => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"  => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "unclear"   => mei.allanystart.parser.UnclearParser.parse(n)
        case n if n.text.trim.nonEmpty         => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object PostCodeParser extends MeiModelDummyParser {
  def parse(node: Node): PostCode = {
    PostCode(
      node.child.collect {
        case n: Elem if n.label == "add"       => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "choice"    => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corr"      => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "damage"    => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "del"       => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "gap"       => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "handShift" => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "orig"      => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "reg"       => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "restore"   => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sic"       => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "subst"     => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"  => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "unclear"   => mei.allanystart.parser.UnclearParser.parse(n)
        case n if n.text.trim.nonEmpty         => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object RegionParser extends MeiModelDummyParser {
  def parse(node: Node): Region = {
    Region(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("nonfiling").map(_.text),
      node.attribute("nymref").map(_.text),
      node.attribute("role").map(_.text),
    )
  }
}
object RoleNameParser extends MeiModelDummyParser {
  def parse(node: Node): RoleName = {
    RoleName(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("nonfiling").map(_.text),
      node.attribute("nymref").map(_.text),
      node.attribute("role").map(_.text),
    )
  }
}
object SettlementParser extends MeiModelDummyParser {
  def parse(node: Node): Settlement = {
    Settlement(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("nonfiling").map(_.text),
      node.attribute("nymref").map(_.text),
      node.attribute("role").map(_.text),
    )
  }
}
object StreetParser extends MeiModelDummyParser {
  def parse(node: Node): Street = {
    Street(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object StyleNameParser extends MeiModelDummyParser {
  def parse(node: Node): StyleName = {
    StyleName(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("nonfiling").map(_.text),
      node.attribute("nymref").map(_.text),
      node.attribute("role").map(_.text),
    )
  }
}
object EpisemaParser extends MeiModelDummyParser {
  def parse(node: Node): Episema = {
    Episema(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("artic.ges").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("enclose").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("loc").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("visible").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("form").map(_.text),
      node.attribute("place").map(_.text),
    )
  }
}
object HispanTickParser extends MeiModelDummyParser {
  def parse(node: Node): HispanTick = {
    HispanTick(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("enclose").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("loc").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("visible").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("tilt").map(_.text),
    )
  }
}
object LiquescentParser extends MeiModelDummyParser {
  def parse(node: Node): Liquescent = {
    Liquescent(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("curve").map(_.text),
      node.attribute("enclose").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("loc").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("visible").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("looped").map(_.text),
    )
  }
}
object NcParser extends MeiModelDummyParser {
  def parse(node: Node): Nc = {
    Nc(
      node.child.collect {
        case n: Elem if n.label == "add"        => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "app"        => mei.allanystart.parser.AppParser.parse(n)
        case n: Elem if n.label == "choice"     => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corr"       => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "damage"     => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "del"        => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "episema"    => mei.allanystart.parser.EpisemaParser.parse(n)
        case n: Elem if n.label == "gap"        => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "handShift"  => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "hispanTick" => mei.allanystart.parser.HispanTickParser.parse(n)
        case n: Elem if n.label == "liquescent" => mei.allanystart.parser.LiquescentParser.parse(n)
        case n: Elem if n.label == "orig"       => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "oriscus"    => mei.allanystart.parser.OriscusParser.parse(n)
        case n: Elem if n.label == "quilisma"   => mei.allanystart.parser.QuilismaParser.parse(n)
        case n: Elem if n.label == "reg"        => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "restore"    => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sic"        => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signifLet"  => mei.allanystart.parser.SignifLetParser.parse(n)
        case n: Elem if n.label == "strophicus" => mei.allanystart.parser.StrophicusParser.parse(n)
        case n: Elem if n.label == "subst"      => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"   => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "unclear"    => mei.allanystart.parser.UnclearParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("deg").map(_.text),
      node.attribute("intm").map(_.text),
      node.attribute("mfunc").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("pclass").map(_.text),
      node.attribute("psolfa").map(_.text),
      node.attribute("accid.ges").map(_.text),
      node.attribute("artic.ges").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
      node.attribute("instr").map(_.text),
      node.attribute("vel").map(_.text),
      node.attribute("oct.ges").map(_.text),
      node.attribute("pname.ges").map(_.text),
      node.attribute("pnum").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("oct").map(_.text),
      node.attribute("pname").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("curve").map(_.text),
      node.attribute("angled").map(_.text),
      node.attribute("con").map(_.text),
      node.attribute("hooked").map(_.text),
      node.attribute("ligated").map(_.text),
      node.attribute("rellen").map(_.text),
      node.attribute("s-shape").map(_.text),
      node.attribute("tilt").map(_.text),
      node.attribute("loc").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("visible").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object NcGrpParser extends MeiModelDummyParser {
  def parse(node: Node): NcGrp = {
    NcGrp(
      node.child.collect {
        case n: Elem if n.label == "add"        => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "app"        => mei.allanystart.parser.AppParser.parse(n)
        case n: Elem if n.label == "choice"     => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corr"       => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "damage"     => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "del"        => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "episema"    => mei.allanystart.parser.EpisemaParser.parse(n)
        case n: Elem if n.label == "gap"        => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "handShift"  => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "hispanTick" => mei.allanystart.parser.HispanTickParser.parse(n)
        case n: Elem if n.label == "nc"         => mei.allanystart.parser.NcParser.parse(n)
        case n: Elem if n.label == "ncGrp"      => mei.allanystart.parser.NcGrpParser.parse(n)
        case n: Elem if n.label == "orig"       => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "reg"        => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "restore"    => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sic"        => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signifLet"  => mei.allanystart.parser.SignifLetParser.parse(n)
        case n: Elem if n.label == "subst"      => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"   => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "unclear"    => mei.allanystart.parser.UnclearParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("syl").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("loc").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("visible").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object NeumeParser extends MeiModelDummyParser {
  def parse(node: Node): Neume = {
    Neume(
      node.child.collect {
        case n: Elem if n.label == "add"        => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "app"        => mei.allanystart.parser.AppParser.parse(n)
        case n: Elem if n.label == "choice"     => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corr"       => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "damage"     => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "del"        => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "episema"    => mei.allanystart.parser.EpisemaParser.parse(n)
        case n: Elem if n.label == "gap"        => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "handShift"  => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "hispanTick" => mei.allanystart.parser.HispanTickParser.parse(n)
        case n: Elem if n.label == "nc"         => mei.allanystart.parser.NcParser.parse(n)
        case n: Elem if n.label == "ncGrp"      => mei.allanystart.parser.NcGrpParser.parse(n)
        case n: Elem if n.label == "orig"       => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "reg"        => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "restore"    => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sic"        => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signifLet"  => mei.allanystart.parser.SignifLetParser.parse(n)
        case n: Elem if n.label == "subst"      => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"   => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "unclear"    => mei.allanystart.parser.UnclearParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("syl").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("enclose").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("loc").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("visible").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object OriscusParser extends MeiModelDummyParser {
  def parse(node: Node): Oriscus = {
    Oriscus(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("enclose").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("loc").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("visible").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object QuilismaParser extends MeiModelDummyParser {
  def parse(node: Node): Quilisma = {
    Quilisma(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("enclose").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("loc").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("visible").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("waves").map(_.text),
    )
  }
}
object SignifLetParser extends MeiModelDummyParser {
  def parse(node: Node): SignifLet = {
    SignifLet(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("enclose").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("loc").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("visible").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("place").map(_.text),
    )
  }
}
object StrophicusParser extends MeiModelDummyParser {
  def parse(node: Node): Strophicus = {
    Strophicus(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("enclose").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("loc").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("visible").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object SyllableParser extends MeiModelDummyParser {
  def parse(node: Node): Syllable = {
    Syllable(
      node.child.collect {
        case n: Elem if n.label == "accid"        => mei.allanystart.parser.AccidParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "app"          => mei.allanystart.parser.AppParser.parse(n)
        case n: Elem if n.label == "bend"         => mei.allanystart.parser.BendParser.parse(n)
        case n: Elem if n.label == "caesura"      => mei.allanystart.parser.CaesuraParser.parse(n)
        case n: Elem if n.label == "cb"           => mei.allanystart.parser.CbParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "clef"         => mei.allanystart.parser.ClefParser.parse(n)
        case n: Elem if n.label == "colLayout"    => mei.allanystart.parser.ColLayoutParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "cpMark"       => mei.allanystart.parser.CpMarkParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "dir"          => mei.allanystart.parser.DirParser.parse(n)
        case n: Elem if n.label == "div"          => mei.allanystart.parser.DivParser.parse(n)
        case n: Elem if n.label == "divLine"      => mei.allanystart.parser.DivLineParser.parse(n)
        case n: Elem if n.label == "dynam"        => mei.allanystart.parser.DynamParser.parse(n)
        case n: Elem if n.label == "episema"      => mei.allanystart.parser.EpisemaParser.parse(n)
        case n: Elem if n.label == "fing"         => mei.allanystart.parser.FingParser.parse(n)
        case n: Elem if n.label == "fingGrp"      => mei.allanystart.parser.FingGrpParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "gliss"        => mei.allanystart.parser.GlissParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "harm"         => mei.allanystart.parser.HarmParser.parse(n)
        case n: Elem if n.label == "hispanTick"   => mei.allanystart.parser.HispanTickParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "metaMark"     => mei.allanystart.parser.MetaMarkParser.parse(n)
        case n: Elem if n.label == "midi"         => mei.allanystart.parser.MidiParser.parse(n)
        case n: Elem if n.label == "neume"        => mei.allanystart.parser.NeumeParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "ornam"        => mei.allanystart.parser.OrnamParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "phrase"       => mei.allanystart.parser.PhraseParser.parse(n)
        case n: Elem if n.label == "refrain"      => mei.allanystart.parser.RefrainParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "repeatMark"   => mei.allanystart.parser.RepeatMarkParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sb"           => mei.allanystart.parser.SbParser.parse(n)
        case n: Elem if n.label == "scoreDef"     => mei.allanystart.parser.ScoreDefParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signifLet"    => mei.allanystart.parser.SignifLetParser.parse(n)
        case n: Elem if n.label == "sp"           => mei.allanystart.parser.SpParser.parse(n)
        case n: Elem if n.label == "staffDef"     => mei.allanystart.parser.StaffDefParser.parse(n)
        case n: Elem if n.label == "staffGrp"     => mei.allanystart.parser.StaffGrpParser.parse(n)
        case n: Elem if n.label == "stageDir"     => mei.allanystart.parser.StageDirParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "syl"          => mei.allanystart.parser.SylParser.parse(n)
        case n: Elem if n.label == "tempo"        => mei.allanystart.parser.TempoParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "verse"        => mei.allanystart.parser.VerseParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("when").map(_.text),
    )
  }
}
object DivLineParser extends MeiModelDummyParser {
  def parse(node: Node): DivLine = {
    DivLine(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("form").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("loc").map(_.text),
      node.attribute("visible").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("ho").map(_.text),
    )
  }
}
object AvFileParser extends MeiModelDummyParser {
  def parse(node: Node): AvFile = {
    AvFile(
      node.child.collect {
        case n: Elem if n.label == "clip" => mei.allanystart.parser.ClipParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("mimetype").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("xlink:actuate").map(_.text),
      node.attribute("xlink:role").map(_.text),
      node.attribute("xlink:show").map(_.text),
      node.attribute("target").map(_.text),
      node.attribute("targettype").map(_.text),
    )
  }
}
object ClipParser extends MeiModelDummyParser {
  def parse(node: Node): Clip = {
    Clip(
      node.child.collect {
        case n: Elem if n.label == "avFile" => mei.allanystart.parser.AvFileParser.parse(n)
        case n: Elem if n.label == "when"   => mei.allanystart.parser.WhenParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("data").map(_.text),
      node.attribute("begin").map(_.text),
      node.attribute("end").map(_.text),
      node.attribute("betype").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("startid").map(_.text),
    )
  }
}
object PerformanceParser extends MeiModelDummyParser {
  def parse(node: Node): Performance = {
    Performance(
      node.child.collect {
        case n: Elem if n.label == "recording" => mei.allanystart.parser.RecordingParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("decls").map(_.text),
    )
  }
}
object RecordingParser extends MeiModelDummyParser {
  def parse(node: Node): Recording = {
    Recording(
      node.child.collect {
        case n: Elem if n.label == "avFile" => mei.allanystart.parser.AvFileParser.parse(n)
        case n: Elem if n.label == "clip"   => mei.allanystart.parser.ClipParser.parse(n)
        case n: Elem if n.label == "when"   => mei.allanystart.parser.WhenParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("data").map(_.text),
      node.attribute("begin").map(_.text),
      node.attribute("end").map(_.text),
      node.attribute("betype").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("startid").map(_.text),
    )
  }
}
object WhenParser extends MeiModelDummyParser {
  def parse(node: Node): When = {
    When(
      node.child.collect {
        case n: Elem if n.label == "extData" => mei.allanystart.parser.ExtDataParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("data").map(_.text),
      node.attribute("absolute").map(_.text),
      node.attribute("interval").map(_.text),
      node.attribute("abstype").map(_.text),
      node.attribute("inttype").map(_.text),
      node.attribute("since").map(_.text),
    )
  }
}
object PtrParser extends MeiModelDummyParser {
  def parse(node: Node): Ptr = {
    Ptr(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("mimetype").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("xlink:actuate").map(_.text),
      node.attribute("xlink:role").map(_.text),
      node.attribute("xlink:show").map(_.text),
      node.attribute("target").map(_.text),
      node.attribute("targettype").map(_.text),
      node.attribute("evaluate").map(_.text),
    )
  }
}
object RefParser extends MeiModelDummyParser {
  def parse(node: Node): Ref = {
    Ref(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("mimetype").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("xlink:actuate").map(_.text),
      node.attribute("xlink:role").map(_.text),
      node.attribute("xlink:show").map(_.text),
      node.attribute("target").map(_.text),
      node.attribute("targettype").map(_.text),
      node.attribute("evaluate").map(_.text),
    )
  }
}
object AccidParser extends MeiModelDummyParser {
  def parse(node: Node): Accid = {
    Accid(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("accid.ges").map(_.text),
      node.attribute("accid").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("func").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("enclose").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("onstaff").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("loc").map(_.text),
      node.attribute("ploc").map(_.text),
      node.attribute("oloc").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object ActorParser extends MeiModelDummyParser {
  def parse(node: Node): Actor = {
    Actor(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object AddressParser extends MeiModelDummyParser {
  def parse(node: Node): Address = {
    Address(
      node.child.collect {
        case n: Elem if n.label == "addrLine"   => mei.allanystart.parser.AddrLineParser.parse(n)
        case n: Elem if n.label == "bloc"       => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "country"    => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "district"   => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "geogFeat"   => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "postBox"    => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"   => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "region"     => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "settlement" => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "street"     => mei.allanystart.parser.StreetParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object AddrLineParser extends MeiModelDummyParser {
  def parse(node: Node): AddrLine = {
    AddrLine(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object AmbitusParser extends MeiModelDummyParser {
  def parse(node: Node): Ambitus = {
    Ambitus(
      node.child.collect {
        case n: Elem if n.label == "ambNote" => mei.allanystart.parser.AmbNoteParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("inth").map(_.text),
      node.attribute("decls").map(_.text),
    )
  }
}
object AmbNoteParser extends MeiModelDummyParser {
  def parse(node: Node): AmbNote = {
    AmbNote(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("accid").map(_.text),
      node.attribute("colored").map(_.text),
      node.attribute("dur").map(_.text),
      node.attribute("pname").map(_.text),
      node.attribute("oct").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("enclose").map(_.text),
      node.attribute("head.altsym").map(_.text),
      node.attribute("head.auth").map(_.text),
      node.attribute("head.color").map(_.text),
      node.attribute("head.fill").map(_.text),
      node.attribute("head.fillcolor").map(_.text),
      node.attribute("head.mod").map(_.text),
      node.attribute("head.rotation").map(_.text),
      node.attribute("head.shape").map(_.text),
      node.attribute("head.visible").map(_.text),
      node.attribute("loc").map(_.text),
      node.attribute("stem.with").map(_.text),
      node.attribute("stem.form").map(_.text),
      node.attribute("stem.dir").map(_.text),
      node.attribute("stem.len").map(_.text),
      node.attribute("stem.mod").map(_.text),
      node.attribute("stem.pos").map(_.text),
      node.attribute("stem.sameas").map(_.text),
      node.attribute("stem.visible").map(_.text),
      node.attribute("stem.x").map(_.text),
      node.attribute("stem.y").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
    )
  }
}
object AnalyticParser extends MeiModelDummyParser {
  def parse(node: Node): Analytic = {
    Analytic(
      node.child.collect {
        case n: Elem if n.label == "arranger"    => mei.allanystart.parser.ArrangerParser.parse(n)
        case n: Elem if n.label == "author"      => mei.allanystart.parser.AuthorParser.parse(n)
        case n: Elem if n.label == "biblScope"   => mei.allanystart.parser.BiblScopeParser.parse(n)
        case n: Elem if n.label == "composer"    => mei.allanystart.parser.ComposerParser.parse(n)
        case n: Elem if n.label == "contributor" => mei.allanystart.parser.ContributorParser.parse(n)
        case n: Elem if n.label == "editor"      => mei.allanystart.parser.EditorParser.parse(n)
        case n: Elem if n.label == "funder"      => mei.allanystart.parser.FunderParser.parse(n)
        case n: Elem if n.label == "identifier"  => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "librettist"  => mei.allanystart.parser.LibrettistParser.parse(n)
        case n: Elem if n.label == "lyricist"    => mei.allanystart.parser.LyricistParser.parse(n)
        case n: Elem if n.label == "respStmt"    => mei.allanystart.parser.RespStmtParser.parse(n)
        case n: Elem if n.label == "sponsor"     => mei.allanystart.parser.SponsorParser.parse(n)
        case n: Elem if n.label == "title"       => mei.allanystart.parser.TitleParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("comptype").map(_.text),
      node.attribute("data").map(_.text),
      node.attribute("xlink:actuate").map(_.text),
      node.attribute("xlink:role").map(_.text),
      node.attribute("xlink:show").map(_.text),
      node.attribute("target").map(_.text),
      node.attribute("targettype").map(_.text),
      node.attribute("recordtype").map(_.text),
      node.attribute("evaluate").map(_.text),
    )
  }
}
object AnnotParser extends MeiModelDummyParser {
  def parse(node: Node): Annot = {
    Annot(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblList"     => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "castList"     => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "eventList"    => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "lg"           => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "list"         => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "quote"        => mei.allanystart.parser.QuoteParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "table"        => mei.allanystart.parser.TableParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("audience").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("data").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("tstamp2.ges").map(_.text),
      node.attribute("tstamp2.real").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("dur").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp2").map(_.text),
      node.attribute("place").map(_.text),
    )
  }
}
object ArrangerParser extends MeiModelDummyParser {
  def parse(node: Node): Arranger = {
    Arranger(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object ArticParser extends MeiModelDummyParser {
  def parse(node: Node): Artic = {
    Artic(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("artic.ges").map(_.text),
      node.attribute("artic").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("enclose").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("onstaff").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("loc").map(_.text),
      node.attribute("ploc").map(_.text),
      node.attribute("oloc").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object AuthorParser extends MeiModelDummyParser {
  def parse(node: Node): Author = {
    Author(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object BarLineParser extends MeiModelDummyParser {
  def parse(node: Node): BarLine = {
    BarLine(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xlink:actuate").map(_.text),
      node.attribute("xlink:role").map(_.text),
      node.attribute("xlink:show").map(_.text),
      node.attribute("target").map(_.text),
      node.attribute("targettype").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("metcon").map(_.text),
      node.attribute("control").map(_.text),
      node.attribute("form").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("visible").map(_.text),
      node.attribute("width").map(_.text),
      node.attribute("len").map(_.text),
      node.attribute("method").map(_.text),
      node.attribute("place").map(_.text),
    )
  }
}
object BiblParser extends MeiModelDummyParser {
  def parse(node: Node): Bibl = {
    Bibl(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "arranger"     => mei.allanystart.parser.ArrangerParser.parse(n)
        case n: Elem if n.label == "author"       => mei.allanystart.parser.AuthorParser.parse(n)
        case n: Elem if n.label == "availability" => mei.allanystart.parser.AvailabilityParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblScope"    => mei.allanystart.parser.BiblScopeParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "composer"     => mei.allanystart.parser.ComposerParser.parse(n)
        case n: Elem if n.label == "contributor"  => mei.allanystart.parser.ContributorParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "creation"     => mei.allanystart.parser.CreationParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "distributor"  => mei.allanystart.parser.DistributorParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "edition"      => mei.allanystart.parser.EditionParser.parse(n)
        case n: Elem if n.label == "editor"       => mei.allanystart.parser.EditorParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "funder"       => mei.allanystart.parser.FunderParser.parse(n)
        case n: Elem if n.label == "genre"        => mei.allanystart.parser.GenreParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "imprint"      => mei.allanystart.parser.ImprintParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "librettist"   => mei.allanystart.parser.LibrettistParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "lyricist"     => mei.allanystart.parser.LyricistParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "perfDuration" => mei.allanystart.parser.PerfDurationParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "physLoc"      => mei.allanystart.parser.PhysLocParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "pubPlace"     => mei.allanystart.parser.PubPlaceParser.parse(n)
        case n: Elem if n.label == "publisher"    => mei.allanystart.parser.PublisherParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "recipient"    => mei.allanystart.parser.RecipientParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relatedItem"  => mei.allanystart.parser.RelatedItemParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "respStmt"     => mei.allanystart.parser.RespStmtParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "series"       => mei.allanystart.parser.SeriesParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "sponsor"      => mei.allanystart.parser.SponsorParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "textLang"     => mei.allanystart.parser.TextLangParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unpub"        => mei.allanystart.parser.UnpubParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("xlink:actuate").map(_.text),
      node.attribute("xlink:role").map(_.text),
      node.attribute("xlink:show").map(_.text),
      node.attribute("target").map(_.text),
      node.attribute("targettype").map(_.text),
    )
  }
}
object BiblListParser extends MeiModelDummyParser {
  def parse(node: Node): BiblList = {
    BiblList(
      node.child.collect {
        case n: Elem if n.label == "bibl"       => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblList"   => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "biblStruct" => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "head"       => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "label"      => mei.allanystart.parser.LabelParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object BiblScopeParser extends MeiModelDummyParser {
  def parse(node: Node): BiblScope = {
    BiblScope(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("unit").map(_.text),
      node.attribute("atleast").map(_.text),
      node.attribute("atmost").map(_.text),
      node.attribute("min").map(_.text),
      node.attribute("max").map(_.text),
      node.attribute("confidence").map(_.text),
      node.attribute("extent").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("from").map(_.text),
      node.attribute("to").map(_.text),
    )
  }
}
object BiblStructParser extends MeiModelDummyParser {
  def parse(node: Node): BiblStruct = {
    BiblStruct(
      node.child.collect {
        case n: Elem if n.label == "analytic"    => mei.allanystart.parser.AnalyticParser.parse(n)
        case n: Elem if n.label == "annot"       => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "identifier"  => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "monogr"      => mei.allanystart.parser.MonogrParser.parse(n)
        case n: Elem if n.label == "relatedItem" => mei.allanystart.parser.RelatedItemParser.parse(n)
        case n: Elem if n.label == "series"      => mei.allanystart.parser.SeriesParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("data").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("xlink:actuate").map(_.text),
      node.attribute("xlink:role").map(_.text),
      node.attribute("xlink:show").map(_.text),
      node.attribute("target").map(_.text),
      node.attribute("targettype").map(_.text),
      node.attribute("recordtype").map(_.text),
      node.attribute("evaluate").map(_.text),
    )
  }
}
object BodyParser extends MeiModelDummyParser {
  def parse(node: Node): Body = {
    Body(
      node.child.collect {
        case n: Elem if n.label == "div"  => mei.allanystart.parser.DivParser.parse(n)
        case n: Elem if n.label == "mdiv" => mei.allanystart.parser.MdivParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("decls").map(_.text),
    )
  }
}
object CaesuraParser extends MeiModelDummyParser {
  def parse(node: Node): Caesura = {
    Caesura(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("loc").map(_.text),
      node.attribute("ploc").map(_.text),
      node.attribute("oloc").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("vgrp").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object CaptionParser extends MeiModelDummyParser {
  def parse(node: Node): Caption = {
    Caption(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object CastGrpParser extends MeiModelDummyParser {
  def parse(node: Node): CastGrp = {
    CastGrp(
      node.child.collect {
        case n: Elem if n.label == "castGrp"  => mei.allanystart.parser.CastGrpParser.parse(n)
        case n: Elem if n.label == "castItem" => mei.allanystart.parser.CastItemParser.parse(n)
        case n: Elem if n.label == "roleDesc" => mei.allanystart.parser.RoleDescParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object CastItemParser extends MeiModelDummyParser {
  def parse(node: Node): CastItem = {
    CastItem(
      node.child.collect {
        case n: Elem if n.label == "actor"    => mei.allanystart.parser.ActorParser.parse(n)
        case n: Elem if n.label == "perfRes"  => mei.allanystart.parser.PerfResParser.parse(n)
        case n: Elem if n.label == "role"     => mei.allanystart.parser.RoleParser.parse(n)
        case n: Elem if n.label == "roleDesc" => mei.allanystart.parser.RoleDescParser.parse(n)
        case n if n.text.trim.nonEmpty        => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object CastListParser extends MeiModelDummyParser {
  def parse(node: Node): CastList = {
    CastList(
      node.child.collect {
        case n: Elem if n.label == "castGrp"  => mei.allanystart.parser.CastGrpParser.parse(n)
        case n: Elem if n.label == "castItem" => mei.allanystart.parser.CastItemParser.parse(n)
        case n: Elem if n.label == "head"     => mei.allanystart.parser.HeadParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object CbParser extends MeiModelDummyParser {
  def parse(node: Node): Cb = {
    Cb(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("n").map(_.text).get,
    )
  }
}
object ChordParser extends MeiModelDummyParser {
  def parse(node: Node): Chord = {
    Chord(
      node.child.collect {
        case n: Elem if n.label == "add"       => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "app"       => mei.allanystart.parser.AppParser.parse(n)
        case n: Elem if n.label == "artic"     => mei.allanystart.parser.ArticParser.parse(n)
        case n: Elem if n.label == "choice"    => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corr"      => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "damage"    => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "del"       => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "gap"       => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "handShift" => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "note"      => mei.allanystart.parser.NoteParser.parse(n)
        case n: Elem if n.label == "orig"      => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "refrain"   => mei.allanystart.parser.RefrainParser.parse(n)
        case n: Elem if n.label == "reg"       => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "restore"   => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sic"       => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "subst"     => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"  => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "syl"       => mei.allanystart.parser.SylParser.parse(n)
        case n: Elem if n.label == "unclear"   => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "verse"     => mei.allanystart.parser.VerseParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("beam").map(_.text),
      node.attribute("fermata").map(_.text),
      node.attribute("lv").map(_.text),
      node.attribute("ornam").map(_.text),
      node.attribute("slur").map(_.text),
      node.attribute("tie").map(_.text),
      node.attribute("tuplet").map(_.text),
      node.attribute("artic.ges").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
      node.attribute("instr").map(_.text),
      node.attribute("artic").map(_.text),
      node.attribute("dots").map(_.text),
      node.attribute("grace").map(_.text),
      node.attribute("grace.time").map(_.text),
      node.attribute("cue").map(_.text),
      node.attribute("dur").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("syl").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("enclose").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("stem.with").map(_.text),
      node.attribute("stem.form").map(_.text),
      node.attribute("stem.dir").map(_.text),
      node.attribute("stem.len").map(_.text),
      node.attribute("stem.mod").map(_.text),
      node.attribute("stem.pos").map(_.text),
      node.attribute("stem.sameas").map(_.text),
      node.attribute("stem.visible").map(_.text),
      node.attribute("stem.x").map(_.text),
      node.attribute("stem.y").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("visible").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("breaksec").map(_.text),
      node.attribute("cluster").map(_.text),
    )
  }
}
object ClefParser extends MeiModelDummyParser {
  def parse(node: Node): Clef = {
    Clef(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("shape").map(_.text),
      node.attribute("line").map(_.text),
      node.attribute("oct").map(_.text),
      node.attribute("dis").map(_.text),
      node.attribute("dis.place").map(_.text),
      node.attribute("cautionary").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("enclose").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("visible").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
    )
  }
}
object ClefGrpParser extends MeiModelDummyParser {
  def parse(node: Node): ClefGrp = {
    ClefGrp(
      node.child.collect {
        case n: Elem if n.label == "clef" => mei.allanystart.parser.ClefParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("facs").map(_.text),
    )
  }
}
object ColLayoutParser extends MeiModelDummyParser {
  def parse(node: Node): ColLayout = {
    ColLayout(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cols").map(_.text).get,
    )
  }
}
object ComposerParser extends MeiModelDummyParser {
  def parse(node: Node): Composer = {
    Composer(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object ContributorParser extends MeiModelDummyParser {
  def parse(node: Node): Contributor = {
    Contributor(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("role").map(_.text),
    )
  }
}
object CreationParser extends MeiModelDummyParser {
  def parse(node: Node): Creation = {
    Creation(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "dedication"   => mei.allanystart.parser.DedicationParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object CustosParser extends MeiModelDummyParser {
  def parse(node: Node): Custos = {
    Custos(
      node.child.collect {
        case n: Elem if n.label == "accid" => mei.allanystart.parser.AccidParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("accid").map(_.text),
      node.attribute("pname").map(_.text),
      node.attribute("oct").map(_.text),
      node.attribute("target").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("loc").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("ho").map(_.text),
    )
  }
}
object DateParser extends MeiModelDummyParser {
  def parse(node: Node): Date = {
    Date(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("calendar").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object DedicateeParser extends MeiModelDummyParser {
  def parse(node: Node): Dedicatee = {
    Dedicatee(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object DepthParser extends MeiModelDummyParser {
  def parse(node: Node): Depth = {
    Depth(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("unit").map(_.text),
      node.attribute("atleast").map(_.text),
      node.attribute("atmost").map(_.text),
      node.attribute("min").map(_.text),
      node.attribute("max").map(_.text),
      node.attribute("confidence").map(_.text),
      node.attribute("quantity").map(_.text),
    )
  }
}
object DimParser extends MeiModelDummyParser {
  def parse(node: Node): Dim = {
    Dim(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("unit").map(_.text),
      node.attribute("atleast").map(_.text),
      node.attribute("atmost").map(_.text),
      node.attribute("min").map(_.text),
      node.attribute("max").map(_.text),
      node.attribute("confidence").map(_.text),
      node.attribute("quantity").map(_.text),
      node.attribute("form").map(_.text).get,
    )
  }
}
object DimensionsParser extends MeiModelDummyParser {
  def parse(node: Node): Dimensions = {
    Dimensions(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("unit").map(_.text),
    )
  }
}
object DirParser extends MeiModelDummyParser {
  def parse(node: Node): Dir = {
    Dir(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
      node.attribute("tstamp2.ges").map(_.text),
      node.attribute("tstamp2.real").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("dur").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("tstamp2").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("lform").map(_.text),
      node.attribute("lwidth").map(_.text),
      node.attribute("lsegs").map(_.text),
      node.attribute("lendsym").map(_.text),
      node.attribute("lendsym.size").map(_.text),
      node.attribute("lstartsym").map(_.text),
      node.attribute("lstartsym.size").map(_.text),
      node.attribute("extender").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("vgrp").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("startho").map(_.text),
      node.attribute("endho").map(_.text),
      node.attribute("startto").map(_.text),
      node.attribute("endto").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object DistributorParser extends MeiModelDummyParser {
  def parse(node: Node): Distributor = {
    Distributor(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object DivParser extends MeiModelDummyParser {
  def parse(node: Node): Div = {
    Div(
      node.child.collect {
        case n: Elem if n.label == "biblList"  => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "castList"  => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "cb"        => mei.allanystart.parser.CbParser.parse(n)
        case n: Elem if n.label == "colLayout" => mei.allanystart.parser.ColLayoutParser.parse(n)
        case n: Elem if n.label == "div"       => mei.allanystart.parser.DivParser.parse(n)
        case n: Elem if n.label == "eventList" => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "fig"       => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "head"      => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "lb"        => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "lg"        => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "list"      => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "p"         => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "pb"        => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "quote"     => mei.allanystart.parser.QuoteParser.parse(n)
        case n: Elem if n.label == "sp"        => mei.allanystart.parser.SpParser.parse(n)
        case n: Elem if n.label == "table"     => mei.allanystart.parser.TableParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("type").map(_.text),
    )
  }
}
object DotParser extends MeiModelDummyParser {
  def parse(node: Node): Dot = {
    Dot(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("form").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("loc").map(_.text),
      node.attribute("ploc").map(_.text),
      node.attribute("oloc").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object DynamParser extends MeiModelDummyParser {
  def parse(node: Node): Dynam = {
    Dynam(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
      node.attribute("val").map(_.text),
      node.attribute("val2").map(_.text),
      node.attribute("tstamp2.ges").map(_.text),
      node.attribute("tstamp2.real").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("dur").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("tstamp2").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("lform").map(_.text),
      node.attribute("lwidth").map(_.text),
      node.attribute("lsegs").map(_.text),
      node.attribute("lendsym").map(_.text),
      node.attribute("lendsym.size").map(_.text),
      node.attribute("lstartsym").map(_.text),
      node.attribute("lstartsym.size").map(_.text),
      node.attribute("extender").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("vgrp").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("startho").map(_.text),
      node.attribute("endho").map(_.text),
      node.attribute("startto").map(_.text),
      node.attribute("endto").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object EditionParser extends MeiModelDummyParser {
  def parse(node: Node): Edition = {
    Edition(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "editor"       => mei.allanystart.parser.EditorParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "respStmt"     => mei.allanystart.parser.RespStmtParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "textLang"     => mei.allanystart.parser.TextLangParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object EditorParser extends MeiModelDummyParser {
  def parse(node: Node): Editor = {
    Editor(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object EndingParser extends MeiModelDummyParser {
  def parse(node: Node): Ending = {
    Ending(
      node.child.collect {
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "app"          => mei.allanystart.parser.AppParser.parse(n)
        case n: Elem if n.label == "cb"           => mei.allanystart.parser.CbParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "colLayout"    => mei.allanystart.parser.ColLayoutParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "expansion"    => mei.allanystart.parser.ExpansionParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "measure"      => mei.allanystart.parser.MeasureParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sb"           => mei.allanystart.parser.SbParser.parse(n)
        case n: Elem if n.label == "scoreDef"     => mei.allanystart.parser.ScoreDefParser.parse(n)
        case n: Elem if n.label == "section"      => mei.allanystart.parser.SectionParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "staff"        => mei.allanystart.parser.StaffParser.parse(n)
        case n: Elem if n.label == "staffDef"     => mei.allanystart.parser.StaffDefParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xlink:actuate").map(_.text),
      node.attribute("xlink:role").map(_.text),
      node.attribute("xlink:show").map(_.text),
      node.attribute("target").map(_.text),
      node.attribute("targettype").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("lform").map(_.text),
      node.attribute("lwidth").map(_.text),
      node.attribute("lsegs").map(_.text),
      node.attribute("lendsym").map(_.text),
      node.attribute("lendsym.size").map(_.text),
      node.attribute("lstartsym").map(_.text),
      node.attribute("lstartsym.size").map(_.text),
    )
  }
}
object EventParser extends MeiModelDummyParser {
  def parse(node: Node): Event = {
    Event(
      node.child.collect {
        case n: Elem if n.label == "address"   => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "biblList"  => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "castList"  => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "corpName"  => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "date"      => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "desc"      => mei.allanystart.parser.DescParser.parse(n)
        case n: Elem if n.label == "eventList" => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "geogName"  => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"      => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "list"      => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "name"      => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "p"         => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "persName"  => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "table"     => mei.allanystart.parser.TableParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("calendar").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object EventListParser extends MeiModelDummyParser {
  def parse(node: Node): EventList = {
    EventList(
      node.child.collect {
        case n: Elem if n.label == "address"   => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "biblList"  => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "corpName"  => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "date"      => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "event"     => mei.allanystart.parser.EventParser.parse(n)
        case n: Elem if n.label == "eventList" => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "geogName"  => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "head"      => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "name"      => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "persName"  => mei.allanystart.parser.PersNameParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("facs").map(_.text),
    )
  }
}
object ExpansionParser extends MeiModelDummyParser {
  def parse(node: Node): Expansion = {
    Expansion(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("evaluate").map(_.text),
    )
  }
}
object ExtentParser extends MeiModelDummyParser {
  def parse(node: Node): Extent = {
    Extent(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("unit").map(_.text),
      node.attribute("atleast").map(_.text),
      node.attribute("atmost").map(_.text),
      node.attribute("min").map(_.text),
      node.attribute("max").map(_.text),
      node.attribute("confidence").map(_.text),
      node.attribute("quantity").map(_.text),
    )
  }
}
object ExtDataParser extends MeiModelDummyParser {
  def parse(node: Node): ExtData = {
    ExtData(
      node.child.collect {
        case n if n.text.trim.nonEmpty => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("xml:space").map(_.text),
      node.attribute("xlink:actuate").map(_.text),
      node.attribute("xlink:role").map(_.text),
      node.attribute("xlink:show").map(_.text),
      node.attribute("target").map(_.text),
      node.attribute("targettype").map(_.text),
      node.attribute("mimetype").map(_.text),
    )
  }
}
object FunderParser extends MeiModelDummyParser {
  def parse(node: Node): Funder = {
    Funder(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object GenreParser extends MeiModelDummyParser {
  def parse(node: Node): Genre = {
    Genre(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object GroupParser extends MeiModelDummyParser {
  def parse(node: Node): Group = {
    Group(
      node.child.collect {
        case n: Elem if n.label == "group" => mei.allanystart.parser.GroupParser.parse(n)
        case n: Elem if n.label == "music" => mei.allanystart.parser.MusicParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("decls").map(_.text),
    )
  }
}
object GrpSymParser extends MeiModelDummyParser {
  def parse(node: Node): GrpSym = {
    GrpSym(
      node.child.collect {
        case n: Elem if n.label == "label"     => mei.allanystart.parser.LabelParser.parse(n)
        case n: Elem if n.label == "labelAbbr" => mei.allanystart.parser.LabelAbbrParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("symbol").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("level").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object HeadParser extends MeiModelDummyParser {
  def parse(node: Node): Head = {
    Head(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object HeightParser extends MeiModelDummyParser {
  def parse(node: Node): Height = {
    Height(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("unit").map(_.text),
      node.attribute("atleast").map(_.text),
      node.attribute("atmost").map(_.text),
      node.attribute("min").map(_.text),
      node.attribute("max").map(_.text),
      node.attribute("confidence").map(_.text),
      node.attribute("quantity").map(_.text),
    )
  }
}
object IdentifierParser extends MeiModelDummyParser {
  def parse(node: Node): Identifier = {
    Identifier(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("facs").map(_.text),
    )
  }
}
object ImprintParser extends MeiModelDummyParser {
  def parse(node: Node): Imprint = {
    Imprint(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "availability" => mei.allanystart.parser.AvailabilityParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "distributor"  => mei.allanystart.parser.DistributorParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "pubPlace"     => mei.allanystart.parser.PubPlaceParser.parse(n)
        case n: Elem if n.label == "publisher"    => mei.allanystart.parser.PublisherParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "respStmt"     => mei.allanystart.parser.RespStmtParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "unpub"        => mei.allanystart.parser.UnpubParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("facs").map(_.text),
    )
  }
}
object IncipParser extends MeiModelDummyParser {
  def parse(node: Node): Incip = {
    Incip(
      node.child.collect {
        case n: Elem if n.label == "annot"       => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "clef"        => mei.allanystart.parser.ClefParser.parse(n)
        case n: Elem if n.label == "clefGrp"     => mei.allanystart.parser.ClefGrpParser.parse(n)
        case n: Elem if n.label == "graphic"     => mei.allanystart.parser.GraphicParser.parse(n)
        case n: Elem if n.label == "head"        => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "incipCode"   => mei.allanystart.parser.IncipCodeParser.parse(n)
        case n: Elem if n.label == "incipText"   => mei.allanystart.parser.IncipTextParser.parse(n)
        case n: Elem if n.label == "key"         => mei.allanystart.parser.KeyParser.parse(n)
        case n: Elem if n.label == "mensuration" => mei.allanystart.parser.MensurationParser.parse(n)
        case n: Elem if n.label == "meter"       => mei.allanystart.parser.MeterParser.parse(n)
        case n: Elem if n.label == "perfRes"     => mei.allanystart.parser.PerfResParser.parse(n)
        case n: Elem if n.label == "perfResList" => mei.allanystart.parser.PerfResListParser.parse(n)
        case n: Elem if n.label == "role"        => mei.allanystart.parser.RoleParser.parse(n)
        case n: Elem if n.label == "score"       => mei.allanystart.parser.ScoreParser.parse(n)
        case n: Elem if n.label == "tempo"       => mei.allanystart.parser.TempoParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
    )
  }
}
object KeyAccidParser extends MeiModelDummyParser {
  def parse(node: Node): KeyAccid = {
    KeyAccid(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("accid").map(_.text),
      node.attribute("pname").map(_.text),
      node.attribute("oct").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("enclose").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("loc").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("form").map(_.text),
    )
  }
}
object KeySigParser extends MeiModelDummyParser {
  def parse(node: Node): KeySig = {
    KeySig(
      node.child.collect {
        case n: Elem if n.label == "add"       => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "corr"      => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "damage"    => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "del"       => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "gap"       => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "handShift" => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "keyAccid"  => mei.allanystart.parser.KeyAccidParser.parse(n)
        case n: Elem if n.label == "orig"      => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "reg"       => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "restore"   => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sic"       => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "supplied"  => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "unclear"   => mei.allanystart.parser.UnclearParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("accid").map(_.text),
      node.attribute("mode").map(_.text),
      node.attribute("pname").map(_.text),
      node.attribute("sig").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("visible").map(_.text),
      node.attribute("cancelaccid").map(_.text),
    )
  }
}
object LabelParser extends MeiModelDummyParser {
  def parse(node: Node): Label = {
    Label(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("source").map(_.text),
    )
  }
}
object LabelAbbrParser extends MeiModelDummyParser {
  def parse(node: Node): LabelAbbr = {
    LabelAbbr(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("source").map(_.text),
    )
  }
}
object LayerParser extends MeiModelDummyParser {
  def parse(node: Node): Layer = {
    Layer(
      node.child.collect {
        case n: Elem if n.label == "accid"        => mei.allanystart.parser.AccidParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "app"          => mei.allanystart.parser.AppParser.parse(n)
        case n: Elem if n.label == "artic"        => mei.allanystart.parser.ArticParser.parse(n)
        case n: Elem if n.label == "bTrem"        => mei.allanystart.parser.BTremParser.parse(n)
        case n: Elem if n.label == "barLine"      => mei.allanystart.parser.BarLineParser.parse(n)
        case n: Elem if n.label == "beam"         => mei.allanystart.parser.BeamParser.parse(n)
        case n: Elem if n.label == "beatRpt"      => mei.allanystart.parser.BeatRptParser.parse(n)
        case n: Elem if n.label == "cb"           => mei.allanystart.parser.CbParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "chord"        => mei.allanystart.parser.ChordParser.parse(n)
        case n: Elem if n.label == "clef"         => mei.allanystart.parser.ClefParser.parse(n)
        case n: Elem if n.label == "clefGrp"      => mei.allanystart.parser.ClefGrpParser.parse(n)
        case n: Elem if n.label == "colLayout"    => mei.allanystart.parser.ColLayoutParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "custos"       => mei.allanystart.parser.CustosParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "divLine"      => mei.allanystart.parser.DivLineParser.parse(n)
        case n: Elem if n.label == "dot"          => mei.allanystart.parser.DotParser.parse(n)
        case n: Elem if n.label == "fTrem"        => mei.allanystart.parser.FTremParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "graceGrp"     => mei.allanystart.parser.GraceGrpParser.parse(n)
        case n: Elem if n.label == "halfmRpt"     => mei.allanystart.parser.HalfmRptParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "keySig"       => mei.allanystart.parser.KeySigParser.parse(n)
        case n: Elem if n.label == "ligature"     => mei.allanystart.parser.LigatureParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "mRest"        => mei.allanystart.parser.MRestParser.parse(n)
        case n: Elem if n.label == "mRpt"         => mei.allanystart.parser.MRptParser.parse(n)
        case n: Elem if n.label == "mRpt2"        => mei.allanystart.parser.MRpt2Parser.parse(n)
        case n: Elem if n.label == "mSpace"       => mei.allanystart.parser.MSpaceParser.parse(n)
        case n: Elem if n.label == "mensur"       => mei.allanystart.parser.MensurParser.parse(n)
        case n: Elem if n.label == "meterSig"     => mei.allanystart.parser.MeterSigParser.parse(n)
        case n: Elem if n.label == "meterSigGrp"  => mei.allanystart.parser.MeterSigGrpParser.parse(n)
        case n: Elem if n.label == "midi"         => mei.allanystart.parser.MidiParser.parse(n)
        case n: Elem if n.label == "multiRest"    => mei.allanystart.parser.MultiRestParser.parse(n)
        case n: Elem if n.label == "multiRpt"     => mei.allanystart.parser.MultiRptParser.parse(n)
        case n: Elem if n.label == "neume"        => mei.allanystart.parser.NeumeParser.parse(n)
        case n: Elem if n.label == "note"         => mei.allanystart.parser.NoteParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pad"          => mei.allanystart.parser.PadParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "proport"      => mei.allanystart.parser.ProportParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "rest"         => mei.allanystart.parser.RestParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sb"           => mei.allanystart.parser.SbParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "space"        => mei.allanystart.parser.SpaceParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "syllable"     => mei.allanystart.parser.SyllableParser.parse(n)
        case n: Elem if n.label == "tabGrp"       => mei.allanystart.parser.TabGrpParser.parse(n)
        case n: Elem if n.label == "tuplet"       => mei.allanystart.parser.TupletParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("cue").map(_.text),
      node.attribute("metcon").map(_.text),
      node.attribute("def").map(_.text),
      node.attribute("visible").map(_.text),
    )
  }
}
object LayerDefParser extends MeiModelDummyParser {
  def parse(node: Node): LayerDef = {
    LayerDef(
      node.child.collect {
        case n: Elem if n.label == "ambitus"     => mei.allanystart.parser.AmbitusParser.parse(n)
        case n: Elem if n.label == "instrDef"    => mei.allanystart.parser.InstrDefParser.parse(n)
        case n: Elem if n.label == "label"       => mei.allanystart.parser.LabelParser.parse(n)
        case n: Elem if n.label == "labelAbbr"   => mei.allanystart.parser.LabelAbbrParser.parse(n)
        case n: Elem if n.label == "meterSig"    => mei.allanystart.parser.MeterSigParser.parse(n)
        case n: Elem if n.label == "meterSigGrp" => mei.allanystart.parser.MeterSigGrpParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("instr").map(_.text),
      node.attribute("tune.Hz").map(_.text),
      node.attribute("tune.pname").map(_.text),
      node.attribute("tune.temper").map(_.text),
      node.attribute("dur.default").map(_.text),
      node.attribute("num.default").map(_.text),
      node.attribute("numbase.default").map(_.text),
      node.attribute("beam.group").map(_.text),
      node.attribute("beam.rests").map(_.text),
      node.attribute("oct.default").map(_.text),
      node.attribute("trans.diat").map(_.text),
      node.attribute("trans.semi").map(_.text),
      node.attribute("beam.color").map(_.text),
      node.attribute("beam.rend").map(_.text),
      node.attribute("beam.slope").map(_.text),
      node.attribute("text.fam").map(_.text),
      node.attribute("text.name").map(_.text),
      node.attribute("text.size").map(_.text),
      node.attribute("text.style").map(_.text),
      node.attribute("text.weight").map(_.text),
      node.attribute("visible").map(_.text),
    )
  }
}
object LbParser extends MeiModelDummyParser {
  def parse(node: Node): Lb = {
    Lb(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("source").map(_.text),
    )
  }
}
object LgParser extends MeiModelDummyParser {
  def parse(node: Node): Lg = {
    Lg(
      node.child.collect {
        case n: Elem if n.label == "head" => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "l"    => mei.allanystart.parser.LParser.parse(n)
        case n: Elem if n.label == "lg"   => mei.allanystart.parser.LgParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
    )
  }
}
object LibrettistParser extends MeiModelDummyParser {
  def parse(node: Node): Librettist = {
    Librettist(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object LyricistParser extends MeiModelDummyParser {
  def parse(node: Node): Lyricist = {
    Lyricist(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object MdivParser extends MeiModelDummyParser {
  def parse(node: Node): Mdiv = {
    Mdiv(
      node.child.collect {
        case n: Elem if n.label == "mdiv"  => mei.allanystart.parser.MdivParser.parse(n)
        case n: Elem if n.label == "parts" => mei.allanystart.parser.PartsParser.parse(n)
        case n: Elem if n.label == "score" => mei.allanystart.parser.ScoreParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("attacca").map(_.text),
      node.attribute("when").map(_.text),
    )
  }
}
object MeiParser extends MeiModelDummyParser {
  def parse(node: Node): Mei = {
    Mei(
      node.child.collect {
        case n: Elem if n.label == "meiHead" => mei.allanystart.parser.MeiHeadParser.parse(n)
        case n: Elem if n.label == "music"   => mei.allanystart.parser.MusicParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("resp").map(_.text),
    )
  }
}
object MonogrParser extends MeiModelDummyParser {
  def parse(node: Node): Monogr = {
    Monogr(
      node.child.collect {
        case n: Elem if n.label == "annot"       => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "arranger"    => mei.allanystart.parser.ArrangerParser.parse(n)
        case n: Elem if n.label == "author"      => mei.allanystart.parser.AuthorParser.parse(n)
        case n: Elem if n.label == "biblScope"   => mei.allanystart.parser.BiblScopeParser.parse(n)
        case n: Elem if n.label == "composer"    => mei.allanystart.parser.ComposerParser.parse(n)
        case n: Elem if n.label == "contributor" => mei.allanystart.parser.ContributorParser.parse(n)
        case n: Elem if n.label == "corpName"    => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "edition"     => mei.allanystart.parser.EditionParser.parse(n)
        case n: Elem if n.label == "editor"      => mei.allanystart.parser.EditorParser.parse(n)
        case n: Elem if n.label == "extent"      => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "funder"      => mei.allanystart.parser.FunderParser.parse(n)
        case n: Elem if n.label == "identifier"  => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "imprint"     => mei.allanystart.parser.ImprintParser.parse(n)
        case n: Elem if n.label == "librettist"  => mei.allanystart.parser.LibrettistParser.parse(n)
        case n: Elem if n.label == "lyricist"    => mei.allanystart.parser.LyricistParser.parse(n)
        case n: Elem if n.label == "respStmt"    => mei.allanystart.parser.RespStmtParser.parse(n)
        case n: Elem if n.label == "sponsor"     => mei.allanystart.parser.SponsorParser.parse(n)
        case n: Elem if n.label == "title"       => mei.allanystart.parser.TitleParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("data").map(_.text),
      node.attribute("xlink:actuate").map(_.text),
      node.attribute("xlink:role").map(_.text),
      node.attribute("xlink:show").map(_.text),
      node.attribute("target").map(_.text),
      node.attribute("targettype").map(_.text),
      node.attribute("recordtype").map(_.text),
      node.attribute("evaluate").map(_.text),
    )
  }
}
object MusicParser extends MeiModelDummyParser {
  def parse(node: Node): Music = {
    Music(
      node.child.collect {
        case n: Elem if n.label == "back"        => mei.allanystart.parser.BackParser.parse(n)
        case n: Elem if n.label == "body"        => mei.allanystart.parser.BodyParser.parse(n)
        case n: Elem if n.label == "facsimile"   => mei.allanystart.parser.FacsimileParser.parse(n)
        case n: Elem if n.label == "front"       => mei.allanystart.parser.FrontParser.parse(n)
        case n: Elem if n.label == "genDesc"     => mei.allanystart.parser.GenDescParser.parse(n)
        case n: Elem if n.label == "group"       => mei.allanystart.parser.GroupParser.parse(n)
        case n: Elem if n.label == "performance" => mei.allanystart.parser.PerformanceParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("decls").map(_.text),
    )
  }
}
object NameParser extends MeiModelDummyParser {
  def parse(node: Node): Name = {
    Name(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("nonfiling").map(_.text),
      node.attribute("nymref").map(_.text),
      node.attribute("role").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("type").map(_.text),
    )
  }
}
object NoteParser extends MeiModelDummyParser {
  def parse(node: Node): Note = {
    Note(
      node.child.collect {
        case n: Elem if n.label == "accid"     => mei.allanystart.parser.AccidParser.parse(n)
        case n: Elem if n.label == "add"       => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "app"       => mei.allanystart.parser.AppParser.parse(n)
        case n: Elem if n.label == "artic"     => mei.allanystart.parser.ArticParser.parse(n)
        case n: Elem if n.label == "choice"    => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corr"      => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "damage"    => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "del"       => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "dot"       => mei.allanystart.parser.DotParser.parse(n)
        case n: Elem if n.label == "gap"       => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "handShift" => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "orig"      => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "plica"     => mei.allanystart.parser.PlicaParser.parse(n)
        case n: Elem if n.label == "refrain"   => mei.allanystart.parser.RefrainParser.parse(n)
        case n: Elem if n.label == "reg"       => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "restore"   => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sic"       => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "stem"      => mei.allanystart.parser.StemParser.parse(n)
        case n: Elem if n.label == "subst"     => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"  => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "syl"       => mei.allanystart.parser.SylParser.parse(n)
        case n: Elem if n.label == "unclear"   => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "verse"     => mei.allanystart.parser.VerseParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("accid").map(_.text),
      node.attribute("artic").map(_.text),
      node.attribute("deg").map(_.text),
      node.attribute("intm").map(_.text),
      node.attribute("mfunc").map(_.text),
      node.attribute("beam").map(_.text),
      node.attribute("gliss").map(_.text),
      node.attribute("lv").map(_.text),
      node.attribute("ornam").map(_.text),
      node.attribute("slur").map(_.text),
      node.attribute("syl").map(_.text),
      node.attribute("tie").map(_.text),
      node.attribute("tuplet").map(_.text),
      node.attribute("fermata").map(_.text),
      node.attribute("pclass").map(_.text),
      node.attribute("psolfa").map(_.text),
      node.attribute("accid.ges").map(_.text),
      node.attribute("artic.ges").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
      node.attribute("instr").map(_.text),
      node.attribute("vel").map(_.text),
      node.attribute("num").map(_.text),
      node.attribute("numbase").map(_.text),
      node.attribute("oct.ges").map(_.text),
      node.attribute("pname.ges").map(_.text),
      node.attribute("pnum").map(_.text),
      node.attribute("tab.fing").map(_.text),
      node.attribute("tab.fret").map(_.text),
      node.attribute("tab.line").map(_.text),
      node.attribute("tab.string").map(_.text),
      node.attribute("tab.course").map(_.text),
      node.attribute("extremis").map(_.text),
      node.attribute("dots").map(_.text),
      node.attribute("colored").map(_.text),
      node.attribute("cue").map(_.text),
      node.attribute("dur").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("grace").map(_.text),
      node.attribute("grace.time").map(_.text),
      node.attribute("pname").map(_.text),
      node.attribute("oct").map(_.text),
      node.attribute("dur.quality").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("enclose").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("head.altsym").map(_.text),
      node.attribute("head.auth").map(_.text),
      node.attribute("head.color").map(_.text),
      node.attribute("head.fill").map(_.text),
      node.attribute("head.fillcolor").map(_.text),
      node.attribute("head.mod").map(_.text),
      node.attribute("head.rotation").map(_.text),
      node.attribute("head.shape").map(_.text),
      node.attribute("head.visible").map(_.text),
      node.attribute("loc").map(_.text),
      node.attribute("stem.with").map(_.text),
      node.attribute("stem.form").map(_.text),
      node.attribute("stem.dir").map(_.text),
      node.attribute("stem.len").map(_.text),
      node.attribute("stem.mod").map(_.text),
      node.attribute("stem.pos").map(_.text),
      node.attribute("stem.sameas").map(_.text),
      node.attribute("stem.visible").map(_.text),
      node.attribute("stem.x").map(_.text),
      node.attribute("stem.y").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("visible").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("breaksec").map(_.text),
      node.attribute("lig").map(_.text),
    )
  }
}
object NumParser extends MeiModelDummyParser {
  def parse(node: Node): Num = {
    Num(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("unit").map(_.text),
      node.attribute("atleast").map(_.text),
      node.attribute("atmost").map(_.text),
      node.attribute("min").map(_.text),
      node.attribute("max").map(_.text),
      node.attribute("confidence").map(_.text),
      node.attribute("value").map(_.text),
    )
  }
}
object OrnamParser extends MeiModelDummyParser {
  def parse(node: Node): Ornam = {
    Ornam(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
      node.attribute("tstamp2.ges").map(_.text),
      node.attribute("tstamp2.real").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("dur").map(_.text),
      node.attribute("accidupper.ges").map(_.text),
      node.attribute("accidlower.ges").map(_.text),
      node.attribute("accidupper").map(_.text),
      node.attribute("accidlower").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("tstamp2").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("enclose").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("vgrp").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("startho").map(_.text),
      node.attribute("endho").map(_.text),
      node.attribute("startto").map(_.text),
      node.attribute("endto").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object PParser extends MeiModelDummyParser {
  def parse(node: Node): P = {
    P(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblList"     => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "castList"     => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "eventList"    => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "lg"           => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "list"         => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "quote"        => mei.allanystart.parser.QuoteParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stageDir"     => mei.allanystart.parser.StageDirParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "table"        => mei.allanystart.parser.TableParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object PadParser extends MeiModelDummyParser {
  def parse(node: Node): Pad = {
    Pad(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("width").map(_.text),
    )
  }
}
object PartParser extends MeiModelDummyParser {
  def parse(node: Node): Part = {
    Part(
      node.child.collect {
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "app"          => mei.allanystart.parser.AppParser.parse(n)
        case n: Elem if n.label == "cb"           => mei.allanystart.parser.CbParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "colLayout"    => mei.allanystart.parser.ColLayoutParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "div"          => mei.allanystart.parser.DivParser.parse(n)
        case n: Elem if n.label == "ending"       => mei.allanystart.parser.EndingParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sb"           => mei.allanystart.parser.SbParser.parse(n)
        case n: Elem if n.label == "scoreDef"     => mei.allanystart.parser.ScoreDefParser.parse(n)
        case n: Elem if n.label == "section"      => mei.allanystart.parser.SectionParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "staffDef"     => mei.allanystart.parser.StaffDefParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("decls").map(_.text),
    )
  }
}
object PartsParser extends MeiModelDummyParser {
  def parse(node: Node): Parts = {
    Parts(
      node.child.collect {
        case n: Elem if n.label == "part" => mei.allanystart.parser.PartParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("decls").map(_.text),
    )
  }
}
object PbParser extends MeiModelDummyParser {
  def parse(node: Node): Pb = {
    Pb(
      node.child.collect {
        case n: Elem if n.label == "pgDesc" => mei.allanystart.parser.PgDescParser.parse(n)
        case n: Elem if n.label == "pgFoot" => mei.allanystart.parser.PgFootParser.parse(n)
        case n: Elem if n.label == "pgHead" => mei.allanystart.parser.PgHeadParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xlink:actuate").map(_.text),
      node.attribute("xlink:role").map(_.text),
      node.attribute("xlink:show").map(_.text),
      node.attribute("target").map(_.text),
      node.attribute("targettype").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("folium").map(_.text),
    )
  }
}
object PgDescParser extends MeiModelDummyParser {
  def parse(node: Node): PgDesc = {
    PgDesc(
      node.child.collect {
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "biblList"     => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "castList"     => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "eventList"    => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "lg"           => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "list"         => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "quote"        => mei.allanystart.parser.QuoteParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "table"        => mei.allanystart.parser.TableParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object PgFootParser extends MeiModelDummyParser {
  def parse(node: Node): PgFoot = {
    PgFoot(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "app"          => mei.allanystart.parser.AppParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblList"     => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "castList"     => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "eventList"    => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "lg"           => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "list"         => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "quote"        => mei.allanystart.parser.QuoteParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "table"        => mei.allanystart.parser.TableParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("func").map(_.text),
      node.attribute("halign").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object PgHeadParser extends MeiModelDummyParser {
  def parse(node: Node): PgHead = {
    PgHead(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "app"          => mei.allanystart.parser.AppParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblList"     => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "castList"     => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "eventList"    => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "lg"           => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "list"         => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "quote"        => mei.allanystart.parser.QuoteParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "table"        => mei.allanystart.parser.TableParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("func").map(_.text),
      node.attribute("halign").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object PhraseParser extends MeiModelDummyParser {
  def parse(node: Node): Phrase = {
    Phrase(
      node.child.collect {
        case n: Elem if n.label == "curve" => mei.allanystart.parser.CurveParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("join").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
      node.attribute("tstamp2.ges").map(_.text),
      node.attribute("tstamp2.real").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("dur").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("tstamp2").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("startho").map(_.text),
      node.attribute("endho").map(_.text),
      node.attribute("startto").map(_.text),
      node.attribute("endto").map(_.text),
      node.attribute("startvo").map(_.text),
      node.attribute("endvo").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("x2").map(_.text),
      node.attribute("y2").map(_.text),
      node.attribute("bezier").map(_.text),
      node.attribute("bulge").map(_.text),
      node.attribute("curvedir").map(_.text),
      node.attribute("lform").map(_.text),
      node.attribute("lwidth").map(_.text),
      node.attribute("lsegs").map(_.text),
    )
  }
}
object PhysLocParser extends MeiModelDummyParser {
  def parse(node: Node): PhysLoc = {
    PhysLoc(
      node.child.collect {
        case n: Elem if n.label == "head"       => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "history"    => mei.allanystart.parser.HistoryParser.parse(n)
        case n: Elem if n.label == "identifier" => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "repository" => mei.allanystart.parser.RepositoryParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("facs").map(_.text),
    )
  }
}
object PublisherParser extends MeiModelDummyParser {
  def parse(node: Node): Publisher = {
    Publisher(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object PubPlaceParser extends MeiModelDummyParser {
  def parse(node: Node): PubPlace = {
    PubPlace(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object RecipientParser extends MeiModelDummyParser {
  def parse(node: Node): Recipient = {
    Recipient(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object RelatedItemParser extends MeiModelDummyParser {
  def parse(node: Node): RelatedItem = {
    RelatedItem(
      node.child.collect {
        case n: Elem if n.label == "bibl"       => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct" => mei.allanystart.parser.BiblStructParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("data").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xlink:actuate").map(_.text),
      node.attribute("xlink:role").map(_.text),
      node.attribute("xlink:show").map(_.text),
      node.attribute("target").map(_.text),
      node.attribute("targettype").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("rel").map(_.text).get,
    )
  }
}
object RelationParser extends MeiModelDummyParser {
  def parse(node: Node): Relation = {
    Relation(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("xlink:actuate").map(_.text),
      node.attribute("xlink:role").map(_.text),
      node.attribute("xlink:show").map(_.text),
      node.attribute("target").map(_.text),
      node.attribute("targettype").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("rel").map(_.text).get,
    )
  }
}
object RelationListParser extends MeiModelDummyParser {
  def parse(node: Node): RelationList = {
    RelationList(
      node.child.collect {
        case n: Elem if n.label == "head"         => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
    )
  }
}
object RendParser extends MeiModelDummyParser {
  def parse(node: Node): Rend = {
    Rend(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("color").map(_.text),
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("halign").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("altrend").map(_.text),
      node.attribute("rend").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("valign").map(_.text),
      node.attribute("xml:space").map(_.text),
      node.attribute("rotation").map(_.text),
    )
  }
}
object RepositoryParser extends MeiModelDummyParser {
  def parse(node: Node): Repository = {
    Repository(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("nonfiling").map(_.text),
      node.attribute("nymref").map(_.text),
      node.attribute("role").map(_.text),
    )
  }
}
object RespParser extends MeiModelDummyParser {
  def parse(node: Node): Resp = {
    Resp(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("enddate").map(_.text),
      node.attribute("isodate").map(_.text),
      node.attribute("notafter").map(_.text),
      node.attribute("notbefore").map(_.text),
      node.attribute("startdate").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object RespStmtParser extends MeiModelDummyParser {
  def parse(node: Node): RespStmt = {
    RespStmt(
      node.child.collect {
        case n: Elem if n.label == "corpName" => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "head"     => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "name"     => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "persName" => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "resp"     => mei.allanystart.parser.RespParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("facs").map(_.text),
    )
  }
}
object RestParser extends MeiModelDummyParser {
  def parse(node: Node): Rest = {
    Rest(
      node.child.collect {
        case n: Elem if n.label == "add"       => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "app"       => mei.allanystart.parser.AppParser.parse(n)
        case n: Elem if n.label == "choice"    => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corr"      => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "damage"    => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "del"       => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "dot"       => mei.allanystart.parser.DotParser.parse(n)
        case n: Elem if n.label == "gap"       => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "handShift" => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "orig"      => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "reg"       => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "restore"   => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sic"       => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "subst"     => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"  => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "unclear"   => mei.allanystart.parser.UnclearParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("beam").map(_.text),
      node.attribute("fermata").map(_.text),
      node.attribute("tuplet").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
      node.attribute("num").map(_.text),
      node.attribute("numbase").map(_.text),
      node.attribute("dots").map(_.text),
      node.attribute("cue").map(_.text),
      node.attribute("dur").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("enclose").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("breaksec").map(_.text),
      node.attribute("spaces").map(_.text),
      node.attribute("loc").map(_.text),
      node.attribute("ploc").map(_.text),
      node.attribute("oloc").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object RoleParser extends MeiModelDummyParser {
  def parse(node: Node): Role = {
    Role(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object RoleDescParser extends MeiModelDummyParser {
  def parse(node: Node): RoleDesc = {
    RoleDesc(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object SbParser extends MeiModelDummyParser {
  def parse(node: Node): Sb = {
    Sb(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("source").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("form").map(_.text),
    )
  }
}
object ScoreParser extends MeiModelDummyParser {
  def parse(node: Node): Score = {
    Score(
      node.child.collect {
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "app"          => mei.allanystart.parser.AppParser.parse(n)
        case n: Elem if n.label == "cb"           => mei.allanystart.parser.CbParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "colLayout"    => mei.allanystart.parser.ColLayoutParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "div"          => mei.allanystart.parser.DivParser.parse(n)
        case n: Elem if n.label == "ending"       => mei.allanystart.parser.EndingParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sb"           => mei.allanystart.parser.SbParser.parse(n)
        case n: Elem if n.label == "scoreDef"     => mei.allanystart.parser.ScoreDefParser.parse(n)
        case n: Elem if n.label == "section"      => mei.allanystart.parser.SectionParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "staffDef"     => mei.allanystart.parser.StaffDefParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("decls").map(_.text),
    )
  }
}
object ScoreDefParser extends MeiModelDummyParser {
  def parse(node: Node): ScoreDef = {
    ScoreDef(
      node.child.collect {
        case n: Elem if n.label == "ambitus"     => mei.allanystart.parser.AmbitusParser.parse(n)
        case n: Elem if n.label == "chordTable"  => mei.allanystart.parser.ChordTableParser.parse(n)
        case n: Elem if n.label == "grpSym"      => mei.allanystart.parser.GrpSymParser.parse(n)
        case n: Elem if n.label == "instrGrp"    => mei.allanystart.parser.InstrGrpParser.parse(n)
        case n: Elem if n.label == "keySig"      => mei.allanystart.parser.KeySigParser.parse(n)
        case n: Elem if n.label == "meterSig"    => mei.allanystart.parser.MeterSigParser.parse(n)
        case n: Elem if n.label == "meterSigGrp" => mei.allanystart.parser.MeterSigGrpParser.parse(n)
        case n: Elem if n.label == "pgFoot"      => mei.allanystart.parser.PgFootParser.parse(n)
        case n: Elem if n.label == "pgHead"      => mei.allanystart.parser.PgHeadParser.parse(n)
        case n: Elem if n.label == "staffGrp"    => mei.allanystart.parser.StaffGrpParser.parse(n)
        case n: Elem if n.label == "symbolTable" => mei.allanystart.parser.SymbolTableParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("key.accid").map(_.text),
      node.attribute("key.mode").map(_.text),
      node.attribute("key.pname").map(_.text),
      node.attribute("midi.channel").map(_.text),
      node.attribute("midi.duty").map(_.text),
      node.attribute("midi.port").map(_.text),
      node.attribute("midi.track").map(_.text),
      node.attribute("ppq").map(_.text),
      node.attribute("tune.Hz").map(_.text),
      node.attribute("tune.pname").map(_.text),
      node.attribute("tune.temper").map(_.text),
      node.attribute("midi.bpm").map(_.text),
      node.attribute("midi.mspb").map(_.text),
      node.attribute("mm").map(_.text),
      node.attribute("mm.unit").map(_.text),
      node.attribute("mm.dots").map(_.text),
      node.attribute("clef.shape").map(_.text),
      node.attribute("clef.line").map(_.text),
      node.attribute("clef.dis").map(_.text),
      node.attribute("clef.dis.place").map(_.text),
      node.attribute("dur.default").map(_.text),
      node.attribute("num.default").map(_.text),
      node.attribute("numbase.default").map(_.text),
      node.attribute("keysig").map(_.text),
      node.attribute("meter.count").map(_.text),
      node.attribute("meter.unit").map(_.text),
      node.attribute("meter.sym").map(_.text),
      node.attribute("oct.default").map(_.text),
      node.attribute("trans.diat").map(_.text),
      node.attribute("trans.semi").map(_.text),
      node.attribute("beam.group").map(_.text),
      node.attribute("beam.rests").map(_.text),
      node.attribute("modusmaior").map(_.text),
      node.attribute("modusminor").map(_.text),
      node.attribute("prolatio").map(_.text),
      node.attribute("tempus").map(_.text),
      node.attribute("divisio").map(_.text),
      node.attribute("proport.num").map(_.text),
      node.attribute("proport.numbase").map(_.text),
      node.attribute("bar.len").map(_.text),
      node.attribute("bar.method").map(_.text),
      node.attribute("bar.place").map(_.text),
      node.attribute("clef.color").map(_.text),
      node.attribute("clef.visible").map(_.text),
      node.attribute("dir.dist").map(_.text),
      node.attribute("dynam.dist").map(_.text),
      node.attribute("harm.dist").map(_.text),
      node.attribute("reh.dist").map(_.text),
      node.attribute("tempo.dist").map(_.text),
      node.attribute("ending.rend").map(_.text),
      node.attribute("keysig.cancelaccid").map(_.text),
      node.attribute("keysig.visible").map(_.text),
      node.attribute("lyric.align").map(_.text),
      node.attribute("lyric.fam").map(_.text),
      node.attribute("lyric.name").map(_.text),
      node.attribute("lyric.size").map(_.text),
      node.attribute("lyric.style").map(_.text),
      node.attribute("lyric.weight").map(_.text),
      node.attribute("mnum.visible").map(_.text),
      node.attribute("meter.form").map(_.text),
      node.attribute("meter.showchange").map(_.text),
      node.attribute("meter.visible").map(_.text),
      node.attribute("multi.number").map(_.text),
      node.attribute("music.name").map(_.text),
      node.attribute("music.size").map(_.text),
      node.attribute("ontheline").map(_.text),
      node.attribute("optimize").map(_.text),
      node.attribute("page.height").map(_.text),
      node.attribute("page.width").map(_.text),
      node.attribute("page.topmar").map(_.text),
      node.attribute("page.botmar").map(_.text),
      node.attribute("page.leftmar").map(_.text),
      node.attribute("page.rightmar").map(_.text),
      node.attribute("page.panels").map(_.text),
      node.attribute("page.scale").map(_.text),
      node.attribute("spacing.packexp").map(_.text),
      node.attribute("spacing.packfact").map(_.text),
      node.attribute("spacing.staff").map(_.text),
      node.attribute("spacing.system").map(_.text),
      node.attribute("aboveorder").map(_.text),
      node.attribute("beloworder").map(_.text),
      node.attribute("betweenorder").map(_.text),
      node.attribute("system.leftline").map(_.text),
      node.attribute("system.leftmar").map(_.text),
      node.attribute("system.rightmar").map(_.text),
      node.attribute("system.topmar").map(_.text),
      node.attribute("text.fam").map(_.text),
      node.attribute("text.name").map(_.text),
      node.attribute("text.size").map(_.text),
      node.attribute("text.style").map(_.text),
      node.attribute("text.weight").map(_.text),
      node.attribute("beam.color").map(_.text),
      node.attribute("beam.rend").map(_.text),
      node.attribute("beam.slope").map(_.text),
      node.attribute("grid.show").map(_.text),
      node.attribute("pedal.style").map(_.text),
      node.attribute("reh.enclose").map(_.text),
      node.attribute("slur.lform").map(_.text),
      node.attribute("slur.lwidth").map(_.text),
      node.attribute("tie.lform").map(_.text),
      node.attribute("tie.lwidth").map(_.text),
      node.attribute("mensur.color").map(_.text),
      node.attribute("mensur.dot").map(_.text),
      node.attribute("mensur.form").map(_.text),
      node.attribute("mensur.loc").map(_.text),
      node.attribute("mensur.orient").map(_.text),
      node.attribute("mensur.sign").map(_.text),
      node.attribute("mensur.size").map(_.text),
      node.attribute("mensur.slash").map(_.text),
      node.attribute("vu.height").map(_.text),
    )
  }
}
object SectionParser extends MeiModelDummyParser {
  def parse(node: Node): Section = {
    Section(
      node.child.collect {
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "app"          => mei.allanystart.parser.AppParser.parse(n)
        case n: Elem if n.label == "cb"           => mei.allanystart.parser.CbParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "colLayout"    => mei.allanystart.parser.ColLayoutParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "div"          => mei.allanystart.parser.DivParser.parse(n)
        case n: Elem if n.label == "ending"       => mei.allanystart.parser.EndingParser.parse(n)
        case n: Elem if n.label == "expansion"    => mei.allanystart.parser.ExpansionParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "measure"      => mei.allanystart.parser.MeasureParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sb"           => mei.allanystart.parser.SbParser.parse(n)
        case n: Elem if n.label == "scoreDef"     => mei.allanystart.parser.ScoreDefParser.parse(n)
        case n: Elem if n.label == "section"      => mei.allanystart.parser.SectionParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "staff"        => mei.allanystart.parser.StaffParser.parse(n)
        case n: Elem if n.label == "staffDef"     => mei.allanystart.parser.StaffDefParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("xlink:actuate").map(_.text),
      node.attribute("xlink:role").map(_.text),
      node.attribute("xlink:show").map(_.text),
      node.attribute("target").map(_.text),
      node.attribute("targettype").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("attacca").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("restart").map(_.text),
    )
  }
}
object SeriesParser extends MeiModelDummyParser {
  def parse(node: Node): Series = {
    Series(
      node.child.collect {
        case n: Elem if n.label == "cb"         => mei.allanystart.parser.CbParser.parse(n)
        case n: Elem if n.label == "colLayout"  => mei.allanystart.parser.ColLayoutParser.parse(n)
        case n: Elem if n.label == "editor"     => mei.allanystart.parser.EditorParser.parse(n)
        case n: Elem if n.label == "extent"     => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "identifier" => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"         => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "pb"         => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "ptr"        => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "ref"        => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "respStmt"   => mei.allanystart.parser.RespStmtParser.parse(n)
        case n: Elem if n.label == "textLang"   => mei.allanystart.parser.TextLangParser.parse(n)
        case n: Elem if n.label == "title"      => mei.allanystart.parser.TitleParser.parse(n)
        case n if n.text.trim.nonEmpty          => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object SpaceParser extends MeiModelDummyParser {
  def parse(node: Node): Space = {
    Space(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("dur.quality").map(_.text),
      node.attribute("dots").map(_.text),
      node.attribute("dur").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("cutout").map(_.text),
      node.attribute("compressable").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
      node.attribute("beam").map(_.text),
      node.attribute("fermata").map(_.text),
      node.attribute("tuplet").map(_.text),
    )
  }
}
object SpeakerParser extends MeiModelDummyParser {
  def parse(node: Node): Speaker = {
    Speaker(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object SponsorParser extends MeiModelDummyParser {
  def parse(node: Node): Sponsor = {
    Sponsor(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("cert").map(_.text),
      node.attribute("evidence").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object StackParser extends MeiModelDummyParser {
  def parse(node: Node): Stack = {
    Stack(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("delim").map(_.text),
      node.attribute("align").map(_.text),
    )
  }
}
object StaffParser extends MeiModelDummyParser {
  def parse(node: Node): Staff = {
    Staff(
      node.child.collect {
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "app"          => mei.allanystart.parser.AppParser.parse(n)
        case n: Elem if n.label == "bend"         => mei.allanystart.parser.BendParser.parse(n)
        case n: Elem if n.label == "caesura"      => mei.allanystart.parser.CaesuraParser.parse(n)
        case n: Elem if n.label == "cb"           => mei.allanystart.parser.CbParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "colLayout"    => mei.allanystart.parser.ColLayoutParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "cpMark"       => mei.allanystart.parser.CpMarkParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "dir"          => mei.allanystart.parser.DirParser.parse(n)
        case n: Elem if n.label == "dynam"        => mei.allanystart.parser.DynamParser.parse(n)
        case n: Elem if n.label == "fing"         => mei.allanystart.parser.FingParser.parse(n)
        case n: Elem if n.label == "fingGrp"      => mei.allanystart.parser.FingGrpParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "gliss"        => mei.allanystart.parser.GlissParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "harm"         => mei.allanystart.parser.HarmParser.parse(n)
        case n: Elem if n.label == "layer"        => mei.allanystart.parser.LayerParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "metaMark"     => mei.allanystart.parser.MetaMarkParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "ornam"        => mei.allanystart.parser.OrnamParser.parse(n)
        case n: Elem if n.label == "ossia"        => mei.allanystart.parser.OssiaParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "phrase"       => mei.allanystart.parser.PhraseParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "repeatMark"   => mei.allanystart.parser.RepeatMarkParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sb"           => mei.allanystart.parser.SbParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "sp"           => mei.allanystart.parser.SpParser.parse(n)
        case n: Elem if n.label == "staffDef"     => mei.allanystart.parser.StaffDefParser.parse(n)
        case n: Elem if n.label == "stageDir"     => mei.allanystart.parser.StageDirParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "tempo"        => mei.allanystart.parser.TempoParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("metcon").map(_.text),
      node.attribute("def").map(_.text),
      node.attribute("visible").map(_.text),
    )
  }
}
object StaffDefParser extends MeiModelDummyParser {
  def parse(node: Node): StaffDef = {
    StaffDef(
      node.child.collect {
        case n: Elem if n.label == "ambitus"     => mei.allanystart.parser.AmbitusParser.parse(n)
        case n: Elem if n.label == "clef"        => mei.allanystart.parser.ClefParser.parse(n)
        case n: Elem if n.label == "clefGrp"     => mei.allanystart.parser.ClefGrpParser.parse(n)
        case n: Elem if n.label == "instrDef"    => mei.allanystart.parser.InstrDefParser.parse(n)
        case n: Elem if n.label == "keySig"      => mei.allanystart.parser.KeySigParser.parse(n)
        case n: Elem if n.label == "label"       => mei.allanystart.parser.LabelParser.parse(n)
        case n: Elem if n.label == "labelAbbr"   => mei.allanystart.parser.LabelAbbrParser.parse(n)
        case n: Elem if n.label == "layerDef"    => mei.allanystart.parser.LayerDefParser.parse(n)
        case n: Elem if n.label == "mensur"      => mei.allanystart.parser.MensurParser.parse(n)
        case n: Elem if n.label == "meterSig"    => mei.allanystart.parser.MeterSigParser.parse(n)
        case n: Elem if n.label == "meterSigGrp" => mei.allanystart.parser.MeterSigGrpParser.parse(n)
        case n: Elem if n.label == "proport"     => mei.allanystart.parser.ProportParser.parse(n)
        case n: Elem if n.label == "tuning"      => mei.allanystart.parser.TuningParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("key.accid").map(_.text),
      node.attribute("key.mode").map(_.text),
      node.attribute("key.pname").map(_.text),
      node.attribute("instr").map(_.text),
      node.attribute("tab.strings").map(_.text),
      node.attribute("tab.courses").map(_.text),
      node.attribute("ppq").map(_.text),
      node.attribute("tune.Hz").map(_.text),
      node.attribute("tune.pname").map(_.text),
      node.attribute("tune.temper").map(_.text),
      node.attribute("clef.shape").map(_.text),
      node.attribute("clef.line").map(_.text),
      node.attribute("clef.dis").map(_.text),
      node.attribute("clef.dis.place").map(_.text),
      node.attribute("dur.default").map(_.text),
      node.attribute("num.default").map(_.text),
      node.attribute("numbase.default").map(_.text),
      node.attribute("keysig").map(_.text),
      node.attribute("meter.count").map(_.text),
      node.attribute("meter.unit").map(_.text),
      node.attribute("meter.sym").map(_.text),
      node.attribute("notationtype").map(_.text),
      node.attribute("notationsubtype").map(_.text),
      node.attribute("oct.default").map(_.text),
      node.attribute("trans.diat").map(_.text),
      node.attribute("trans.semi").map(_.text),
      node.attribute("beam.group").map(_.text),
      node.attribute("beam.rests").map(_.text),
      node.attribute("modusmaior").map(_.text),
      node.attribute("modusminor").map(_.text),
      node.attribute("prolatio").map(_.text),
      node.attribute("tempus").map(_.text),
      node.attribute("divisio").map(_.text),
      node.attribute("proport.num").map(_.text),
      node.attribute("proport.numbase").map(_.text),
      node.attribute("lines").map(_.text),
      node.attribute("bar.len").map(_.text),
      node.attribute("bar.method").map(_.text),
      node.attribute("bar.place").map(_.text),
      node.attribute("clef.color").map(_.text),
      node.attribute("clef.visible").map(_.text),
      node.attribute("dir.dist").map(_.text),
      node.attribute("dynam.dist").map(_.text),
      node.attribute("harm.dist").map(_.text),
      node.attribute("reh.dist").map(_.text),
      node.attribute("tempo.dist").map(_.text),
      node.attribute("grid.show").map(_.text),
      node.attribute("keysig.cancelaccid").map(_.text),
      node.attribute("keysig.visible").map(_.text),
      node.attribute("lyric.align").map(_.text),
      node.attribute("lyric.fam").map(_.text),
      node.attribute("lyric.name").map(_.text),
      node.attribute("lyric.size").map(_.text),
      node.attribute("lyric.style").map(_.text),
      node.attribute("lyric.weight").map(_.text),
      node.attribute("meter.form").map(_.text),
      node.attribute("meter.showchange").map(_.text),
      node.attribute("meter.visible").map(_.text),
      node.attribute("multi.number").map(_.text),
      node.attribute("music.name").map(_.text),
      node.attribute("music.size").map(_.text),
      node.attribute("ontheline").map(_.text),
      node.attribute("scale").map(_.text),
      node.attribute("aboveorder").map(_.text),
      node.attribute("beloworder").map(_.text),
      node.attribute("betweenorder").map(_.text),
      node.attribute("text.fam").map(_.text),
      node.attribute("text.name").map(_.text),
      node.attribute("text.size").map(_.text),
      node.attribute("text.style").map(_.text),
      node.attribute("text.weight").map(_.text),
      node.attribute("visible").map(_.text),
      node.attribute("beam.color").map(_.text),
      node.attribute("beam.rend").map(_.text),
      node.attribute("beam.slope").map(_.text),
      node.attribute("pedal.style").map(_.text),
      node.attribute("reh.enclose").map(_.text),
      node.attribute("slur.lform").map(_.text),
      node.attribute("slur.lwidth").map(_.text),
      node.attribute("tie.lform").map(_.text),
      node.attribute("tie.lwidth").map(_.text),
      node.attribute("mensur.color").map(_.text),
      node.attribute("mensur.dot").map(_.text),
      node.attribute("mensur.form").map(_.text),
      node.attribute("mensur.loc").map(_.text),
      node.attribute("mensur.orient").map(_.text),
      node.attribute("mensur.sign").map(_.text),
      node.attribute("mensur.size").map(_.text),
      node.attribute("mensur.slash").map(_.text),
      node.attribute("tab.align").map(_.text),
      node.attribute("tab.anchorline").map(_.text),
      node.attribute("layerscheme").map(_.text),
      node.attribute("lines.color").map(_.text),
      node.attribute("lines.visible").map(_.text),
      node.attribute("spacing").map(_.text),
    )
  }
}
object StaffGrpParser extends MeiModelDummyParser {
  def parse(node: Node): StaffGrp = {
    StaffGrp(
      node.child.collect {
        case n: Elem if n.label == "grpSym"    => mei.allanystart.parser.GrpSymParser.parse(n)
        case n: Elem if n.label == "instrDef"  => mei.allanystart.parser.InstrDefParser.parse(n)
        case n: Elem if n.label == "label"     => mei.allanystart.parser.LabelParser.parse(n)
        case n: Elem if n.label == "labelAbbr" => mei.allanystart.parser.LabelAbbrParser.parse(n)
        case n: Elem if n.label == "staffDef"  => mei.allanystart.parser.StaffDefParser.parse(n)
        case n: Elem if n.label == "staffGrp"  => mei.allanystart.parser.StaffGrpParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("decls").map(_.text),
      node.attribute("instr").map(_.text),
      node.attribute("bar.len").map(_.text),
      node.attribute("bar.method").map(_.text),
      node.attribute("bar.place").map(_.text),
      node.attribute("symbol").map(_.text),
      node.attribute("visible").map(_.text),
      node.attribute("bar.thru").map(_.text),
    )
  }
}
object SylParser extends MeiModelDummyParser {
  def parse(node: Node): Syl = {
    Syl(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "app"          => mei.allanystart.parser.AppParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("con").map(_.text),
      node.attribute("wordpos").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("halign").map(_.text),
    )
  }
}
object TempoParser extends MeiModelDummyParser {
  def parse(node: Node): Tempo = {
    Tempo(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("midi.bpm").map(_.text),
      node.attribute("midi.mspb").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("plist").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("evaluate").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("mm").map(_.text),
      node.attribute("mm.unit").map(_.text),
      node.attribute("mm.dots").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("tstamp2").map(_.text),
      node.attribute("func").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("lform").map(_.text),
      node.attribute("lwidth").map(_.text),
      node.attribute("lsegs").map(_.text),
      node.attribute("lendsym").map(_.text),
      node.attribute("lendsym.size").map(_.text),
      node.attribute("lstartsym").map(_.text),
      node.attribute("lstartsym.size").map(_.text),
      node.attribute("extender").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("vgrp").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("startho").map(_.text),
      node.attribute("endho").map(_.text),
      node.attribute("startto").map(_.text),
      node.attribute("endto").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object TermParser extends MeiModelDummyParser {
  def parse(node: Node): Term = {
    Term(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("data").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object TextLangParser extends MeiModelDummyParser {
  def parse(node: Node): TextLang = {
    TextLang(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("lang.main").map(_.text),
      node.attribute("lang.other").map(_.text),
    )
  }
}
object TitlePageParser extends MeiModelDummyParser {
  def parse(node: Node): TitlePage = {
    TitlePage(
      node.child.collect {
        case n: Elem if n.label == "accessRestrict" => mei.allanystart.parser.AccessRestrictParser.parse(n)
        case n: Elem if n.label == "add"            => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "argument"       => mei.allanystart.parser.ArgumentParser.parse(n)
        case n: Elem if n.label == "arranger"       => mei.allanystart.parser.ArrangerParser.parse(n)
        case n: Elem if n.label == "author"         => mei.allanystart.parser.AuthorParser.parse(n)
        case n: Elem if n.label == "availability"   => mei.allanystart.parser.AvailabilityParser.parse(n)
        case n: Elem if n.label == "biblList"       => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "byline"         => mei.allanystart.parser.BylineParser.parse(n)
        case n: Elem if n.label == "castList"       => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "cb"             => mei.allanystart.parser.CbParser.parse(n)
        case n: Elem if n.label == "colLayout"      => mei.allanystart.parser.ColLayoutParser.parse(n)
        case n: Elem if n.label == "composer"       => mei.allanystart.parser.ComposerParser.parse(n)
        case n: Elem if n.label == "contents"       => mei.allanystart.parser.ContentsParser.parse(n)
        case n: Elem if n.label == "contributor"    => mei.allanystart.parser.ContributorParser.parse(n)
        case n: Elem if n.label == "corr"           => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "damage"         => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"           => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedication"     => mei.allanystart.parser.DedicationParser.parse(n)
        case n: Elem if n.label == "del"            => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "distributor"    => mei.allanystart.parser.DistributorParser.parse(n)
        case n: Elem if n.label == "edition"        => mei.allanystart.parser.EditionParser.parse(n)
        case n: Elem if n.label == "editor"         => mei.allanystart.parser.EditorParser.parse(n)
        case n: Elem if n.label == "epigraph"       => mei.allanystart.parser.EpigraphParser.parse(n)
        case n: Elem if n.label == "eventList"      => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "fig"            => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "funder"         => mei.allanystart.parser.FunderParser.parse(n)
        case n: Elem if n.label == "gap"            => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "handShift"      => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "head"           => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "identifier"     => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "imprimatur"     => mei.allanystart.parser.ImprimaturParser.parse(n)
        case n: Elem if n.label == "imprint"        => mei.allanystart.parser.ImprintParser.parse(n)
        case n: Elem if n.label == "lb"             => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "lg"             => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "librettist"     => mei.allanystart.parser.LibrettistParser.parse(n)
        case n: Elem if n.label == "list"           => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "lyricist"       => mei.allanystart.parser.LyricistParser.parse(n)
        case n: Elem if n.label == "orig"           => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "p"              => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "pb"             => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "perfDuration"   => mei.allanystart.parser.PerfDurationParser.parse(n)
        case n: Elem if n.label == "perfMedium"     => mei.allanystart.parser.PerfMediumParser.parse(n)
        case n: Elem if n.label == "plateNum"       => mei.allanystart.parser.PlateNumParser.parse(n)
        case n: Elem if n.label == "price"          => mei.allanystart.parser.PriceParser.parse(n)
        case n: Elem if n.label == "pubPlace"       => mei.allanystart.parser.PubPlaceParser.parse(n)
        case n: Elem if n.label == "publisher"      => mei.allanystart.parser.PublisherParser.parse(n)
        case n: Elem if n.label == "quote"          => mei.allanystart.parser.QuoteParser.parse(n)
        case n: Elem if n.label == "reg"            => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "restore"        => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "series"         => mei.allanystart.parser.SeriesParser.parse(n)
        case n: Elem if n.label == "sic"            => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "sponsor"        => mei.allanystart.parser.SponsorParser.parse(n)
        case n: Elem if n.label == "supplied"       => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "sysReq"         => mei.allanystart.parser.SysReqParser.parse(n)
        case n: Elem if n.label == "table"          => mei.allanystart.parser.TableParser.parse(n)
        case n: Elem if n.label == "title"          => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "titlePart"      => mei.allanystart.parser.TitlePartParser.parse(n)
        case n: Elem if n.label == "unclear"        => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "unpub"          => mei.allanystart.parser.UnpubParser.parse(n)
        case n: Elem if n.label == "useRestrict"    => mei.allanystart.parser.UseRestrictParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object TitlePartParser extends MeiModelDummyParser {
  def parse(node: Node): TitlePart = {
    TitlePart(
      node.child.collect {
        case n: Elem if n.label == "abbr"       => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"        => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"    => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"      => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"       => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct" => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"       => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "choice"     => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"   => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"       => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"    => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"     => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"       => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "del"        => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "district"   => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"      => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "fig"        => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"        => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"   => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"   => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"  => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "identifier" => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"         => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "name"       => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"        => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"       => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"         => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName" => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"   => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"    => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"   => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"        => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "ref"        => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"        => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"     => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "rend"       => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository" => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"    => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "settlement" => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"        => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "stack"      => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "street"     => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"  => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"      => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"   => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"     => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "title"      => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"    => mei.allanystart.parser.UnclearParser.parse(n)
        case n if n.text.trim.nonEmpty          => n.text
      }.toList,
      node.attribute("codedval").map(_.text),
      node.attribute("auth").map(_.text),
      node.attribute("auth.uri").map(_.text),
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("nonfiling").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("type").map(_.text),
    )
  }
}
object TuningParser extends MeiModelDummyParser {
  def parse(node: Node): Tuning = {
    Tuning(
      node.child.collect {
        case n: Elem if n.label == "course" => mei.allanystart.parser.CourseParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("tuning.standard").map(_.text),
    )
  }
}
object WidthParser extends MeiModelDummyParser {
  def parse(node: Node): Width = {
    Width(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("analog").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("unit").map(_.text),
      node.attribute("atleast").map(_.text),
      node.attribute("atmost").map(_.text),
      node.attribute("min").map(_.text),
      node.attribute("max").map(_.text),
      node.attribute("confidence").map(_.text),
      node.attribute("quantity").map(_.text),
    )
  }
}
object BarreParser extends MeiModelDummyParser {
  def parse(node: Node): Barre = {
    Barre(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("fret").map(_.text),
    )
  }
}
object CourseParser extends MeiModelDummyParser {
  def parse(node: Node): Course = {
    Course(
      node.child.collect {
        case n: Elem if n.label == "string" => mei.allanystart.parser.StringParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("accid").map(_.text),
      node.attribute("pname").map(_.text),
      node.attribute("oct").map(_.text),
    )
  }
}
object StringParser extends MeiModelDummyParser {
  def parse(node: Node): String = {
    String(
      node.child.collect {
        case n: Elem if n.label == "string" => mei.allanystart.parser.StringParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("accid").map(_.text),
      node.attribute("pname").map(_.text),
      node.attribute("oct").map(_.text),
    )
  }
}
object TabDurSymParser extends MeiModelDummyParser {
  def parse(node: Node): TabDurSym = {
    TabDurSym(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("tab.fing").map(_.text),
      node.attribute("tab.fret").map(_.text),
      node.attribute("tab.line").map(_.text),
      node.attribute("tab.string").map(_.text),
      node.attribute("tab.course").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("part").map(_.text),
      node.attribute("partstaff").map(_.text),
      node.attribute("altsym").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("glyph.auth").map(_.text),
      node.attribute("glyph.uri").map(_.text),
      node.attribute("glyph.name").map(_.text),
      node.attribute("glyph.num").map(_.text),
      node.attribute("place").map(_.text),
      node.attribute("fontfam").map(_.text),
      node.attribute("fontname").map(_.text),
      node.attribute("fontsize").map(_.text),
      node.attribute("fontstyle").map(_.text),
      node.attribute("fontweight").map(_.text),
      node.attribute("letterspacing").map(_.text),
      node.attribute("lineheight").map(_.text),
      node.attribute("visible").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object TabGrpParser extends MeiModelDummyParser {
  def parse(node: Node): TabGrp = {
    TabGrp(
      node.child.collect {
        case n: Elem if n.label == "add"       => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "app"       => mei.allanystart.parser.AppParser.parse(n)
        case n: Elem if n.label == "choice"    => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corr"      => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "damage"    => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "del"       => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "gap"       => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "handShift" => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "note"      => mei.allanystart.parser.NoteParser.parse(n)
        case n: Elem if n.label == "orig"      => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "reg"       => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "rest"      => mei.allanystart.parser.RestParser.parse(n)
        case n: Elem if n.label == "restore"   => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "sic"       => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "subst"     => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"  => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "tabDurSym" => mei.allanystart.parser.TabDurSymParser.parse(n)
        case n: Elem if n.label == "unclear"   => mei.allanystart.parser.UnclearParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("dots").map(_.text),
      node.attribute("dur").map(_.text),
      node.attribute("when").map(_.text),
      node.attribute("layer").map(_.text),
      node.attribute("staff").map(_.text),
      node.attribute("tstamp.ges").map(_.text),
      node.attribute("tstamp.real").map(_.text),
      node.attribute("tstamp").map(_.text),
      node.attribute("visible").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("dur.ges").map(_.text),
      node.attribute("dots.ges").map(_.text),
      node.attribute("dur.metrical").map(_.text),
      node.attribute("dur.ppq").map(_.text),
      node.attribute("dur.real").map(_.text),
      node.attribute("dur.recip").map(_.text),
    )
  }
}
object ArgumentParser extends MeiModelDummyParser {
  def parse(node: Node): Argument = {
    Argument(
      node.child.collect {
        case n: Elem if n.label == "biblList"  => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "castList"  => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "cb"        => mei.allanystart.parser.CbParser.parse(n)
        case n: Elem if n.label == "colLayout" => mei.allanystart.parser.ColLayoutParser.parse(n)
        case n: Elem if n.label == "eventList" => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "fig"       => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "head"      => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "lb"        => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "lg"        => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "list"      => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "p"         => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "pb"        => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "quote"     => mei.allanystart.parser.QuoteParser.parse(n)
        case n: Elem if n.label == "sp"        => mei.allanystart.parser.SpParser.parse(n)
        case n: Elem if n.label == "table"     => mei.allanystart.parser.TableParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("decls").map(_.text),
    )
  }
}
object BackParser extends MeiModelDummyParser {
  def parse(node: Node): Back = {
    Back(
      node.child.collect {
        case n: Elem if n.label == "cb"           => mei.allanystart.parser.CbParser.parse(n)
        case n: Elem if n.label == "colLayout"    => mei.allanystart.parser.ColLayoutParser.parse(n)
        case n: Elem if n.label == "div"          => mei.allanystart.parser.DivParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "titlePage"    => mei.allanystart.parser.TitlePageParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("decls").map(_.text),
    )
  }
}
object EpigraphParser extends MeiModelDummyParser {
  def parse(node: Node): Epigraph = {
    Epigraph(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblList"     => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "castList"     => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "eventList"    => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "lg"           => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "list"         => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "quote"        => mei.allanystart.parser.QuoteParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stageDir"     => mei.allanystart.parser.StageDirParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "table"        => mei.allanystart.parser.TableParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("decls").map(_.text),
    )
  }
}
object FrontParser extends MeiModelDummyParser {
  def parse(node: Node): Front = {
    Front(
      node.child.collect {
        case n: Elem if n.label == "cb"           => mei.allanystart.parser.CbParser.parse(n)
        case n: Elem if n.label == "colLayout"    => mei.allanystart.parser.ColLayoutParser.parse(n)
        case n: Elem if n.label == "div"          => mei.allanystart.parser.DivParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "titlePage"    => mei.allanystart.parser.TitlePageParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("decls").map(_.text),
    )
  }
}
object ImprimaturParser extends MeiModelDummyParser {
  def parse(node: Node): Imprimatur = {
    Imprimatur(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblList"     => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "castList"     => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "eventList"    => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "lg"           => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "list"         => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "quote"        => mei.allanystart.parser.QuoteParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stageDir"     => mei.allanystart.parser.StageDirParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "table"        => mei.allanystart.parser.TableParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("decls").map(_.text),
    )
  }
}
object LParser extends MeiModelDummyParser {
  def parse(node: Node): L = {
    L(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "syl"          => mei.allanystart.parser.SylParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("rhythm").map(_.text),
    )
  }
}
object LiParser extends MeiModelDummyParser {
  def parse(node: Node): Li = {
    Li(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblList"     => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "castList"     => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "eventList"    => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "lg"           => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "list"         => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "quote"        => mei.allanystart.parser.QuoteParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "table"        => mei.allanystart.parser.TableParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object ListParser extends MeiModelDummyParser {
  def parse(node: Node): List = {
    List(
      node.child.collect {
        case n: Elem if n.label == "head"  => mei.allanystart.parser.HeadParser.parse(n)
        case n: Elem if n.label == "label" => mei.allanystart.parser.LabelParser.parse(n)
        case n: Elem if n.label == "li"    => mei.allanystart.parser.LiParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("form").map(_.text),
      node.attribute("type").map(_.text),
    )
  }
}
object QParser extends MeiModelDummyParser {
  def parse(node: Node): Q = {
    Q(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("altrend").map(_.text),
      node.attribute("rend").map(_.text),
      node.attribute("type").map(_.text),
    )
  }
}
object QuoteParser extends MeiModelDummyParser {
  def parse(node: Node): Quote = {
    Quote(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblList"     => mei.allanystart.parser.BiblListParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "castList"     => mei.allanystart.parser.CastListParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "eventList"    => mei.allanystart.parser.EventListParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "lg"           => mei.allanystart.parser.LgParser.parse(n)
        case n: Elem if n.label == "list"         => mei.allanystart.parser.ListParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "p"            => mei.allanystart.parser.PParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "quote"        => mei.allanystart.parser.QuoteParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "sp"           => mei.allanystart.parser.SpParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stageDir"     => mei.allanystart.parser.StageDirParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "table"        => mei.allanystart.parser.TableParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("altrend").map(_.text),
      node.attribute("rend").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object SegParser extends MeiModelDummyParser {
  def parse(node: Node): Seg = {
    Seg(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "pb"           => mei.allanystart.parser.PbParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "syl"          => mei.allanystart.parser.SylParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
    )
  }
}
object AnchoredTextParser extends MeiModelDummyParser {
  def parse(node: Node): AnchoredText = {
    AnchoredText(
      node.child.collect {
        case n: Elem if n.label == "abbr"         => mei.allanystart.parser.AbbrParser.parse(n)
        case n: Elem if n.label == "add"          => mei.allanystart.parser.AddParser.parse(n)
        case n: Elem if n.label == "address"      => mei.allanystart.parser.AddressParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "bibl"         => mei.allanystart.parser.BiblParser.parse(n)
        case n: Elem if n.label == "biblStruct"   => mei.allanystart.parser.BiblStructParser.parse(n)
        case n: Elem if n.label == "bloc"         => mei.allanystart.parser.BlocParser.parse(n)
        case n: Elem if n.label == "catchwords"   => mei.allanystart.parser.CatchwordsParser.parse(n)
        case n: Elem if n.label == "choice"       => mei.allanystart.parser.ChoiceParser.parse(n)
        case n: Elem if n.label == "corpName"     => mei.allanystart.parser.CorpNameParser.parse(n)
        case n: Elem if n.label == "corr"         => mei.allanystart.parser.CorrParser.parse(n)
        case n: Elem if n.label == "country"      => mei.allanystart.parser.CountryParser.parse(n)
        case n: Elem if n.label == "damage"       => mei.allanystart.parser.DamageParser.parse(n)
        case n: Elem if n.label == "date"         => mei.allanystart.parser.DateParser.parse(n)
        case n: Elem if n.label == "dedicatee"    => mei.allanystart.parser.DedicateeParser.parse(n)
        case n: Elem if n.label == "del"          => mei.allanystart.parser.DelParser.parse(n)
        case n: Elem if n.label == "depth"        => mei.allanystart.parser.DepthParser.parse(n)
        case n: Elem if n.label == "dim"          => mei.allanystart.parser.DimParser.parse(n)
        case n: Elem if n.label == "dimensions"   => mei.allanystart.parser.DimensionsParser.parse(n)
        case n: Elem if n.label == "district"     => mei.allanystart.parser.DistrictParser.parse(n)
        case n: Elem if n.label == "expan"        => mei.allanystart.parser.ExpanParser.parse(n)
        case n: Elem if n.label == "extent"       => mei.allanystart.parser.ExtentParser.parse(n)
        case n: Elem if n.label == "fig"          => mei.allanystart.parser.FigParser.parse(n)
        case n: Elem if n.label == "gap"          => mei.allanystart.parser.GapParser.parse(n)
        case n: Elem if n.label == "geogFeat"     => mei.allanystart.parser.GeogFeatParser.parse(n)
        case n: Elem if n.label == "geogName"     => mei.allanystart.parser.GeogNameParser.parse(n)
        case n: Elem if n.label == "handShift"    => mei.allanystart.parser.HandShiftParser.parse(n)
        case n: Elem if n.label == "height"       => mei.allanystart.parser.HeightParser.parse(n)
        case n: Elem if n.label == "heraldry"     => mei.allanystart.parser.HeraldryParser.parse(n)
        case n: Elem if n.label == "identifier"   => mei.allanystart.parser.IdentifierParser.parse(n)
        case n: Elem if n.label == "lb"           => mei.allanystart.parser.LbParser.parse(n)
        case n: Elem if n.label == "locus"        => mei.allanystart.parser.LocusParser.parse(n)
        case n: Elem if n.label == "locusGrp"     => mei.allanystart.parser.LocusGrpParser.parse(n)
        case n: Elem if n.label == "name"         => mei.allanystart.parser.NameParser.parse(n)
        case n: Elem if n.label == "num"          => mei.allanystart.parser.NumParser.parse(n)
        case n: Elem if n.label == "orig"         => mei.allanystart.parser.OrigParser.parse(n)
        case n: Elem if n.label == "periodName"   => mei.allanystart.parser.PeriodNameParser.parse(n)
        case n: Elem if n.label == "persName"     => mei.allanystart.parser.PersNameParser.parse(n)
        case n: Elem if n.label == "postBox"      => mei.allanystart.parser.PostBoxParser.parse(n)
        case n: Elem if n.label == "postCode"     => mei.allanystart.parser.PostCodeParser.parse(n)
        case n: Elem if n.label == "ptr"          => mei.allanystart.parser.PtrParser.parse(n)
        case n: Elem if n.label == "q"            => mei.allanystart.parser.QParser.parse(n)
        case n: Elem if n.label == "ref"          => mei.allanystart.parser.RefParser.parse(n)
        case n: Elem if n.label == "reg"          => mei.allanystart.parser.RegParser.parse(n)
        case n: Elem if n.label == "region"       => mei.allanystart.parser.RegionParser.parse(n)
        case n: Elem if n.label == "relation"     => mei.allanystart.parser.RelationParser.parse(n)
        case n: Elem if n.label == "relationList" => mei.allanystart.parser.RelationListParser.parse(n)
        case n: Elem if n.label == "rend"         => mei.allanystart.parser.RendParser.parse(n)
        case n: Elem if n.label == "repository"   => mei.allanystart.parser.RepositoryParser.parse(n)
        case n: Elem if n.label == "restore"      => mei.allanystart.parser.RestoreParser.parse(n)
        case n: Elem if n.label == "secFolio"     => mei.allanystart.parser.SecFolioParser.parse(n)
        case n: Elem if n.label == "seg"          => mei.allanystart.parser.SegParser.parse(n)
        case n: Elem if n.label == "settlement"   => mei.allanystart.parser.SettlementParser.parse(n)
        case n: Elem if n.label == "sic"          => mei.allanystart.parser.SicParser.parse(n)
        case n: Elem if n.label == "signatures"   => mei.allanystart.parser.SignaturesParser.parse(n)
        case n: Elem if n.label == "stack"        => mei.allanystart.parser.StackParser.parse(n)
        case n: Elem if n.label == "stamp"        => mei.allanystart.parser.StampParser.parse(n)
        case n: Elem if n.label == "street"       => mei.allanystart.parser.StreetParser.parse(n)
        case n: Elem if n.label == "styleName"    => mei.allanystart.parser.StyleNameParser.parse(n)
        case n: Elem if n.label == "subst"        => mei.allanystart.parser.SubstParser.parse(n)
        case n: Elem if n.label == "supplied"     => mei.allanystart.parser.SuppliedParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
        case n: Elem if n.label == "term"         => mei.allanystart.parser.TermParser.parse(n)
        case n: Elem if n.label == "title"        => mei.allanystart.parser.TitleParser.parse(n)
        case n: Elem if n.label == "unclear"      => mei.allanystart.parser.UnclearParser.parse(n)
        case n: Elem if n.label == "watermark"    => mei.allanystart.parser.WatermarkParser.parse(n)
        case n: Elem if n.label == "width"        => mei.allanystart.parser.WidthParser.parse(n)
        case n if n.text.trim.nonEmpty            => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("facs").map(_.text),
      node.attribute("xml:lang").map(_.text),
      node.attribute("translit").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("func").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
    )
  }
}
object CurveParser extends MeiModelDummyParser {
  def parse(node: Node): Curve = {
    Curve(
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("startid").map(_.text),
      node.attribute("endid").map(_.text),
      node.attribute("func").map(_.text),
      node.attribute("color").map(_.text),
      node.attribute("bezier").map(_.text),
      node.attribute("bulge").map(_.text),
      node.attribute("curvedir").map(_.text),
      node.attribute("lform").map(_.text),
      node.attribute("lwidth").map(_.text),
      node.attribute("lsegs").map(_.text),
      node.attribute("ho").map(_.text),
      node.attribute("to").map(_.text),
      node.attribute("vo").map(_.text),
      node.attribute("startho").map(_.text),
      node.attribute("endho").map(_.text),
      node.attribute("startto").map(_.text),
      node.attribute("endto").map(_.text),
      node.attribute("startvo").map(_.text),
      node.attribute("endvo").map(_.text),
      node.attribute("x").map(_.text),
      node.attribute("y").map(_.text),
      node.attribute("x2").map(_.text),
      node.attribute("y2").map(_.text),
      node.attribute("facs").map(_.text),
    )
  }
}
object MappingParser extends MeiModelDummyParser {
  def parse(node: Node): Mapping = {
    Mapping(
      node.child.collect {
        case n: Elem if n.label == "symbol" => mei.allanystart.parser.SymbolParser.parse(n)
        case n if n.text.trim.nonEmpty      => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
    )
  }
}
object PropNameParser extends MeiModelDummyParser {
  def parse(node: Node): PropName = {
    PropName(
      node.child.collect {
        case n if n.text.trim.nonEmpty => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("type").map(_.text).get,
    )
  }
}
object PropValueParser extends MeiModelDummyParser {
  def parse(node: Node): PropValue = {
    PropValue(
      node.child.collect {
        case n if n.text.trim.nonEmpty => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
    )
  }
}
object SymbolDefParser extends MeiModelDummyParser {
  def parse(node: Node): SymbolDef = {
    SymbolDef(
      node.child.collect {
        case n: Elem if n.label == "anchoredText" => mei.allanystart.parser.AnchoredTextParser.parse(n)
        case n: Elem if n.label == "annot"        => mei.allanystart.parser.AnnotParser.parse(n)
        case n: Elem if n.label == "curve"        => mei.allanystart.parser.CurveParser.parse(n)
        case n: Elem if n.label == "graphic"      => mei.allanystart.parser.GraphicParser.parse(n)
        case n: Elem if n.label == "line"         => mei.allanystart.parser.LineParser.parse(n)
        case n: Elem if n.label == "mapping"      => mei.allanystart.parser.MappingParser.parse(n)
        case n: Elem if n.label == "svg"          => mei.allanystart.parser.SvgParser.parse(n)
        case n: Elem if n.label == "symName"      => mei.allanystart.parser.SymNameParser.parse(n)
        case n: Elem if n.label == "symProp"      => mei.allanystart.parser.SymPropParser.parse(n)
        case n: Elem if n.label == "symbol"       => mei.allanystart.parser.SymbolParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
      node.attribute("ulx").map(_.text),
      node.attribute("uly").map(_.text),
      node.attribute("lrx").map(_.text),
      node.attribute("lry").map(_.text),
      node.attribute("rotate").map(_.text),
      node.attribute("select").map(_.text),
    )
  }
}
object SymNameParser extends MeiModelDummyParser {
  def parse(node: Node): SymName = {
    SymName(
      node.child.collect {
        case n if n.text.trim.nonEmpty => n.text
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
    )
  }
}
object SymPropParser extends MeiModelDummyParser {
  def parse(node: Node): SymProp = {
    SymProp(
      node.child.collect {
        case n: Elem if n.label == "propName"  => mei.allanystart.parser.PropNameParser.parse(n)
        case n: Elem if n.label == "propValue" => mei.allanystart.parser.PropValueParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
    )
  }
}
object SymbolTableParser extends MeiModelDummyParser {
  def parse(node: Node): SymbolTable = {
    SymbolTable(
      node.child.collect {
        case n: Elem if n.label == "symbolDef" => mei.allanystart.parser.SymbolDefParser.parse(n)
      }.toList,
      node.attribute("xml:id").map(_.text),
      node.attribute("meiversion").map(_.text),
      node.attribute("xml:base").map(_.text),
      node.attribute("label").map(_.text),
      node.attribute("copyof").map(_.text),
      node.attribute("corresp").map(_.text),
      node.attribute("follows").map(_.text),
      node.attribute("next").map(_.text),
      node.attribute("precedes").map(_.text),
      node.attribute("prev").map(_.text),
      node.attribute("sameas").map(_.text),
      node.attribute("synch").map(_.text),
      node.attribute("n").map(_.text),
      node.attribute("resp").map(_.text),
      node.attribute("class").map(_.text),
      node.attribute("type").map(_.text),
    )
  }
}
