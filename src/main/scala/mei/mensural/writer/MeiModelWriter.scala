package mei.mensural.writer

// This file is auto-generated from MEI RelaxNG schema.
// Do not edit this file manually.

import scala.xml.{Node, Elem, Null, TopScope, UnprefixedAttribute, MetaData}
import mei.mensural.*

/** Although this abstraction is not strictly necessary, it is provided to prevent the linter from warning about an unused variable in def toXml for a class that has no fields. */
trait MeiModelDummyWriter[T] {
  def toXml(obj: T): scala.xml.Node
}

object MeiXmlWriter {
  def toXml(obj: Any): Node = obj match {
    case x: A                   => AWriter.toXml(x)
    case x: Abbr                => AbbrWriter.toXml(x)
    case x: AccMat              => AccMatWriter.toXml(x)
    case x: AccessRestrict      => AccessRestrictWriter.toXml(x)
    case x: Accid               => AccidWriter.toXml(x)
    case x: Acquisition         => AcquisitionWriter.toXml(x)
    case x: Actor               => ActorWriter.toXml(x)
    case x: Add                 => AddWriter.toXml(x)
    case x: AddDesc             => AddDescWriter.toXml(x)
    case x: AddName             => AddNameWriter.toXml(x)
    case x: AddrLine            => AddrLineWriter.toXml(x)
    case x: Address             => AddressWriter.toXml(x)
    case x: AltGlyph            => AltGlyphWriter.toXml(x)
    case x: AltGlyphDef         => AltGlyphDefWriter.toXml(x)
    case x: AltGlyphItem        => AltGlyphItemWriter.toXml(x)
    case x: AltId               => AltIdWriter.toXml(x)
    case x: AmbNote             => AmbNoteWriter.toXml(x)
    case x: Ambitus             => AmbitusWriter.toXml(x)
    case x: Analytic            => AnalyticWriter.toXml(x)
    case x: AnchoredText        => AnchoredTextWriter.toXml(x)
    case x: Animate             => AnimateWriter.toXml(x)
    case x: AnimateColor        => AnimateColorWriter.toXml(x)
    case x: AnimateMotion       => AnimateMotionWriter.toXml(x)
    case x: AnimateTransform    => AnimateTransformWriter.toXml(x)
    case x: Annot               => AnnotWriter.toXml(x)
    case x: App                 => AppWriter.toXml(x)
    case x: AppInfo             => AppInfoWriter.toXml(x)
    case x: Application         => ApplicationWriter.toXml(x)
    case x: Argument            => ArgumentWriter.toXml(x)
    case x: Arranger            => ArrangerWriter.toXml(x)
    case x: Artic               => ArticWriter.toXml(x)
    case x: AttUsage            => AttUsageWriter.toXml(x)
    case x: Audience            => AudienceWriter.toXml(x)
    case x: Author              => AuthorWriter.toXml(x)
    case x: AvFile              => AvFileWriter.toXml(x)
    case x: Availability        => AvailabilityWriter.toXml(x)
    case x: Back                => BackWriter.toXml(x)
    case x: BarLine             => BarLineWriter.toXml(x)
    case x: Barre               => BarreWriter.toXml(x)
    case x: Bibl                => BiblWriter.toXml(x)
    case x: BiblList            => BiblListWriter.toXml(x)
    case x: BiblScope           => BiblScopeWriter.toXml(x)
    case x: BiblStruct          => BiblStructWriter.toXml(x)
    case x: Bifolium            => BifoliumWriter.toXml(x)
    case x: Binding             => BindingWriter.toXml(x)
    case x: BindingDesc         => BindingDescWriter.toXml(x)
    case x: Bloc                => BlocWriter.toXml(x)
    case x: Body                => BodyWriter.toXml(x)
    case x: Byline              => BylineWriter.toXml(x)
    case x: Caesura             => CaesuraWriter.toXml(x)
    case x: Caption             => CaptionWriter.toXml(x)
    case x: CaptureMode         => CaptureModeWriter.toXml(x)
    case x: CarrierForm         => CarrierFormWriter.toXml(x)
    case x: CastGrp             => CastGrpWriter.toXml(x)
    case x: CastItem            => CastItemWriter.toXml(x)
    case x: CastList            => CastListWriter.toXml(x)
    case x: CatRel              => CatRelWriter.toXml(x)
    case x: Catchwords          => CatchwordsWriter.toXml(x)
    case x: Category            => CategoryWriter.toXml(x)
    case x: Cb                  => CbWriter.toXml(x)
    case x: Cc                  => CcWriter.toXml(x)
    case x: Chan                => ChanWriter.toXml(x)
    case x: ChanPr              => ChanPrWriter.toXml(x)
    case x: Change              => ChangeWriter.toXml(x)
    case x: ChangeDesc          => ChangeDescWriter.toXml(x)
    case x: Choice              => ChoiceWriter.toXml(x)
    case x: Chord               => ChordWriter.toXml(x)
    case x: ChordDef            => ChordDefWriter.toXml(x)
    case x: ChordMember         => ChordMemberWriter.toXml(x)
    case x: ChordTable          => ChordTableWriter.toXml(x)
    case x: Circle              => CircleWriter.toXml(x)
    case x: ClassDecls          => ClassDeclsWriter.toXml(x)
    case x: Classification      => ClassificationWriter.toXml(x)
    case x: Clef                => ClefWriter.toXml(x)
    case x: ClefGrp             => ClefGrpWriter.toXml(x)
    case x: Clip                => ClipWriter.toXml(x)
    case x: ClipPath            => ClipPathWriter.toXml(x)
    case x: ColLayout           => ColLayoutWriter.toXml(x)
    case x: Collation           => CollationWriter.toXml(x)
    case x: Colophon            => ColophonWriter.toXml(x)
    case x: ColorProfile        => ColorProfileWriter.toXml(x)
    case x: ComponentList       => ComponentListWriter.toXml(x)
    case x: Composer            => ComposerWriter.toXml(x)
    case x: Condition           => ConditionWriter.toXml(x)
    case x: ContentItem         => ContentItemWriter.toXml(x)
    case x: Contents            => ContentsWriter.toXml(x)
    case x: Context             => ContextWriter.toXml(x)
    case x: Contributor         => ContributorWriter.toXml(x)
    case x: CorpName            => CorpNameWriter.toXml(x)
    case x: Corr                => CorrWriter.toXml(x)
    case x: Correction          => CorrectionWriter.toXml(x)
    case x: Country             => CountryWriter.toXml(x)
    case x: Course              => CourseWriter.toXml(x)
    case x: CpMark              => CpMarkWriter.toXml(x)
    case x: Creation            => CreationWriter.toXml(x)
    case x: Cue                 => CueWriter.toXml(x)
    case x: Cursor              => CursorWriter.toXml(x)
    case x: Curve               => CurveWriter.toXml(x)
    case x: Custos              => CustosWriter.toXml(x)
    case x: Cutout              => CutoutWriter.toXml(x)
    case x: Damage              => DamageWriter.toXml(x)
    case x: Date                => DateWriter.toXml(x)
    case x: DecoDesc            => DecoDescWriter.toXml(x)
    case x: DecoNote            => DecoNoteWriter.toXml(x)
    case x: Dedicatee           => DedicateeWriter.toXml(x)
    case x: Dedication          => DedicationWriter.toXml(x)
    case x: DefinitionSrc       => DefinitionSrcWriter.toXml(x)
    case x: Defs                => DefsWriter.toXml(x)
    case x: Del                 => DelWriter.toXml(x)
    case x: Depth               => DepthWriter.toXml(x)
    case x: Desc                => DescWriter.toXml(x)
    case x: Dim                 => DimWriter.toXml(x)
    case x: Dimensions          => DimensionsWriter.toXml(x)
    case x: Dir                 => DirWriter.toXml(x)
    case x: Distributor         => DistributorWriter.toXml(x)
    case x: District            => DistrictWriter.toXml(x)
    case x: Div                 => DivWriter.toXml(x)
    case x: DomainsDecl         => DomainsDeclWriter.toXml(x)
    case x: Dot                 => DotWriter.toXml(x)
    case x: Dynam               => DynamWriter.toXml(x)
    case x: Edition             => EditionWriter.toXml(x)
    case x: EditionStmt         => EditionStmtWriter.toXml(x)
    case x: Editor              => EditorWriter.toXml(x)
    case x: EditorialDecl       => EditorialDeclWriter.toXml(x)
    case x: Ellipse             => EllipseWriter.toXml(x)
    case x: EncodingDesc        => EncodingDescWriter.toXml(x)
    case x: Ending              => EndingWriter.toXml(x)
    case x: Epigraph            => EpigraphWriter.toXml(x)
    case x: Event               => EventWriter.toXml(x)
    case x: EventList           => EventListWriter.toXml(x)
    case x: ExhibHist           => ExhibHistWriter.toXml(x)
    case x: Expan               => ExpanWriter.toXml(x)
    case x: Expansion           => ExpansionWriter.toXml(x)
    case x: Explicit            => ExplicitWriter.toXml(x)
    case x: Expression          => ExpressionWriter.toXml(x)
    case x: ExpressionList      => ExpressionListWriter.toXml(x)
    case x: ExtData             => ExtDataWriter.toXml(x)
    case x: ExtMeta             => ExtMetaWriter.toXml(x)
    case x: Extent              => ExtentWriter.toXml(x)
    case x: F                   => FWriter.toXml(x)
    case x: Facsimile           => FacsimileWriter.toXml(x)
    case x: FamName             => FamNameWriter.toXml(x)
    case x: Fb                  => FbWriter.toXml(x)
    case x: FeBlend             => FeBlendWriter.toXml(x)
    case x: FeColorMatrix       => FeColorMatrixWriter.toXml(x)
    case x: FeComponentTransfer => FeComponentTransferWriter.toXml(x)
    case x: FeComposite         => FeCompositeWriter.toXml(x)
    case x: FeConvolveMatrix    => FeConvolveMatrixWriter.toXml(x)
    case x: FeDiffuseLighting   => FeDiffuseLightingWriter.toXml(x)
    case x: FeDisplacementMap   => FeDisplacementMapWriter.toXml(x)
    case x: FeDistantLight      => FeDistantLightWriter.toXml(x)
    case x: FeFlood             => FeFloodWriter.toXml(x)
    case x: FeFuncA             => FeFuncAWriter.toXml(x)
    case x: FeFuncB             => FeFuncBWriter.toXml(x)
    case x: FeFuncG             => FeFuncGWriter.toXml(x)
    case x: FeFuncR             => FeFuncRWriter.toXml(x)
    case x: FeGaussianBlur      => FeGaussianBlurWriter.toXml(x)
    case x: FeImage             => FeImageWriter.toXml(x)
    case x: FeMerge             => FeMergeWriter.toXml(x)
    case x: FeMergeNode         => FeMergeNodeWriter.toXml(x)
    case x: FeMorphology        => FeMorphologyWriter.toXml(x)
    case x: FeOffset            => FeOffsetWriter.toXml(x)
    case x: FePointLight        => FePointLightWriter.toXml(x)
    case x: FeSpecularLighting  => FeSpecularLightingWriter.toXml(x)
    case x: FeSpotLight         => FeSpotLightWriter.toXml(x)
    case x: FeTile              => FeTileWriter.toXml(x)
    case x: FeTurbulence        => FeTurbulenceWriter.toXml(x)
    case x: Fig                 => FigWriter.toXml(x)
    case x: FigDesc             => FigDescWriter.toXml(x)
    case x: FileChar            => FileCharWriter.toXml(x)
    case x: FileDesc            => FileDescWriter.toXml(x)
    case x: Filter              => FilterWriter.toXml(x)
    case x: Fing                => FingWriter.toXml(x)
    case x: FingGrp             => FingGrpWriter.toXml(x)
    case x: FoliaDesc           => FoliaDescWriter.toXml(x)
    case x: Foliation           => FoliationWriter.toXml(x)
    case x: Folium              => FoliumWriter.toXml(x)
    case x: Font                => FontWriter.toXml(x)
    case x: FontFace            => FontFaceWriter.toXml(x)
    case x: FontFaceFormat      => FontFaceFormatWriter.toXml(x)
    case x: FontFaceName        => FontFaceNameWriter.toXml(x)
    case x: FontFaceSrc         => FontFaceSrcWriter.toXml(x)
    case x: FontFaceUri         => FontFaceUriWriter.toXml(x)
    case x: ForeName            => ForeNameWriter.toXml(x)
    case x: ForeignObject       => ForeignObjectWriter.toXml(x)
    case x: Front               => FrontWriter.toXml(x)
    case x: Funder              => FunderWriter.toXml(x)
    case x: G                   => GWriter.toXml(x)
    case x: Gap                 => GapWriter.toXml(x)
    case x: GenDesc             => GenDescWriter.toXml(x)
    case x: GenName             => GenNameWriter.toXml(x)
    case x: GenState            => GenStateWriter.toXml(x)
    case x: Genre               => GenreWriter.toXml(x)
    case x: GeogFeat            => GeogFeatWriter.toXml(x)
    case x: GeogName            => GeogNameWriter.toXml(x)
    case x: Glyph               => GlyphWriter.toXml(x)
    case x: GlyphRef            => GlyphRefWriter.toXml(x)
    case x: Graphic             => GraphicWriter.toXml(x)
    case x: Group               => GroupWriter.toXml(x)
    case x: GrpSym              => GrpSymWriter.toXml(x)
    case x: Hand                => HandWriter.toXml(x)
    case x: HandList            => HandListWriter.toXml(x)
    case x: HandShift           => HandShiftWriter.toXml(x)
    case x: Harm                => HarmWriter.toXml(x)
    case x: Head                => HeadWriter.toXml(x)
    case x: Height              => HeightWriter.toXml(x)
    case x: Heraldry            => HeraldryWriter.toXml(x)
    case x: Hex                 => HexWriter.toXml(x)
    case x: History             => HistoryWriter.toXml(x)
    case x: Hkern               => HkernWriter.toXml(x)
    case x: Identifier          => IdentifierWriter.toXml(x)
    case x: Image               => ImageWriter.toXml(x)
    case x: Imprimatur          => ImprimaturWriter.toXml(x)
    case x: Imprint             => ImprintWriter.toXml(x)
    case x: Incip               => IncipWriter.toXml(x)
    case x: IncipCode           => IncipCodeWriter.toXml(x)
    case x: IncipText           => IncipTextWriter.toXml(x)
    case x: Inscription         => InscriptionWriter.toXml(x)
    case x: InstrDef            => InstrDefWriter.toXml(x)
    case x: InstrGrp            => InstrGrpWriter.toXml(x)
    case x: Interpretation      => InterpretationWriter.toXml(x)
    case x: Item                => ItemWriter.toXml(x)
    case x: ItemList            => ItemListWriter.toXml(x)
    case x: Key                 => KeyWriter.toXml(x)
    case x: KeyAccid            => KeyAccidWriter.toXml(x)
    case x: KeySig              => KeySigWriter.toXml(x)
    case x: L                   => LWriter.toXml(x)
    case x: Label               => LabelWriter.toXml(x)
    case x: LabelAbbr           => LabelAbbrWriter.toXml(x)
    case x: LangUsage           => LangUsageWriter.toXml(x)
    case x: Language            => LanguageWriter.toXml(x)
    case x: Layer               => LayerWriter.toXml(x)
    case x: LayerDef            => LayerDefWriter.toXml(x)
    case x: Layout              => LayoutWriter.toXml(x)
    case x: LayoutDesc          => LayoutDescWriter.toXml(x)
    case x: Lb                  => LbWriter.toXml(x)
    case x: Lem                 => LemWriter.toXml(x)
    case x: Lg                  => LgWriter.toXml(x)
    case x: Li                  => LiWriter.toXml(x)
    case x: Librettist          => LibrettistWriter.toXml(x)
    case x: Ligature            => LigatureWriter.toXml(x)
    case x: Line                => LineWriter.toXml(x)
    case x: LinearGradient      => LinearGradientWriter.toXml(x)
    case x: List                => ListWriter.toXml(x)
    case x: Locus               => LocusWriter.toXml(x)
    case x: LocusGrp            => LocusGrpWriter.toXml(x)
    case x: Lyricist            => LyricistWriter.toXml(x)
    case x: Manifestation       => ManifestationWriter.toXml(x)
    case x: ManifestationList   => ManifestationListWriter.toXml(x)
    case x: Mapping             => MappingWriter.toXml(x)
    case x: Marker              => MarkerWriter.toXml(x)
    case x: Mask                => MaskWriter.toXml(x)
    case x: Mdiv                => MdivWriter.toXml(x)
    case x: Mei                 => MeiWriter.toXml(x)
    case x: MeiCorpus           => MeiCorpusWriter.toXml(x)
    case x: MeiHead             => MeiHeadWriter.toXml(x)
    case x: Mensur              => MensurWriter.toXml(x)
    case x: Mensuration         => MensurationWriter.toXml(x)
    case x: MetaMark            => MetaMarkWriter.toXml(x)
    case x: MetaText            => MetaTextWriter.toXml(x)
    case x: Metadata            => MetadataWriter.toXml(x)
    case x: Meter               => MeterWriter.toXml(x)
    case x: Midi                => MidiWriter.toXml(x)
    case x: MissingGlyph        => MissingGlyphWriter.toXml(x)
    case x: Monogr              => MonogrWriter.toXml(x)
    case x: Mpath               => MpathWriter.toXml(x)
    case x: Music               => MusicWriter.toXml(x)
    case x: Name                => NameWriter.toXml(x)
    case x: NameLink            => NameLinkWriter.toXml(x)
    case x: Namespace           => NamespaceWriter.toXml(x)
    case x: Normalization       => NormalizationWriter.toXml(x)
    case x: Note                => NoteWriter.toXml(x)
    case x: NoteOff             => NoteOffWriter.toXml(x)
    case x: NoteOn              => NoteOnWriter.toXml(x)
    case x: NotesStmt           => NotesStmtWriter.toXml(x)
    case x: Num                 => NumWriter.toXml(x)
    case x: Orig                => OrigWriter.toXml(x)
    case x: Ornam               => OrnamWriter.toXml(x)
    case x: OtherChar           => OtherCharWriter.toXml(x)
    case x: P                   => PWriter.toXml(x)
    case x: Pad                 => PadWriter.toXml(x)
    case x: Part                => PartWriter.toXml(x)
    case x: Parts               => PartsWriter.toXml(x)
    case x: Patch               => PatchWriter.toXml(x)
    case x: Path                => PathWriter.toXml(x)
    case x: Pattern             => PatternWriter.toXml(x)
    case x: Pb                  => PbWriter.toXml(x)
    case x: PerfDuration        => PerfDurationWriter.toXml(x)
    case x: PerfMedium          => PerfMediumWriter.toXml(x)
    case x: PerfRes             => PerfResWriter.toXml(x)
    case x: PerfResList         => PerfResListWriter.toXml(x)
    case x: Performance         => PerformanceWriter.toXml(x)
    case x: PeriodName          => PeriodNameWriter.toXml(x)
    case x: PersName            => PersNameWriter.toXml(x)
    case x: PgDesc              => PgDescWriter.toXml(x)
    case x: PgFoot              => PgFootWriter.toXml(x)
    case x: PgHead              => PgHeadWriter.toXml(x)
    case x: Phrase              => PhraseWriter.toXml(x)
    case x: PhysDesc            => PhysDescWriter.toXml(x)
    case x: PhysLoc             => PhysLocWriter.toXml(x)
    case x: PhysMedium          => PhysMediumWriter.toXml(x)
    case x: PlateNum            => PlateNumWriter.toXml(x)
    case x: PlayingSpeed        => PlayingSpeedWriter.toXml(x)
    case x: Plica               => PlicaWriter.toXml(x)
    case x: Polygon             => PolygonWriter.toXml(x)
    case x: Polyline            => PolylineWriter.toXml(x)
    case x: Port                => PortWriter.toXml(x)
    case x: PostBox             => PostBoxWriter.toXml(x)
    case x: PostCode            => PostCodeWriter.toXml(x)
    case x: Price               => PriceWriter.toXml(x)
    case x: Prog                => ProgWriter.toXml(x)
    case x: ProjectDesc         => ProjectDescWriter.toXml(x)
    case x: PropName            => PropNameWriter.toXml(x)
    case x: PropValue           => PropValueWriter.toXml(x)
    case x: Proport             => ProportWriter.toXml(x)
    case x: Provenance          => ProvenanceWriter.toXml(x)
    case x: Ptr                 => PtrWriter.toXml(x)
    case x: PubPlace            => PubPlaceWriter.toXml(x)
    case x: PubStmt             => PubStmtWriter.toXml(x)
    case x: Publisher           => PublisherWriter.toXml(x)
    case x: Q                   => QWriter.toXml(x)
    case x: Quote               => QuoteWriter.toXml(x)
    case x: RadialGradient      => RadialGradientWriter.toXml(x)
    case x: Rdg                 => RdgWriter.toXml(x)
    case x: Recipient           => RecipientWriter.toXml(x)
    case x: Recording           => RecordingWriter.toXml(x)
    case x: Rect                => RectWriter.toXml(x)
    case x: Ref                 => RefWriter.toXml(x)
    case x: Refrain             => RefrainWriter.toXml(x)
    case x: Reg                 => RegWriter.toXml(x)
    case x: Region              => RegionWriter.toXml(x)
    case x: RelatedItem         => RelatedItemWriter.toXml(x)
    case x: Relation            => RelationWriter.toXml(x)
    case x: RelationList        => RelationListWriter.toXml(x)
    case x: Rend                => RendWriter.toXml(x)
    case x: Repository          => RepositoryWriter.toXml(x)
    case x: Resp                => RespWriter.toXml(x)
    case x: RespStmt            => RespStmtWriter.toXml(x)
    case x: Rest                => RestWriter.toXml(x)
    case x: Restore             => RestoreWriter.toXml(x)
    case x: RevisionDesc        => RevisionDescWriter.toXml(x)
    case x: Role                => RoleWriter.toXml(x)
    case x: RoleDesc            => RoleDescWriter.toXml(x)
    case x: RoleName            => RoleNameWriter.toXml(x)
    case x: Rubric              => RubricWriter.toXml(x)
    case x: SamplingDecl        => SamplingDeclWriter.toXml(x)
    case x: Sb                  => SbWriter.toXml(x)
    case x: Score               => ScoreWriter.toXml(x)
    case x: ScoreDef            => ScoreDefWriter.toXml(x)
    case x: ScoreFormat         => ScoreFormatWriter.toXml(x)
    case x: Script              => ScriptWriter.toXml(x)
    case x: ScriptDesc          => ScriptDescWriter.toXml(x)
    case x: ScriptNote          => ScriptNoteWriter.toXml(x)
    case x: Seal                => SealWriter.toXml(x)
    case x: SealDesc            => SealDescWriter.toXml(x)
    case x: SecFolio            => SecFolioWriter.toXml(x)
    case x: Section             => SectionWriter.toXml(x)
    case x: Seg                 => SegWriter.toXml(x)
    case x: Segmentation        => SegmentationWriter.toXml(x)
    case x: SeqNum              => SeqNumWriter.toXml(x)
    case x: Series              => SeriesWriter.toXml(x)
    case x: SeriesStmt          => SeriesStmtWriter.toXml(x)
    case x: Set                 => SetWriter.toXml(x)
    case x: Settlement          => SettlementWriter.toXml(x)
    case x: Sic                 => SicWriter.toXml(x)
    case x: Signatures          => SignaturesWriter.toXml(x)
    case x: SoundChan           => SoundChanWriter.toXml(x)
    case x: Source              => SourceWriter.toXml(x)
    case x: SourceDesc          => SourceDescWriter.toXml(x)
    case x: Sp                  => SpWriter.toXml(x)
    case x: Space               => SpaceWriter.toXml(x)
    case x: Speaker             => SpeakerWriter.toXml(x)
    case x: SpecRepro           => SpecReproWriter.toXml(x)
    case x: Sponsor             => SponsorWriter.toXml(x)
    case x: Stack               => StackWriter.toXml(x)
    case x: Staff               => StaffWriter.toXml(x)
    case x: StaffDef            => StaffDefWriter.toXml(x)
    case x: StaffGrp            => StaffGrpWriter.toXml(x)
    case x: StageDir            => StageDirWriter.toXml(x)
    case x: Stamp               => StampWriter.toXml(x)
    case x: StdVals             => StdValsWriter.toXml(x)
    case x: Stem                => StemWriter.toXml(x)
    case x: Stop                => StopWriter.toXml(x)
    case x: Street              => StreetWriter.toXml(x)
    case x: String              => StringWriter.toXml(x)
    case x: Style               => StyleWriter.toXml(x)
    case x: StyleName           => StyleNameWriter.toXml(x)
    case x: Subst               => SubstWriter.toXml(x)
    case x: Supplied            => SuppliedWriter.toXml(x)
    case x: Support             => SupportWriter.toXml(x)
    case x: SupportDesc         => SupportDescWriter.toXml(x)
    case x: Surface             => SurfaceWriter.toXml(x)
    case x: Svg                 => SvgWriter.toXml(x)
    case x: Switch              => SwitchWriter.toXml(x)
    case x: Syl                 => SylWriter.toXml(x)
    case x: SymName             => SymNameWriter.toXml(x)
    case x: SymProp             => SymPropWriter.toXml(x)
    case x: Symbol              => SymbolWriter.toXml(x)
    case x: SymbolDef           => SymbolDefWriter.toXml(x)
    case x: SymbolTable         => SymbolTableWriter.toXml(x)
    case x: SysReq              => SysReqWriter.toXml(x)
    case x: TabDurSym           => TabDurSymWriter.toXml(x)
    case x: TabGrp              => TabGrpWriter.toXml(x)
    case x: Table               => TableWriter.toXml(x)
    case x: TagUsage            => TagUsageWriter.toXml(x)
    case x: TagsDecl            => TagsDeclWriter.toXml(x)
    case x: Taxonomy            => TaxonomyWriter.toXml(x)
    case x: Td                  => TdWriter.toXml(x)
    case x: Tempo               => TempoWriter.toXml(x)
    case x: Term                => TermWriter.toXml(x)
    case x: TermList            => TermListWriter.toXml(x)
    case x: Text                => TextWriter.toXml(x)
    case x: TextLang            => TextLangWriter.toXml(x)
    case x: TextPath            => TextPathWriter.toXml(x)
    case x: Th                  => ThWriter.toXml(x)
    case x: Title               => TitleWriter.toXml(x)
    case x: TitlePage           => TitlePageWriter.toXml(x)
    case x: TitlePart           => TitlePartWriter.toXml(x)
    case x: TitleStmt           => TitleStmtWriter.toXml(x)
    case x: Tr                  => TrWriter.toXml(x)
    case x: TrackConfig         => TrackConfigWriter.toXml(x)
    case x: TreatHist           => TreatHistWriter.toXml(x)
    case x: TreatSched          => TreatSchedWriter.toXml(x)
    case x: Tref                => TrefWriter.toXml(x)
    case x: TrkName             => TrkNameWriter.toXml(x)
    case x: Tspan               => TspanWriter.toXml(x)
    case x: Tuning              => TuningWriter.toXml(x)
    case x: TypeDesc            => TypeDescWriter.toXml(x)
    case x: TypeNote            => TypeNoteWriter.toXml(x)
    case x: Unclear             => UnclearWriter.toXml(x)
    case x: Unpub               => UnpubWriter.toXml(x)
    case x: Use                 => UseWriter.toXml(x)
    case x: UseRestrict         => UseRestrictWriter.toXml(x)
    case x: Vel                 => VelWriter.toXml(x)
    case x: Verse               => VerseWriter.toXml(x)
    case x: View                => ViewWriter.toXml(x)
    case x: Vkern               => VkernWriter.toXml(x)
    case x: Volta               => VoltaWriter.toXml(x)
    case x: Watermark           => WatermarkWriter.toXml(x)
    case x: When                => WhenWriter.toXml(x)
    case x: Width               => WidthWriter.toXml(x)
    case x: Work                => WorkWriter.toXml(x)
    case x: WorkList            => WorkListWriter.toXml(x)
    case x: Zone                => ZoneWriter.toXml(x)
    case x: java.lang.String    => scala.xml.Text(x)
    case x: scala.xml.Node      => x
    case x: Seq[_]              =>
      throw new IllegalArgumentException(s"Seq passed to toXml (should be handled by caller or flattened): $x")
    case _ => scala.xml.Text(obj.toString)
  }
}

object AWriter extends MeiModelDummyWriter[A] {
  def toXml(obj: A): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "a", Null, TopScope, true, children*)
  }
}

object AbbrWriter extends MeiModelDummyWriter[Abbr] {
  def toXml(obj: Abbr): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@instant`.foreach { v => attributes = new UnprefixedAttribute("instant", v, attributes) }
    obj.`@state`.foreach { v => attributes = new UnprefixedAttribute("state", v, attributes) }
    obj.`@hand`.foreach { v => attributes = new UnprefixedAttribute("hand", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }
    obj.`@seq`.foreach { v => attributes = new UnprefixedAttribute("seq", v, attributes) }
    obj.`@expan`.foreach { v => attributes = new UnprefixedAttribute("expan", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "abbr", attributes, TopScope, true, children*)
  }
}

object AccMatWriter extends MeiModelDummyWriter[AccMat] {
  def toXml(obj: AccMat): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "accMat", attributes, TopScope, true, children*)
  }
}

object AccessRestrictWriter extends MeiModelDummyWriter[AccessRestrict] {
  def toXml(obj: AccessRestrict): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "accessRestrict", attributes, TopScope, true, children*)
  }
}

object AccidWriter extends MeiModelDummyWriter[Accid] {
  def toXml(obj: Accid): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@accid.ges`.foreach { v => attributes = new UnprefixedAttribute("accid.ges", v, attributes) }
    obj.`@accid`.foreach { v => attributes = new UnprefixedAttribute("accid", v, attributes) }
    obj.`@when`.foreach { v => attributes = new UnprefixedAttribute("when", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@plist`.foreach { v => attributes = new UnprefixedAttribute("plist", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    obj.`@func`.foreach { v => attributes = new UnprefixedAttribute("func", v, attributes) }
    obj.`@altsym`.foreach { v => attributes = new UnprefixedAttribute("altsym", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@enclose`.foreach { v => attributes = new UnprefixedAttribute("enclose", v, attributes) }
    obj.`@glyph.auth`.foreach { v => attributes = new UnprefixedAttribute("glyph.auth", v, attributes) }
    obj.`@glyph.uri`.foreach { v => attributes = new UnprefixedAttribute("glyph.uri", v, attributes) }
    obj.`@glyph.name`.foreach { v => attributes = new UnprefixedAttribute("glyph.name", v, attributes) }
    obj.`@glyph.num`.foreach { v => attributes = new UnprefixedAttribute("glyph.num", v, attributes) }
    obj.`@onstaff`.foreach { v => attributes = new UnprefixedAttribute("onstaff", v, attributes) }
    obj.`@place`.foreach { v => attributes = new UnprefixedAttribute("place", v, attributes) }
    obj.`@loc`.foreach { v => attributes = new UnprefixedAttribute("loc", v, attributes) }
    obj.`@ploc`.foreach { v => attributes = new UnprefixedAttribute("ploc", v, attributes) }
    obj.`@oloc`.foreach { v => attributes = new UnprefixedAttribute("oloc", v, attributes) }
    obj.`@fontfam`.foreach { v => attributes = new UnprefixedAttribute("fontfam", v, attributes) }
    obj.`@fontname`.foreach { v => attributes = new UnprefixedAttribute("fontname", v, attributes) }
    obj.`@fontsize`.foreach { v => attributes = new UnprefixedAttribute("fontsize", v, attributes) }
    obj.`@fontstyle`.foreach { v => attributes = new UnprefixedAttribute("fontstyle", v, attributes) }
    obj.`@fontweight`.foreach { v => attributes = new UnprefixedAttribute("fontweight", v, attributes) }
    obj.`@letterspacing`.foreach { v => attributes = new UnprefixedAttribute("letterspacing", v, attributes) }
    obj.`@lineheight`.foreach { v => attributes = new UnprefixedAttribute("lineheight", v, attributes) }
    obj.`@ho`.foreach { v => attributes = new UnprefixedAttribute("ho", v, attributes) }
    obj.`@vo`.foreach { v => attributes = new UnprefixedAttribute("vo", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "accid", attributes, TopScope, true, children*)
  }
}

object AcquisitionWriter extends MeiModelDummyWriter[Acquisition] {
  def toXml(obj: Acquisition): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "acquisition", attributes, TopScope, true, children*)
  }
}

object ActorWriter extends MeiModelDummyWriter[Actor] {
  def toXml(obj: Actor): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "actor", attributes, TopScope, true, children*)
  }
}

object AddWriter extends MeiModelDummyWriter[Add] {
  def toXml(obj: Add): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@unit`.foreach { v => attributes = new UnprefixedAttribute("unit", v, attributes) }
    obj.`@atleast`.foreach { v => attributes = new UnprefixedAttribute("atleast", v, attributes) }
    obj.`@atmost`.foreach { v => attributes = new UnprefixedAttribute("atmost", v, attributes) }
    obj.`@min`.foreach { v => attributes = new UnprefixedAttribute("min", v, attributes) }
    obj.`@max`.foreach { v => attributes = new UnprefixedAttribute("max", v, attributes) }
    obj.`@confidence`.foreach { v => attributes = new UnprefixedAttribute("confidence", v, attributes) }
    obj.`@extent`.foreach { v => attributes = new UnprefixedAttribute("extent", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@instant`.foreach { v => attributes = new UnprefixedAttribute("instant", v, attributes) }
    obj.`@state`.foreach { v => attributes = new UnprefixedAttribute("state", v, attributes) }
    obj.`@hand`.foreach { v => attributes = new UnprefixedAttribute("hand", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }
    obj.`@seq`.foreach { v => attributes = new UnprefixedAttribute("seq", v, attributes) }
    obj.`@place`.foreach { v => attributes = new UnprefixedAttribute("place", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "add", attributes, TopScope, true, children*)
  }
}

object AddDescWriter extends MeiModelDummyWriter[AddDesc] {
  def toXml(obj: AddDesc): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "addDesc", attributes, TopScope, true, children*)
  }
}

object AddNameWriter extends MeiModelDummyWriter[AddName] {
  def toXml(obj: AddName): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@nonfiling`.foreach { v => attributes = new UnprefixedAttribute("nonfiling", v, attributes) }
    obj.`@nymref`.foreach { v => attributes = new UnprefixedAttribute("nymref", v, attributes) }
    obj.`@role`.foreach { v => attributes = new UnprefixedAttribute("role", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "addName", attributes, TopScope, true, children*)
  }
}

object AddrLineWriter extends MeiModelDummyWriter[AddrLine] {
  def toXml(obj: AddrLine): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "addrLine", attributes, TopScope, true, children*)
  }
}

object AddressWriter extends MeiModelDummyWriter[Address] {
  def toXml(obj: Address): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "address", attributes, TopScope, true, children*)
  }
}

object AltGlyphWriter extends MeiModelDummyWriter[AltGlyph] {
  def toXml(obj: AltGlyph): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "altGlyph", Null, TopScope, true, children*)
  }
}

object AltGlyphDefWriter extends MeiModelDummyWriter[AltGlyphDef] {
  def toXml(obj: AltGlyphDef): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "altGlyphDef", Null, TopScope, true, children*)
  }
}

object AltGlyphItemWriter extends MeiModelDummyWriter[AltGlyphItem] {
  def toXml(obj: AltGlyphItem): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "altGlyphItem", Null, TopScope, true, children*)
  }
}

object AltIdWriter extends MeiModelDummyWriter[AltId] {
  def toXml(obj: AltId): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "altId", attributes, TopScope, true, children*)
  }
}

object AmbNoteWriter extends MeiModelDummyWriter[AmbNote] {
  def toXml(obj: AmbNote): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@accid`.foreach { v => attributes = new UnprefixedAttribute("accid", v, attributes) }
    obj.`@colored`.foreach { v => attributes = new UnprefixedAttribute("colored", v, attributes) }
    obj.`@dur`.foreach { v => attributes = new UnprefixedAttribute("dur", v, attributes) }
    obj.`@pname`.foreach { v => attributes = new UnprefixedAttribute("pname", v, attributes) }
    obj.`@oct`.foreach { v => attributes = new UnprefixedAttribute("oct", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@enclose`.foreach { v => attributes = new UnprefixedAttribute("enclose", v, attributes) }
    obj.`@head.altsym`.foreach { v => attributes = new UnprefixedAttribute("head.altsym", v, attributes) }
    obj.`@head.auth`.foreach { v => attributes = new UnprefixedAttribute("head.auth", v, attributes) }
    obj.`@head.color`.foreach { v => attributes = new UnprefixedAttribute("head.color", v, attributes) }
    obj.`@head.fill`.foreach { v => attributes = new UnprefixedAttribute("head.fill", v, attributes) }
    obj.`@head.fillcolor`.foreach { v => attributes = new UnprefixedAttribute("head.fillcolor", v, attributes) }
    obj.`@head.mod`.foreach { v => attributes = new UnprefixedAttribute("head.mod", v, attributes) }
    obj.`@head.rotation`.foreach { v => attributes = new UnprefixedAttribute("head.rotation", v, attributes) }
    obj.`@head.shape`.foreach { v => attributes = new UnprefixedAttribute("head.shape", v, attributes) }
    obj.`@head.visible`.foreach { v => attributes = new UnprefixedAttribute("head.visible", v, attributes) }
    obj.`@loc`.foreach { v => attributes = new UnprefixedAttribute("loc", v, attributes) }
    obj.`@stem.form`.foreach { v => attributes = new UnprefixedAttribute("stem.form", v, attributes) }
    obj.`@stem.dir`.foreach { v => attributes = new UnprefixedAttribute("stem.dir", v, attributes) }
    obj.`@stem.len`.foreach { v => attributes = new UnprefixedAttribute("stem.len", v, attributes) }
    obj.`@stem.mod`.foreach { v => attributes = new UnprefixedAttribute("stem.mod", v, attributes) }
    obj.`@stem.pos`.foreach { v => attributes = new UnprefixedAttribute("stem.pos", v, attributes) }
    obj.`@stem.sameas`.foreach { v => attributes = new UnprefixedAttribute("stem.sameas", v, attributes) }
    obj.`@stem.visible`.foreach { v => attributes = new UnprefixedAttribute("stem.visible", v, attributes) }
    obj.`@stem.x`.foreach { v => attributes = new UnprefixedAttribute("stem.x", v, attributes) }
    obj.`@stem.y`.foreach { v => attributes = new UnprefixedAttribute("stem.y", v, attributes) }
    obj.`@fontfam`.foreach { v => attributes = new UnprefixedAttribute("fontfam", v, attributes) }
    obj.`@fontname`.foreach { v => attributes = new UnprefixedAttribute("fontname", v, attributes) }
    obj.`@fontsize`.foreach { v => attributes = new UnprefixedAttribute("fontsize", v, attributes) }
    obj.`@fontstyle`.foreach { v => attributes = new UnprefixedAttribute("fontstyle", v, attributes) }
    obj.`@fontweight`.foreach { v => attributes = new UnprefixedAttribute("fontweight", v, attributes) }
    obj.`@letterspacing`.foreach { v => attributes = new UnprefixedAttribute("letterspacing", v, attributes) }
    obj.`@lineheight`.foreach { v => attributes = new UnprefixedAttribute("lineheight", v, attributes) }

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "ambNote", attributes, TopScope, true, children*)
  }
}

object AmbitusWriter extends MeiModelDummyWriter[Ambitus] {
  def toXml(obj: Ambitus): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@inth`.foreach { v => attributes = new UnprefixedAttribute("inth", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "ambitus", attributes, TopScope, true, children*)
  }
}

object AnalyticWriter extends MeiModelDummyWriter[Analytic] {
  def toXml(obj: Analytic): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@comptype`.foreach { v => attributes = new UnprefixedAttribute("comptype", v, attributes) }
    obj.`@data`.foreach { v => attributes = new UnprefixedAttribute("data", v, attributes) }
    obj.`@xlink:actuate`.foreach { v => attributes = new UnprefixedAttribute("xlink:actuate", v, attributes) }
    obj.`@xlink:role`.foreach { v => attributes = new UnprefixedAttribute("xlink:role", v, attributes) }
    obj.`@xlink:show`.foreach { v => attributes = new UnprefixedAttribute("xlink:show", v, attributes) }
    obj.`@target`.foreach { v => attributes = new UnprefixedAttribute("target", v, attributes) }
    obj.`@targettype`.foreach { v => attributes = new UnprefixedAttribute("targettype", v, attributes) }
    obj.`@recordtype`.foreach { v => attributes = new UnprefixedAttribute("recordtype", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "analytic", attributes, TopScope, true, children*)
  }
}

object AnchoredTextWriter extends MeiModelDummyWriter[AnchoredText] {
  def toXml(obj: AnchoredText): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@startid`.foreach { v => attributes = new UnprefixedAttribute("startid", v, attributes) }
    obj.`@func`.foreach { v => attributes = new UnprefixedAttribute("func", v, attributes) }
    obj.`@ho`.foreach { v => attributes = new UnprefixedAttribute("ho", v, attributes) }
    obj.`@to`.foreach { v => attributes = new UnprefixedAttribute("to", v, attributes) }
    obj.`@vo`.foreach { v => attributes = new UnprefixedAttribute("vo", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "anchoredText", attributes, TopScope, true, children*)
  }
}

object AnimateWriter extends MeiModelDummyWriter[Animate] {
  def toXml(obj: Animate): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "animate", Null, TopScope, true, children*)
  }
}

object AnimateColorWriter extends MeiModelDummyWriter[AnimateColor] {
  def toXml(obj: AnimateColor): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "animateColor", Null, TopScope, true, children*)
  }
}

object AnimateMotionWriter extends MeiModelDummyWriter[AnimateMotion] {
  def toXml(obj: AnimateMotion): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "animateMotion", Null, TopScope, true, children*)
  }
}

object AnimateTransformWriter extends MeiModelDummyWriter[AnimateTransform] {
  def toXml(obj: AnimateTransform): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "animateTransform", Null, TopScope, true, children*)
  }
}

object AnnotWriter extends MeiModelDummyWriter[Annot] {
  def toXml(obj: Annot): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@audience`.foreach { v => attributes = new UnprefixedAttribute("audience", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@data`.foreach { v => attributes = new UnprefixedAttribute("data", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@plist`.foreach { v => attributes = new UnprefixedAttribute("plist", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }
    obj.`@dur.ges`.foreach { v => attributes = new UnprefixedAttribute("dur.ges", v, attributes) }
    obj.`@dots.ges`.foreach { v => attributes = new UnprefixedAttribute("dots.ges", v, attributes) }
    obj.`@dur.metrical`.foreach { v => attributes = new UnprefixedAttribute("dur.metrical", v, attributes) }
    obj.`@dur.ppq`.foreach { v => attributes = new UnprefixedAttribute("dur.ppq", v, attributes) }
    obj.`@dur.real`.foreach { v => attributes = new UnprefixedAttribute("dur.real", v, attributes) }
    obj.`@dur.recip`.foreach { v => attributes = new UnprefixedAttribute("dur.recip", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    obj.`@tstamp2.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp2.ges", v, attributes) }
    obj.`@tstamp2.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp2.real", v, attributes) }
    obj.`@when`.foreach { v => attributes = new UnprefixedAttribute("when", v, attributes) }
    obj.`@dur`.foreach { v => attributes = new UnprefixedAttribute("dur", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@startid`.foreach { v => attributes = new UnprefixedAttribute("startid", v, attributes) }
    obj.`@endid`.foreach { v => attributes = new UnprefixedAttribute("endid", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@tstamp2`.foreach { v => attributes = new UnprefixedAttribute("tstamp2", v, attributes) }
    obj.`@place`.foreach { v => attributes = new UnprefixedAttribute("place", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "annot", attributes, TopScope, true, children*)
  }
}

object AppWriter extends MeiModelDummyWriter[App] {
  def toXml(obj: App): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "app", attributes, TopScope, true, children*)
  }
}

object AppInfoWriter extends MeiModelDummyWriter[AppInfo] {
  def toXml(obj: AppInfo): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "appInfo", attributes, TopScope, true, children*)
  }
}

object ApplicationWriter extends MeiModelDummyWriter[Application] {
  def toXml(obj: Application): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@version`.foreach { v => attributes = new UnprefixedAttribute("version", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "application", attributes, TopScope, true, children*)
  }
}

object ArgumentWriter extends MeiModelDummyWriter[Argument] {
  def toXml(obj: Argument): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "argument", attributes, TopScope, true, children*)
  }
}

object ArrangerWriter extends MeiModelDummyWriter[Arranger] {
  def toXml(obj: Arranger): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "arranger", attributes, TopScope, true, children*)
  }
}

object ArticWriter extends MeiModelDummyWriter[Artic] {
  def toXml(obj: Artic): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@artic.ges`.foreach { v => attributes = new UnprefixedAttribute("artic.ges", v, attributes) }
    obj.`@artic`.foreach { v => attributes = new UnprefixedAttribute("artic", v, attributes) }
    obj.`@when`.foreach { v => attributes = new UnprefixedAttribute("when", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@plist`.foreach { v => attributes = new UnprefixedAttribute("plist", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    obj.`@altsym`.foreach { v => attributes = new UnprefixedAttribute("altsym", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@enclose`.foreach { v => attributes = new UnprefixedAttribute("enclose", v, attributes) }
    obj.`@glyph.auth`.foreach { v => attributes = new UnprefixedAttribute("glyph.auth", v, attributes) }
    obj.`@glyph.uri`.foreach { v => attributes = new UnprefixedAttribute("glyph.uri", v, attributes) }
    obj.`@glyph.name`.foreach { v => attributes = new UnprefixedAttribute("glyph.name", v, attributes) }
    obj.`@glyph.num`.foreach { v => attributes = new UnprefixedAttribute("glyph.num", v, attributes) }
    obj.`@onstaff`.foreach { v => attributes = new UnprefixedAttribute("onstaff", v, attributes) }
    obj.`@place`.foreach { v => attributes = new UnprefixedAttribute("place", v, attributes) }
    obj.`@loc`.foreach { v => attributes = new UnprefixedAttribute("loc", v, attributes) }
    obj.`@ploc`.foreach { v => attributes = new UnprefixedAttribute("ploc", v, attributes) }
    obj.`@oloc`.foreach { v => attributes = new UnprefixedAttribute("oloc", v, attributes) }
    obj.`@fontfam`.foreach { v => attributes = new UnprefixedAttribute("fontfam", v, attributes) }
    obj.`@fontname`.foreach { v => attributes = new UnprefixedAttribute("fontname", v, attributes) }
    obj.`@fontsize`.foreach { v => attributes = new UnprefixedAttribute("fontsize", v, attributes) }
    obj.`@fontstyle`.foreach { v => attributes = new UnprefixedAttribute("fontstyle", v, attributes) }
    obj.`@fontweight`.foreach { v => attributes = new UnprefixedAttribute("fontweight", v, attributes) }
    obj.`@letterspacing`.foreach { v => attributes = new UnprefixedAttribute("letterspacing", v, attributes) }
    obj.`@lineheight`.foreach { v => attributes = new UnprefixedAttribute("lineheight", v, attributes) }
    obj.`@ho`.foreach { v => attributes = new UnprefixedAttribute("ho", v, attributes) }
    obj.`@to`.foreach { v => attributes = new UnprefixedAttribute("to", v, attributes) }
    obj.`@vo`.foreach { v => attributes = new UnprefixedAttribute("vo", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "artic", attributes, TopScope, true, children*)
  }
}

object AttUsageWriter extends MeiModelDummyWriter[AttUsage] {
  def toXml(obj: AttUsage): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    attributes = new UnprefixedAttribute("name", obj.`@name`, attributes)
    obj.`@context`.foreach { v => attributes = new UnprefixedAttribute("context", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "attUsage", attributes, TopScope, true, children*)
  }
}

object AudienceWriter extends MeiModelDummyWriter[Audience] {
  def toXml(obj: Audience): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "audience", attributes, TopScope, true, children*)
  }
}

object AuthorWriter extends MeiModelDummyWriter[Author] {
  def toXml(obj: Author): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "author", attributes, TopScope, true, children*)
  }
}

object AvFileWriter extends MeiModelDummyWriter[AvFile] {
  def toXml(obj: AvFile): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@mimetype`.foreach { v => attributes = new UnprefixedAttribute("mimetype", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }
    obj.`@xlink:actuate`.foreach { v => attributes = new UnprefixedAttribute("xlink:actuate", v, attributes) }
    obj.`@xlink:role`.foreach { v => attributes = new UnprefixedAttribute("xlink:role", v, attributes) }
    obj.`@xlink:show`.foreach { v => attributes = new UnprefixedAttribute("xlink:show", v, attributes) }
    obj.`@target`.foreach { v => attributes = new UnprefixedAttribute("target", v, attributes) }
    obj.`@targettype`.foreach { v => attributes = new UnprefixedAttribute("targettype", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "avFile", attributes, TopScope, true, children*)
  }
}

object AvailabilityWriter extends MeiModelDummyWriter[Availability] {
  def toXml(obj: Availability): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@data`.foreach { v => attributes = new UnprefixedAttribute("data", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "availability", attributes, TopScope, true, children*)
  }
}

object BackWriter extends MeiModelDummyWriter[Back] {
  def toXml(obj: Back): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "back", attributes, TopScope, true, children*)
  }
}

object BarLineWriter extends MeiModelDummyWriter[BarLine] {
  def toXml(obj: BarLine): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xlink:actuate`.foreach { v => attributes = new UnprefixedAttribute("xlink:actuate", v, attributes) }
    obj.`@xlink:role`.foreach { v => attributes = new UnprefixedAttribute("xlink:role", v, attributes) }
    obj.`@xlink:show`.foreach { v => attributes = new UnprefixedAttribute("xlink:show", v, attributes) }
    obj.`@target`.foreach { v => attributes = new UnprefixedAttribute("target", v, attributes) }
    obj.`@targettype`.foreach { v => attributes = new UnprefixedAttribute("targettype", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }
    obj.`@metcon`.foreach { v => attributes = new UnprefixedAttribute("metcon", v, attributes) }
    obj.`@control`.foreach { v => attributes = new UnprefixedAttribute("control", v, attributes) }
    obj.`@form`.foreach { v => attributes = new UnprefixedAttribute("form", v, attributes) }
    obj.`@altsym`.foreach { v => attributes = new UnprefixedAttribute("altsym", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@glyph.auth`.foreach { v => attributes = new UnprefixedAttribute("glyph.auth", v, attributes) }
    obj.`@glyph.uri`.foreach { v => attributes = new UnprefixedAttribute("glyph.uri", v, attributes) }
    obj.`@glyph.name`.foreach { v => attributes = new UnprefixedAttribute("glyph.name", v, attributes) }
    obj.`@glyph.num`.foreach { v => attributes = new UnprefixedAttribute("glyph.num", v, attributes) }
    obj.`@visible`.foreach { v => attributes = new UnprefixedAttribute("visible", v, attributes) }
    obj.`@width`.foreach { v => attributes = new UnprefixedAttribute("width", v, attributes) }
    obj.`@len`.foreach { v => attributes = new UnprefixedAttribute("len", v, attributes) }
    obj.`@method`.foreach { v => attributes = new UnprefixedAttribute("method", v, attributes) }
    obj.`@place`.foreach { v => attributes = new UnprefixedAttribute("place", v, attributes) }

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "barLine", attributes, TopScope, true, children*)
  }
}

object BarreWriter extends MeiModelDummyWriter[Barre] {
  def toXml(obj: Barre): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@startid`.foreach { v => attributes = new UnprefixedAttribute("startid", v, attributes) }
    obj.`@endid`.foreach { v => attributes = new UnprefixedAttribute("endid", v, attributes) }
    obj.`@fret`.foreach { v => attributes = new UnprefixedAttribute("fret", v, attributes) }

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "barre", attributes, TopScope, true, children*)
  }
}

object BiblWriter extends MeiModelDummyWriter[Bibl] {
  def toXml(obj: Bibl): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@xlink:actuate`.foreach { v => attributes = new UnprefixedAttribute("xlink:actuate", v, attributes) }
    obj.`@xlink:role`.foreach { v => attributes = new UnprefixedAttribute("xlink:role", v, attributes) }
    obj.`@xlink:show`.foreach { v => attributes = new UnprefixedAttribute("xlink:show", v, attributes) }
    obj.`@target`.foreach { v => attributes = new UnprefixedAttribute("target", v, attributes) }
    obj.`@targettype`.foreach { v => attributes = new UnprefixedAttribute("targettype", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "bibl", attributes, TopScope, true, children*)
  }
}

object BiblListWriter extends MeiModelDummyWriter[BiblList] {
  def toXml(obj: BiblList): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "biblList", attributes, TopScope, true, children*)
  }
}

object BiblScopeWriter extends MeiModelDummyWriter[BiblScope] {
  def toXml(obj: BiblScope): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@unit`.foreach { v => attributes = new UnprefixedAttribute("unit", v, attributes) }
    obj.`@atleast`.foreach { v => attributes = new UnprefixedAttribute("atleast", v, attributes) }
    obj.`@atmost`.foreach { v => attributes = new UnprefixedAttribute("atmost", v, attributes) }
    obj.`@min`.foreach { v => attributes = new UnprefixedAttribute("min", v, attributes) }
    obj.`@max`.foreach { v => attributes = new UnprefixedAttribute("max", v, attributes) }
    obj.`@confidence`.foreach { v => attributes = new UnprefixedAttribute("confidence", v, attributes) }
    obj.`@extent`.foreach { v => attributes = new UnprefixedAttribute("extent", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@from`.foreach { v => attributes = new UnprefixedAttribute("from", v, attributes) }
    obj.`@to`.foreach { v => attributes = new UnprefixedAttribute("to", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "biblScope", attributes, TopScope, true, children*)
  }
}

object BiblStructWriter extends MeiModelDummyWriter[BiblStruct] {
  def toXml(obj: BiblStruct): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@data`.foreach { v => attributes = new UnprefixedAttribute("data", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@xlink:actuate`.foreach { v => attributes = new UnprefixedAttribute("xlink:actuate", v, attributes) }
    obj.`@xlink:role`.foreach { v => attributes = new UnprefixedAttribute("xlink:role", v, attributes) }
    obj.`@xlink:show`.foreach { v => attributes = new UnprefixedAttribute("xlink:show", v, attributes) }
    obj.`@target`.foreach { v => attributes = new UnprefixedAttribute("target", v, attributes) }
    obj.`@targettype`.foreach { v => attributes = new UnprefixedAttribute("targettype", v, attributes) }
    obj.`@recordtype`.foreach { v => attributes = new UnprefixedAttribute("recordtype", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "biblStruct", attributes, TopScope, true, children*)
  }
}

object BifoliumWriter extends MeiModelDummyWriter[Bifolium] {
  def toXml(obj: Bifolium): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@height`.foreach { v => attributes = new UnprefixedAttribute("height", v, attributes) }
    obj.`@width`.foreach { v => attributes = new UnprefixedAttribute("width", v, attributes) }
    obj.`@unit`.foreach { v => attributes = new UnprefixedAttribute("unit", v, attributes) }
    obj.`@outer.recto`.foreach { v => attributes = new UnprefixedAttribute("outer.recto", v, attributes) }
    obj.`@inner.verso`.foreach { v => attributes = new UnprefixedAttribute("inner.verso", v, attributes) }
    obj.`@inner.recto`.foreach { v => attributes = new UnprefixedAttribute("inner.recto", v, attributes) }
    obj.`@outer.verso`.foreach { v => attributes = new UnprefixedAttribute("outer.verso", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "bifolium", attributes, TopScope, true, children*)
  }
}

object BindingWriter extends MeiModelDummyWriter[Binding] {
  def toXml(obj: Binding): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@contemporary`.foreach { v => attributes = new UnprefixedAttribute("contemporary", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "binding", attributes, TopScope, true, children*)
  }
}

object BindingDescWriter extends MeiModelDummyWriter[BindingDesc] {
  def toXml(obj: BindingDesc): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "bindingDesc", attributes, TopScope, true, children*)
  }
}

object BlocWriter extends MeiModelDummyWriter[Bloc] {
  def toXml(obj: Bloc): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@nonfiling`.foreach { v => attributes = new UnprefixedAttribute("nonfiling", v, attributes) }
    obj.`@nymref`.foreach { v => attributes = new UnprefixedAttribute("nymref", v, attributes) }
    obj.`@role`.foreach { v => attributes = new UnprefixedAttribute("role", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "bloc", attributes, TopScope, true, children*)
  }
}

object BodyWriter extends MeiModelDummyWriter[Body] {
  def toXml(obj: Body): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "body", attributes, TopScope, true, children*)
  }
}

object BylineWriter extends MeiModelDummyWriter[Byline] {
  def toXml(obj: Byline): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "byline", attributes, TopScope, true, children*)
  }
}

object CaesuraWriter extends MeiModelDummyWriter[Caesura] {
  def toXml(obj: Caesura): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@dur.ges`.foreach { v => attributes = new UnprefixedAttribute("dur.ges", v, attributes) }
    obj.`@dots.ges`.foreach { v => attributes = new UnprefixedAttribute("dots.ges", v, attributes) }
    obj.`@dur.metrical`.foreach { v => attributes = new UnprefixedAttribute("dur.metrical", v, attributes) }
    obj.`@dur.ppq`.foreach { v => attributes = new UnprefixedAttribute("dur.ppq", v, attributes) }
    obj.`@dur.real`.foreach { v => attributes = new UnprefixedAttribute("dur.real", v, attributes) }
    obj.`@dur.recip`.foreach { v => attributes = new UnprefixedAttribute("dur.recip", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@startid`.foreach { v => attributes = new UnprefixedAttribute("startid", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@altsym`.foreach { v => attributes = new UnprefixedAttribute("altsym", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@glyph.auth`.foreach { v => attributes = new UnprefixedAttribute("glyph.auth", v, attributes) }
    obj.`@glyph.uri`.foreach { v => attributes = new UnprefixedAttribute("glyph.uri", v, attributes) }
    obj.`@glyph.name`.foreach { v => attributes = new UnprefixedAttribute("glyph.name", v, attributes) }
    obj.`@glyph.num`.foreach { v => attributes = new UnprefixedAttribute("glyph.num", v, attributes) }
    obj.`@place`.foreach { v => attributes = new UnprefixedAttribute("place", v, attributes) }
    obj.`@loc`.foreach { v => attributes = new UnprefixedAttribute("loc", v, attributes) }
    obj.`@ploc`.foreach { v => attributes = new UnprefixedAttribute("ploc", v, attributes) }
    obj.`@oloc`.foreach { v => attributes = new UnprefixedAttribute("oloc", v, attributes) }
    obj.`@fontfam`.foreach { v => attributes = new UnprefixedAttribute("fontfam", v, attributes) }
    obj.`@fontname`.foreach { v => attributes = new UnprefixedAttribute("fontname", v, attributes) }
    obj.`@fontsize`.foreach { v => attributes = new UnprefixedAttribute("fontsize", v, attributes) }
    obj.`@fontstyle`.foreach { v => attributes = new UnprefixedAttribute("fontstyle", v, attributes) }
    obj.`@fontweight`.foreach { v => attributes = new UnprefixedAttribute("fontweight", v, attributes) }
    obj.`@letterspacing`.foreach { v => attributes = new UnprefixedAttribute("letterspacing", v, attributes) }
    obj.`@lineheight`.foreach { v => attributes = new UnprefixedAttribute("lineheight", v, attributes) }
    obj.`@vgrp`.foreach { v => attributes = new UnprefixedAttribute("vgrp", v, attributes) }
    obj.`@ho`.foreach { v => attributes = new UnprefixedAttribute("ho", v, attributes) }
    obj.`@to`.foreach { v => attributes = new UnprefixedAttribute("to", v, attributes) }
    obj.`@vo`.foreach { v => attributes = new UnprefixedAttribute("vo", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "caesura", attributes, TopScope, true, children*)
  }
}

object CaptionWriter extends MeiModelDummyWriter[Caption] {
  def toXml(obj: Caption): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "caption", attributes, TopScope, true, children*)
  }
}

object CaptureModeWriter extends MeiModelDummyWriter[CaptureMode] {
  def toXml(obj: CaptureMode): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "captureMode", attributes, TopScope, true, children*)
  }
}

object CarrierFormWriter extends MeiModelDummyWriter[CarrierForm] {
  def toXml(obj: CarrierForm): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "carrierForm", attributes, TopScope, true, children*)
  }
}

object CastGrpWriter extends MeiModelDummyWriter[CastGrp] {
  def toXml(obj: CastGrp): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "castGrp", attributes, TopScope, true, children*)
  }
}

object CastItemWriter extends MeiModelDummyWriter[CastItem] {
  def toXml(obj: CastItem): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "castItem", attributes, TopScope, true, children*)
  }
}

object CastListWriter extends MeiModelDummyWriter[CastList] {
  def toXml(obj: CastList): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "castList", attributes, TopScope, true, children*)
  }
}

object CatRelWriter extends MeiModelDummyWriter[CatRel] {
  def toXml(obj: CatRel): Elem = {
    var attributes: MetaData = Null
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    attributes = new UnprefixedAttribute("type", obj.`@type`, attributes)

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "catRel", attributes, TopScope, true, children*)
  }
}

object CatchwordsWriter extends MeiModelDummyWriter[Catchwords] {
  def toXml(obj: Catchwords): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "catchwords", attributes, TopScope, true, children*)
  }
}

object CategoryWriter extends MeiModelDummyWriter[Category] {
  def toXml(obj: Category): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@data`.foreach { v => attributes = new UnprefixedAttribute("data", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "category", attributes, TopScope, true, children*)
  }
}

object CbWriter extends MeiModelDummyWriter[Cb] {
  def toXml(obj: Cb): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    attributes = new UnprefixedAttribute("n", obj.`@n`, attributes)

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "cb", attributes, TopScope, true, children*)
  }
}

object CcWriter extends MeiModelDummyWriter[Cc] {
  def toXml(obj: Cc): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    attributes = new UnprefixedAttribute("num", obj.`@num`, attributes)
    obj.`@val`.foreach { v => attributes = new UnprefixedAttribute("val", v, attributes) }

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "cc", attributes, TopScope, true, children*)
  }
}

object ChanWriter extends MeiModelDummyWriter[Chan] {
  def toXml(obj: Chan): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    attributes = new UnprefixedAttribute("num", obj.`@num`, attributes)

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "chan", attributes, TopScope, true, children*)
  }
}

object ChanPrWriter extends MeiModelDummyWriter[ChanPr] {
  def toXml(obj: ChanPr): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    attributes = new UnprefixedAttribute("num", obj.`@num`, attributes)

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "chanPr", attributes, TopScope, true, children*)
  }
}

object ChangeWriter extends MeiModelDummyWriter[Change] {
  def toXml(obj: Change): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "change", attributes, TopScope, true, children*)
  }
}

object ChangeDescWriter extends MeiModelDummyWriter[ChangeDesc] {
  def toXml(obj: ChangeDesc): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "changeDesc", attributes, TopScope, true, children*)
  }
}

object ChoiceWriter extends MeiModelDummyWriter[Choice] {
  def toXml(obj: Choice): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "choice", attributes, TopScope, true, children*)
  }
}

object ChordWriter extends MeiModelDummyWriter[Chord] {
  def toXml(obj: Chord): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@artic.ges`.foreach { v => attributes = new UnprefixedAttribute("artic.ges", v, attributes) }
    obj.`@dur.ges`.foreach { v => attributes = new UnprefixedAttribute("dur.ges", v, attributes) }
    obj.`@dots.ges`.foreach { v => attributes = new UnprefixedAttribute("dots.ges", v, attributes) }
    obj.`@dur.metrical`.foreach { v => attributes = new UnprefixedAttribute("dur.metrical", v, attributes) }
    obj.`@dur.ppq`.foreach { v => attributes = new UnprefixedAttribute("dur.ppq", v, attributes) }
    obj.`@dur.real`.foreach { v => attributes = new UnprefixedAttribute("dur.real", v, attributes) }
    obj.`@dur.recip`.foreach { v => attributes = new UnprefixedAttribute("dur.recip", v, attributes) }
    obj.`@instr`.foreach { v => attributes = new UnprefixedAttribute("instr", v, attributes) }
    obj.`@artic`.foreach { v => attributes = new UnprefixedAttribute("artic", v, attributes) }
    obj.`@cue`.foreach { v => attributes = new UnprefixedAttribute("cue", v, attributes) }
    obj.`@dur`.foreach { v => attributes = new UnprefixedAttribute("dur", v, attributes) }
    obj.`@when`.foreach { v => attributes = new UnprefixedAttribute("when", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@syl`.foreach { v => attributes = new UnprefixedAttribute("syl", v, attributes) }
    obj.`@altsym`.foreach { v => attributes = new UnprefixedAttribute("altsym", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@enclose`.foreach { v => attributes = new UnprefixedAttribute("enclose", v, attributes) }
    obj.`@glyph.auth`.foreach { v => attributes = new UnprefixedAttribute("glyph.auth", v, attributes) }
    obj.`@glyph.uri`.foreach { v => attributes = new UnprefixedAttribute("glyph.uri", v, attributes) }
    obj.`@glyph.name`.foreach { v => attributes = new UnprefixedAttribute("glyph.name", v, attributes) }
    obj.`@glyph.num`.foreach { v => attributes = new UnprefixedAttribute("glyph.num", v, attributes) }
    obj.`@stem.form`.foreach { v => attributes = new UnprefixedAttribute("stem.form", v, attributes) }
    obj.`@stem.dir`.foreach { v => attributes = new UnprefixedAttribute("stem.dir", v, attributes) }
    obj.`@stem.len`.foreach { v => attributes = new UnprefixedAttribute("stem.len", v, attributes) }
    obj.`@stem.mod`.foreach { v => attributes = new UnprefixedAttribute("stem.mod", v, attributes) }
    obj.`@stem.pos`.foreach { v => attributes = new UnprefixedAttribute("stem.pos", v, attributes) }
    obj.`@stem.sameas`.foreach { v => attributes = new UnprefixedAttribute("stem.sameas", v, attributes) }
    obj.`@stem.visible`.foreach { v => attributes = new UnprefixedAttribute("stem.visible", v, attributes) }
    obj.`@stem.x`.foreach { v => attributes = new UnprefixedAttribute("stem.x", v, attributes) }
    obj.`@stem.y`.foreach { v => attributes = new UnprefixedAttribute("stem.y", v, attributes) }
    obj.`@fontfam`.foreach { v => attributes = new UnprefixedAttribute("fontfam", v, attributes) }
    obj.`@fontname`.foreach { v => attributes = new UnprefixedAttribute("fontname", v, attributes) }
    obj.`@fontsize`.foreach { v => attributes = new UnprefixedAttribute("fontsize", v, attributes) }
    obj.`@fontstyle`.foreach { v => attributes = new UnprefixedAttribute("fontstyle", v, attributes) }
    obj.`@fontweight`.foreach { v => attributes = new UnprefixedAttribute("fontweight", v, attributes) }
    obj.`@letterspacing`.foreach { v => attributes = new UnprefixedAttribute("letterspacing", v, attributes) }
    obj.`@lineheight`.foreach { v => attributes = new UnprefixedAttribute("lineheight", v, attributes) }
    obj.`@visible`.foreach { v => attributes = new UnprefixedAttribute("visible", v, attributes) }
    obj.`@ho`.foreach { v => attributes = new UnprefixedAttribute("ho", v, attributes) }
    obj.`@to`.foreach { v => attributes = new UnprefixedAttribute("to", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }
    obj.`@cluster`.foreach { v => attributes = new UnprefixedAttribute("cluster", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "chord", attributes, TopScope, true, children*)
  }
}

object ChordDefWriter extends MeiModelDummyWriter[ChordDef] {
  def toXml(obj: ChordDef): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@tab.pos`.foreach { v => attributes = new UnprefixedAttribute("tab.pos", v, attributes) }
    obj.`@tab.strings`.foreach { v => attributes = new UnprefixedAttribute("tab.strings", v, attributes) }
    obj.`@tab.courses`.foreach { v => attributes = new UnprefixedAttribute("tab.courses", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "chordDef", attributes, TopScope, true, children*)
  }
}

object ChordMemberWriter extends MeiModelDummyWriter[ChordMember] {
  def toXml(obj: ChordMember): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@inth`.foreach { v => attributes = new UnprefixedAttribute("inth", v, attributes) }
    obj.`@accid.ges`.foreach { v => attributes = new UnprefixedAttribute("accid.ges", v, attributes) }
    obj.`@pname`.foreach { v => attributes = new UnprefixedAttribute("pname", v, attributes) }
    obj.`@oct`.foreach { v => attributes = new UnprefixedAttribute("oct", v, attributes) }
    obj.`@tab.fing`.foreach { v => attributes = new UnprefixedAttribute("tab.fing", v, attributes) }
    obj.`@tab.fret`.foreach { v => attributes = new UnprefixedAttribute("tab.fret", v, attributes) }
    obj.`@tab.line`.foreach { v => attributes = new UnprefixedAttribute("tab.line", v, attributes) }
    obj.`@tab.string`.foreach { v => attributes = new UnprefixedAttribute("tab.string", v, attributes) }
    obj.`@tab.course`.foreach { v => attributes = new UnprefixedAttribute("tab.course", v, attributes) }

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "chordMember", attributes, TopScope, true, children*)
  }
}

object ChordTableWriter extends MeiModelDummyWriter[ChordTable] {
  def toXml(obj: ChordTable): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "chordTable", attributes, TopScope, true, children*)
  }
}

object CircleWriter extends MeiModelDummyWriter[Circle] {
  def toXml(obj: Circle): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "circle", Null, TopScope, true, children*)
  }
}

object ClassDeclsWriter extends MeiModelDummyWriter[ClassDecls] {
  def toXml(obj: ClassDecls): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "classDecls", attributes, TopScope, true, children*)
  }
}

object ClassificationWriter extends MeiModelDummyWriter[Classification] {
  def toXml(obj: Classification): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@data`.foreach { v => attributes = new UnprefixedAttribute("data", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "classification", attributes, TopScope, true, children*)
  }
}

object ClefWriter extends MeiModelDummyWriter[Clef] {
  def toXml(obj: Clef): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@when`.foreach { v => attributes = new UnprefixedAttribute("when", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@shape`.foreach { v => attributes = new UnprefixedAttribute("shape", v, attributes) }
    obj.`@line`.foreach { v => attributes = new UnprefixedAttribute("line", v, attributes) }
    obj.`@oct`.foreach { v => attributes = new UnprefixedAttribute("oct", v, attributes) }
    obj.`@dis`.foreach { v => attributes = new UnprefixedAttribute("dis", v, attributes) }
    obj.`@dis.place`.foreach { v => attributes = new UnprefixedAttribute("dis.place", v, attributes) }
    obj.`@cautionary`.foreach { v => attributes = new UnprefixedAttribute("cautionary", v, attributes) }
    obj.`@altsym`.foreach { v => attributes = new UnprefixedAttribute("altsym", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@enclose`.foreach { v => attributes = new UnprefixedAttribute("enclose", v, attributes) }
    obj.`@glyph.auth`.foreach { v => attributes = new UnprefixedAttribute("glyph.auth", v, attributes) }
    obj.`@glyph.uri`.foreach { v => attributes = new UnprefixedAttribute("glyph.uri", v, attributes) }
    obj.`@glyph.name`.foreach { v => attributes = new UnprefixedAttribute("glyph.name", v, attributes) }
    obj.`@glyph.num`.foreach { v => attributes = new UnprefixedAttribute("glyph.num", v, attributes) }
    obj.`@fontfam`.foreach { v => attributes = new UnprefixedAttribute("fontfam", v, attributes) }
    obj.`@fontname`.foreach { v => attributes = new UnprefixedAttribute("fontname", v, attributes) }
    obj.`@fontsize`.foreach { v => attributes = new UnprefixedAttribute("fontsize", v, attributes) }
    obj.`@fontstyle`.foreach { v => attributes = new UnprefixedAttribute("fontstyle", v, attributes) }
    obj.`@fontweight`.foreach { v => attributes = new UnprefixedAttribute("fontweight", v, attributes) }
    obj.`@letterspacing`.foreach { v => attributes = new UnprefixedAttribute("letterspacing", v, attributes) }
    obj.`@lineheight`.foreach { v => attributes = new UnprefixedAttribute("lineheight", v, attributes) }
    obj.`@visible`.foreach { v => attributes = new UnprefixedAttribute("visible", v, attributes) }
    obj.`@ho`.foreach { v => attributes = new UnprefixedAttribute("ho", v, attributes) }
    obj.`@to`.foreach { v => attributes = new UnprefixedAttribute("to", v, attributes) }
    obj.`@vo`.foreach { v => attributes = new UnprefixedAttribute("vo", v, attributes) }

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "clef", attributes, TopScope, true, children*)
  }
}

object ClefGrpWriter extends MeiModelDummyWriter[ClefGrp] {
  def toXml(obj: ClefGrp): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@when`.foreach { v => attributes = new UnprefixedAttribute("when", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "clefGrp", attributes, TopScope, true, children*)
  }
}

object ClipWriter extends MeiModelDummyWriter[Clip] {
  def toXml(obj: Clip): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@data`.foreach { v => attributes = new UnprefixedAttribute("data", v, attributes) }
    obj.`@begin`.foreach { v => attributes = new UnprefixedAttribute("begin", v, attributes) }
    obj.`@end`.foreach { v => attributes = new UnprefixedAttribute("end", v, attributes) }
    obj.`@betype`.foreach { v => attributes = new UnprefixedAttribute("betype", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }
    obj.`@startid`.foreach { v => attributes = new UnprefixedAttribute("startid", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "clip", attributes, TopScope, true, children*)
  }
}

object ClipPathWriter extends MeiModelDummyWriter[ClipPath] {
  def toXml(obj: ClipPath): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "clipPath", Null, TopScope, true, children*)
  }
}

object ColLayoutWriter extends MeiModelDummyWriter[ColLayout] {
  def toXml(obj: ColLayout): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    attributes = new UnprefixedAttribute("cols", obj.`@cols`, attributes)

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "colLayout", attributes, TopScope, true, children*)
  }
}

object CollationWriter extends MeiModelDummyWriter[Collation] {
  def toXml(obj: Collation): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "collation", attributes, TopScope, true, children*)
  }
}

object ColophonWriter extends MeiModelDummyWriter[Colophon] {
  def toXml(obj: Colophon): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "colophon", attributes, TopScope, true, children*)
  }
}

object ColorProfileWriter extends MeiModelDummyWriter[ColorProfile] {
  def toXml(obj: ColorProfile): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "color-profile", Null, TopScope, true, children*)
  }
}

object ComponentListWriter extends MeiModelDummyWriter[ComponentList] {
  def toXml(obj: ComponentList): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "componentList", attributes, TopScope, true, children*)
  }
}

object ComposerWriter extends MeiModelDummyWriter[Composer] {
  def toXml(obj: Composer): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "composer", attributes, TopScope, true, children*)
  }
}

object ConditionWriter extends MeiModelDummyWriter[Condition] {
  def toXml(obj: Condition): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "condition", attributes, TopScope, true, children*)
  }
}

object ContentItemWriter extends MeiModelDummyWriter[ContentItem] {
  def toXml(obj: ContentItem): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "contentItem", attributes, TopScope, true, children*)
  }
}

object ContentsWriter extends MeiModelDummyWriter[Contents] {
  def toXml(obj: Contents): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xlink:actuate`.foreach { v => attributes = new UnprefixedAttribute("xlink:actuate", v, attributes) }
    obj.`@xlink:role`.foreach { v => attributes = new UnprefixedAttribute("xlink:role", v, attributes) }
    obj.`@xlink:show`.foreach { v => attributes = new UnprefixedAttribute("xlink:show", v, attributes) }
    obj.`@target`.foreach { v => attributes = new UnprefixedAttribute("target", v, attributes) }
    obj.`@targettype`.foreach { v => attributes = new UnprefixedAttribute("targettype", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "contents", attributes, TopScope, true, children*)
  }
}

object ContextWriter extends MeiModelDummyWriter[Context] {
  def toXml(obj: Context): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "context", attributes, TopScope, true, children*)
  }
}

object ContributorWriter extends MeiModelDummyWriter[Contributor] {
  def toXml(obj: Contributor): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@role`.foreach { v => attributes = new UnprefixedAttribute("role", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "contributor", attributes, TopScope, true, children*)
  }
}

object CorpNameWriter extends MeiModelDummyWriter[CorpName] {
  def toXml(obj: CorpName): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@nonfiling`.foreach { v => attributes = new UnprefixedAttribute("nonfiling", v, attributes) }
    obj.`@nymref`.foreach { v => attributes = new UnprefixedAttribute("nymref", v, attributes) }
    obj.`@role`.foreach { v => attributes = new UnprefixedAttribute("role", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "corpName", attributes, TopScope, true, children*)
  }
}

object CorrWriter extends MeiModelDummyWriter[Corr] {
  def toXml(obj: Corr): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@unit`.foreach { v => attributes = new UnprefixedAttribute("unit", v, attributes) }
    obj.`@atleast`.foreach { v => attributes = new UnprefixedAttribute("atleast", v, attributes) }
    obj.`@atmost`.foreach { v => attributes = new UnprefixedAttribute("atmost", v, attributes) }
    obj.`@min`.foreach { v => attributes = new UnprefixedAttribute("min", v, attributes) }
    obj.`@max`.foreach { v => attributes = new UnprefixedAttribute("max", v, attributes) }
    obj.`@confidence`.foreach { v => attributes = new UnprefixedAttribute("confidence", v, attributes) }
    obj.`@extent`.foreach { v => attributes = new UnprefixedAttribute("extent", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@instant`.foreach { v => attributes = new UnprefixedAttribute("instant", v, attributes) }
    obj.`@state`.foreach { v => attributes = new UnprefixedAttribute("state", v, attributes) }
    obj.`@hand`.foreach { v => attributes = new UnprefixedAttribute("hand", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }
    obj.`@seq`.foreach { v => attributes = new UnprefixedAttribute("seq", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "corr", attributes, TopScope, true, children*)
  }
}

object CorrectionWriter extends MeiModelDummyWriter[Correction] {
  def toXml(obj: Correction): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@data`.foreach { v => attributes = new UnprefixedAttribute("data", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@method`.foreach { v => attributes = new UnprefixedAttribute("method", v, attributes) }
    obj.`@corrlevel`.foreach { v => attributes = new UnprefixedAttribute("corrlevel", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "correction", attributes, TopScope, true, children*)
  }
}

object CountryWriter extends MeiModelDummyWriter[Country] {
  def toXml(obj: Country): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@nonfiling`.foreach { v => attributes = new UnprefixedAttribute("nonfiling", v, attributes) }
    obj.`@nymref`.foreach { v => attributes = new UnprefixedAttribute("nymref", v, attributes) }
    obj.`@role`.foreach { v => attributes = new UnprefixedAttribute("role", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "country", attributes, TopScope, true, children*)
  }
}

object CourseWriter extends MeiModelDummyWriter[Course] {
  def toXml(obj: Course): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@accid`.foreach { v => attributes = new UnprefixedAttribute("accid", v, attributes) }
    obj.`@pname`.foreach { v => attributes = new UnprefixedAttribute("pname", v, attributes) }
    obj.`@oct`.foreach { v => attributes = new UnprefixedAttribute("oct", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "course", attributes, TopScope, true, children*)
  }
}

object CpMarkWriter extends MeiModelDummyWriter[CpMark] {
  def toXml(obj: CpMark): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@when`.foreach { v => attributes = new UnprefixedAttribute("when", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@plist`.foreach { v => attributes = new UnprefixedAttribute("plist", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    obj.`@origin.tstamp`.foreach { v => attributes = new UnprefixedAttribute("origin.tstamp", v, attributes) }
    obj.`@origin.tstamp2`.foreach { v => attributes = new UnprefixedAttribute("origin.tstamp2", v, attributes) }
    obj.`@origin.staff`.foreach { v => attributes = new UnprefixedAttribute("origin.staff", v, attributes) }
    obj.`@origin.layer`.foreach { v => attributes = new UnprefixedAttribute("origin.layer", v, attributes) }
    obj.`@origin.startid`.foreach { v => attributes = new UnprefixedAttribute("origin.startid", v, attributes) }
    obj.`@origin.endid`.foreach { v => attributes = new UnprefixedAttribute("origin.endid", v, attributes) }
    obj.`@dis`.foreach { v => attributes = new UnprefixedAttribute("dis", v, attributes) }
    obj.`@dis.place`.foreach { v => attributes = new UnprefixedAttribute("dis.place", v, attributes) }
    obj.`@startid`.foreach { v => attributes = new UnprefixedAttribute("startid", v, attributes) }
    obj.`@endid`.foreach { v => attributes = new UnprefixedAttribute("endid", v, attributes) }
    obj.`@tstamp2`.foreach { v => attributes = new UnprefixedAttribute("tstamp2", v, attributes) }
    obj.`@altsym`.foreach { v => attributes = new UnprefixedAttribute("altsym", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@enclose`.foreach { v => attributes = new UnprefixedAttribute("enclose", v, attributes) }
    obj.`@glyph.auth`.foreach { v => attributes = new UnprefixedAttribute("glyph.auth", v, attributes) }
    obj.`@glyph.uri`.foreach { v => attributes = new UnprefixedAttribute("glyph.uri", v, attributes) }
    obj.`@glyph.name`.foreach { v => attributes = new UnprefixedAttribute("glyph.name", v, attributes) }
    obj.`@glyph.num`.foreach { v => attributes = new UnprefixedAttribute("glyph.num", v, attributes) }
    obj.`@place`.foreach { v => attributes = new UnprefixedAttribute("place", v, attributes) }
    obj.`@fontfam`.foreach { v => attributes = new UnprefixedAttribute("fontfam", v, attributes) }
    obj.`@fontname`.foreach { v => attributes = new UnprefixedAttribute("fontname", v, attributes) }
    obj.`@fontsize`.foreach { v => attributes = new UnprefixedAttribute("fontsize", v, attributes) }
    obj.`@fontstyle`.foreach { v => attributes = new UnprefixedAttribute("fontstyle", v, attributes) }
    obj.`@fontweight`.foreach { v => attributes = new UnprefixedAttribute("fontweight", v, attributes) }
    obj.`@letterspacing`.foreach { v => attributes = new UnprefixedAttribute("letterspacing", v, attributes) }
    obj.`@lineheight`.foreach { v => attributes = new UnprefixedAttribute("lineheight", v, attributes) }
    obj.`@vgrp`.foreach { v => attributes = new UnprefixedAttribute("vgrp", v, attributes) }
    obj.`@ho`.foreach { v => attributes = new UnprefixedAttribute("ho", v, attributes) }
    obj.`@to`.foreach { v => attributes = new UnprefixedAttribute("to", v, attributes) }
    obj.`@vo`.foreach { v => attributes = new UnprefixedAttribute("vo", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }
    obj.`@dur.ges`.foreach { v => attributes = new UnprefixedAttribute("dur.ges", v, attributes) }
    obj.`@dots.ges`.foreach { v => attributes = new UnprefixedAttribute("dots.ges", v, attributes) }
    obj.`@dur.metrical`.foreach { v => attributes = new UnprefixedAttribute("dur.metrical", v, attributes) }
    obj.`@dur.ppq`.foreach { v => attributes = new UnprefixedAttribute("dur.ppq", v, attributes) }
    obj.`@dur.real`.foreach { v => attributes = new UnprefixedAttribute("dur.real", v, attributes) }
    obj.`@dur.recip`.foreach { v => attributes = new UnprefixedAttribute("dur.recip", v, attributes) }
    obj.`@tstamp2.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp2.ges", v, attributes) }
    obj.`@tstamp2.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp2.real", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "cpMark", attributes, TopScope, true, children*)
  }
}

object CreationWriter extends MeiModelDummyWriter[Creation] {
  def toXml(obj: Creation): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "creation", attributes, TopScope, true, children*)
  }
}

object CueWriter extends MeiModelDummyWriter[Cue] {
  def toXml(obj: Cue): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "cue", attributes, TopScope, true, children*)
  }
}

object CursorWriter extends MeiModelDummyWriter[Cursor] {
  def toXml(obj: Cursor): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "cursor", Null, TopScope, true, children*)
  }
}

object CurveWriter extends MeiModelDummyWriter[Curve] {
  def toXml(obj: Curve): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@startid`.foreach { v => attributes = new UnprefixedAttribute("startid", v, attributes) }
    obj.`@endid`.foreach { v => attributes = new UnprefixedAttribute("endid", v, attributes) }
    obj.`@func`.foreach { v => attributes = new UnprefixedAttribute("func", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@bezier`.foreach { v => attributes = new UnprefixedAttribute("bezier", v, attributes) }
    obj.`@bulge`.foreach { v => attributes = new UnprefixedAttribute("bulge", v, attributes) }
    obj.`@curvedir`.foreach { v => attributes = new UnprefixedAttribute("curvedir", v, attributes) }
    obj.`@lform`.foreach { v => attributes = new UnprefixedAttribute("lform", v, attributes) }
    obj.`@lwidth`.foreach { v => attributes = new UnprefixedAttribute("lwidth", v, attributes) }
    obj.`@lsegs`.foreach { v => attributes = new UnprefixedAttribute("lsegs", v, attributes) }
    obj.`@ho`.foreach { v => attributes = new UnprefixedAttribute("ho", v, attributes) }
    obj.`@to`.foreach { v => attributes = new UnprefixedAttribute("to", v, attributes) }
    obj.`@vo`.foreach { v => attributes = new UnprefixedAttribute("vo", v, attributes) }
    obj.`@startho`.foreach { v => attributes = new UnprefixedAttribute("startho", v, attributes) }
    obj.`@endho`.foreach { v => attributes = new UnprefixedAttribute("endho", v, attributes) }
    obj.`@startto`.foreach { v => attributes = new UnprefixedAttribute("startto", v, attributes) }
    obj.`@endto`.foreach { v => attributes = new UnprefixedAttribute("endto", v, attributes) }
    obj.`@startvo`.foreach { v => attributes = new UnprefixedAttribute("startvo", v, attributes) }
    obj.`@endvo`.foreach { v => attributes = new UnprefixedAttribute("endvo", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }
    obj.`@x2`.foreach { v => attributes = new UnprefixedAttribute("x2", v, attributes) }
    obj.`@y2`.foreach { v => attributes = new UnprefixedAttribute("y2", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "curve", attributes, TopScope, true, children*)
  }
}

object CustosWriter extends MeiModelDummyWriter[Custos] {
  def toXml(obj: Custos): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@accid`.foreach { v => attributes = new UnprefixedAttribute("accid", v, attributes) }
    obj.`@pname`.foreach { v => attributes = new UnprefixedAttribute("pname", v, attributes) }
    obj.`@oct`.foreach { v => attributes = new UnprefixedAttribute("oct", v, attributes) }
    obj.`@target`.foreach { v => attributes = new UnprefixedAttribute("target", v, attributes) }
    obj.`@altsym`.foreach { v => attributes = new UnprefixedAttribute("altsym", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@glyph.auth`.foreach { v => attributes = new UnprefixedAttribute("glyph.auth", v, attributes) }
    obj.`@glyph.uri`.foreach { v => attributes = new UnprefixedAttribute("glyph.uri", v, attributes) }
    obj.`@glyph.name`.foreach { v => attributes = new UnprefixedAttribute("glyph.name", v, attributes) }
    obj.`@glyph.num`.foreach { v => attributes = new UnprefixedAttribute("glyph.num", v, attributes) }
    obj.`@loc`.foreach { v => attributes = new UnprefixedAttribute("loc", v, attributes) }
    obj.`@fontfam`.foreach { v => attributes = new UnprefixedAttribute("fontfam", v, attributes) }
    obj.`@fontname`.foreach { v => attributes = new UnprefixedAttribute("fontname", v, attributes) }
    obj.`@fontsize`.foreach { v => attributes = new UnprefixedAttribute("fontsize", v, attributes) }
    obj.`@fontstyle`.foreach { v => attributes = new UnprefixedAttribute("fontstyle", v, attributes) }
    obj.`@fontweight`.foreach { v => attributes = new UnprefixedAttribute("fontweight", v, attributes) }
    obj.`@letterspacing`.foreach { v => attributes = new UnprefixedAttribute("letterspacing", v, attributes) }
    obj.`@lineheight`.foreach { v => attributes = new UnprefixedAttribute("lineheight", v, attributes) }
    obj.`@ho`.foreach { v => attributes = new UnprefixedAttribute("ho", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "custos", attributes, TopScope, true, children*)
  }
}

object CutoutWriter extends MeiModelDummyWriter[Cutout] {
  def toXml(obj: Cutout): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@height`.foreach { v => attributes = new UnprefixedAttribute("height", v, attributes) }
    obj.`@width`.foreach { v => attributes = new UnprefixedAttribute("width", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@unit`.foreach { v => attributes = new UnprefixedAttribute("unit", v, attributes) }
    obj.`@instant`.foreach { v => attributes = new UnprefixedAttribute("instant", v, attributes) }
    obj.`@state`.foreach { v => attributes = new UnprefixedAttribute("state", v, attributes) }
    obj.`@hand`.foreach { v => attributes = new UnprefixedAttribute("hand", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }
    obj.`@seq`.foreach { v => attributes = new UnprefixedAttribute("seq", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }
    attributes = new UnprefixedAttribute("removed.from", obj.`@removed.from`, attributes)
    obj.`@removed.by`.foreach { v => attributes = new UnprefixedAttribute("removed.by", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "cutout", attributes, TopScope, true, children*)
  }
}

object DamageWriter extends MeiModelDummyWriter[Damage] {
  def toXml(obj: Damage): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@agent`.foreach { v => attributes = new UnprefixedAttribute("agent", v, attributes) }
    obj.`@unit`.foreach { v => attributes = new UnprefixedAttribute("unit", v, attributes) }
    obj.`@atleast`.foreach { v => attributes = new UnprefixedAttribute("atleast", v, attributes) }
    obj.`@atmost`.foreach { v => attributes = new UnprefixedAttribute("atmost", v, attributes) }
    obj.`@min`.foreach { v => attributes = new UnprefixedAttribute("min", v, attributes) }
    obj.`@max`.foreach { v => attributes = new UnprefixedAttribute("max", v, attributes) }
    obj.`@confidence`.foreach { v => attributes = new UnprefixedAttribute("confidence", v, attributes) }
    obj.`@extent`.foreach { v => attributes = new UnprefixedAttribute("extent", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@hand`.foreach { v => attributes = new UnprefixedAttribute("hand", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@degree`.foreach { v => attributes = new UnprefixedAttribute("degree", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "damage", attributes, TopScope, true, children*)
  }
}

object DateWriter extends MeiModelDummyWriter[Date] {
  def toXml(obj: Date): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@calendar`.foreach { v => attributes = new UnprefixedAttribute("calendar", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "date", attributes, TopScope, true, children*)
  }
}

object DecoDescWriter extends MeiModelDummyWriter[DecoDesc] {
  def toXml(obj: DecoDesc): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "decoDesc", attributes, TopScope, true, children*)
  }
}

object DecoNoteWriter extends MeiModelDummyWriter[DecoNote] {
  def toXml(obj: DecoNote): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "decoNote", attributes, TopScope, true, children*)
  }
}

object DedicateeWriter extends MeiModelDummyWriter[Dedicatee] {
  def toXml(obj: Dedicatee): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "dedicatee", attributes, TopScope, true, children*)
  }
}

object DedicationWriter extends MeiModelDummyWriter[Dedication] {
  def toXml(obj: Dedication): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "dedication", attributes, TopScope, true, children*)
  }
}

object DefinitionSrcWriter extends MeiModelDummyWriter[DefinitionSrc] {
  def toXml(obj: DefinitionSrc): Elem = {

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "definition-src", Null, TopScope, true, children*)
  }
}

object DefsWriter extends MeiModelDummyWriter[Defs] {
  def toXml(obj: Defs): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "defs", Null, TopScope, true, children*)
  }
}

object DelWriter extends MeiModelDummyWriter[Del] {
  def toXml(obj: Del): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@unit`.foreach { v => attributes = new UnprefixedAttribute("unit", v, attributes) }
    obj.`@atleast`.foreach { v => attributes = new UnprefixedAttribute("atleast", v, attributes) }
    obj.`@atmost`.foreach { v => attributes = new UnprefixedAttribute("atmost", v, attributes) }
    obj.`@min`.foreach { v => attributes = new UnprefixedAttribute("min", v, attributes) }
    obj.`@max`.foreach { v => attributes = new UnprefixedAttribute("max", v, attributes) }
    obj.`@confidence`.foreach { v => attributes = new UnprefixedAttribute("confidence", v, attributes) }
    obj.`@extent`.foreach { v => attributes = new UnprefixedAttribute("extent", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@altrend`.foreach { v => attributes = new UnprefixedAttribute("altrend", v, attributes) }
    obj.`@rend`.foreach { v => attributes = new UnprefixedAttribute("rend", v, attributes) }
    obj.`@instant`.foreach { v => attributes = new UnprefixedAttribute("instant", v, attributes) }
    obj.`@state`.foreach { v => attributes = new UnprefixedAttribute("state", v, attributes) }
    obj.`@hand`.foreach { v => attributes = new UnprefixedAttribute("hand", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }
    obj.`@seq`.foreach { v => attributes = new UnprefixedAttribute("seq", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "del", attributes, TopScope, true, children*)
  }
}

object DepthWriter extends MeiModelDummyWriter[Depth] {
  def toXml(obj: Depth): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@unit`.foreach { v => attributes = new UnprefixedAttribute("unit", v, attributes) }
    obj.`@atleast`.foreach { v => attributes = new UnprefixedAttribute("atleast", v, attributes) }
    obj.`@atmost`.foreach { v => attributes = new UnprefixedAttribute("atmost", v, attributes) }
    obj.`@min`.foreach { v => attributes = new UnprefixedAttribute("min", v, attributes) }
    obj.`@max`.foreach { v => attributes = new UnprefixedAttribute("max", v, attributes) }
    obj.`@confidence`.foreach { v => attributes = new UnprefixedAttribute("confidence", v, attributes) }
    obj.`@quantity`.foreach { v => attributes = new UnprefixedAttribute("quantity", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "depth", attributes, TopScope, true, children*)
  }
}

object DescWriter extends MeiModelDummyWriter[Desc] {
  def toXml(obj: Desc): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "desc", Null, TopScope, true, children*)
  }
}

object DimWriter extends MeiModelDummyWriter[Dim] {
  def toXml(obj: Dim): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@unit`.foreach { v => attributes = new UnprefixedAttribute("unit", v, attributes) }
    obj.`@atleast`.foreach { v => attributes = new UnprefixedAttribute("atleast", v, attributes) }
    obj.`@atmost`.foreach { v => attributes = new UnprefixedAttribute("atmost", v, attributes) }
    obj.`@min`.foreach { v => attributes = new UnprefixedAttribute("min", v, attributes) }
    obj.`@max`.foreach { v => attributes = new UnprefixedAttribute("max", v, attributes) }
    obj.`@confidence`.foreach { v => attributes = new UnprefixedAttribute("confidence", v, attributes) }
    obj.`@quantity`.foreach { v => attributes = new UnprefixedAttribute("quantity", v, attributes) }
    attributes = new UnprefixedAttribute("form", obj.`@form`, attributes)

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "dim", attributes, TopScope, true, children*)
  }
}

object DimensionsWriter extends MeiModelDummyWriter[Dimensions] {
  def toXml(obj: Dimensions): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@unit`.foreach { v => attributes = new UnprefixedAttribute("unit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "dimensions", attributes, TopScope, true, children*)
  }
}

object DirWriter extends MeiModelDummyWriter[Dir] {
  def toXml(obj: Dir): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@dur.ges`.foreach { v => attributes = new UnprefixedAttribute("dur.ges", v, attributes) }
    obj.`@dots.ges`.foreach { v => attributes = new UnprefixedAttribute("dots.ges", v, attributes) }
    obj.`@dur.metrical`.foreach { v => attributes = new UnprefixedAttribute("dur.metrical", v, attributes) }
    obj.`@dur.ppq`.foreach { v => attributes = new UnprefixedAttribute("dur.ppq", v, attributes) }
    obj.`@dur.real`.foreach { v => attributes = new UnprefixedAttribute("dur.real", v, attributes) }
    obj.`@dur.recip`.foreach { v => attributes = new UnprefixedAttribute("dur.recip", v, attributes) }
    obj.`@tstamp2.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp2.ges", v, attributes) }
    obj.`@tstamp2.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp2.real", v, attributes) }
    obj.`@when`.foreach { v => attributes = new UnprefixedAttribute("when", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@plist`.foreach { v => attributes = new UnprefixedAttribute("plist", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    obj.`@dur`.foreach { v => attributes = new UnprefixedAttribute("dur", v, attributes) }
    obj.`@startid`.foreach { v => attributes = new UnprefixedAttribute("startid", v, attributes) }
    obj.`@endid`.foreach { v => attributes = new UnprefixedAttribute("endid", v, attributes) }
    obj.`@tstamp2`.foreach { v => attributes = new UnprefixedAttribute("tstamp2", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@lform`.foreach { v => attributes = new UnprefixedAttribute("lform", v, attributes) }
    obj.`@lwidth`.foreach { v => attributes = new UnprefixedAttribute("lwidth", v, attributes) }
    obj.`@lsegs`.foreach { v => attributes = new UnprefixedAttribute("lsegs", v, attributes) }
    obj.`@lendsym`.foreach { v => attributes = new UnprefixedAttribute("lendsym", v, attributes) }
    obj.`@lendsym.size`.foreach { v => attributes = new UnprefixedAttribute("lendsym.size", v, attributes) }
    obj.`@lstartsym`.foreach { v => attributes = new UnprefixedAttribute("lstartsym", v, attributes) }
    obj.`@lstartsym.size`.foreach { v => attributes = new UnprefixedAttribute("lstartsym.size", v, attributes) }
    obj.`@extender`.foreach { v => attributes = new UnprefixedAttribute("extender", v, attributes) }
    obj.`@place`.foreach { v => attributes = new UnprefixedAttribute("place", v, attributes) }
    obj.`@vgrp`.foreach { v => attributes = new UnprefixedAttribute("vgrp", v, attributes) }
    obj.`@ho`.foreach { v => attributes = new UnprefixedAttribute("ho", v, attributes) }
    obj.`@to`.foreach { v => attributes = new UnprefixedAttribute("to", v, attributes) }
    obj.`@vo`.foreach { v => attributes = new UnprefixedAttribute("vo", v, attributes) }
    obj.`@startho`.foreach { v => attributes = new UnprefixedAttribute("startho", v, attributes) }
    obj.`@endho`.foreach { v => attributes = new UnprefixedAttribute("endho", v, attributes) }
    obj.`@startto`.foreach { v => attributes = new UnprefixedAttribute("startto", v, attributes) }
    obj.`@endto`.foreach { v => attributes = new UnprefixedAttribute("endto", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "dir", attributes, TopScope, true, children*)
  }
}

object DistributorWriter extends MeiModelDummyWriter[Distributor] {
  def toXml(obj: Distributor): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "distributor", attributes, TopScope, true, children*)
  }
}

object DistrictWriter extends MeiModelDummyWriter[District] {
  def toXml(obj: District): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@nonfiling`.foreach { v => attributes = new UnprefixedAttribute("nonfiling", v, attributes) }
    obj.`@nymref`.foreach { v => attributes = new UnprefixedAttribute("nymref", v, attributes) }
    obj.`@role`.foreach { v => attributes = new UnprefixedAttribute("role", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "district", attributes, TopScope, true, children*)
  }
}

object DivWriter extends MeiModelDummyWriter[Div] {
  def toXml(obj: Div): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "div", attributes, TopScope, true, children*)
  }
}

object DomainsDeclWriter extends MeiModelDummyWriter[DomainsDecl] {
  def toXml(obj: DomainsDecl): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    attributes = new UnprefixedAttribute("anl", obj.`@anl`, attributes)
    attributes = new UnprefixedAttribute("ges", obj.`@ges`, attributes)
    attributes = new UnprefixedAttribute("vis", obj.`@vis`, attributes)

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "domainsDecl", attributes, TopScope, true, children*)
  }
}

object DotWriter extends MeiModelDummyWriter[Dot] {
  def toXml(obj: Dot): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@when`.foreach { v => attributes = new UnprefixedAttribute("when", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@plist`.foreach { v => attributes = new UnprefixedAttribute("plist", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    obj.`@form`.foreach { v => attributes = new UnprefixedAttribute("form", v, attributes) }
    obj.`@altsym`.foreach { v => attributes = new UnprefixedAttribute("altsym", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@glyph.auth`.foreach { v => attributes = new UnprefixedAttribute("glyph.auth", v, attributes) }
    obj.`@glyph.uri`.foreach { v => attributes = new UnprefixedAttribute("glyph.uri", v, attributes) }
    obj.`@glyph.name`.foreach { v => attributes = new UnprefixedAttribute("glyph.name", v, attributes) }
    obj.`@glyph.num`.foreach { v => attributes = new UnprefixedAttribute("glyph.num", v, attributes) }
    obj.`@loc`.foreach { v => attributes = new UnprefixedAttribute("loc", v, attributes) }
    obj.`@ploc`.foreach { v => attributes = new UnprefixedAttribute("ploc", v, attributes) }
    obj.`@oloc`.foreach { v => attributes = new UnprefixedAttribute("oloc", v, attributes) }
    obj.`@fontfam`.foreach { v => attributes = new UnprefixedAttribute("fontfam", v, attributes) }
    obj.`@fontname`.foreach { v => attributes = new UnprefixedAttribute("fontname", v, attributes) }
    obj.`@fontsize`.foreach { v => attributes = new UnprefixedAttribute("fontsize", v, attributes) }
    obj.`@fontstyle`.foreach { v => attributes = new UnprefixedAttribute("fontstyle", v, attributes) }
    obj.`@fontweight`.foreach { v => attributes = new UnprefixedAttribute("fontweight", v, attributes) }
    obj.`@letterspacing`.foreach { v => attributes = new UnprefixedAttribute("letterspacing", v, attributes) }
    obj.`@lineheight`.foreach { v => attributes = new UnprefixedAttribute("lineheight", v, attributes) }
    obj.`@ho`.foreach { v => attributes = new UnprefixedAttribute("ho", v, attributes) }
    obj.`@vo`.foreach { v => attributes = new UnprefixedAttribute("vo", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "dot", attributes, TopScope, true, children*)
  }
}

object DynamWriter extends MeiModelDummyWriter[Dynam] {
  def toXml(obj: Dynam): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@dur.ges`.foreach { v => attributes = new UnprefixedAttribute("dur.ges", v, attributes) }
    obj.`@dots.ges`.foreach { v => attributes = new UnprefixedAttribute("dots.ges", v, attributes) }
    obj.`@dur.metrical`.foreach { v => attributes = new UnprefixedAttribute("dur.metrical", v, attributes) }
    obj.`@dur.ppq`.foreach { v => attributes = new UnprefixedAttribute("dur.ppq", v, attributes) }
    obj.`@dur.real`.foreach { v => attributes = new UnprefixedAttribute("dur.real", v, attributes) }
    obj.`@dur.recip`.foreach { v => attributes = new UnprefixedAttribute("dur.recip", v, attributes) }
    obj.`@val`.foreach { v => attributes = new UnprefixedAttribute("val", v, attributes) }
    obj.`@val2`.foreach { v => attributes = new UnprefixedAttribute("val2", v, attributes) }
    obj.`@tstamp2.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp2.ges", v, attributes) }
    obj.`@tstamp2.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp2.real", v, attributes) }
    obj.`@when`.foreach { v => attributes = new UnprefixedAttribute("when", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@plist`.foreach { v => attributes = new UnprefixedAttribute("plist", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    obj.`@dur`.foreach { v => attributes = new UnprefixedAttribute("dur", v, attributes) }
    obj.`@startid`.foreach { v => attributes = new UnprefixedAttribute("startid", v, attributes) }
    obj.`@endid`.foreach { v => attributes = new UnprefixedAttribute("endid", v, attributes) }
    obj.`@tstamp2`.foreach { v => attributes = new UnprefixedAttribute("tstamp2", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@lform`.foreach { v => attributes = new UnprefixedAttribute("lform", v, attributes) }
    obj.`@lwidth`.foreach { v => attributes = new UnprefixedAttribute("lwidth", v, attributes) }
    obj.`@lsegs`.foreach { v => attributes = new UnprefixedAttribute("lsegs", v, attributes) }
    obj.`@lendsym`.foreach { v => attributes = new UnprefixedAttribute("lendsym", v, attributes) }
    obj.`@lendsym.size`.foreach { v => attributes = new UnprefixedAttribute("lendsym.size", v, attributes) }
    obj.`@lstartsym`.foreach { v => attributes = new UnprefixedAttribute("lstartsym", v, attributes) }
    obj.`@lstartsym.size`.foreach { v => attributes = new UnprefixedAttribute("lstartsym.size", v, attributes) }
    obj.`@extender`.foreach { v => attributes = new UnprefixedAttribute("extender", v, attributes) }
    obj.`@place`.foreach { v => attributes = new UnprefixedAttribute("place", v, attributes) }
    obj.`@vgrp`.foreach { v => attributes = new UnprefixedAttribute("vgrp", v, attributes) }
    obj.`@ho`.foreach { v => attributes = new UnprefixedAttribute("ho", v, attributes) }
    obj.`@to`.foreach { v => attributes = new UnprefixedAttribute("to", v, attributes) }
    obj.`@vo`.foreach { v => attributes = new UnprefixedAttribute("vo", v, attributes) }
    obj.`@startho`.foreach { v => attributes = new UnprefixedAttribute("startho", v, attributes) }
    obj.`@endho`.foreach { v => attributes = new UnprefixedAttribute("endho", v, attributes) }
    obj.`@startto`.foreach { v => attributes = new UnprefixedAttribute("startto", v, attributes) }
    obj.`@endto`.foreach { v => attributes = new UnprefixedAttribute("endto", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "dynam", attributes, TopScope, true, children*)
  }
}

object EditionWriter extends MeiModelDummyWriter[Edition] {
  def toXml(obj: Edition): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "edition", attributes, TopScope, true, children*)
  }
}

object EditionStmtWriter extends MeiModelDummyWriter[EditionStmt] {
  def toXml(obj: EditionStmt): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "editionStmt", attributes, TopScope, true, children*)
  }
}

object EditorWriter extends MeiModelDummyWriter[Editor] {
  def toXml(obj: Editor): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "editor", attributes, TopScope, true, children*)
  }
}

object EditorialDeclWriter extends MeiModelDummyWriter[EditorialDecl] {
  def toXml(obj: EditorialDecl): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@data`.foreach { v => attributes = new UnprefixedAttribute("data", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "editorialDecl", attributes, TopScope, true, children*)
  }
}

object EllipseWriter extends MeiModelDummyWriter[Ellipse] {
  def toXml(obj: Ellipse): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "ellipse", Null, TopScope, true, children*)
  }
}

object EncodingDescWriter extends MeiModelDummyWriter[EncodingDesc] {
  def toXml(obj: EncodingDesc): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "encodingDesc", attributes, TopScope, true, children*)
  }
}

object EndingWriter extends MeiModelDummyWriter[Ending] {
  def toXml(obj: Ending): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xlink:actuate`.foreach { v => attributes = new UnprefixedAttribute("xlink:actuate", v, attributes) }
    obj.`@xlink:role`.foreach { v => attributes = new UnprefixedAttribute("xlink:role", v, attributes) }
    obj.`@xlink:show`.foreach { v => attributes = new UnprefixedAttribute("xlink:show", v, attributes) }
    obj.`@target`.foreach { v => attributes = new UnprefixedAttribute("target", v, attributes) }
    obj.`@targettype`.foreach { v => attributes = new UnprefixedAttribute("targettype", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }
    obj.`@when`.foreach { v => attributes = new UnprefixedAttribute("when", v, attributes) }
    obj.`@lform`.foreach { v => attributes = new UnprefixedAttribute("lform", v, attributes) }
    obj.`@lwidth`.foreach { v => attributes = new UnprefixedAttribute("lwidth", v, attributes) }
    obj.`@lsegs`.foreach { v => attributes = new UnprefixedAttribute("lsegs", v, attributes) }
    obj.`@lendsym`.foreach { v => attributes = new UnprefixedAttribute("lendsym", v, attributes) }
    obj.`@lendsym.size`.foreach { v => attributes = new UnprefixedAttribute("lendsym.size", v, attributes) }
    obj.`@lstartsym`.foreach { v => attributes = new UnprefixedAttribute("lstartsym", v, attributes) }
    obj.`@lstartsym.size`.foreach { v => attributes = new UnprefixedAttribute("lstartsym.size", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "ending", attributes, TopScope, true, children*)
  }
}

object EpigraphWriter extends MeiModelDummyWriter[Epigraph] {
  def toXml(obj: Epigraph): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "epigraph", attributes, TopScope, true, children*)
  }
}

object EventWriter extends MeiModelDummyWriter[Event] {
  def toXml(obj: Event): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@calendar`.foreach { v => attributes = new UnprefixedAttribute("calendar", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "event", attributes, TopScope, true, children*)
  }
}

object EventListWriter extends MeiModelDummyWriter[EventList] {
  def toXml(obj: EventList): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "eventList", attributes, TopScope, true, children*)
  }
}

object ExhibHistWriter extends MeiModelDummyWriter[ExhibHist] {
  def toXml(obj: ExhibHist): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "exhibHist", attributes, TopScope, true, children*)
  }
}

object ExpanWriter extends MeiModelDummyWriter[Expan] {
  def toXml(obj: Expan): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@unit`.foreach { v => attributes = new UnprefixedAttribute("unit", v, attributes) }
    obj.`@atleast`.foreach { v => attributes = new UnprefixedAttribute("atleast", v, attributes) }
    obj.`@atmost`.foreach { v => attributes = new UnprefixedAttribute("atmost", v, attributes) }
    obj.`@min`.foreach { v => attributes = new UnprefixedAttribute("min", v, attributes) }
    obj.`@max`.foreach { v => attributes = new UnprefixedAttribute("max", v, attributes) }
    obj.`@confidence`.foreach { v => attributes = new UnprefixedAttribute("confidence", v, attributes) }
    obj.`@extent`.foreach { v => attributes = new UnprefixedAttribute("extent", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@instant`.foreach { v => attributes = new UnprefixedAttribute("instant", v, attributes) }
    obj.`@state`.foreach { v => attributes = new UnprefixedAttribute("state", v, attributes) }
    obj.`@hand`.foreach { v => attributes = new UnprefixedAttribute("hand", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }
    obj.`@seq`.foreach { v => attributes = new UnprefixedAttribute("seq", v, attributes) }
    obj.`@abbr`.foreach { v => attributes = new UnprefixedAttribute("abbr", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "expan", attributes, TopScope, true, children*)
  }
}

object ExpansionWriter extends MeiModelDummyWriter[Expansion] {
  def toXml(obj: Expansion): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@plist`.foreach { v => attributes = new UnprefixedAttribute("plist", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "expansion", attributes, TopScope, true, children*)
  }
}

object ExplicitWriter extends MeiModelDummyWriter[Explicit] {
  def toXml(obj: Explicit): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "explicit", attributes, TopScope, true, children*)
  }
}

object ExpressionWriter extends MeiModelDummyWriter[Expression] {
  def toXml(obj: Expression): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@data`.foreach { v => attributes = new UnprefixedAttribute("data", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "expression", attributes, TopScope, true, children*)
  }
}

object ExpressionListWriter extends MeiModelDummyWriter[ExpressionList] {
  def toXml(obj: ExpressionList): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "expressionList", attributes, TopScope, true, children*)
  }
}

object ExtDataWriter extends MeiModelDummyWriter[ExtData] {
  def toXml(obj: ExtData): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@xml:space`.foreach { v => attributes = new UnprefixedAttribute("xml:space", v, attributes) }
    obj.`@xlink:actuate`.foreach { v => attributes = new UnprefixedAttribute("xlink:actuate", v, attributes) }
    obj.`@xlink:role`.foreach { v => attributes = new UnprefixedAttribute("xlink:role", v, attributes) }
    obj.`@xlink:show`.foreach { v => attributes = new UnprefixedAttribute("xlink:show", v, attributes) }
    obj.`@target`.foreach { v => attributes = new UnprefixedAttribute("target", v, attributes) }
    obj.`@targettype`.foreach { v => attributes = new UnprefixedAttribute("targettype", v, attributes) }
    obj.`@mimetype`.foreach { v => attributes = new UnprefixedAttribute("mimetype", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "extData", attributes, TopScope, true, children*)
  }
}

object ExtMetaWriter extends MeiModelDummyWriter[ExtMeta] {
  def toXml(obj: ExtMeta): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:space`.foreach { v => attributes = new UnprefixedAttribute("xml:space", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "extMeta", attributes, TopScope, true, children*)
  }
}

object ExtentWriter extends MeiModelDummyWriter[Extent] {
  def toXml(obj: Extent): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@unit`.foreach { v => attributes = new UnprefixedAttribute("unit", v, attributes) }
    obj.`@atleast`.foreach { v => attributes = new UnprefixedAttribute("atleast", v, attributes) }
    obj.`@atmost`.foreach { v => attributes = new UnprefixedAttribute("atmost", v, attributes) }
    obj.`@min`.foreach { v => attributes = new UnprefixedAttribute("min", v, attributes) }
    obj.`@max`.foreach { v => attributes = new UnprefixedAttribute("max", v, attributes) }
    obj.`@confidence`.foreach { v => attributes = new UnprefixedAttribute("confidence", v, attributes) }
    obj.`@quantity`.foreach { v => attributes = new UnprefixedAttribute("quantity", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "extent", attributes, TopScope, true, children*)
  }
}

object FWriter extends MeiModelDummyWriter[F] {
  def toXml(obj: F): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@dur.ges`.foreach { v => attributes = new UnprefixedAttribute("dur.ges", v, attributes) }
    obj.`@dots.ges`.foreach { v => attributes = new UnprefixedAttribute("dots.ges", v, attributes) }
    obj.`@dur.metrical`.foreach { v => attributes = new UnprefixedAttribute("dur.metrical", v, attributes) }
    obj.`@dur.ppq`.foreach { v => attributes = new UnprefixedAttribute("dur.ppq", v, attributes) }
    obj.`@dur.real`.foreach { v => attributes = new UnprefixedAttribute("dur.real", v, attributes) }
    obj.`@dur.recip`.foreach { v => attributes = new UnprefixedAttribute("dur.recip", v, attributes) }
    obj.`@tstamp2.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp2.ges", v, attributes) }
    obj.`@tstamp2.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp2.real", v, attributes) }
    obj.`@when`.foreach { v => attributes = new UnprefixedAttribute("when", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@plist`.foreach { v => attributes = new UnprefixedAttribute("plist", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    obj.`@dur`.foreach { v => attributes = new UnprefixedAttribute("dur", v, attributes) }
    obj.`@startid`.foreach { v => attributes = new UnprefixedAttribute("startid", v, attributes) }
    obj.`@endid`.foreach { v => attributes = new UnprefixedAttribute("endid", v, attributes) }
    obj.`@tstamp2`.foreach { v => attributes = new UnprefixedAttribute("tstamp2", v, attributes) }
    obj.`@altsym`.foreach { v => attributes = new UnprefixedAttribute("altsym", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@lform`.foreach { v => attributes = new UnprefixedAttribute("lform", v, attributes) }
    obj.`@lwidth`.foreach { v => attributes = new UnprefixedAttribute("lwidth", v, attributes) }
    obj.`@lsegs`.foreach { v => attributes = new UnprefixedAttribute("lsegs", v, attributes) }
    obj.`@lendsym`.foreach { v => attributes = new UnprefixedAttribute("lendsym", v, attributes) }
    obj.`@lendsym.size`.foreach { v => attributes = new UnprefixedAttribute("lendsym.size", v, attributes) }
    obj.`@lstartsym`.foreach { v => attributes = new UnprefixedAttribute("lstartsym", v, attributes) }
    obj.`@lstartsym.size`.foreach { v => attributes = new UnprefixedAttribute("lstartsym.size", v, attributes) }
    obj.`@extender`.foreach { v => attributes = new UnprefixedAttribute("extender", v, attributes) }
    obj.`@place`.foreach { v => attributes = new UnprefixedAttribute("place", v, attributes) }
    obj.`@ho`.foreach { v => attributes = new UnprefixedAttribute("ho", v, attributes) }
    obj.`@to`.foreach { v => attributes = new UnprefixedAttribute("to", v, attributes) }
    obj.`@vo`.foreach { v => attributes = new UnprefixedAttribute("vo", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "f", attributes, TopScope, true, children*)
  }
}

object FacsimileWriter extends MeiModelDummyWriter[Facsimile] {
  def toXml(obj: Facsimile): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "facsimile", attributes, TopScope, true, children*)
  }
}

object FamNameWriter extends MeiModelDummyWriter[FamName] {
  def toXml(obj: FamName): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@nonfiling`.foreach { v => attributes = new UnprefixedAttribute("nonfiling", v, attributes) }
    obj.`@nymref`.foreach { v => attributes = new UnprefixedAttribute("nymref", v, attributes) }
    obj.`@role`.foreach { v => attributes = new UnprefixedAttribute("role", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "famName", attributes, TopScope, true, children*)
  }
}

object FbWriter extends MeiModelDummyWriter[Fb] {
  def toXml(obj: Fb): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "fb", attributes, TopScope, true, children*)
  }
}

object FeBlendWriter extends MeiModelDummyWriter[FeBlend] {
  def toXml(obj: FeBlend): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "feBlend", Null, TopScope, true, children*)
  }
}

object FeColorMatrixWriter extends MeiModelDummyWriter[FeColorMatrix] {
  def toXml(obj: FeColorMatrix): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "feColorMatrix", Null, TopScope, true, children*)
  }
}

object FeComponentTransferWriter extends MeiModelDummyWriter[FeComponentTransfer] {
  def toXml(obj: FeComponentTransfer): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "feComponentTransfer", Null, TopScope, true, children*)
  }
}

object FeCompositeWriter extends MeiModelDummyWriter[FeComposite] {
  def toXml(obj: FeComposite): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "feComposite", Null, TopScope, true, children*)
  }
}

object FeConvolveMatrixWriter extends MeiModelDummyWriter[FeConvolveMatrix] {
  def toXml(obj: FeConvolveMatrix): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "feConvolveMatrix", Null, TopScope, true, children*)
  }
}

object FeDiffuseLightingWriter extends MeiModelDummyWriter[FeDiffuseLighting] {
  def toXml(obj: FeDiffuseLighting): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "feDiffuseLighting", Null, TopScope, true, children*)
  }
}

object FeDisplacementMapWriter extends MeiModelDummyWriter[FeDisplacementMap] {
  def toXml(obj: FeDisplacementMap): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "feDisplacementMap", Null, TopScope, true, children*)
  }
}

object FeDistantLightWriter extends MeiModelDummyWriter[FeDistantLight] {
  def toXml(obj: FeDistantLight): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "feDistantLight", Null, TopScope, true, children*)
  }
}

object FeFloodWriter extends MeiModelDummyWriter[FeFlood] {
  def toXml(obj: FeFlood): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "feFlood", Null, TopScope, true, children*)
  }
}

object FeFuncAWriter extends MeiModelDummyWriter[FeFuncA] {
  def toXml(obj: FeFuncA): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "feFuncA", Null, TopScope, true, children*)
  }
}

object FeFuncBWriter extends MeiModelDummyWriter[FeFuncB] {
  def toXml(obj: FeFuncB): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "feFuncB", Null, TopScope, true, children*)
  }
}

object FeFuncGWriter extends MeiModelDummyWriter[FeFuncG] {
  def toXml(obj: FeFuncG): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "feFuncG", Null, TopScope, true, children*)
  }
}

object FeFuncRWriter extends MeiModelDummyWriter[FeFuncR] {
  def toXml(obj: FeFuncR): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "feFuncR", Null, TopScope, true, children*)
  }
}

object FeGaussianBlurWriter extends MeiModelDummyWriter[FeGaussianBlur] {
  def toXml(obj: FeGaussianBlur): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "feGaussianBlur", Null, TopScope, true, children*)
  }
}

object FeImageWriter extends MeiModelDummyWriter[FeImage] {
  def toXml(obj: FeImage): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "feImage", Null, TopScope, true, children*)
  }
}

object FeMergeWriter extends MeiModelDummyWriter[FeMerge] {
  def toXml(obj: FeMerge): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "feMerge", Null, TopScope, true, children*)
  }
}

object FeMergeNodeWriter extends MeiModelDummyWriter[FeMergeNode] {
  def toXml(obj: FeMergeNode): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "feMergeNode", Null, TopScope, true, children*)
  }
}

object FeMorphologyWriter extends MeiModelDummyWriter[FeMorphology] {
  def toXml(obj: FeMorphology): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "feMorphology", Null, TopScope, true, children*)
  }
}

object FeOffsetWriter extends MeiModelDummyWriter[FeOffset] {
  def toXml(obj: FeOffset): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "feOffset", Null, TopScope, true, children*)
  }
}

object FePointLightWriter extends MeiModelDummyWriter[FePointLight] {
  def toXml(obj: FePointLight): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "fePointLight", Null, TopScope, true, children*)
  }
}

object FeSpecularLightingWriter extends MeiModelDummyWriter[FeSpecularLighting] {
  def toXml(obj: FeSpecularLighting): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "feSpecularLighting", Null, TopScope, true, children*)
  }
}

object FeSpotLightWriter extends MeiModelDummyWriter[FeSpotLight] {
  def toXml(obj: FeSpotLight): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "feSpotLight", Null, TopScope, true, children*)
  }
}

object FeTileWriter extends MeiModelDummyWriter[FeTile] {
  def toXml(obj: FeTile): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "feTile", Null, TopScope, true, children*)
  }
}

object FeTurbulenceWriter extends MeiModelDummyWriter[FeTurbulence] {
  def toXml(obj: FeTurbulence): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "feTurbulence", Null, TopScope, true, children*)
  }
}

object FigWriter extends MeiModelDummyWriter[Fig] {
  def toXml(obj: Fig): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@halign`.foreach { v => attributes = new UnprefixedAttribute("halign", v, attributes) }
    obj.`@valign`.foreach { v => attributes = new UnprefixedAttribute("valign", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "fig", attributes, TopScope, true, children*)
  }
}

object FigDescWriter extends MeiModelDummyWriter[FigDesc] {
  def toXml(obj: FigDesc): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "figDesc", attributes, TopScope, true, children*)
  }
}

object FileCharWriter extends MeiModelDummyWriter[FileChar] {
  def toXml(obj: FileChar): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "fileChar", attributes, TopScope, true, children*)
  }
}

object FileDescWriter extends MeiModelDummyWriter[FileDesc] {
  def toXml(obj: FileDesc): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "fileDesc", attributes, TopScope, true, children*)
  }
}

object FilterWriter extends MeiModelDummyWriter[Filter] {
  def toXml(obj: Filter): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "filter", Null, TopScope, true, children*)
  }
}

object FingWriter extends MeiModelDummyWriter[Fing] {
  def toXml(obj: Fing): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@dur.ges`.foreach { v => attributes = new UnprefixedAttribute("dur.ges", v, attributes) }
    obj.`@dots.ges`.foreach { v => attributes = new UnprefixedAttribute("dots.ges", v, attributes) }
    obj.`@dur.metrical`.foreach { v => attributes = new UnprefixedAttribute("dur.metrical", v, attributes) }
    obj.`@dur.ppq`.foreach { v => attributes = new UnprefixedAttribute("dur.ppq", v, attributes) }
    obj.`@dur.real`.foreach { v => attributes = new UnprefixedAttribute("dur.real", v, attributes) }
    obj.`@dur.recip`.foreach { v => attributes = new UnprefixedAttribute("dur.recip", v, attributes) }
    obj.`@tstamp2.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp2.ges", v, attributes) }
    obj.`@tstamp2.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp2.real", v, attributes) }
    obj.`@when`.foreach { v => attributes = new UnprefixedAttribute("when", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@plist`.foreach { v => attributes = new UnprefixedAttribute("plist", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    obj.`@dur`.foreach { v => attributes = new UnprefixedAttribute("dur", v, attributes) }
    obj.`@startid`.foreach { v => attributes = new UnprefixedAttribute("startid", v, attributes) }
    obj.`@endid`.foreach { v => attributes = new UnprefixedAttribute("endid", v, attributes) }
    obj.`@tstamp2`.foreach { v => attributes = new UnprefixedAttribute("tstamp2", v, attributes) }
    obj.`@altsym`.foreach { v => attributes = new UnprefixedAttribute("altsym", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@lform`.foreach { v => attributes = new UnprefixedAttribute("lform", v, attributes) }
    obj.`@lwidth`.foreach { v => attributes = new UnprefixedAttribute("lwidth", v, attributes) }
    obj.`@lsegs`.foreach { v => attributes = new UnprefixedAttribute("lsegs", v, attributes) }
    obj.`@lendsym`.foreach { v => attributes = new UnprefixedAttribute("lendsym", v, attributes) }
    obj.`@lendsym.size`.foreach { v => attributes = new UnprefixedAttribute("lendsym.size", v, attributes) }
    obj.`@lstartsym`.foreach { v => attributes = new UnprefixedAttribute("lstartsym", v, attributes) }
    obj.`@lstartsym.size`.foreach { v => attributes = new UnprefixedAttribute("lstartsym.size", v, attributes) }
    obj.`@extender`.foreach { v => attributes = new UnprefixedAttribute("extender", v, attributes) }
    obj.`@place`.foreach { v => attributes = new UnprefixedAttribute("place", v, attributes) }
    obj.`@vgrp`.foreach { v => attributes = new UnprefixedAttribute("vgrp", v, attributes) }
    obj.`@ho`.foreach { v => attributes = new UnprefixedAttribute("ho", v, attributes) }
    obj.`@to`.foreach { v => attributes = new UnprefixedAttribute("to", v, attributes) }
    obj.`@vo`.foreach { v => attributes = new UnprefixedAttribute("vo", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "fing", attributes, TopScope, true, children*)
  }
}

object FingGrpWriter extends MeiModelDummyWriter[FingGrp] {
  def toXml(obj: FingGrp): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@dur.ges`.foreach { v => attributes = new UnprefixedAttribute("dur.ges", v, attributes) }
    obj.`@dots.ges`.foreach { v => attributes = new UnprefixedAttribute("dots.ges", v, attributes) }
    obj.`@dur.metrical`.foreach { v => attributes = new UnprefixedAttribute("dur.metrical", v, attributes) }
    obj.`@dur.ppq`.foreach { v => attributes = new UnprefixedAttribute("dur.ppq", v, attributes) }
    obj.`@dur.real`.foreach { v => attributes = new UnprefixedAttribute("dur.real", v, attributes) }
    obj.`@dur.recip`.foreach { v => attributes = new UnprefixedAttribute("dur.recip", v, attributes) }
    obj.`@tstamp2.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp2.ges", v, attributes) }
    obj.`@tstamp2.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp2.real", v, attributes) }
    obj.`@when`.foreach { v => attributes = new UnprefixedAttribute("when", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@plist`.foreach { v => attributes = new UnprefixedAttribute("plist", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    obj.`@dur`.foreach { v => attributes = new UnprefixedAttribute("dur", v, attributes) }
    obj.`@startid`.foreach { v => attributes = new UnprefixedAttribute("startid", v, attributes) }
    obj.`@endid`.foreach { v => attributes = new UnprefixedAttribute("endid", v, attributes) }
    obj.`@tstamp2`.foreach { v => attributes = new UnprefixedAttribute("tstamp2", v, attributes) }
    obj.`@form`.foreach { v => attributes = new UnprefixedAttribute("form", v, attributes) }
    obj.`@altsym`.foreach { v => attributes = new UnprefixedAttribute("altsym", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@lform`.foreach { v => attributes = new UnprefixedAttribute("lform", v, attributes) }
    obj.`@lwidth`.foreach { v => attributes = new UnprefixedAttribute("lwidth", v, attributes) }
    obj.`@lsegs`.foreach { v => attributes = new UnprefixedAttribute("lsegs", v, attributes) }
    obj.`@lendsym`.foreach { v => attributes = new UnprefixedAttribute("lendsym", v, attributes) }
    obj.`@lendsym.size`.foreach { v => attributes = new UnprefixedAttribute("lendsym.size", v, attributes) }
    obj.`@lstartsym`.foreach { v => attributes = new UnprefixedAttribute("lstartsym", v, attributes) }
    obj.`@lstartsym.size`.foreach { v => attributes = new UnprefixedAttribute("lstartsym.size", v, attributes) }
    obj.`@extender`.foreach { v => attributes = new UnprefixedAttribute("extender", v, attributes) }
    obj.`@place`.foreach { v => attributes = new UnprefixedAttribute("place", v, attributes) }
    obj.`@vgrp`.foreach { v => attributes = new UnprefixedAttribute("vgrp", v, attributes) }
    obj.`@ho`.foreach { v => attributes = new UnprefixedAttribute("ho", v, attributes) }
    obj.`@to`.foreach { v => attributes = new UnprefixedAttribute("to", v, attributes) }
    obj.`@vo`.foreach { v => attributes = new UnprefixedAttribute("vo", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }
    obj.`@orient`.foreach { v => attributes = new UnprefixedAttribute("orient", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "fingGrp", attributes, TopScope, true, children*)
  }
}

object FoliaDescWriter extends MeiModelDummyWriter[FoliaDesc] {
  def toXml(obj: FoliaDesc): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "foliaDesc", attributes, TopScope, true, children*)
  }
}

object FoliationWriter extends MeiModelDummyWriter[Foliation] {
  def toXml(obj: Foliation): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "foliation", attributes, TopScope, true, children*)
  }
}

object FoliumWriter extends MeiModelDummyWriter[Folium] {
  def toXml(obj: Folium): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@height`.foreach { v => attributes = new UnprefixedAttribute("height", v, attributes) }
    obj.`@width`.foreach { v => attributes = new UnprefixedAttribute("width", v, attributes) }
    obj.`@unit`.foreach { v => attributes = new UnprefixedAttribute("unit", v, attributes) }
    obj.`@recto`.foreach { v => attributes = new UnprefixedAttribute("recto", v, attributes) }
    obj.`@verso`.foreach { v => attributes = new UnprefixedAttribute("verso", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "folium", attributes, TopScope, true, children*)
  }
}

object FontWriter extends MeiModelDummyWriter[Font] {
  def toXml(obj: Font): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "font", Null, TopScope, true, children*)
  }
}

object FontFaceWriter extends MeiModelDummyWriter[FontFace] {
  def toXml(obj: FontFace): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "font-face", Null, TopScope, true, children*)
  }
}

object FontFaceFormatWriter extends MeiModelDummyWriter[FontFaceFormat] {
  def toXml(obj: FontFaceFormat): Elem = {

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "font-face-format", Null, TopScope, true, children*)
  }
}

object FontFaceNameWriter extends MeiModelDummyWriter[FontFaceName] {
  def toXml(obj: FontFaceName): Elem = {

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "font-face-name", Null, TopScope, true, children*)
  }
}

object FontFaceSrcWriter extends MeiModelDummyWriter[FontFaceSrc] {
  def toXml(obj: FontFaceSrc): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "font-face-src", Null, TopScope, true, children*)
  }
}

object FontFaceUriWriter extends MeiModelDummyWriter[FontFaceUri] {
  def toXml(obj: FontFaceUri): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "font-face-uri", Null, TopScope, true, children*)
  }
}

object ForeNameWriter extends MeiModelDummyWriter[ForeName] {
  def toXml(obj: ForeName): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@nonfiling`.foreach { v => attributes = new UnprefixedAttribute("nonfiling", v, attributes) }
    obj.`@nymref`.foreach { v => attributes = new UnprefixedAttribute("nymref", v, attributes) }
    obj.`@role`.foreach { v => attributes = new UnprefixedAttribute("role", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "foreName", attributes, TopScope, true, children*)
  }
}

object ForeignObjectWriter extends MeiModelDummyWriter[ForeignObject] {
  def toXml(obj: ForeignObject): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "foreignObject", Null, TopScope, true, children*)
  }
}

object FrontWriter extends MeiModelDummyWriter[Front] {
  def toXml(obj: Front): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "front", attributes, TopScope, true, children*)
  }
}

object FunderWriter extends MeiModelDummyWriter[Funder] {
  def toXml(obj: Funder): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "funder", attributes, TopScope, true, children*)
  }
}

object GWriter extends MeiModelDummyWriter[G] {
  def toXml(obj: G): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "g", Null, TopScope, true, children*)
  }
}

object GapWriter extends MeiModelDummyWriter[Gap] {
  def toXml(obj: Gap): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@unit`.foreach { v => attributes = new UnprefixedAttribute("unit", v, attributes) }
    obj.`@atleast`.foreach { v => attributes = new UnprefixedAttribute("atleast", v, attributes) }
    obj.`@atmost`.foreach { v => attributes = new UnprefixedAttribute("atmost", v, attributes) }
    obj.`@min`.foreach { v => attributes = new UnprefixedAttribute("min", v, attributes) }
    obj.`@max`.foreach { v => attributes = new UnprefixedAttribute("max", v, attributes) }
    obj.`@confidence`.foreach { v => attributes = new UnprefixedAttribute("confidence", v, attributes) }
    obj.`@extent`.foreach { v => attributes = new UnprefixedAttribute("extent", v, attributes) }
    obj.`@hand`.foreach { v => attributes = new UnprefixedAttribute("hand", v, attributes) }
    obj.`@reason`.foreach { v => attributes = new UnprefixedAttribute("reason", v, attributes) }

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "gap", attributes, TopScope, true, children*)
  }
}

object GenDescWriter extends MeiModelDummyWriter[GenDesc] {
  def toXml(obj: GenDesc): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }
    obj.`@ordered`.foreach { v => attributes = new UnprefixedAttribute("ordered", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "genDesc", attributes, TopScope, true, children*)
  }
}

object GenNameWriter extends MeiModelDummyWriter[GenName] {
  def toXml(obj: GenName): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@nonfiling`.foreach { v => attributes = new UnprefixedAttribute("nonfiling", v, attributes) }
    obj.`@nymref`.foreach { v => attributes = new UnprefixedAttribute("nymref", v, attributes) }
    obj.`@role`.foreach { v => attributes = new UnprefixedAttribute("role", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "genName", attributes, TopScope, true, children*)
  }
}

object GenStateWriter extends MeiModelDummyWriter[GenState] {
  def toXml(obj: GenState): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@data`.foreach { v => attributes = new UnprefixedAttribute("data", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }
    obj.`@xlink:actuate`.foreach { v => attributes = new UnprefixedAttribute("xlink:actuate", v, attributes) }
    obj.`@xlink:role`.foreach { v => attributes = new UnprefixedAttribute("xlink:role", v, attributes) }
    obj.`@xlink:show`.foreach { v => attributes = new UnprefixedAttribute("xlink:show", v, attributes) }
    obj.`@target`.foreach { v => attributes = new UnprefixedAttribute("target", v, attributes) }
    obj.`@targettype`.foreach { v => attributes = new UnprefixedAttribute("targettype", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "genState", attributes, TopScope, true, children*)
  }
}

object GenreWriter extends MeiModelDummyWriter[Genre] {
  def toXml(obj: Genre): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "genre", attributes, TopScope, true, children*)
  }
}

object GeogFeatWriter extends MeiModelDummyWriter[GeogFeat] {
  def toXml(obj: GeogFeat): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@nonfiling`.foreach { v => attributes = new UnprefixedAttribute("nonfiling", v, attributes) }
    obj.`@nymref`.foreach { v => attributes = new UnprefixedAttribute("nymref", v, attributes) }
    obj.`@role`.foreach { v => attributes = new UnprefixedAttribute("role", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "geogFeat", attributes, TopScope, true, children*)
  }
}

object GeogNameWriter extends MeiModelDummyWriter[GeogName] {
  def toXml(obj: GeogName): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@nonfiling`.foreach { v => attributes = new UnprefixedAttribute("nonfiling", v, attributes) }
    obj.`@nymref`.foreach { v => attributes = new UnprefixedAttribute("nymref", v, attributes) }
    obj.`@role`.foreach { v => attributes = new UnprefixedAttribute("role", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "geogName", attributes, TopScope, true, children*)
  }
}

object GlyphWriter extends MeiModelDummyWriter[Glyph] {
  def toXml(obj: Glyph): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "glyph", Null, TopScope, true, children*)
  }
}

object GlyphRefWriter extends MeiModelDummyWriter[GlyphRef] {
  def toXml(obj: GlyphRef): Elem = {

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "glyphRef", Null, TopScope, true, children*)
  }
}

object GraphicWriter extends MeiModelDummyWriter[Graphic] {
  def toXml(obj: Graphic): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@ulx`.foreach { v => attributes = new UnprefixedAttribute("ulx", v, attributes) }
    obj.`@uly`.foreach { v => attributes = new UnprefixedAttribute("uly", v, attributes) }
    obj.`@height`.foreach { v => attributes = new UnprefixedAttribute("height", v, attributes) }
    obj.`@width`.foreach { v => attributes = new UnprefixedAttribute("width", v, attributes) }
    obj.`@mimetype`.foreach { v => attributes = new UnprefixedAttribute("mimetype", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }
    obj.`@xlink:actuate`.foreach { v => attributes = new UnprefixedAttribute("xlink:actuate", v, attributes) }
    obj.`@xlink:role`.foreach { v => attributes = new UnprefixedAttribute("xlink:role", v, attributes) }
    obj.`@xlink:show`.foreach { v => attributes = new UnprefixedAttribute("xlink:show", v, attributes) }
    obj.`@target`.foreach { v => attributes = new UnprefixedAttribute("target", v, attributes) }
    obj.`@targettype`.foreach { v => attributes = new UnprefixedAttribute("targettype", v, attributes) }
    obj.`@startid`.foreach { v => attributes = new UnprefixedAttribute("startid", v, attributes) }
    obj.`@ho`.foreach { v => attributes = new UnprefixedAttribute("ho", v, attributes) }
    obj.`@to`.foreach { v => attributes = new UnprefixedAttribute("to", v, attributes) }
    obj.`@vo`.foreach { v => attributes = new UnprefixedAttribute("vo", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "graphic", attributes, TopScope, true, children*)
  }
}

object GroupWriter extends MeiModelDummyWriter[Group] {
  def toXml(obj: Group): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "group", attributes, TopScope, true, children*)
  }
}

object GrpSymWriter extends MeiModelDummyWriter[GrpSym] {
  def toXml(obj: GrpSym): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@symbol`.foreach { v => attributes = new UnprefixedAttribute("symbol", v, attributes) }
    obj.`@startid`.foreach { v => attributes = new UnprefixedAttribute("startid", v, attributes) }
    obj.`@endid`.foreach { v => attributes = new UnprefixedAttribute("endid", v, attributes) }
    obj.`@level`.foreach { v => attributes = new UnprefixedAttribute("level", v, attributes) }
    obj.`@altsym`.foreach { v => attributes = new UnprefixedAttribute("altsym", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@glyph.auth`.foreach { v => attributes = new UnprefixedAttribute("glyph.auth", v, attributes) }
    obj.`@glyph.uri`.foreach { v => attributes = new UnprefixedAttribute("glyph.uri", v, attributes) }
    obj.`@glyph.name`.foreach { v => attributes = new UnprefixedAttribute("glyph.name", v, attributes) }
    obj.`@glyph.num`.foreach { v => attributes = new UnprefixedAttribute("glyph.num", v, attributes) }
    obj.`@fontfam`.foreach { v => attributes = new UnprefixedAttribute("fontfam", v, attributes) }
    obj.`@fontname`.foreach { v => attributes = new UnprefixedAttribute("fontname", v, attributes) }
    obj.`@fontsize`.foreach { v => attributes = new UnprefixedAttribute("fontsize", v, attributes) }
    obj.`@fontstyle`.foreach { v => attributes = new UnprefixedAttribute("fontstyle", v, attributes) }
    obj.`@fontweight`.foreach { v => attributes = new UnprefixedAttribute("fontweight", v, attributes) }
    obj.`@letterspacing`.foreach { v => attributes = new UnprefixedAttribute("letterspacing", v, attributes) }
    obj.`@lineheight`.foreach { v => attributes = new UnprefixedAttribute("lineheight", v, attributes) }
    obj.`@ho`.foreach { v => attributes = new UnprefixedAttribute("ho", v, attributes) }
    obj.`@to`.foreach { v => attributes = new UnprefixedAttribute("to", v, attributes) }
    obj.`@vo`.foreach { v => attributes = new UnprefixedAttribute("vo", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "grpSym", attributes, TopScope, true, children*)
  }
}

object HandWriter extends MeiModelDummyWriter[Hand] {
  def toXml(obj: Hand): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@medium`.foreach { v => attributes = new UnprefixedAttribute("medium", v, attributes) }
    obj.`@initial`.foreach { v => attributes = new UnprefixedAttribute("initial", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "hand", attributes, TopScope, true, children*)
  }
}

object HandListWriter extends MeiModelDummyWriter[HandList] {
  def toXml(obj: HandList): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "handList", attributes, TopScope, true, children*)
  }
}

object HandShiftWriter extends MeiModelDummyWriter[HandShift] {
  def toXml(obj: HandShift): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@medium`.foreach { v => attributes = new UnprefixedAttribute("medium", v, attributes) }
    obj.`@character`.foreach { v => attributes = new UnprefixedAttribute("character", v, attributes) }
    obj.`@new`.foreach { v => attributes = new UnprefixedAttribute("new", v, attributes) }
    obj.`@old`.foreach { v => attributes = new UnprefixedAttribute("old", v, attributes) }

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "handShift", attributes, TopScope, true, children*)
  }
}

object HarmWriter extends MeiModelDummyWriter[Harm] {
  def toXml(obj: Harm): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@inth`.foreach { v => attributes = new UnprefixedAttribute("inth", v, attributes) }
    obj.`@form`.foreach { v => attributes = new UnprefixedAttribute("form", v, attributes) }
    obj.`@dur.ges`.foreach { v => attributes = new UnprefixedAttribute("dur.ges", v, attributes) }
    obj.`@dots.ges`.foreach { v => attributes = new UnprefixedAttribute("dots.ges", v, attributes) }
    obj.`@dur.metrical`.foreach { v => attributes = new UnprefixedAttribute("dur.metrical", v, attributes) }
    obj.`@dur.ppq`.foreach { v => attributes = new UnprefixedAttribute("dur.ppq", v, attributes) }
    obj.`@dur.real`.foreach { v => attributes = new UnprefixedAttribute("dur.real", v, attributes) }
    obj.`@dur.recip`.foreach { v => attributes = new UnprefixedAttribute("dur.recip", v, attributes) }
    obj.`@tstamp2.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp2.ges", v, attributes) }
    obj.`@tstamp2.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp2.real", v, attributes) }
    obj.`@when`.foreach { v => attributes = new UnprefixedAttribute("when", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@plist`.foreach { v => attributes = new UnprefixedAttribute("plist", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    obj.`@dur`.foreach { v => attributes = new UnprefixedAttribute("dur", v, attributes) }
    obj.`@startid`.foreach { v => attributes = new UnprefixedAttribute("startid", v, attributes) }
    obj.`@endid`.foreach { v => attributes = new UnprefixedAttribute("endid", v, attributes) }
    obj.`@tstamp2`.foreach { v => attributes = new UnprefixedAttribute("tstamp2", v, attributes) }
    obj.`@chordref`.foreach { v => attributes = new UnprefixedAttribute("chordref", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@lform`.foreach { v => attributes = new UnprefixedAttribute("lform", v, attributes) }
    obj.`@lwidth`.foreach { v => attributes = new UnprefixedAttribute("lwidth", v, attributes) }
    obj.`@lsegs`.foreach { v => attributes = new UnprefixedAttribute("lsegs", v, attributes) }
    obj.`@lendsym`.foreach { v => attributes = new UnprefixedAttribute("lendsym", v, attributes) }
    obj.`@lendsym.size`.foreach { v => attributes = new UnprefixedAttribute("lendsym.size", v, attributes) }
    obj.`@lstartsym`.foreach { v => attributes = new UnprefixedAttribute("lstartsym", v, attributes) }
    obj.`@lstartsym.size`.foreach { v => attributes = new UnprefixedAttribute("lstartsym.size", v, attributes) }
    obj.`@extender`.foreach { v => attributes = new UnprefixedAttribute("extender", v, attributes) }
    obj.`@place`.foreach { v => attributes = new UnprefixedAttribute("place", v, attributes) }
    obj.`@vgrp`.foreach { v => attributes = new UnprefixedAttribute("vgrp", v, attributes) }
    obj.`@ho`.foreach { v => attributes = new UnprefixedAttribute("ho", v, attributes) }
    obj.`@to`.foreach { v => attributes = new UnprefixedAttribute("to", v, attributes) }
    obj.`@vo`.foreach { v => attributes = new UnprefixedAttribute("vo", v, attributes) }
    obj.`@startho`.foreach { v => attributes = new UnprefixedAttribute("startho", v, attributes) }
    obj.`@endho`.foreach { v => attributes = new UnprefixedAttribute("endho", v, attributes) }
    obj.`@startto`.foreach { v => attributes = new UnprefixedAttribute("startto", v, attributes) }
    obj.`@endto`.foreach { v => attributes = new UnprefixedAttribute("endto", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }
    obj.`@rendgrid`.foreach { v => attributes = new UnprefixedAttribute("rendgrid", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "harm", attributes, TopScope, true, children*)
  }
}

object HeadWriter extends MeiModelDummyWriter[Head] {
  def toXml(obj: Head): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "head", attributes, TopScope, true, children*)
  }
}

object HeightWriter extends MeiModelDummyWriter[Height] {
  def toXml(obj: Height): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@unit`.foreach { v => attributes = new UnprefixedAttribute("unit", v, attributes) }
    obj.`@atleast`.foreach { v => attributes = new UnprefixedAttribute("atleast", v, attributes) }
    obj.`@atmost`.foreach { v => attributes = new UnprefixedAttribute("atmost", v, attributes) }
    obj.`@min`.foreach { v => attributes = new UnprefixedAttribute("min", v, attributes) }
    obj.`@max`.foreach { v => attributes = new UnprefixedAttribute("max", v, attributes) }
    obj.`@confidence`.foreach { v => attributes = new UnprefixedAttribute("confidence", v, attributes) }
    obj.`@quantity`.foreach { v => attributes = new UnprefixedAttribute("quantity", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "height", attributes, TopScope, true, children*)
  }
}

object HeraldryWriter extends MeiModelDummyWriter[Heraldry] {
  def toXml(obj: Heraldry): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "heraldry", attributes, TopScope, true, children*)
  }
}

object HexWriter extends MeiModelDummyWriter[Hex] {
  def toXml(obj: Hex): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "hex", attributes, TopScope, true, children*)
  }
}

object HistoryWriter extends MeiModelDummyWriter[History] {
  def toXml(obj: History): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "history", attributes, TopScope, true, children*)
  }
}

object HkernWriter extends MeiModelDummyWriter[Hkern] {
  def toXml(obj: Hkern): Elem = {

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "hkern", Null, TopScope, true, children*)
  }
}

object IdentifierWriter extends MeiModelDummyWriter[Identifier] {
  def toXml(obj: Identifier): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "identifier", attributes, TopScope, true, children*)
  }
}

object ImageWriter extends MeiModelDummyWriter[Image] {
  def toXml(obj: Image): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "image", Null, TopScope, true, children*)
  }
}

object ImprimaturWriter extends MeiModelDummyWriter[Imprimatur] {
  def toXml(obj: Imprimatur): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "imprimatur", attributes, TopScope, true, children*)
  }
}

object ImprintWriter extends MeiModelDummyWriter[Imprint] {
  def toXml(obj: Imprint): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "imprint", attributes, TopScope, true, children*)
  }
}

object IncipWriter extends MeiModelDummyWriter[Incip] {
  def toXml(obj: Incip): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "incip", attributes, TopScope, true, children*)
  }
}

object IncipCodeWriter extends MeiModelDummyWriter[IncipCode] {
  def toXml(obj: IncipCode): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@mimetype`.foreach { v => attributes = new UnprefixedAttribute("mimetype", v, attributes) }
    obj.`@xlink:actuate`.foreach { v => attributes = new UnprefixedAttribute("xlink:actuate", v, attributes) }
    obj.`@xlink:role`.foreach { v => attributes = new UnprefixedAttribute("xlink:role", v, attributes) }
    obj.`@xlink:show`.foreach { v => attributes = new UnprefixedAttribute("xlink:show", v, attributes) }
    obj.`@target`.foreach { v => attributes = new UnprefixedAttribute("target", v, attributes) }
    obj.`@targettype`.foreach { v => attributes = new UnprefixedAttribute("targettype", v, attributes) }
    obj.`@xml:space`.foreach { v => attributes = new UnprefixedAttribute("xml:space", v, attributes) }
    obj.`@form`.foreach { v => attributes = new UnprefixedAttribute("form", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "incipCode", attributes, TopScope, true, children*)
  }
}

object IncipTextWriter extends MeiModelDummyWriter[IncipText] {
  def toXml(obj: IncipText): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@xlink:actuate`.foreach { v => attributes = new UnprefixedAttribute("xlink:actuate", v, attributes) }
    obj.`@xlink:role`.foreach { v => attributes = new UnprefixedAttribute("xlink:role", v, attributes) }
    obj.`@xlink:show`.foreach { v => attributes = new UnprefixedAttribute("xlink:show", v, attributes) }
    obj.`@target`.foreach { v => attributes = new UnprefixedAttribute("target", v, attributes) }
    obj.`@targettype`.foreach { v => attributes = new UnprefixedAttribute("targettype", v, attributes) }
    obj.`@mimetype`.foreach { v => attributes = new UnprefixedAttribute("mimetype", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "incipText", attributes, TopScope, true, children*)
  }
}

object InscriptionWriter extends MeiModelDummyWriter[Inscription] {
  def toXml(obj: Inscription): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "inscription", attributes, TopScope, true, children*)
  }
}

object InstrDefWriter extends MeiModelDummyWriter[InstrDef] {
  def toXml(obj: InstrDef): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@midi.channel`.foreach { v => attributes = new UnprefixedAttribute("midi.channel", v, attributes) }
    obj.`@midi.duty`.foreach { v => attributes = new UnprefixedAttribute("midi.duty", v, attributes) }
    obj.`@midi.port`.foreach { v => attributes = new UnprefixedAttribute("midi.port", v, attributes) }
    obj.`@midi.track`.foreach { v => attributes = new UnprefixedAttribute("midi.track", v, attributes) }
    obj.`@midi.instrnum`.foreach { v => attributes = new UnprefixedAttribute("midi.instrnum", v, attributes) }
    obj.`@midi.instrname`.foreach { v => attributes = new UnprefixedAttribute("midi.instrname", v, attributes) }
    obj.`@midi.pan`.foreach { v => attributes = new UnprefixedAttribute("midi.pan", v, attributes) }
    obj.`@midi.patchname`.foreach { v => attributes = new UnprefixedAttribute("midi.patchname", v, attributes) }
    obj.`@midi.patchnum`.foreach { v => attributes = new UnprefixedAttribute("midi.patchnum", v, attributes) }
    obj.`@midi.volume`.foreach { v => attributes = new UnprefixedAttribute("midi.volume", v, attributes) }
    obj.`@azimuth`.foreach { v => attributes = new UnprefixedAttribute("azimuth", v, attributes) }
    obj.`@elevation`.foreach { v => attributes = new UnprefixedAttribute("elevation", v, attributes) }

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "instrDef", attributes, TopScope, true, children*)
  }
}

object InstrGrpWriter extends MeiModelDummyWriter[InstrGrp] {
  def toXml(obj: InstrGrp): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "instrGrp", attributes, TopScope, true, children*)
  }
}

object InterpretationWriter extends MeiModelDummyWriter[Interpretation] {
  def toXml(obj: Interpretation): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@data`.foreach { v => attributes = new UnprefixedAttribute("data", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "interpretation", attributes, TopScope, true, children*)
  }
}

object ItemWriter extends MeiModelDummyWriter[Item] {
  def toXml(obj: Item): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@data`.foreach { v => attributes = new UnprefixedAttribute("data", v, attributes) }
    obj.`@xlink:actuate`.foreach { v => attributes = new UnprefixedAttribute("xlink:actuate", v, attributes) }
    obj.`@xlink:role`.foreach { v => attributes = new UnprefixedAttribute("xlink:role", v, attributes) }
    obj.`@xlink:show`.foreach { v => attributes = new UnprefixedAttribute("xlink:show", v, attributes) }
    obj.`@target`.foreach { v => attributes = new UnprefixedAttribute("target", v, attributes) }
    obj.`@targettype`.foreach { v => attributes = new UnprefixedAttribute("targettype", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "item", attributes, TopScope, true, children*)
  }
}

object ItemListWriter extends MeiModelDummyWriter[ItemList] {
  def toXml(obj: ItemList): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "itemList", attributes, TopScope, true, children*)
  }
}

object KeyWriter extends MeiModelDummyWriter[Key] {
  def toXml(obj: Key): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@accid`.foreach { v => attributes = new UnprefixedAttribute("accid", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@mode`.foreach { v => attributes = new UnprefixedAttribute("mode", v, attributes) }
    obj.`@pname`.foreach { v => attributes = new UnprefixedAttribute("pname", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "key", attributes, TopScope, true, children*)
  }
}

object KeyAccidWriter extends MeiModelDummyWriter[KeyAccid] {
  def toXml(obj: KeyAccid): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@accid`.foreach { v => attributes = new UnprefixedAttribute("accid", v, attributes) }
    obj.`@pname`.foreach { v => attributes = new UnprefixedAttribute("pname", v, attributes) }
    obj.`@oct`.foreach { v => attributes = new UnprefixedAttribute("oct", v, attributes) }
    obj.`@altsym`.foreach { v => attributes = new UnprefixedAttribute("altsym", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@enclose`.foreach { v => attributes = new UnprefixedAttribute("enclose", v, attributes) }
    obj.`@glyph.auth`.foreach { v => attributes = new UnprefixedAttribute("glyph.auth", v, attributes) }
    obj.`@glyph.uri`.foreach { v => attributes = new UnprefixedAttribute("glyph.uri", v, attributes) }
    obj.`@glyph.name`.foreach { v => attributes = new UnprefixedAttribute("glyph.name", v, attributes) }
    obj.`@glyph.num`.foreach { v => attributes = new UnprefixedAttribute("glyph.num", v, attributes) }
    obj.`@loc`.foreach { v => attributes = new UnprefixedAttribute("loc", v, attributes) }
    obj.`@fontfam`.foreach { v => attributes = new UnprefixedAttribute("fontfam", v, attributes) }
    obj.`@fontname`.foreach { v => attributes = new UnprefixedAttribute("fontname", v, attributes) }
    obj.`@fontsize`.foreach { v => attributes = new UnprefixedAttribute("fontsize", v, attributes) }
    obj.`@fontstyle`.foreach { v => attributes = new UnprefixedAttribute("fontstyle", v, attributes) }
    obj.`@fontweight`.foreach { v => attributes = new UnprefixedAttribute("fontweight", v, attributes) }
    obj.`@letterspacing`.foreach { v => attributes = new UnprefixedAttribute("letterspacing", v, attributes) }
    obj.`@lineheight`.foreach { v => attributes = new UnprefixedAttribute("lineheight", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }
    obj.`@form`.foreach { v => attributes = new UnprefixedAttribute("form", v, attributes) }

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "keyAccid", attributes, TopScope, true, children*)
  }
}

object KeySigWriter extends MeiModelDummyWriter[KeySig] {
  def toXml(obj: KeySig): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@accid`.foreach { v => attributes = new UnprefixedAttribute("accid", v, attributes) }
    obj.`@mode`.foreach { v => attributes = new UnprefixedAttribute("mode", v, attributes) }
    obj.`@pname`.foreach { v => attributes = new UnprefixedAttribute("pname", v, attributes) }
    obj.`@sig`.foreach { v => attributes = new UnprefixedAttribute("sig", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@visible`.foreach { v => attributes = new UnprefixedAttribute("visible", v, attributes) }
    obj.`@cancelaccid`.foreach { v => attributes = new UnprefixedAttribute("cancelaccid", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "keySig", attributes, TopScope, true, children*)
  }
}

object LWriter extends MeiModelDummyWriter[L] {
  def toXml(obj: L): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@rhythm`.foreach { v => attributes = new UnprefixedAttribute("rhythm", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "l", attributes, TopScope, true, children*)
  }
}

object LabelWriter extends MeiModelDummyWriter[Label] {
  def toXml(obj: Label): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "label", attributes, TopScope, true, children*)
  }
}

object LabelAbbrWriter extends MeiModelDummyWriter[LabelAbbr] {
  def toXml(obj: LabelAbbr): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "labelAbbr", attributes, TopScope, true, children*)
  }
}

object LangUsageWriter extends MeiModelDummyWriter[LangUsage] {
  def toXml(obj: LangUsage): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@data`.foreach { v => attributes = new UnprefixedAttribute("data", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "langUsage", attributes, TopScope, true, children*)
  }
}

object LanguageWriter extends MeiModelDummyWriter[Language] {
  def toXml(obj: Language): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "language", attributes, TopScope, true, children*)
  }
}

object LayerWriter extends MeiModelDummyWriter[Layer] {
  def toXml(obj: Layer): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@cue`.foreach { v => attributes = new UnprefixedAttribute("cue", v, attributes) }
    obj.`@metcon`.foreach { v => attributes = new UnprefixedAttribute("metcon", v, attributes) }
    obj.`@def`.foreach { v => attributes = new UnprefixedAttribute("def", v, attributes) }
    obj.`@visible`.foreach { v => attributes = new UnprefixedAttribute("visible", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "layer", attributes, TopScope, true, children*)
  }
}

object LayerDefWriter extends MeiModelDummyWriter[LayerDef] {
  def toXml(obj: LayerDef): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@instr`.foreach { v => attributes = new UnprefixedAttribute("instr", v, attributes) }
    obj.`@tune.Hz`.foreach { v => attributes = new UnprefixedAttribute("tune.Hz", v, attributes) }
    obj.`@tune.pname`.foreach { v => attributes = new UnprefixedAttribute("tune.pname", v, attributes) }
    obj.`@tune.temper`.foreach { v => attributes = new UnprefixedAttribute("tune.temper", v, attributes) }
    obj.`@dur.default`.foreach { v => attributes = new UnprefixedAttribute("dur.default", v, attributes) }
    obj.`@num.default`.foreach { v => attributes = new UnprefixedAttribute("num.default", v, attributes) }
    obj.`@numbase.default`.foreach { v => attributes = new UnprefixedAttribute("numbase.default", v, attributes) }
    obj.`@oct.default`.foreach { v => attributes = new UnprefixedAttribute("oct.default", v, attributes) }
    obj.`@trans.diat`.foreach { v => attributes = new UnprefixedAttribute("trans.diat", v, attributes) }
    obj.`@trans.semi`.foreach { v => attributes = new UnprefixedAttribute("trans.semi", v, attributes) }
    obj.`@beam.color`.foreach { v => attributes = new UnprefixedAttribute("beam.color", v, attributes) }
    obj.`@beam.rend`.foreach { v => attributes = new UnprefixedAttribute("beam.rend", v, attributes) }
    obj.`@beam.slope`.foreach { v => attributes = new UnprefixedAttribute("beam.slope", v, attributes) }
    obj.`@text.fam`.foreach { v => attributes = new UnprefixedAttribute("text.fam", v, attributes) }
    obj.`@text.name`.foreach { v => attributes = new UnprefixedAttribute("text.name", v, attributes) }
    obj.`@text.size`.foreach { v => attributes = new UnprefixedAttribute("text.size", v, attributes) }
    obj.`@text.style`.foreach { v => attributes = new UnprefixedAttribute("text.style", v, attributes) }
    obj.`@text.weight`.foreach { v => attributes = new UnprefixedAttribute("text.weight", v, attributes) }
    obj.`@visible`.foreach { v => attributes = new UnprefixedAttribute("visible", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "layerDef", attributes, TopScope, true, children*)
  }
}

object LayoutWriter extends MeiModelDummyWriter[Layout] {
  def toXml(obj: Layout): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@cols`.foreach { v => attributes = new UnprefixedAttribute("cols", v, attributes) }
    obj.`@ruledlines`.foreach { v => attributes = new UnprefixedAttribute("ruledlines", v, attributes) }
    obj.`@writtenlines`.foreach { v => attributes = new UnprefixedAttribute("writtenlines", v, attributes) }
    obj.`@ruledstaves`.foreach { v => attributes = new UnprefixedAttribute("ruledstaves", v, attributes) }
    obj.`@writtenstaves`.foreach { v => attributes = new UnprefixedAttribute("writtenstaves", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "layout", attributes, TopScope, true, children*)
  }
}

object LayoutDescWriter extends MeiModelDummyWriter[LayoutDesc] {
  def toXml(obj: LayoutDesc): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "layoutDesc", attributes, TopScope, true, children*)
  }
}

object LbWriter extends MeiModelDummyWriter[Lb] {
  def toXml(obj: Lb): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "lb", attributes, TopScope, true, children*)
  }
}

object LemWriter extends MeiModelDummyWriter[Lem] {
  def toXml(obj: Lem): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@hand`.foreach { v => attributes = new UnprefixedAttribute("hand", v, attributes) }
    obj.`@seq`.foreach { v => attributes = new UnprefixedAttribute("seq", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cause`.foreach { v => attributes = new UnprefixedAttribute("cause", v, attributes) }
    obj.`@xlink:actuate`.foreach { v => attributes = new UnprefixedAttribute("xlink:actuate", v, attributes) }
    obj.`@xlink:role`.foreach { v => attributes = new UnprefixedAttribute("xlink:role", v, attributes) }
    obj.`@xlink:show`.foreach { v => attributes = new UnprefixedAttribute("xlink:show", v, attributes) }
    obj.`@target`.foreach { v => attributes = new UnprefixedAttribute("target", v, attributes) }
    obj.`@targettype`.foreach { v => attributes = new UnprefixedAttribute("targettype", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "lem", attributes, TopScope, true, children*)
  }
}

object LgWriter extends MeiModelDummyWriter[Lg] {
  def toXml(obj: Lg): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@place`.foreach { v => attributes = new UnprefixedAttribute("place", v, attributes) }
    obj.`@fontfam`.foreach { v => attributes = new UnprefixedAttribute("fontfam", v, attributes) }
    obj.`@fontname`.foreach { v => attributes = new UnprefixedAttribute("fontname", v, attributes) }
    obj.`@fontsize`.foreach { v => attributes = new UnprefixedAttribute("fontsize", v, attributes) }
    obj.`@fontstyle`.foreach { v => attributes = new UnprefixedAttribute("fontstyle", v, attributes) }
    obj.`@fontweight`.foreach { v => attributes = new UnprefixedAttribute("fontweight", v, attributes) }
    obj.`@letterspacing`.foreach { v => attributes = new UnprefixedAttribute("letterspacing", v, attributes) }
    obj.`@lineheight`.foreach { v => attributes = new UnprefixedAttribute("lineheight", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "lg", attributes, TopScope, true, children*)
  }
}

object LiWriter extends MeiModelDummyWriter[Li] {
  def toXml(obj: Li): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "li", attributes, TopScope, true, children*)
  }
}

object LibrettistWriter extends MeiModelDummyWriter[Librettist] {
  def toXml(obj: Librettist): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "librettist", attributes, TopScope, true, children*)
  }
}

object LigatureWriter extends MeiModelDummyWriter[Ligature] {
  def toXml(obj: Ligature): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@form`.foreach { v => attributes = new UnprefixedAttribute("form", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "ligature", attributes, TopScope, true, children*)
  }
}

object LineWriter extends MeiModelDummyWriter[Line] {
  def toXml(obj: Line): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "line", Null, TopScope, true, children*)
  }
}

object LinearGradientWriter extends MeiModelDummyWriter[LinearGradient] {
  def toXml(obj: LinearGradient): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "linearGradient", Null, TopScope, true, children*)
  }
}

object ListWriter extends MeiModelDummyWriter[List] {
  def toXml(obj: List): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }
    obj.`@form`.foreach { v => attributes = new UnprefixedAttribute("form", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "list", attributes, TopScope, true, children*)
  }
}

object LocusWriter extends MeiModelDummyWriter[Locus] {
  def toXml(obj: Locus): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@scheme`.foreach { v => attributes = new UnprefixedAttribute("scheme", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@from`.foreach { v => attributes = new UnprefixedAttribute("from", v, attributes) }
    obj.`@to`.foreach { v => attributes = new UnprefixedAttribute("to", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "locus", attributes, TopScope, true, children*)
  }
}

object LocusGrpWriter extends MeiModelDummyWriter[LocusGrp] {
  def toXml(obj: LocusGrp): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@scheme`.foreach { v => attributes = new UnprefixedAttribute("scheme", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "locusGrp", attributes, TopScope, true, children*)
  }
}

object LyricistWriter extends MeiModelDummyWriter[Lyricist] {
  def toXml(obj: Lyricist): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "lyricist", attributes, TopScope, true, children*)
  }
}

object ManifestationWriter extends MeiModelDummyWriter[Manifestation] {
  def toXml(obj: Manifestation): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@comptype`.foreach { v => attributes = new UnprefixedAttribute("comptype", v, attributes) }
    obj.`@data`.foreach { v => attributes = new UnprefixedAttribute("data", v, attributes) }
    obj.`@xlink:actuate`.foreach { v => attributes = new UnprefixedAttribute("xlink:actuate", v, attributes) }
    obj.`@xlink:role`.foreach { v => attributes = new UnprefixedAttribute("xlink:role", v, attributes) }
    obj.`@xlink:show`.foreach { v => attributes = new UnprefixedAttribute("xlink:show", v, attributes) }
    obj.`@target`.foreach { v => attributes = new UnprefixedAttribute("target", v, attributes) }
    obj.`@targettype`.foreach { v => attributes = new UnprefixedAttribute("targettype", v, attributes) }
    obj.`@recordtype`.foreach { v => attributes = new UnprefixedAttribute("recordtype", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }
    obj.`@singleton`.foreach { v => attributes = new UnprefixedAttribute("singleton", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "manifestation", attributes, TopScope, true, children*)
  }
}

object ManifestationListWriter extends MeiModelDummyWriter[ManifestationList] {
  def toXml(obj: ManifestationList): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "manifestationList", attributes, TopScope, true, children*)
  }
}

object MappingWriter extends MeiModelDummyWriter[Mapping] {
  def toXml(obj: Mapping): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "mapping", attributes, TopScope, true, children*)
  }
}

object MarkerWriter extends MeiModelDummyWriter[Marker] {
  def toXml(obj: Marker): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "marker", Null, TopScope, true, children*)
  }
}

object MaskWriter extends MeiModelDummyWriter[Mask] {
  def toXml(obj: Mask): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "mask", Null, TopScope, true, children*)
  }
}

object MdivWriter extends MeiModelDummyWriter[Mdiv] {
  def toXml(obj: Mdiv): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }
    obj.`@attacca`.foreach { v => attributes = new UnprefixedAttribute("attacca", v, attributes) }
    obj.`@when`.foreach { v => attributes = new UnprefixedAttribute("when", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "mdiv", attributes, TopScope, true, children*)
  }
}

object MeiWriter extends MeiModelDummyWriter[Mei] {
  def toXml(obj: Mei): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@meiversion`.foreach { v => attributes = new UnprefixedAttribute("meiversion", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "mei", attributes, TopScope, true, children*)
  }
}

object MeiCorpusWriter extends MeiModelDummyWriter[MeiCorpus] {
  def toXml(obj: MeiCorpus): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@meiversion`.foreach { v => attributes = new UnprefixedAttribute("meiversion", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "meiCorpus", attributes, TopScope, true, children*)
  }
}

object MeiHeadWriter extends MeiModelDummyWriter[MeiHead] {
  def toXml(obj: MeiHead): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@meiversion`.foreach { v => attributes = new UnprefixedAttribute("meiversion", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "meiHead", attributes, TopScope, true, children*)
  }
}

object MensurWriter extends MeiModelDummyWriter[Mensur] {
  def toXml(obj: Mensur): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@num`.foreach { v => attributes = new UnprefixedAttribute("num", v, attributes) }
    obj.`@numbase`.foreach { v => attributes = new UnprefixedAttribute("numbase", v, attributes) }
    obj.`@modusmaior`.foreach { v => attributes = new UnprefixedAttribute("modusmaior", v, attributes) }
    obj.`@modusminor`.foreach { v => attributes = new UnprefixedAttribute("modusminor", v, attributes) }
    obj.`@prolatio`.foreach { v => attributes = new UnprefixedAttribute("prolatio", v, attributes) }
    obj.`@tempus`.foreach { v => attributes = new UnprefixedAttribute("tempus", v, attributes) }
    obj.`@divisio`.foreach { v => attributes = new UnprefixedAttribute("divisio", v, attributes) }
    obj.`@level`.foreach { v => attributes = new UnprefixedAttribute("level", v, attributes) }
    obj.`@altsym`.foreach { v => attributes = new UnprefixedAttribute("altsym", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@glyph.auth`.foreach { v => attributes = new UnprefixedAttribute("glyph.auth", v, attributes) }
    obj.`@glyph.uri`.foreach { v => attributes = new UnprefixedAttribute("glyph.uri", v, attributes) }
    obj.`@glyph.name`.foreach { v => attributes = new UnprefixedAttribute("glyph.name", v, attributes) }
    obj.`@glyph.num`.foreach { v => attributes = new UnprefixedAttribute("glyph.num", v, attributes) }
    obj.`@loc`.foreach { v => attributes = new UnprefixedAttribute("loc", v, attributes) }
    obj.`@fontfam`.foreach { v => attributes = new UnprefixedAttribute("fontfam", v, attributes) }
    obj.`@fontname`.foreach { v => attributes = new UnprefixedAttribute("fontname", v, attributes) }
    obj.`@fontsize`.foreach { v => attributes = new UnprefixedAttribute("fontsize", v, attributes) }
    obj.`@fontstyle`.foreach { v => attributes = new UnprefixedAttribute("fontstyle", v, attributes) }
    obj.`@fontweight`.foreach { v => attributes = new UnprefixedAttribute("fontweight", v, attributes) }
    obj.`@letterspacing`.foreach { v => attributes = new UnprefixedAttribute("letterspacing", v, attributes) }
    obj.`@lineheight`.foreach { v => attributes = new UnprefixedAttribute("lineheight", v, attributes) }
    obj.`@slash`.foreach { v => attributes = new UnprefixedAttribute("slash", v, attributes) }
    obj.`@dot`.foreach { v => attributes = new UnprefixedAttribute("dot", v, attributes) }
    obj.`@form`.foreach { v => attributes = new UnprefixedAttribute("form", v, attributes) }
    obj.`@orient`.foreach { v => attributes = new UnprefixedAttribute("orient", v, attributes) }
    obj.`@sign`.foreach { v => attributes = new UnprefixedAttribute("sign", v, attributes) }

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "mensur", attributes, TopScope, true, children*)
  }
}

object MensurationWriter extends MeiModelDummyWriter[Mensuration] {
  def toXml(obj: Mensuration): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@num`.foreach { v => attributes = new UnprefixedAttribute("num", v, attributes) }
    obj.`@numbase`.foreach { v => attributes = new UnprefixedAttribute("numbase", v, attributes) }
    obj.`@modusmaior`.foreach { v => attributes = new UnprefixedAttribute("modusmaior", v, attributes) }
    obj.`@modusminor`.foreach { v => attributes = new UnprefixedAttribute("modusminor", v, attributes) }
    obj.`@prolatio`.foreach { v => attributes = new UnprefixedAttribute("prolatio", v, attributes) }
    obj.`@tempus`.foreach { v => attributes = new UnprefixedAttribute("tempus", v, attributes) }
    obj.`@divisio`.foreach { v => attributes = new UnprefixedAttribute("divisio", v, attributes) }
    obj.`@level`.foreach { v => attributes = new UnprefixedAttribute("level", v, attributes) }
    obj.`@altsym`.foreach { v => attributes = new UnprefixedAttribute("altsym", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@glyph.auth`.foreach { v => attributes = new UnprefixedAttribute("glyph.auth", v, attributes) }
    obj.`@glyph.uri`.foreach { v => attributes = new UnprefixedAttribute("glyph.uri", v, attributes) }
    obj.`@glyph.name`.foreach { v => attributes = new UnprefixedAttribute("glyph.name", v, attributes) }
    obj.`@glyph.num`.foreach { v => attributes = new UnprefixedAttribute("glyph.num", v, attributes) }
    obj.`@loc`.foreach { v => attributes = new UnprefixedAttribute("loc", v, attributes) }
    obj.`@fontfam`.foreach { v => attributes = new UnprefixedAttribute("fontfam", v, attributes) }
    obj.`@fontname`.foreach { v => attributes = new UnprefixedAttribute("fontname", v, attributes) }
    obj.`@fontsize`.foreach { v => attributes = new UnprefixedAttribute("fontsize", v, attributes) }
    obj.`@fontstyle`.foreach { v => attributes = new UnprefixedAttribute("fontstyle", v, attributes) }
    obj.`@fontweight`.foreach { v => attributes = new UnprefixedAttribute("fontweight", v, attributes) }
    obj.`@letterspacing`.foreach { v => attributes = new UnprefixedAttribute("letterspacing", v, attributes) }
    obj.`@lineheight`.foreach { v => attributes = new UnprefixedAttribute("lineheight", v, attributes) }
    obj.`@slash`.foreach { v => attributes = new UnprefixedAttribute("slash", v, attributes) }
    obj.`@dot`.foreach { v => attributes = new UnprefixedAttribute("dot", v, attributes) }
    obj.`@form`.foreach { v => attributes = new UnprefixedAttribute("form", v, attributes) }
    obj.`@orient`.foreach { v => attributes = new UnprefixedAttribute("orient", v, attributes) }
    obj.`@sign`.foreach { v => attributes = new UnprefixedAttribute("sign", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "mensuration", attributes, TopScope, true, children*)
  }
}

object MetaMarkWriter extends MeiModelDummyWriter[MetaMark] {
  def toXml(obj: MetaMark): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@when`.foreach { v => attributes = new UnprefixedAttribute("when", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@plist`.foreach { v => attributes = new UnprefixedAttribute("plist", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    obj.`@startid`.foreach { v => attributes = new UnprefixedAttribute("startid", v, attributes) }
    obj.`@endid`.foreach { v => attributes = new UnprefixedAttribute("endid", v, attributes) }
    obj.`@tstamp2`.foreach { v => attributes = new UnprefixedAttribute("tstamp2", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@instant`.foreach { v => attributes = new UnprefixedAttribute("instant", v, attributes) }
    obj.`@state`.foreach { v => attributes = new UnprefixedAttribute("state", v, attributes) }
    obj.`@hand`.foreach { v => attributes = new UnprefixedAttribute("hand", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }
    obj.`@seq`.foreach { v => attributes = new UnprefixedAttribute("seq", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@place`.foreach { v => attributes = new UnprefixedAttribute("place", v, attributes) }
    obj.`@vgrp`.foreach { v => attributes = new UnprefixedAttribute("vgrp", v, attributes) }
    obj.`@dur.ges`.foreach { v => attributes = new UnprefixedAttribute("dur.ges", v, attributes) }
    obj.`@dots.ges`.foreach { v => attributes = new UnprefixedAttribute("dots.ges", v, attributes) }
    obj.`@dur.metrical`.foreach { v => attributes = new UnprefixedAttribute("dur.metrical", v, attributes) }
    obj.`@dur.ppq`.foreach { v => attributes = new UnprefixedAttribute("dur.ppq", v, attributes) }
    obj.`@dur.real`.foreach { v => attributes = new UnprefixedAttribute("dur.real", v, attributes) }
    obj.`@dur.recip`.foreach { v => attributes = new UnprefixedAttribute("dur.recip", v, attributes) }
    obj.`@tstamp2.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp2.ges", v, attributes) }
    obj.`@tstamp2.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp2.real", v, attributes) }
    obj.`@xlink:actuate`.foreach { v => attributes = new UnprefixedAttribute("xlink:actuate", v, attributes) }
    obj.`@xlink:role`.foreach { v => attributes = new UnprefixedAttribute("xlink:role", v, attributes) }
    obj.`@xlink:show`.foreach { v => attributes = new UnprefixedAttribute("xlink:show", v, attributes) }
    obj.`@target`.foreach { v => attributes = new UnprefixedAttribute("target", v, attributes) }
    obj.`@targettype`.foreach { v => attributes = new UnprefixedAttribute("targettype", v, attributes) }
    obj.`@function`.foreach { v => attributes = new UnprefixedAttribute("function", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "metaMark", attributes, TopScope, true, children*)
  }
}

object MetaTextWriter extends MeiModelDummyWriter[MetaText] {
  def toXml(obj: MetaText): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "metaText", attributes, TopScope, true, children*)
  }
}

object MetadataWriter extends MeiModelDummyWriter[Metadata] {
  def toXml(obj: Metadata): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "metadata", Null, TopScope, true, children*)
  }
}

object MeterWriter extends MeiModelDummyWriter[Meter] {
  def toXml(obj: Meter): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@count`.foreach { v => attributes = new UnprefixedAttribute("count", v, attributes) }
    obj.`@sym`.foreach { v => attributes = new UnprefixedAttribute("sym", v, attributes) }
    obj.`@unit`.foreach { v => attributes = new UnprefixedAttribute("unit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "meter", attributes, TopScope, true, children*)
  }
}

object MidiWriter extends MeiModelDummyWriter[Midi] {
  def toXml(obj: Midi): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "midi", attributes, TopScope, true, children*)
  }
}

object MissingGlyphWriter extends MeiModelDummyWriter[MissingGlyph] {
  def toXml(obj: MissingGlyph): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "missing-glyph", Null, TopScope, true, children*)
  }
}

object MonogrWriter extends MeiModelDummyWriter[Monogr] {
  def toXml(obj: Monogr): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@data`.foreach { v => attributes = new UnprefixedAttribute("data", v, attributes) }
    obj.`@xlink:actuate`.foreach { v => attributes = new UnprefixedAttribute("xlink:actuate", v, attributes) }
    obj.`@xlink:role`.foreach { v => attributes = new UnprefixedAttribute("xlink:role", v, attributes) }
    obj.`@xlink:show`.foreach { v => attributes = new UnprefixedAttribute("xlink:show", v, attributes) }
    obj.`@target`.foreach { v => attributes = new UnprefixedAttribute("target", v, attributes) }
    obj.`@targettype`.foreach { v => attributes = new UnprefixedAttribute("targettype", v, attributes) }
    obj.`@recordtype`.foreach { v => attributes = new UnprefixedAttribute("recordtype", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "monogr", attributes, TopScope, true, children*)
  }
}

object MpathWriter extends MeiModelDummyWriter[Mpath] {
  def toXml(obj: Mpath): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "mpath", Null, TopScope, true, children*)
  }
}

object MusicWriter extends MeiModelDummyWriter[Music] {
  def toXml(obj: Music): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@meiversion`.foreach { v => attributes = new UnprefixedAttribute("meiversion", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "music", attributes, TopScope, true, children*)
  }
}

object NameWriter extends MeiModelDummyWriter[Name] {
  def toXml(obj: Name): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@nonfiling`.foreach { v => attributes = new UnprefixedAttribute("nonfiling", v, attributes) }
    obj.`@nymref`.foreach { v => attributes = new UnprefixedAttribute("nymref", v, attributes) }
    obj.`@role`.foreach { v => attributes = new UnprefixedAttribute("role", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "name", attributes, TopScope, true, children*)
  }
}

object NameLinkWriter extends MeiModelDummyWriter[NameLink] {
  def toXml(obj: NameLink): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@nonfiling`.foreach { v => attributes = new UnprefixedAttribute("nonfiling", v, attributes) }
    obj.`@nymref`.foreach { v => attributes = new UnprefixedAttribute("nymref", v, attributes) }
    obj.`@role`.foreach { v => attributes = new UnprefixedAttribute("role", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "nameLink", attributes, TopScope, true, children*)
  }
}

object NamespaceWriter extends MeiModelDummyWriter[Namespace] {
  def toXml(obj: Namespace): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    attributes = new UnprefixedAttribute("name", obj.`@name`, attributes)
    obj.`@prefix`.foreach { v => attributes = new UnprefixedAttribute("prefix", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "namespace", attributes, TopScope, true, children*)
  }
}

object NormalizationWriter extends MeiModelDummyWriter[Normalization] {
  def toXml(obj: Normalization): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@data`.foreach { v => attributes = new UnprefixedAttribute("data", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@method`.foreach { v => attributes = new UnprefixedAttribute("method", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "normalization", attributes, TopScope, true, children*)
  }
}

object NoteWriter extends MeiModelDummyWriter[Note] {
  def toXml(obj: Note): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@accid`.foreach { v => attributes = new UnprefixedAttribute("accid", v, attributes) }
    obj.`@artic`.foreach { v => attributes = new UnprefixedAttribute("artic", v, attributes) }
    obj.`@deg`.foreach { v => attributes = new UnprefixedAttribute("deg", v, attributes) }
    obj.`@intm`.foreach { v => attributes = new UnprefixedAttribute("intm", v, attributes) }
    obj.`@mfunc`.foreach { v => attributes = new UnprefixedAttribute("mfunc", v, attributes) }
    obj.`@fermata`.foreach { v => attributes = new UnprefixedAttribute("fermata", v, attributes) }
    obj.`@pclass`.foreach { v => attributes = new UnprefixedAttribute("pclass", v, attributes) }
    obj.`@psolfa`.foreach { v => attributes = new UnprefixedAttribute("psolfa", v, attributes) }
    obj.`@accid.ges`.foreach { v => attributes = new UnprefixedAttribute("accid.ges", v, attributes) }
    obj.`@artic.ges`.foreach { v => attributes = new UnprefixedAttribute("artic.ges", v, attributes) }
    obj.`@dur.ges`.foreach { v => attributes = new UnprefixedAttribute("dur.ges", v, attributes) }
    obj.`@dots.ges`.foreach { v => attributes = new UnprefixedAttribute("dots.ges", v, attributes) }
    obj.`@dur.metrical`.foreach { v => attributes = new UnprefixedAttribute("dur.metrical", v, attributes) }
    obj.`@dur.ppq`.foreach { v => attributes = new UnprefixedAttribute("dur.ppq", v, attributes) }
    obj.`@dur.real`.foreach { v => attributes = new UnprefixedAttribute("dur.real", v, attributes) }
    obj.`@dur.recip`.foreach { v => attributes = new UnprefixedAttribute("dur.recip", v, attributes) }
    obj.`@instr`.foreach { v => attributes = new UnprefixedAttribute("instr", v, attributes) }
    obj.`@vel`.foreach { v => attributes = new UnprefixedAttribute("vel", v, attributes) }
    obj.`@num`.foreach { v => attributes = new UnprefixedAttribute("num", v, attributes) }
    obj.`@numbase`.foreach { v => attributes = new UnprefixedAttribute("numbase", v, attributes) }
    obj.`@oct.ges`.foreach { v => attributes = new UnprefixedAttribute("oct.ges", v, attributes) }
    obj.`@pname.ges`.foreach { v => attributes = new UnprefixedAttribute("pname.ges", v, attributes) }
    obj.`@pnum`.foreach { v => attributes = new UnprefixedAttribute("pnum", v, attributes) }
    obj.`@tab.fing`.foreach { v => attributes = new UnprefixedAttribute("tab.fing", v, attributes) }
    obj.`@tab.fret`.foreach { v => attributes = new UnprefixedAttribute("tab.fret", v, attributes) }
    obj.`@tab.line`.foreach { v => attributes = new UnprefixedAttribute("tab.line", v, attributes) }
    obj.`@tab.string`.foreach { v => attributes = new UnprefixedAttribute("tab.string", v, attributes) }
    obj.`@tab.course`.foreach { v => attributes = new UnprefixedAttribute("tab.course", v, attributes) }
    obj.`@extremis`.foreach { v => attributes = new UnprefixedAttribute("extremis", v, attributes) }
    obj.`@colored`.foreach { v => attributes = new UnprefixedAttribute("colored", v, attributes) }
    obj.`@cue`.foreach { v => attributes = new UnprefixedAttribute("cue", v, attributes) }
    obj.`@dur`.foreach { v => attributes = new UnprefixedAttribute("dur", v, attributes) }
    obj.`@when`.foreach { v => attributes = new UnprefixedAttribute("when", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@pname`.foreach { v => attributes = new UnprefixedAttribute("pname", v, attributes) }
    obj.`@oct`.foreach { v => attributes = new UnprefixedAttribute("oct", v, attributes) }
    obj.`@dur.quality`.foreach { v => attributes = new UnprefixedAttribute("dur.quality", v, attributes) }
    obj.`@altsym`.foreach { v => attributes = new UnprefixedAttribute("altsym", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@enclose`.foreach { v => attributes = new UnprefixedAttribute("enclose", v, attributes) }
    obj.`@glyph.auth`.foreach { v => attributes = new UnprefixedAttribute("glyph.auth", v, attributes) }
    obj.`@glyph.uri`.foreach { v => attributes = new UnprefixedAttribute("glyph.uri", v, attributes) }
    obj.`@glyph.name`.foreach { v => attributes = new UnprefixedAttribute("glyph.name", v, attributes) }
    obj.`@glyph.num`.foreach { v => attributes = new UnprefixedAttribute("glyph.num", v, attributes) }
    obj.`@head.altsym`.foreach { v => attributes = new UnprefixedAttribute("head.altsym", v, attributes) }
    obj.`@head.auth`.foreach { v => attributes = new UnprefixedAttribute("head.auth", v, attributes) }
    obj.`@head.color`.foreach { v => attributes = new UnprefixedAttribute("head.color", v, attributes) }
    obj.`@head.fill`.foreach { v => attributes = new UnprefixedAttribute("head.fill", v, attributes) }
    obj.`@head.fillcolor`.foreach { v => attributes = new UnprefixedAttribute("head.fillcolor", v, attributes) }
    obj.`@head.mod`.foreach { v => attributes = new UnprefixedAttribute("head.mod", v, attributes) }
    obj.`@head.rotation`.foreach { v => attributes = new UnprefixedAttribute("head.rotation", v, attributes) }
    obj.`@head.shape`.foreach { v => attributes = new UnprefixedAttribute("head.shape", v, attributes) }
    obj.`@head.visible`.foreach { v => attributes = new UnprefixedAttribute("head.visible", v, attributes) }
    obj.`@loc`.foreach { v => attributes = new UnprefixedAttribute("loc", v, attributes) }
    obj.`@stem.form`.foreach { v => attributes = new UnprefixedAttribute("stem.form", v, attributes) }
    obj.`@stem.dir`.foreach { v => attributes = new UnprefixedAttribute("stem.dir", v, attributes) }
    obj.`@stem.len`.foreach { v => attributes = new UnprefixedAttribute("stem.len", v, attributes) }
    obj.`@stem.mod`.foreach { v => attributes = new UnprefixedAttribute("stem.mod", v, attributes) }
    obj.`@stem.pos`.foreach { v => attributes = new UnprefixedAttribute("stem.pos", v, attributes) }
    obj.`@stem.sameas`.foreach { v => attributes = new UnprefixedAttribute("stem.sameas", v, attributes) }
    obj.`@stem.visible`.foreach { v => attributes = new UnprefixedAttribute("stem.visible", v, attributes) }
    obj.`@stem.x`.foreach { v => attributes = new UnprefixedAttribute("stem.x", v, attributes) }
    obj.`@stem.y`.foreach { v => attributes = new UnprefixedAttribute("stem.y", v, attributes) }
    obj.`@fontfam`.foreach { v => attributes = new UnprefixedAttribute("fontfam", v, attributes) }
    obj.`@fontname`.foreach { v => attributes = new UnprefixedAttribute("fontname", v, attributes) }
    obj.`@fontsize`.foreach { v => attributes = new UnprefixedAttribute("fontsize", v, attributes) }
    obj.`@fontstyle`.foreach { v => attributes = new UnprefixedAttribute("fontstyle", v, attributes) }
    obj.`@fontweight`.foreach { v => attributes = new UnprefixedAttribute("fontweight", v, attributes) }
    obj.`@letterspacing`.foreach { v => attributes = new UnprefixedAttribute("letterspacing", v, attributes) }
    obj.`@lineheight`.foreach { v => attributes = new UnprefixedAttribute("lineheight", v, attributes) }
    obj.`@visible`.foreach { v => attributes = new UnprefixedAttribute("visible", v, attributes) }
    obj.`@ho`.foreach { v => attributes = new UnprefixedAttribute("ho", v, attributes) }
    obj.`@to`.foreach { v => attributes = new UnprefixedAttribute("to", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }
    obj.`@lig`.foreach { v => attributes = new UnprefixedAttribute("lig", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "note", attributes, TopScope, true, children*)
  }
}

object NoteOffWriter extends MeiModelDummyWriter[NoteOff] {
  def toXml(obj: NoteOff): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    attributes = new UnprefixedAttribute("num", obj.`@num`, attributes)

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "noteOff", attributes, TopScope, true, children*)
  }
}

object NoteOnWriter extends MeiModelDummyWriter[NoteOn] {
  def toXml(obj: NoteOn): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    attributes = new UnprefixedAttribute("num", obj.`@num`, attributes)

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "noteOn", attributes, TopScope, true, children*)
  }
}

object NotesStmtWriter extends MeiModelDummyWriter[NotesStmt] {
  def toXml(obj: NotesStmt): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "notesStmt", attributes, TopScope, true, children*)
  }
}

object NumWriter extends MeiModelDummyWriter[Num] {
  def toXml(obj: Num): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@unit`.foreach { v => attributes = new UnprefixedAttribute("unit", v, attributes) }
    obj.`@atleast`.foreach { v => attributes = new UnprefixedAttribute("atleast", v, attributes) }
    obj.`@atmost`.foreach { v => attributes = new UnprefixedAttribute("atmost", v, attributes) }
    obj.`@min`.foreach { v => attributes = new UnprefixedAttribute("min", v, attributes) }
    obj.`@max`.foreach { v => attributes = new UnprefixedAttribute("max", v, attributes) }
    obj.`@confidence`.foreach { v => attributes = new UnprefixedAttribute("confidence", v, attributes) }
    obj.`@value`.foreach { v => attributes = new UnprefixedAttribute("value", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "num", attributes, TopScope, true, children*)
  }
}

object OrigWriter extends MeiModelDummyWriter[Orig] {
  def toXml(obj: Orig): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@unit`.foreach { v => attributes = new UnprefixedAttribute("unit", v, attributes) }
    obj.`@atleast`.foreach { v => attributes = new UnprefixedAttribute("atleast", v, attributes) }
    obj.`@atmost`.foreach { v => attributes = new UnprefixedAttribute("atmost", v, attributes) }
    obj.`@min`.foreach { v => attributes = new UnprefixedAttribute("min", v, attributes) }
    obj.`@max`.foreach { v => attributes = new UnprefixedAttribute("max", v, attributes) }
    obj.`@confidence`.foreach { v => attributes = new UnprefixedAttribute("confidence", v, attributes) }
    obj.`@extent`.foreach { v => attributes = new UnprefixedAttribute("extent", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "orig", attributes, TopScope, true, children*)
  }
}

object OrnamWriter extends MeiModelDummyWriter[Ornam] {
  def toXml(obj: Ornam): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@dur.ges`.foreach { v => attributes = new UnprefixedAttribute("dur.ges", v, attributes) }
    obj.`@dots.ges`.foreach { v => attributes = new UnprefixedAttribute("dots.ges", v, attributes) }
    obj.`@dur.metrical`.foreach { v => attributes = new UnprefixedAttribute("dur.metrical", v, attributes) }
    obj.`@dur.ppq`.foreach { v => attributes = new UnprefixedAttribute("dur.ppq", v, attributes) }
    obj.`@dur.real`.foreach { v => attributes = new UnprefixedAttribute("dur.real", v, attributes) }
    obj.`@dur.recip`.foreach { v => attributes = new UnprefixedAttribute("dur.recip", v, attributes) }
    obj.`@tstamp2.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp2.ges", v, attributes) }
    obj.`@tstamp2.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp2.real", v, attributes) }
    obj.`@when`.foreach { v => attributes = new UnprefixedAttribute("when", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@plist`.foreach { v => attributes = new UnprefixedAttribute("plist", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    obj.`@dur`.foreach { v => attributes = new UnprefixedAttribute("dur", v, attributes) }
    obj.`@startid`.foreach { v => attributes = new UnprefixedAttribute("startid", v, attributes) }
    obj.`@endid`.foreach { v => attributes = new UnprefixedAttribute("endid", v, attributes) }
    obj.`@tstamp2`.foreach { v => attributes = new UnprefixedAttribute("tstamp2", v, attributes) }
    obj.`@altsym`.foreach { v => attributes = new UnprefixedAttribute("altsym", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@enclose`.foreach { v => attributes = new UnprefixedAttribute("enclose", v, attributes) }
    obj.`@place`.foreach { v => attributes = new UnprefixedAttribute("place", v, attributes) }
    obj.`@vgrp`.foreach { v => attributes = new UnprefixedAttribute("vgrp", v, attributes) }
    obj.`@ho`.foreach { v => attributes = new UnprefixedAttribute("ho", v, attributes) }
    obj.`@to`.foreach { v => attributes = new UnprefixedAttribute("to", v, attributes) }
    obj.`@vo`.foreach { v => attributes = new UnprefixedAttribute("vo", v, attributes) }
    obj.`@startho`.foreach { v => attributes = new UnprefixedAttribute("startho", v, attributes) }
    obj.`@endho`.foreach { v => attributes = new UnprefixedAttribute("endho", v, attributes) }
    obj.`@startto`.foreach { v => attributes = new UnprefixedAttribute("startto", v, attributes) }
    obj.`@endto`.foreach { v => attributes = new UnprefixedAttribute("endto", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "ornam", attributes, TopScope, true, children*)
  }
}

object OtherCharWriter extends MeiModelDummyWriter[OtherChar] {
  def toXml(obj: OtherChar): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "otherChar", attributes, TopScope, true, children*)
  }
}

object PWriter extends MeiModelDummyWriter[P] {
  def toXml(obj: P): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "p", attributes, TopScope, true, children*)
  }
}

object PadWriter extends MeiModelDummyWriter[Pad] {
  def toXml(obj: Pad): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@when`.foreach { v => attributes = new UnprefixedAttribute("when", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@width`.foreach { v => attributes = new UnprefixedAttribute("width", v, attributes) }

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "pad", attributes, TopScope, true, children*)
  }
}

object PartWriter extends MeiModelDummyWriter[Part] {
  def toXml(obj: Part): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "part", attributes, TopScope, true, children*)
  }
}

object PartsWriter extends MeiModelDummyWriter[Parts] {
  def toXml(obj: Parts): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "parts", attributes, TopScope, true, children*)
  }
}

object PatchWriter extends MeiModelDummyWriter[Patch] {
  def toXml(obj: Patch): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@unit`.foreach { v => attributes = new UnprefixedAttribute("unit", v, attributes) }
    obj.`@instant`.foreach { v => attributes = new UnprefixedAttribute("instant", v, attributes) }
    obj.`@state`.foreach { v => attributes = new UnprefixedAttribute("state", v, attributes) }
    obj.`@hand`.foreach { v => attributes = new UnprefixedAttribute("hand", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }
    obj.`@seq`.foreach { v => attributes = new UnprefixedAttribute("seq", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }
    attributes = new UnprefixedAttribute("attached.to", obj.`@attached.to`, attributes)
    obj.`@attached.by`.foreach { v => attributes = new UnprefixedAttribute("attached.by", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "patch", attributes, TopScope, true, children*)
  }
}

object PathWriter extends MeiModelDummyWriter[Path] {
  def toXml(obj: Path): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "path", Null, TopScope, true, children*)
  }
}

object PatternWriter extends MeiModelDummyWriter[Pattern] {
  def toXml(obj: Pattern): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "pattern", Null, TopScope, true, children*)
  }
}

object PbWriter extends MeiModelDummyWriter[Pb] {
  def toXml(obj: Pb): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xlink:actuate`.foreach { v => attributes = new UnprefixedAttribute("xlink:actuate", v, attributes) }
    obj.`@xlink:role`.foreach { v => attributes = new UnprefixedAttribute("xlink:role", v, attributes) }
    obj.`@xlink:show`.foreach { v => attributes = new UnprefixedAttribute("xlink:show", v, attributes) }
    obj.`@target`.foreach { v => attributes = new UnprefixedAttribute("target", v, attributes) }
    obj.`@targettype`.foreach { v => attributes = new UnprefixedAttribute("targettype", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@when`.foreach { v => attributes = new UnprefixedAttribute("when", v, attributes) }
    obj.`@folium`.foreach { v => attributes = new UnprefixedAttribute("folium", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "pb", attributes, TopScope, true, children*)
  }
}

object PerfDurationWriter extends MeiModelDummyWriter[PerfDuration] {
  def toXml(obj: PerfDuration): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@isodur`.foreach { v => attributes = new UnprefixedAttribute("isodur", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "perfDuration", attributes, TopScope, true, children*)
  }
}

object PerfMediumWriter extends MeiModelDummyWriter[PerfMedium] {
  def toXml(obj: PerfMedium): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "perfMedium", attributes, TopScope, true, children*)
  }
}

object PerfResWriter extends MeiModelDummyWriter[PerfRes] {
  def toXml(obj: PerfRes): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@adlib`.foreach { v => attributes = new UnprefixedAttribute("adlib", v, attributes) }
    obj.`@count`.foreach { v => attributes = new UnprefixedAttribute("count", v, attributes) }
    obj.`@trans.diat`.foreach { v => attributes = new UnprefixedAttribute("trans.diat", v, attributes) }
    obj.`@trans.semi`.foreach { v => attributes = new UnprefixedAttribute("trans.semi", v, attributes) }
    obj.`@solo`.foreach { v => attributes = new UnprefixedAttribute("solo", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "perfRes", attributes, TopScope, true, children*)
  }
}

object PerfResListWriter extends MeiModelDummyWriter[PerfResList] {
  def toXml(obj: PerfResList): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@adlib`.foreach { v => attributes = new UnprefixedAttribute("adlib", v, attributes) }
    obj.`@count`.foreach { v => attributes = new UnprefixedAttribute("count", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "perfResList", attributes, TopScope, true, children*)
  }
}

object PerformanceWriter extends MeiModelDummyWriter[Performance] {
  def toXml(obj: Performance): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "performance", attributes, TopScope, true, children*)
  }
}

object PeriodNameWriter extends MeiModelDummyWriter[PeriodName] {
  def toXml(obj: PeriodName): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@nonfiling`.foreach { v => attributes = new UnprefixedAttribute("nonfiling", v, attributes) }
    obj.`@nymref`.foreach { v => attributes = new UnprefixedAttribute("nymref", v, attributes) }
    obj.`@role`.foreach { v => attributes = new UnprefixedAttribute("role", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "periodName", attributes, TopScope, true, children*)
  }
}

object PersNameWriter extends MeiModelDummyWriter[PersName] {
  def toXml(obj: PersName): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@nonfiling`.foreach { v => attributes = new UnprefixedAttribute("nonfiling", v, attributes) }
    obj.`@nymref`.foreach { v => attributes = new UnprefixedAttribute("nymref", v, attributes) }
    obj.`@role`.foreach { v => attributes = new UnprefixedAttribute("role", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "persName", attributes, TopScope, true, children*)
  }
}

object PgDescWriter extends MeiModelDummyWriter[PgDesc] {
  def toXml(obj: PgDesc): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "pgDesc", attributes, TopScope, true, children*)
  }
}

object PgFootWriter extends MeiModelDummyWriter[PgFoot] {
  def toXml(obj: PgFoot): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@func`.foreach { v => attributes = new UnprefixedAttribute("func", v, attributes) }
    obj.`@halign`.foreach { v => attributes = new UnprefixedAttribute("halign", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "pgFoot", attributes, TopScope, true, children*)
  }
}

object PgHeadWriter extends MeiModelDummyWriter[PgHead] {
  def toXml(obj: PgHead): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@func`.foreach { v => attributes = new UnprefixedAttribute("func", v, attributes) }
    obj.`@halign`.foreach { v => attributes = new UnprefixedAttribute("halign", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "pgHead", attributes, TopScope, true, children*)
  }
}

object PhraseWriter extends MeiModelDummyWriter[Phrase] {
  def toXml(obj: Phrase): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@join`.foreach { v => attributes = new UnprefixedAttribute("join", v, attributes) }
    obj.`@dur.ges`.foreach { v => attributes = new UnprefixedAttribute("dur.ges", v, attributes) }
    obj.`@dots.ges`.foreach { v => attributes = new UnprefixedAttribute("dots.ges", v, attributes) }
    obj.`@dur.metrical`.foreach { v => attributes = new UnprefixedAttribute("dur.metrical", v, attributes) }
    obj.`@dur.ppq`.foreach { v => attributes = new UnprefixedAttribute("dur.ppq", v, attributes) }
    obj.`@dur.real`.foreach { v => attributes = new UnprefixedAttribute("dur.real", v, attributes) }
    obj.`@dur.recip`.foreach { v => attributes = new UnprefixedAttribute("dur.recip", v, attributes) }
    obj.`@tstamp2.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp2.ges", v, attributes) }
    obj.`@tstamp2.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp2.real", v, attributes) }
    obj.`@when`.foreach { v => attributes = new UnprefixedAttribute("when", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@plist`.foreach { v => attributes = new UnprefixedAttribute("plist", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    obj.`@dur`.foreach { v => attributes = new UnprefixedAttribute("dur", v, attributes) }
    obj.`@startid`.foreach { v => attributes = new UnprefixedAttribute("startid", v, attributes) }
    obj.`@endid`.foreach { v => attributes = new UnprefixedAttribute("endid", v, attributes) }
    obj.`@tstamp2`.foreach { v => attributes = new UnprefixedAttribute("tstamp2", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@ho`.foreach { v => attributes = new UnprefixedAttribute("ho", v, attributes) }
    obj.`@to`.foreach { v => attributes = new UnprefixedAttribute("to", v, attributes) }
    obj.`@vo`.foreach { v => attributes = new UnprefixedAttribute("vo", v, attributes) }
    obj.`@startho`.foreach { v => attributes = new UnprefixedAttribute("startho", v, attributes) }
    obj.`@endho`.foreach { v => attributes = new UnprefixedAttribute("endho", v, attributes) }
    obj.`@startto`.foreach { v => attributes = new UnprefixedAttribute("startto", v, attributes) }
    obj.`@endto`.foreach { v => attributes = new UnprefixedAttribute("endto", v, attributes) }
    obj.`@startvo`.foreach { v => attributes = new UnprefixedAttribute("startvo", v, attributes) }
    obj.`@endvo`.foreach { v => attributes = new UnprefixedAttribute("endvo", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }
    obj.`@x2`.foreach { v => attributes = new UnprefixedAttribute("x2", v, attributes) }
    obj.`@y2`.foreach { v => attributes = new UnprefixedAttribute("y2", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "phrase", attributes, TopScope, true, children*)
  }
}

object PhysDescWriter extends MeiModelDummyWriter[PhysDesc] {
  def toXml(obj: PhysDesc): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "physDesc", attributes, TopScope, true, children*)
  }
}

object PhysLocWriter extends MeiModelDummyWriter[PhysLoc] {
  def toXml(obj: PhysLoc): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "physLoc", attributes, TopScope, true, children*)
  }
}

object PhysMediumWriter extends MeiModelDummyWriter[PhysMedium] {
  def toXml(obj: PhysMedium): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "physMedium", attributes, TopScope, true, children*)
  }
}

object PlateNumWriter extends MeiModelDummyWriter[PlateNum] {
  def toXml(obj: PlateNum): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "plateNum", attributes, TopScope, true, children*)
  }
}

object PlayingSpeedWriter extends MeiModelDummyWriter[PlayingSpeed] {
  def toXml(obj: PlayingSpeed): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "playingSpeed", attributes, TopScope, true, children*)
  }
}

object PlicaWriter extends MeiModelDummyWriter[Plica] {
  def toXml(obj: Plica): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@dir`.foreach { v => attributes = new UnprefixedAttribute("dir", v, attributes) }
    obj.`@len`.foreach { v => attributes = new UnprefixedAttribute("len", v, attributes) }

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "plica", attributes, TopScope, true, children*)
  }
}

object PolygonWriter extends MeiModelDummyWriter[Polygon] {
  def toXml(obj: Polygon): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "polygon", Null, TopScope, true, children*)
  }
}

object PolylineWriter extends MeiModelDummyWriter[Polyline] {
  def toXml(obj: Polyline): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "polyline", Null, TopScope, true, children*)
  }
}

object PortWriter extends MeiModelDummyWriter[Port] {
  def toXml(obj: Port): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    attributes = new UnprefixedAttribute("num", obj.`@num`, attributes)

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "port", attributes, TopScope, true, children*)
  }
}

object PostBoxWriter extends MeiModelDummyWriter[PostBox] {
  def toXml(obj: PostBox): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "postBox", attributes, TopScope, true, children*)
  }
}

object PostCodeWriter extends MeiModelDummyWriter[PostCode] {
  def toXml(obj: PostCode): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "postCode", attributes, TopScope, true, children*)
  }
}

object PriceWriter extends MeiModelDummyWriter[Price] {
  def toXml(obj: Price): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@amount`.foreach { v => attributes = new UnprefixedAttribute("amount", v, attributes) }
    obj.`@currency`.foreach { v => attributes = new UnprefixedAttribute("currency", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "price", attributes, TopScope, true, children*)
  }
}

object ProgWriter extends MeiModelDummyWriter[Prog] {
  def toXml(obj: Prog): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    attributes = new UnprefixedAttribute("num", obj.`@num`, attributes)

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "prog", attributes, TopScope, true, children*)
  }
}

object ProjectDescWriter extends MeiModelDummyWriter[ProjectDesc] {
  def toXml(obj: ProjectDesc): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@data`.foreach { v => attributes = new UnprefixedAttribute("data", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "projectDesc", attributes, TopScope, true, children*)
  }
}

object PropNameWriter extends MeiModelDummyWriter[PropName] {
  def toXml(obj: PropName): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    attributes = new UnprefixedAttribute("type", obj.`@type`, attributes)

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "propName", attributes, TopScope, true, children*)
  }
}

object PropValueWriter extends MeiModelDummyWriter[PropValue] {
  def toXml(obj: PropValue): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "propValue", attributes, TopScope, true, children*)
  }
}

object ProportWriter extends MeiModelDummyWriter[Proport] {
  def toXml(obj: Proport): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@num`.foreach { v => attributes = new UnprefixedAttribute("num", v, attributes) }
    obj.`@numbase`.foreach { v => attributes = new UnprefixedAttribute("numbase", v, attributes) }
    obj.`@altsym`.foreach { v => attributes = new UnprefixedAttribute("altsym", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@glyph.auth`.foreach { v => attributes = new UnprefixedAttribute("glyph.auth", v, attributes) }
    obj.`@glyph.uri`.foreach { v => attributes = new UnprefixedAttribute("glyph.uri", v, attributes) }
    obj.`@glyph.name`.foreach { v => attributes = new UnprefixedAttribute("glyph.name", v, attributes) }
    obj.`@glyph.num`.foreach { v => attributes = new UnprefixedAttribute("glyph.num", v, attributes) }
    obj.`@loc`.foreach { v => attributes = new UnprefixedAttribute("loc", v, attributes) }
    obj.`@fontfam`.foreach { v => attributes = new UnprefixedAttribute("fontfam", v, attributes) }
    obj.`@fontname`.foreach { v => attributes = new UnprefixedAttribute("fontname", v, attributes) }
    obj.`@fontsize`.foreach { v => attributes = new UnprefixedAttribute("fontsize", v, attributes) }
    obj.`@fontstyle`.foreach { v => attributes = new UnprefixedAttribute("fontstyle", v, attributes) }
    obj.`@fontweight`.foreach { v => attributes = new UnprefixedAttribute("fontweight", v, attributes) }
    obj.`@letterspacing`.foreach { v => attributes = new UnprefixedAttribute("letterspacing", v, attributes) }
    obj.`@lineheight`.foreach { v => attributes = new UnprefixedAttribute("lineheight", v, attributes) }

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "proport", attributes, TopScope, true, children*)
  }
}

object ProvenanceWriter extends MeiModelDummyWriter[Provenance] {
  def toXml(obj: Provenance): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "provenance", attributes, TopScope, true, children*)
  }
}

object PtrWriter extends MeiModelDummyWriter[Ptr] {
  def toXml(obj: Ptr): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@mimetype`.foreach { v => attributes = new UnprefixedAttribute("mimetype", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }
    obj.`@xlink:actuate`.foreach { v => attributes = new UnprefixedAttribute("xlink:actuate", v, attributes) }
    obj.`@xlink:role`.foreach { v => attributes = new UnprefixedAttribute("xlink:role", v, attributes) }
    obj.`@xlink:show`.foreach { v => attributes = new UnprefixedAttribute("xlink:show", v, attributes) }
    obj.`@target`.foreach { v => attributes = new UnprefixedAttribute("target", v, attributes) }
    obj.`@targettype`.foreach { v => attributes = new UnprefixedAttribute("targettype", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "ptr", attributes, TopScope, true, children*)
  }
}

object PubPlaceWriter extends MeiModelDummyWriter[PubPlace] {
  def toXml(obj: PubPlace): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "pubPlace", attributes, TopScope, true, children*)
  }
}

object PubStmtWriter extends MeiModelDummyWriter[PubStmt] {
  def toXml(obj: PubStmt): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "pubStmt", attributes, TopScope, true, children*)
  }
}

object PublisherWriter extends MeiModelDummyWriter[Publisher] {
  def toXml(obj: Publisher): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "publisher", attributes, TopScope, true, children*)
  }
}

object QWriter extends MeiModelDummyWriter[Q] {
  def toXml(obj: Q): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@altrend`.foreach { v => attributes = new UnprefixedAttribute("altrend", v, attributes) }
    obj.`@rend`.foreach { v => attributes = new UnprefixedAttribute("rend", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "q", attributes, TopScope, true, children*)
  }
}

object QuoteWriter extends MeiModelDummyWriter[Quote] {
  def toXml(obj: Quote): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@altrend`.foreach { v => attributes = new UnprefixedAttribute("altrend", v, attributes) }
    obj.`@rend`.foreach { v => attributes = new UnprefixedAttribute("rend", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "quote", attributes, TopScope, true, children*)
  }
}

object RadialGradientWriter extends MeiModelDummyWriter[RadialGradient] {
  def toXml(obj: RadialGradient): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "radialGradient", Null, TopScope, true, children*)
  }
}

object RdgWriter extends MeiModelDummyWriter[Rdg] {
  def toXml(obj: Rdg): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@hand`.foreach { v => attributes = new UnprefixedAttribute("hand", v, attributes) }
    obj.`@seq`.foreach { v => attributes = new UnprefixedAttribute("seq", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cause`.foreach { v => attributes = new UnprefixedAttribute("cause", v, attributes) }
    obj.`@xlink:actuate`.foreach { v => attributes = new UnprefixedAttribute("xlink:actuate", v, attributes) }
    obj.`@xlink:role`.foreach { v => attributes = new UnprefixedAttribute("xlink:role", v, attributes) }
    obj.`@xlink:show`.foreach { v => attributes = new UnprefixedAttribute("xlink:show", v, attributes) }
    obj.`@target`.foreach { v => attributes = new UnprefixedAttribute("target", v, attributes) }
    obj.`@targettype`.foreach { v => attributes = new UnprefixedAttribute("targettype", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "rdg", attributes, TopScope, true, children*)
  }
}

object RecipientWriter extends MeiModelDummyWriter[Recipient] {
  def toXml(obj: Recipient): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "recipient", attributes, TopScope, true, children*)
  }
}

object RecordingWriter extends MeiModelDummyWriter[Recording] {
  def toXml(obj: Recording): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@data`.foreach { v => attributes = new UnprefixedAttribute("data", v, attributes) }
    obj.`@begin`.foreach { v => attributes = new UnprefixedAttribute("begin", v, attributes) }
    obj.`@end`.foreach { v => attributes = new UnprefixedAttribute("end", v, attributes) }
    obj.`@betype`.foreach { v => attributes = new UnprefixedAttribute("betype", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }
    obj.`@startid`.foreach { v => attributes = new UnprefixedAttribute("startid", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "recording", attributes, TopScope, true, children*)
  }
}

object RectWriter extends MeiModelDummyWriter[Rect] {
  def toXml(obj: Rect): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "rect", Null, TopScope, true, children*)
  }
}

object RefWriter extends MeiModelDummyWriter[Ref] {
  def toXml(obj: Ref): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@mimetype`.foreach { v => attributes = new UnprefixedAttribute("mimetype", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }
    obj.`@xlink:actuate`.foreach { v => attributes = new UnprefixedAttribute("xlink:actuate", v, attributes) }
    obj.`@xlink:role`.foreach { v => attributes = new UnprefixedAttribute("xlink:role", v, attributes) }
    obj.`@xlink:show`.foreach { v => attributes = new UnprefixedAttribute("xlink:show", v, attributes) }
    obj.`@target`.foreach { v => attributes = new UnprefixedAttribute("target", v, attributes) }
    obj.`@targettype`.foreach { v => attributes = new UnprefixedAttribute("targettype", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "ref", attributes, TopScope, true, children*)
  }
}

object RefrainWriter extends MeiModelDummyWriter[Refrain] {
  def toXml(obj: Refrain): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@place`.foreach { v => attributes = new UnprefixedAttribute("place", v, attributes) }
    obj.`@fontfam`.foreach { v => attributes = new UnprefixedAttribute("fontfam", v, attributes) }
    obj.`@fontname`.foreach { v => attributes = new UnprefixedAttribute("fontname", v, attributes) }
    obj.`@fontsize`.foreach { v => attributes = new UnprefixedAttribute("fontsize", v, attributes) }
    obj.`@fontstyle`.foreach { v => attributes = new UnprefixedAttribute("fontstyle", v, attributes) }
    obj.`@fontweight`.foreach { v => attributes = new UnprefixedAttribute("fontweight", v, attributes) }
    obj.`@letterspacing`.foreach { v => attributes = new UnprefixedAttribute("letterspacing", v, attributes) }
    obj.`@lineheight`.foreach { v => attributes = new UnprefixedAttribute("lineheight", v, attributes) }
    obj.`@to`.foreach { v => attributes = new UnprefixedAttribute("to", v, attributes) }
    obj.`@vo`.foreach { v => attributes = new UnprefixedAttribute("vo", v, attributes) }
    obj.`@voltasym`.foreach { v => attributes = new UnprefixedAttribute("voltasym", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "refrain", attributes, TopScope, true, children*)
  }
}

object RegWriter extends MeiModelDummyWriter[Reg] {
  def toXml(obj: Reg): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@unit`.foreach { v => attributes = new UnprefixedAttribute("unit", v, attributes) }
    obj.`@atleast`.foreach { v => attributes = new UnprefixedAttribute("atleast", v, attributes) }
    obj.`@atmost`.foreach { v => attributes = new UnprefixedAttribute("atmost", v, attributes) }
    obj.`@min`.foreach { v => attributes = new UnprefixedAttribute("min", v, attributes) }
    obj.`@max`.foreach { v => attributes = new UnprefixedAttribute("max", v, attributes) }
    obj.`@confidence`.foreach { v => attributes = new UnprefixedAttribute("confidence", v, attributes) }
    obj.`@extent`.foreach { v => attributes = new UnprefixedAttribute("extent", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "reg", attributes, TopScope, true, children*)
  }
}

object RegionWriter extends MeiModelDummyWriter[Region] {
  def toXml(obj: Region): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@nonfiling`.foreach { v => attributes = new UnprefixedAttribute("nonfiling", v, attributes) }
    obj.`@nymref`.foreach { v => attributes = new UnprefixedAttribute("nymref", v, attributes) }
    obj.`@role`.foreach { v => attributes = new UnprefixedAttribute("role", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "region", attributes, TopScope, true, children*)
  }
}

object RelatedItemWriter extends MeiModelDummyWriter[RelatedItem] {
  def toXml(obj: RelatedItem): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@data`.foreach { v => attributes = new UnprefixedAttribute("data", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xlink:actuate`.foreach { v => attributes = new UnprefixedAttribute("xlink:actuate", v, attributes) }
    obj.`@xlink:role`.foreach { v => attributes = new UnprefixedAttribute("xlink:role", v, attributes) }
    obj.`@xlink:show`.foreach { v => attributes = new UnprefixedAttribute("xlink:show", v, attributes) }
    obj.`@target`.foreach { v => attributes = new UnprefixedAttribute("target", v, attributes) }
    obj.`@targettype`.foreach { v => attributes = new UnprefixedAttribute("targettype", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }
    attributes = new UnprefixedAttribute("rel", obj.`@rel`, attributes)

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "relatedItem", attributes, TopScope, true, children*)
  }
}

object RelationWriter extends MeiModelDummyWriter[Relation] {
  def toXml(obj: Relation): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@plist`.foreach { v => attributes = new UnprefixedAttribute("plist", v, attributes) }
    obj.`@xlink:actuate`.foreach { v => attributes = new UnprefixedAttribute("xlink:actuate", v, attributes) }
    obj.`@xlink:role`.foreach { v => attributes = new UnprefixedAttribute("xlink:role", v, attributes) }
    obj.`@xlink:show`.foreach { v => attributes = new UnprefixedAttribute("xlink:show", v, attributes) }
    obj.`@target`.foreach { v => attributes = new UnprefixedAttribute("target", v, attributes) }
    obj.`@targettype`.foreach { v => attributes = new UnprefixedAttribute("targettype", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }
    attributes = new UnprefixedAttribute("rel", obj.`@rel`, attributes)

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "relation", attributes, TopScope, true, children*)
  }
}

object RelationListWriter extends MeiModelDummyWriter[RelationList] {
  def toXml(obj: RelationList): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "relationList", attributes, TopScope, true, children*)
  }
}

object RendWriter extends MeiModelDummyWriter[Rend] {
  def toXml(obj: Rend): Elem = {
    var attributes: MetaData = Null
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@glyph.auth`.foreach { v => attributes = new UnprefixedAttribute("glyph.auth", v, attributes) }
    obj.`@glyph.uri`.foreach { v => attributes = new UnprefixedAttribute("glyph.uri", v, attributes) }
    obj.`@halign`.foreach { v => attributes = new UnprefixedAttribute("halign", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@altrend`.foreach { v => attributes = new UnprefixedAttribute("altrend", v, attributes) }
    obj.`@rend`.foreach { v => attributes = new UnprefixedAttribute("rend", v, attributes) }
    obj.`@fontfam`.foreach { v => attributes = new UnprefixedAttribute("fontfam", v, attributes) }
    obj.`@fontname`.foreach { v => attributes = new UnprefixedAttribute("fontname", v, attributes) }
    obj.`@fontsize`.foreach { v => attributes = new UnprefixedAttribute("fontsize", v, attributes) }
    obj.`@fontstyle`.foreach { v => attributes = new UnprefixedAttribute("fontstyle", v, attributes) }
    obj.`@fontweight`.foreach { v => attributes = new UnprefixedAttribute("fontweight", v, attributes) }
    obj.`@letterspacing`.foreach { v => attributes = new UnprefixedAttribute("letterspacing", v, attributes) }
    obj.`@lineheight`.foreach { v => attributes = new UnprefixedAttribute("lineheight", v, attributes) }
    obj.`@valign`.foreach { v => attributes = new UnprefixedAttribute("valign", v, attributes) }
    obj.`@xml:space`.foreach { v => attributes = new UnprefixedAttribute("xml:space", v, attributes) }
    obj.`@rotation`.foreach { v => attributes = new UnprefixedAttribute("rotation", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "rend", attributes, TopScope, true, children*)
  }
}

object RepositoryWriter extends MeiModelDummyWriter[Repository] {
  def toXml(obj: Repository): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@nonfiling`.foreach { v => attributes = new UnprefixedAttribute("nonfiling", v, attributes) }
    obj.`@nymref`.foreach { v => attributes = new UnprefixedAttribute("nymref", v, attributes) }
    obj.`@role`.foreach { v => attributes = new UnprefixedAttribute("role", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "repository", attributes, TopScope, true, children*)
  }
}

object RespWriter extends MeiModelDummyWriter[Resp] {
  def toXml(obj: Resp): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "resp", attributes, TopScope, true, children*)
  }
}

object RespStmtWriter extends MeiModelDummyWriter[RespStmt] {
  def toXml(obj: RespStmt): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "respStmt", attributes, TopScope, true, children*)
  }
}

object RestWriter extends MeiModelDummyWriter[Rest] {
  def toXml(obj: Rest): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@dur.ges`.foreach { v => attributes = new UnprefixedAttribute("dur.ges", v, attributes) }
    obj.`@dots.ges`.foreach { v => attributes = new UnprefixedAttribute("dots.ges", v, attributes) }
    obj.`@dur.metrical`.foreach { v => attributes = new UnprefixedAttribute("dur.metrical", v, attributes) }
    obj.`@dur.ppq`.foreach { v => attributes = new UnprefixedAttribute("dur.ppq", v, attributes) }
    obj.`@dur.real`.foreach { v => attributes = new UnprefixedAttribute("dur.real", v, attributes) }
    obj.`@dur.recip`.foreach { v => attributes = new UnprefixedAttribute("dur.recip", v, attributes) }
    obj.`@num`.foreach { v => attributes = new UnprefixedAttribute("num", v, attributes) }
    obj.`@numbase`.foreach { v => attributes = new UnprefixedAttribute("numbase", v, attributes) }
    obj.`@cue`.foreach { v => attributes = new UnprefixedAttribute("cue", v, attributes) }
    obj.`@dur`.foreach { v => attributes = new UnprefixedAttribute("dur", v, attributes) }
    obj.`@when`.foreach { v => attributes = new UnprefixedAttribute("when", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@altsym`.foreach { v => attributes = new UnprefixedAttribute("altsym", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@enclose`.foreach { v => attributes = new UnprefixedAttribute("enclose", v, attributes) }
    obj.`@glyph.auth`.foreach { v => attributes = new UnprefixedAttribute("glyph.auth", v, attributes) }
    obj.`@glyph.uri`.foreach { v => attributes = new UnprefixedAttribute("glyph.uri", v, attributes) }
    obj.`@glyph.name`.foreach { v => attributes = new UnprefixedAttribute("glyph.name", v, attributes) }
    obj.`@glyph.num`.foreach { v => attributes = new UnprefixedAttribute("glyph.num", v, attributes) }
    obj.`@spaces`.foreach { v => attributes = new UnprefixedAttribute("spaces", v, attributes) }
    obj.`@loc`.foreach { v => attributes = new UnprefixedAttribute("loc", v, attributes) }
    obj.`@ploc`.foreach { v => attributes = new UnprefixedAttribute("ploc", v, attributes) }
    obj.`@oloc`.foreach { v => attributes = new UnprefixedAttribute("oloc", v, attributes) }
    obj.`@fontfam`.foreach { v => attributes = new UnprefixedAttribute("fontfam", v, attributes) }
    obj.`@fontname`.foreach { v => attributes = new UnprefixedAttribute("fontname", v, attributes) }
    obj.`@fontsize`.foreach { v => attributes = new UnprefixedAttribute("fontsize", v, attributes) }
    obj.`@fontstyle`.foreach { v => attributes = new UnprefixedAttribute("fontstyle", v, attributes) }
    obj.`@fontweight`.foreach { v => attributes = new UnprefixedAttribute("fontweight", v, attributes) }
    obj.`@letterspacing`.foreach { v => attributes = new UnprefixedAttribute("letterspacing", v, attributes) }
    obj.`@lineheight`.foreach { v => attributes = new UnprefixedAttribute("lineheight", v, attributes) }
    obj.`@ho`.foreach { v => attributes = new UnprefixedAttribute("ho", v, attributes) }
    obj.`@to`.foreach { v => attributes = new UnprefixedAttribute("to", v, attributes) }
    obj.`@vo`.foreach { v => attributes = new UnprefixedAttribute("vo", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "rest", attributes, TopScope, true, children*)
  }
}

object RestoreWriter extends MeiModelDummyWriter[Restore] {
  def toXml(obj: Restore): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@unit`.foreach { v => attributes = new UnprefixedAttribute("unit", v, attributes) }
    obj.`@atleast`.foreach { v => attributes = new UnprefixedAttribute("atleast", v, attributes) }
    obj.`@atmost`.foreach { v => attributes = new UnprefixedAttribute("atmost", v, attributes) }
    obj.`@min`.foreach { v => attributes = new UnprefixedAttribute("min", v, attributes) }
    obj.`@max`.foreach { v => attributes = new UnprefixedAttribute("max", v, attributes) }
    obj.`@confidence`.foreach { v => attributes = new UnprefixedAttribute("confidence", v, attributes) }
    obj.`@extent`.foreach { v => attributes = new UnprefixedAttribute("extent", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@instant`.foreach { v => attributes = new UnprefixedAttribute("instant", v, attributes) }
    obj.`@state`.foreach { v => attributes = new UnprefixedAttribute("state", v, attributes) }
    obj.`@hand`.foreach { v => attributes = new UnprefixedAttribute("hand", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }
    obj.`@seq`.foreach { v => attributes = new UnprefixedAttribute("seq", v, attributes) }
    obj.`@desc`.foreach { v => attributes = new UnprefixedAttribute("desc", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "restore", attributes, TopScope, true, children*)
  }
}

object RevisionDescWriter extends MeiModelDummyWriter[RevisionDesc] {
  def toXml(obj: RevisionDesc): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "revisionDesc", attributes, TopScope, true, children*)
  }
}

object RoleWriter extends MeiModelDummyWriter[Role] {
  def toXml(obj: Role): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "role", attributes, TopScope, true, children*)
  }
}

object RoleDescWriter extends MeiModelDummyWriter[RoleDesc] {
  def toXml(obj: RoleDesc): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "roleDesc", attributes, TopScope, true, children*)
  }
}

object RoleNameWriter extends MeiModelDummyWriter[RoleName] {
  def toXml(obj: RoleName): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@nonfiling`.foreach { v => attributes = new UnprefixedAttribute("nonfiling", v, attributes) }
    obj.`@nymref`.foreach { v => attributes = new UnprefixedAttribute("nymref", v, attributes) }
    obj.`@role`.foreach { v => attributes = new UnprefixedAttribute("role", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "roleName", attributes, TopScope, true, children*)
  }
}

object RubricWriter extends MeiModelDummyWriter[Rubric] {
  def toXml(obj: Rubric): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@func`.foreach { v => attributes = new UnprefixedAttribute("func", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "rubric", attributes, TopScope, true, children*)
  }
}

object SamplingDeclWriter extends MeiModelDummyWriter[SamplingDecl] {
  def toXml(obj: SamplingDecl): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@data`.foreach { v => attributes = new UnprefixedAttribute("data", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "samplingDecl", attributes, TopScope, true, children*)
  }
}

object SbWriter extends MeiModelDummyWriter[Sb] {
  def toXml(obj: Sb): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@when`.foreach { v => attributes = new UnprefixedAttribute("when", v, attributes) }
    obj.`@altsym`.foreach { v => attributes = new UnprefixedAttribute("altsym", v, attributes) }
    obj.`@glyph.auth`.foreach { v => attributes = new UnprefixedAttribute("glyph.auth", v, attributes) }
    obj.`@glyph.uri`.foreach { v => attributes = new UnprefixedAttribute("glyph.uri", v, attributes) }
    obj.`@glyph.name`.foreach { v => attributes = new UnprefixedAttribute("glyph.name", v, attributes) }
    obj.`@glyph.num`.foreach { v => attributes = new UnprefixedAttribute("glyph.num", v, attributes) }
    obj.`@fontfam`.foreach { v => attributes = new UnprefixedAttribute("fontfam", v, attributes) }
    obj.`@fontname`.foreach { v => attributes = new UnprefixedAttribute("fontname", v, attributes) }
    obj.`@fontsize`.foreach { v => attributes = new UnprefixedAttribute("fontsize", v, attributes) }
    obj.`@fontstyle`.foreach { v => attributes = new UnprefixedAttribute("fontstyle", v, attributes) }
    obj.`@fontweight`.foreach { v => attributes = new UnprefixedAttribute("fontweight", v, attributes) }
    obj.`@letterspacing`.foreach { v => attributes = new UnprefixedAttribute("letterspacing", v, attributes) }
    obj.`@lineheight`.foreach { v => attributes = new UnprefixedAttribute("lineheight", v, attributes) }
    obj.`@form`.foreach { v => attributes = new UnprefixedAttribute("form", v, attributes) }

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "sb", attributes, TopScope, true, children*)
  }
}

object ScoreWriter extends MeiModelDummyWriter[Score] {
  def toXml(obj: Score): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "score", attributes, TopScope, true, children*)
  }
}

object ScoreDefWriter extends MeiModelDummyWriter[ScoreDef] {
  def toXml(obj: ScoreDef): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@key.accid`.foreach { v => attributes = new UnprefixedAttribute("key.accid", v, attributes) }
    obj.`@key.mode`.foreach { v => attributes = new UnprefixedAttribute("key.mode", v, attributes) }
    obj.`@key.pname`.foreach { v => attributes = new UnprefixedAttribute("key.pname", v, attributes) }
    obj.`@midi.channel`.foreach { v => attributes = new UnprefixedAttribute("midi.channel", v, attributes) }
    obj.`@midi.duty`.foreach { v => attributes = new UnprefixedAttribute("midi.duty", v, attributes) }
    obj.`@midi.port`.foreach { v => attributes = new UnprefixedAttribute("midi.port", v, attributes) }
    obj.`@midi.track`.foreach { v => attributes = new UnprefixedAttribute("midi.track", v, attributes) }
    obj.`@ppq`.foreach { v => attributes = new UnprefixedAttribute("ppq", v, attributes) }
    obj.`@tune.Hz`.foreach { v => attributes = new UnprefixedAttribute("tune.Hz", v, attributes) }
    obj.`@tune.pname`.foreach { v => attributes = new UnprefixedAttribute("tune.pname", v, attributes) }
    obj.`@tune.temper`.foreach { v => attributes = new UnprefixedAttribute("tune.temper", v, attributes) }
    obj.`@midi.bpm`.foreach { v => attributes = new UnprefixedAttribute("midi.bpm", v, attributes) }
    obj.`@midi.mspb`.foreach { v => attributes = new UnprefixedAttribute("midi.mspb", v, attributes) }
    obj.`@mm`.foreach { v => attributes = new UnprefixedAttribute("mm", v, attributes) }
    obj.`@mm.unit`.foreach { v => attributes = new UnprefixedAttribute("mm.unit", v, attributes) }
    obj.`@mm.dots`.foreach { v => attributes = new UnprefixedAttribute("mm.dots", v, attributes) }
    obj.`@clef.shape`.foreach { v => attributes = new UnprefixedAttribute("clef.shape", v, attributes) }
    obj.`@clef.line`.foreach { v => attributes = new UnprefixedAttribute("clef.line", v, attributes) }
    obj.`@clef.dis`.foreach { v => attributes = new UnprefixedAttribute("clef.dis", v, attributes) }
    obj.`@clef.dis.place`.foreach { v => attributes = new UnprefixedAttribute("clef.dis.place", v, attributes) }
    obj.`@dur.default`.foreach { v => attributes = new UnprefixedAttribute("dur.default", v, attributes) }
    obj.`@num.default`.foreach { v => attributes = new UnprefixedAttribute("num.default", v, attributes) }
    obj.`@numbase.default`.foreach { v => attributes = new UnprefixedAttribute("numbase.default", v, attributes) }
    obj.`@keysig`.foreach { v => attributes = new UnprefixedAttribute("keysig", v, attributes) }
    obj.`@meter.count`.foreach { v => attributes = new UnprefixedAttribute("meter.count", v, attributes) }
    obj.`@meter.unit`.foreach { v => attributes = new UnprefixedAttribute("meter.unit", v, attributes) }
    obj.`@meter.sym`.foreach { v => attributes = new UnprefixedAttribute("meter.sym", v, attributes) }
    obj.`@oct.default`.foreach { v => attributes = new UnprefixedAttribute("oct.default", v, attributes) }
    obj.`@trans.diat`.foreach { v => attributes = new UnprefixedAttribute("trans.diat", v, attributes) }
    obj.`@trans.semi`.foreach { v => attributes = new UnprefixedAttribute("trans.semi", v, attributes) }
    obj.`@modusmaior`.foreach { v => attributes = new UnprefixedAttribute("modusmaior", v, attributes) }
    obj.`@modusminor`.foreach { v => attributes = new UnprefixedAttribute("modusminor", v, attributes) }
    obj.`@prolatio`.foreach { v => attributes = new UnprefixedAttribute("prolatio", v, attributes) }
    obj.`@tempus`.foreach { v => attributes = new UnprefixedAttribute("tempus", v, attributes) }
    obj.`@divisio`.foreach { v => attributes = new UnprefixedAttribute("divisio", v, attributes) }
    obj.`@proport.num`.foreach { v => attributes = new UnprefixedAttribute("proport.num", v, attributes) }
    obj.`@proport.numbase`.foreach { v => attributes = new UnprefixedAttribute("proport.numbase", v, attributes) }
    obj.`@bar.len`.foreach { v => attributes = new UnprefixedAttribute("bar.len", v, attributes) }
    obj.`@bar.method`.foreach { v => attributes = new UnprefixedAttribute("bar.method", v, attributes) }
    obj.`@bar.place`.foreach { v => attributes = new UnprefixedAttribute("bar.place", v, attributes) }
    obj.`@clef.color`.foreach { v => attributes = new UnprefixedAttribute("clef.color", v, attributes) }
    obj.`@clef.visible`.foreach { v => attributes = new UnprefixedAttribute("clef.visible", v, attributes) }
    obj.`@dir.dist`.foreach { v => attributes = new UnprefixedAttribute("dir.dist", v, attributes) }
    obj.`@dynam.dist`.foreach { v => attributes = new UnprefixedAttribute("dynam.dist", v, attributes) }
    obj.`@harm.dist`.foreach { v => attributes = new UnprefixedAttribute("harm.dist", v, attributes) }
    obj.`@reh.dist`.foreach { v => attributes = new UnprefixedAttribute("reh.dist", v, attributes) }
    obj.`@tempo.dist`.foreach { v => attributes = new UnprefixedAttribute("tempo.dist", v, attributes) }
    obj.`@ending.rend`.foreach { v => attributes = new UnprefixedAttribute("ending.rend", v, attributes) }
    obj.`@keysig.cancelaccid`.foreach { v => attributes = new UnprefixedAttribute("keysig.cancelaccid", v, attributes) }
    obj.`@keysig.visible`.foreach { v => attributes = new UnprefixedAttribute("keysig.visible", v, attributes) }
    obj.`@lyric.align`.foreach { v => attributes = new UnprefixedAttribute("lyric.align", v, attributes) }
    obj.`@lyric.fam`.foreach { v => attributes = new UnprefixedAttribute("lyric.fam", v, attributes) }
    obj.`@lyric.name`.foreach { v => attributes = new UnprefixedAttribute("lyric.name", v, attributes) }
    obj.`@lyric.size`.foreach { v => attributes = new UnprefixedAttribute("lyric.size", v, attributes) }
    obj.`@lyric.style`.foreach { v => attributes = new UnprefixedAttribute("lyric.style", v, attributes) }
    obj.`@lyric.weight`.foreach { v => attributes = new UnprefixedAttribute("lyric.weight", v, attributes) }
    obj.`@mnum.visible`.foreach { v => attributes = new UnprefixedAttribute("mnum.visible", v, attributes) }
    obj.`@meter.form`.foreach { v => attributes = new UnprefixedAttribute("meter.form", v, attributes) }
    obj.`@meter.showchange`.foreach { v => attributes = new UnprefixedAttribute("meter.showchange", v, attributes) }
    obj.`@meter.visible`.foreach { v => attributes = new UnprefixedAttribute("meter.visible", v, attributes) }
    obj.`@multi.number`.foreach { v => attributes = new UnprefixedAttribute("multi.number", v, attributes) }
    obj.`@music.name`.foreach { v => attributes = new UnprefixedAttribute("music.name", v, attributes) }
    obj.`@music.size`.foreach { v => attributes = new UnprefixedAttribute("music.size", v, attributes) }
    obj.`@ontheline`.foreach { v => attributes = new UnprefixedAttribute("ontheline", v, attributes) }
    obj.`@optimize`.foreach { v => attributes = new UnprefixedAttribute("optimize", v, attributes) }
    obj.`@page.height`.foreach { v => attributes = new UnprefixedAttribute("page.height", v, attributes) }
    obj.`@page.width`.foreach { v => attributes = new UnprefixedAttribute("page.width", v, attributes) }
    obj.`@page.topmar`.foreach { v => attributes = new UnprefixedAttribute("page.topmar", v, attributes) }
    obj.`@page.botmar`.foreach { v => attributes = new UnprefixedAttribute("page.botmar", v, attributes) }
    obj.`@page.leftmar`.foreach { v => attributes = new UnprefixedAttribute("page.leftmar", v, attributes) }
    obj.`@page.rightmar`.foreach { v => attributes = new UnprefixedAttribute("page.rightmar", v, attributes) }
    obj.`@page.panels`.foreach { v => attributes = new UnprefixedAttribute("page.panels", v, attributes) }
    obj.`@page.scale`.foreach { v => attributes = new UnprefixedAttribute("page.scale", v, attributes) }
    obj.`@spacing.packexp`.foreach { v => attributes = new UnprefixedAttribute("spacing.packexp", v, attributes) }
    obj.`@spacing.packfact`.foreach { v => attributes = new UnprefixedAttribute("spacing.packfact", v, attributes) }
    obj.`@spacing.staff`.foreach { v => attributes = new UnprefixedAttribute("spacing.staff", v, attributes) }
    obj.`@spacing.system`.foreach { v => attributes = new UnprefixedAttribute("spacing.system", v, attributes) }
    obj.`@aboveorder`.foreach { v => attributes = new UnprefixedAttribute("aboveorder", v, attributes) }
    obj.`@beloworder`.foreach { v => attributes = new UnprefixedAttribute("beloworder", v, attributes) }
    obj.`@betweenorder`.foreach { v => attributes = new UnprefixedAttribute("betweenorder", v, attributes) }
    obj.`@system.leftline`.foreach { v => attributes = new UnprefixedAttribute("system.leftline", v, attributes) }
    obj.`@system.leftmar`.foreach { v => attributes = new UnprefixedAttribute("system.leftmar", v, attributes) }
    obj.`@system.rightmar`.foreach { v => attributes = new UnprefixedAttribute("system.rightmar", v, attributes) }
    obj.`@system.topmar`.foreach { v => attributes = new UnprefixedAttribute("system.topmar", v, attributes) }
    obj.`@text.fam`.foreach { v => attributes = new UnprefixedAttribute("text.fam", v, attributes) }
    obj.`@text.name`.foreach { v => attributes = new UnprefixedAttribute("text.name", v, attributes) }
    obj.`@text.size`.foreach { v => attributes = new UnprefixedAttribute("text.size", v, attributes) }
    obj.`@text.style`.foreach { v => attributes = new UnprefixedAttribute("text.style", v, attributes) }
    obj.`@text.weight`.foreach { v => attributes = new UnprefixedAttribute("text.weight", v, attributes) }
    obj.`@mensur.color`.foreach { v => attributes = new UnprefixedAttribute("mensur.color", v, attributes) }
    obj.`@mensur.dot`.foreach { v => attributes = new UnprefixedAttribute("mensur.dot", v, attributes) }
    obj.`@mensur.form`.foreach { v => attributes = new UnprefixedAttribute("mensur.form", v, attributes) }
    obj.`@mensur.loc`.foreach { v => attributes = new UnprefixedAttribute("mensur.loc", v, attributes) }
    obj.`@mensur.orient`.foreach { v => attributes = new UnprefixedAttribute("mensur.orient", v, attributes) }
    obj.`@mensur.sign`.foreach { v => attributes = new UnprefixedAttribute("mensur.sign", v, attributes) }
    obj.`@mensur.size`.foreach { v => attributes = new UnprefixedAttribute("mensur.size", v, attributes) }
    obj.`@mensur.slash`.foreach { v => attributes = new UnprefixedAttribute("mensur.slash", v, attributes) }
    obj.`@vu.height`.foreach { v => attributes = new UnprefixedAttribute("vu.height", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "scoreDef", attributes, TopScope, true, children*)
  }
}

object ScoreFormatWriter extends MeiModelDummyWriter[ScoreFormat] {
  def toXml(obj: ScoreFormat): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "scoreFormat", attributes, TopScope, true, children*)
  }
}

object ScriptWriter extends MeiModelDummyWriter[Script] {
  def toXml(obj: Script): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "script", Null, TopScope, true, children*)
  }
}

object ScriptDescWriter extends MeiModelDummyWriter[ScriptDesc] {
  def toXml(obj: ScriptDesc): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "scriptDesc", attributes, TopScope, true, children*)
  }
}

object ScriptNoteWriter extends MeiModelDummyWriter[ScriptNote] {
  def toXml(obj: ScriptNote): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "scriptNote", attributes, TopScope, true, children*)
  }
}

object SealWriter extends MeiModelDummyWriter[Seal] {
  def toXml(obj: Seal): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@contemporary`.foreach { v => attributes = new UnprefixedAttribute("contemporary", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "seal", attributes, TopScope, true, children*)
  }
}

object SealDescWriter extends MeiModelDummyWriter[SealDesc] {
  def toXml(obj: SealDesc): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "sealDesc", attributes, TopScope, true, children*)
  }
}

object SecFolioWriter extends MeiModelDummyWriter[SecFolio] {
  def toXml(obj: SecFolio): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "secFolio", attributes, TopScope, true, children*)
  }
}

object SectionWriter extends MeiModelDummyWriter[Section] {
  def toXml(obj: Section): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }
    obj.`@xlink:actuate`.foreach { v => attributes = new UnprefixedAttribute("xlink:actuate", v, attributes) }
    obj.`@xlink:role`.foreach { v => attributes = new UnprefixedAttribute("xlink:role", v, attributes) }
    obj.`@xlink:show`.foreach { v => attributes = new UnprefixedAttribute("xlink:show", v, attributes) }
    obj.`@target`.foreach { v => attributes = new UnprefixedAttribute("target", v, attributes) }
    obj.`@targettype`.foreach { v => attributes = new UnprefixedAttribute("targettype", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }
    obj.`@attacca`.foreach { v => attributes = new UnprefixedAttribute("attacca", v, attributes) }
    obj.`@when`.foreach { v => attributes = new UnprefixedAttribute("when", v, attributes) }
    obj.`@restart`.foreach { v => attributes = new UnprefixedAttribute("restart", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "section", attributes, TopScope, true, children*)
  }
}

object SegWriter extends MeiModelDummyWriter[Seg] {
  def toXml(obj: Seg): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "seg", attributes, TopScope, true, children*)
  }
}

object SegmentationWriter extends MeiModelDummyWriter[Segmentation] {
  def toXml(obj: Segmentation): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@data`.foreach { v => attributes = new UnprefixedAttribute("data", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "segmentation", attributes, TopScope, true, children*)
  }
}

object SeqNumWriter extends MeiModelDummyWriter[SeqNum] {
  def toXml(obj: SeqNum): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    attributes = new UnprefixedAttribute("num", obj.`@num`, attributes)

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "seqNum", attributes, TopScope, true, children*)
  }
}

object SeriesWriter extends MeiModelDummyWriter[Series] {
  def toXml(obj: Series): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "series", attributes, TopScope, true, children*)
  }
}

object SeriesStmtWriter extends MeiModelDummyWriter[SeriesStmt] {
  def toXml(obj: SeriesStmt): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "seriesStmt", attributes, TopScope, true, children*)
  }
}

object SetWriter extends MeiModelDummyWriter[Set] {
  def toXml(obj: Set): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "set", Null, TopScope, true, children*)
  }
}

object SettlementWriter extends MeiModelDummyWriter[Settlement] {
  def toXml(obj: Settlement): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@nonfiling`.foreach { v => attributes = new UnprefixedAttribute("nonfiling", v, attributes) }
    obj.`@nymref`.foreach { v => attributes = new UnprefixedAttribute("nymref", v, attributes) }
    obj.`@role`.foreach { v => attributes = new UnprefixedAttribute("role", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "settlement", attributes, TopScope, true, children*)
  }
}

object SicWriter extends MeiModelDummyWriter[Sic] {
  def toXml(obj: Sic): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@unit`.foreach { v => attributes = new UnprefixedAttribute("unit", v, attributes) }
    obj.`@atleast`.foreach { v => attributes = new UnprefixedAttribute("atleast", v, attributes) }
    obj.`@atmost`.foreach { v => attributes = new UnprefixedAttribute("atmost", v, attributes) }
    obj.`@min`.foreach { v => attributes = new UnprefixedAttribute("min", v, attributes) }
    obj.`@max`.foreach { v => attributes = new UnprefixedAttribute("max", v, attributes) }
    obj.`@confidence`.foreach { v => attributes = new UnprefixedAttribute("confidence", v, attributes) }
    obj.`@extent`.foreach { v => attributes = new UnprefixedAttribute("extent", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "sic", attributes, TopScope, true, children*)
  }
}

object SignaturesWriter extends MeiModelDummyWriter[Signatures] {
  def toXml(obj: Signatures): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "signatures", attributes, TopScope, true, children*)
  }
}

object SoundChanWriter extends MeiModelDummyWriter[SoundChan] {
  def toXml(obj: SoundChan): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@num`.foreach { v => attributes = new UnprefixedAttribute("num", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "soundChan", attributes, TopScope, true, children*)
  }
}

object SourceWriter extends MeiModelDummyWriter[Source] {
  def toXml(obj: Source): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@comptype`.foreach { v => attributes = new UnprefixedAttribute("comptype", v, attributes) }
    obj.`@data`.foreach { v => attributes = new UnprefixedAttribute("data", v, attributes) }
    obj.`@xlink:actuate`.foreach { v => attributes = new UnprefixedAttribute("xlink:actuate", v, attributes) }
    obj.`@xlink:role`.foreach { v => attributes = new UnprefixedAttribute("xlink:role", v, attributes) }
    obj.`@xlink:show`.foreach { v => attributes = new UnprefixedAttribute("xlink:show", v, attributes) }
    obj.`@target`.foreach { v => attributes = new UnprefixedAttribute("target", v, attributes) }
    obj.`@targettype`.foreach { v => attributes = new UnprefixedAttribute("targettype", v, attributes) }
    obj.`@recordtype`.foreach { v => attributes = new UnprefixedAttribute("recordtype", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "source", attributes, TopScope, true, children*)
  }
}

object SourceDescWriter extends MeiModelDummyWriter[SourceDesc] {
  def toXml(obj: SourceDesc): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "sourceDesc", attributes, TopScope, true, children*)
  }
}

object SpWriter extends MeiModelDummyWriter[Sp] {
  def toXml(obj: Sp): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@tstamp2.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp2.ges", v, attributes) }
    obj.`@tstamp2.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp2.real", v, attributes) }
    obj.`@when`.foreach { v => attributes = new UnprefixedAttribute("when", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@plist`.foreach { v => attributes = new UnprefixedAttribute("plist", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    obj.`@startid`.foreach { v => attributes = new UnprefixedAttribute("startid", v, attributes) }
    obj.`@endid`.foreach { v => attributes = new UnprefixedAttribute("endid", v, attributes) }
    obj.`@tstamp2`.foreach { v => attributes = new UnprefixedAttribute("tstamp2", v, attributes) }
    obj.`@place`.foreach { v => attributes = new UnprefixedAttribute("place", v, attributes) }
    obj.`@ho`.foreach { v => attributes = new UnprefixedAttribute("ho", v, attributes) }
    obj.`@to`.foreach { v => attributes = new UnprefixedAttribute("to", v, attributes) }
    obj.`@vo`.foreach { v => attributes = new UnprefixedAttribute("vo", v, attributes) }
    obj.`@startho`.foreach { v => attributes = new UnprefixedAttribute("startho", v, attributes) }
    obj.`@endho`.foreach { v => attributes = new UnprefixedAttribute("endho", v, attributes) }
    obj.`@startto`.foreach { v => attributes = new UnprefixedAttribute("startto", v, attributes) }
    obj.`@endto`.foreach { v => attributes = new UnprefixedAttribute("endto", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "sp", attributes, TopScope, true, children*)
  }
}

object SpaceWriter extends MeiModelDummyWriter[Space] {
  def toXml(obj: Space): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@dur.quality`.foreach { v => attributes = new UnprefixedAttribute("dur.quality", v, attributes) }
    obj.`@dur`.foreach { v => attributes = new UnprefixedAttribute("dur", v, attributes) }
    obj.`@when`.foreach { v => attributes = new UnprefixedAttribute("when", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@compressable`.foreach { v => attributes = new UnprefixedAttribute("compressable", v, attributes) }
    obj.`@dur.ges`.foreach { v => attributes = new UnprefixedAttribute("dur.ges", v, attributes) }
    obj.`@dots.ges`.foreach { v => attributes = new UnprefixedAttribute("dots.ges", v, attributes) }
    obj.`@dur.metrical`.foreach { v => attributes = new UnprefixedAttribute("dur.metrical", v, attributes) }
    obj.`@dur.ppq`.foreach { v => attributes = new UnprefixedAttribute("dur.ppq", v, attributes) }
    obj.`@dur.real`.foreach { v => attributes = new UnprefixedAttribute("dur.real", v, attributes) }
    obj.`@dur.recip`.foreach { v => attributes = new UnprefixedAttribute("dur.recip", v, attributes) }

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "space", attributes, TopScope, true, children*)
  }
}

object SpeakerWriter extends MeiModelDummyWriter[Speaker] {
  def toXml(obj: Speaker): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "speaker", attributes, TopScope, true, children*)
  }
}

object SpecReproWriter extends MeiModelDummyWriter[SpecRepro] {
  def toXml(obj: SpecRepro): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "specRepro", attributes, TopScope, true, children*)
  }
}

object SponsorWriter extends MeiModelDummyWriter[Sponsor] {
  def toXml(obj: Sponsor): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "sponsor", attributes, TopScope, true, children*)
  }
}

object StackWriter extends MeiModelDummyWriter[Stack] {
  def toXml(obj: Stack): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@delim`.foreach { v => attributes = new UnprefixedAttribute("delim", v, attributes) }
    obj.`@align`.foreach { v => attributes = new UnprefixedAttribute("align", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "stack", attributes, TopScope, true, children*)
  }
}

object StaffWriter extends MeiModelDummyWriter[Staff] {
  def toXml(obj: Staff): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@metcon`.foreach { v => attributes = new UnprefixedAttribute("metcon", v, attributes) }
    obj.`@def`.foreach { v => attributes = new UnprefixedAttribute("def", v, attributes) }
    obj.`@visible`.foreach { v => attributes = new UnprefixedAttribute("visible", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "staff", attributes, TopScope, true, children*)
  }
}

object StaffDefWriter extends MeiModelDummyWriter[StaffDef] {
  def toXml(obj: StaffDef): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@key.accid`.foreach { v => attributes = new UnprefixedAttribute("key.accid", v, attributes) }
    obj.`@key.mode`.foreach { v => attributes = new UnprefixedAttribute("key.mode", v, attributes) }
    obj.`@key.pname`.foreach { v => attributes = new UnprefixedAttribute("key.pname", v, attributes) }
    obj.`@instr`.foreach { v => attributes = new UnprefixedAttribute("instr", v, attributes) }
    obj.`@tab.strings`.foreach { v => attributes = new UnprefixedAttribute("tab.strings", v, attributes) }
    obj.`@tab.courses`.foreach { v => attributes = new UnprefixedAttribute("tab.courses", v, attributes) }
    obj.`@ppq`.foreach { v => attributes = new UnprefixedAttribute("ppq", v, attributes) }
    obj.`@tune.Hz`.foreach { v => attributes = new UnprefixedAttribute("tune.Hz", v, attributes) }
    obj.`@tune.pname`.foreach { v => attributes = new UnprefixedAttribute("tune.pname", v, attributes) }
    obj.`@tune.temper`.foreach { v => attributes = new UnprefixedAttribute("tune.temper", v, attributes) }
    obj.`@clef.shape`.foreach { v => attributes = new UnprefixedAttribute("clef.shape", v, attributes) }
    obj.`@clef.line`.foreach { v => attributes = new UnprefixedAttribute("clef.line", v, attributes) }
    obj.`@clef.dis`.foreach { v => attributes = new UnprefixedAttribute("clef.dis", v, attributes) }
    obj.`@clef.dis.place`.foreach { v => attributes = new UnprefixedAttribute("clef.dis.place", v, attributes) }
    obj.`@dur.default`.foreach { v => attributes = new UnprefixedAttribute("dur.default", v, attributes) }
    obj.`@num.default`.foreach { v => attributes = new UnprefixedAttribute("num.default", v, attributes) }
    obj.`@numbase.default`.foreach { v => attributes = new UnprefixedAttribute("numbase.default", v, attributes) }
    obj.`@keysig`.foreach { v => attributes = new UnprefixedAttribute("keysig", v, attributes) }
    obj.`@meter.count`.foreach { v => attributes = new UnprefixedAttribute("meter.count", v, attributes) }
    obj.`@meter.unit`.foreach { v => attributes = new UnprefixedAttribute("meter.unit", v, attributes) }
    obj.`@meter.sym`.foreach { v => attributes = new UnprefixedAttribute("meter.sym", v, attributes) }
    obj.`@notationtype`.foreach { v => attributes = new UnprefixedAttribute("notationtype", v, attributes) }
    obj.`@notationsubtype`.foreach { v => attributes = new UnprefixedAttribute("notationsubtype", v, attributes) }
    obj.`@oct.default`.foreach { v => attributes = new UnprefixedAttribute("oct.default", v, attributes) }
    obj.`@trans.diat`.foreach { v => attributes = new UnprefixedAttribute("trans.diat", v, attributes) }
    obj.`@trans.semi`.foreach { v => attributes = new UnprefixedAttribute("trans.semi", v, attributes) }
    obj.`@modusmaior`.foreach { v => attributes = new UnprefixedAttribute("modusmaior", v, attributes) }
    obj.`@modusminor`.foreach { v => attributes = new UnprefixedAttribute("modusminor", v, attributes) }
    obj.`@prolatio`.foreach { v => attributes = new UnprefixedAttribute("prolatio", v, attributes) }
    obj.`@tempus`.foreach { v => attributes = new UnprefixedAttribute("tempus", v, attributes) }
    obj.`@divisio`.foreach { v => attributes = new UnprefixedAttribute("divisio", v, attributes) }
    obj.`@proport.num`.foreach { v => attributes = new UnprefixedAttribute("proport.num", v, attributes) }
    obj.`@proport.numbase`.foreach { v => attributes = new UnprefixedAttribute("proport.numbase", v, attributes) }
    obj.`@lines`.foreach { v => attributes = new UnprefixedAttribute("lines", v, attributes) }
    obj.`@bar.len`.foreach { v => attributes = new UnprefixedAttribute("bar.len", v, attributes) }
    obj.`@bar.method`.foreach { v => attributes = new UnprefixedAttribute("bar.method", v, attributes) }
    obj.`@bar.place`.foreach { v => attributes = new UnprefixedAttribute("bar.place", v, attributes) }
    obj.`@clef.color`.foreach { v => attributes = new UnprefixedAttribute("clef.color", v, attributes) }
    obj.`@clef.visible`.foreach { v => attributes = new UnprefixedAttribute("clef.visible", v, attributes) }
    obj.`@dir.dist`.foreach { v => attributes = new UnprefixedAttribute("dir.dist", v, attributes) }
    obj.`@dynam.dist`.foreach { v => attributes = new UnprefixedAttribute("dynam.dist", v, attributes) }
    obj.`@harm.dist`.foreach { v => attributes = new UnprefixedAttribute("harm.dist", v, attributes) }
    obj.`@reh.dist`.foreach { v => attributes = new UnprefixedAttribute("reh.dist", v, attributes) }
    obj.`@tempo.dist`.foreach { v => attributes = new UnprefixedAttribute("tempo.dist", v, attributes) }
    obj.`@grid.show`.foreach { v => attributes = new UnprefixedAttribute("grid.show", v, attributes) }
    obj.`@keysig.cancelaccid`.foreach { v => attributes = new UnprefixedAttribute("keysig.cancelaccid", v, attributes) }
    obj.`@keysig.visible`.foreach { v => attributes = new UnprefixedAttribute("keysig.visible", v, attributes) }
    obj.`@lyric.align`.foreach { v => attributes = new UnprefixedAttribute("lyric.align", v, attributes) }
    obj.`@lyric.fam`.foreach { v => attributes = new UnprefixedAttribute("lyric.fam", v, attributes) }
    obj.`@lyric.name`.foreach { v => attributes = new UnprefixedAttribute("lyric.name", v, attributes) }
    obj.`@lyric.size`.foreach { v => attributes = new UnprefixedAttribute("lyric.size", v, attributes) }
    obj.`@lyric.style`.foreach { v => attributes = new UnprefixedAttribute("lyric.style", v, attributes) }
    obj.`@lyric.weight`.foreach { v => attributes = new UnprefixedAttribute("lyric.weight", v, attributes) }
    obj.`@meter.form`.foreach { v => attributes = new UnprefixedAttribute("meter.form", v, attributes) }
    obj.`@meter.showchange`.foreach { v => attributes = new UnprefixedAttribute("meter.showchange", v, attributes) }
    obj.`@meter.visible`.foreach { v => attributes = new UnprefixedAttribute("meter.visible", v, attributes) }
    obj.`@multi.number`.foreach { v => attributes = new UnprefixedAttribute("multi.number", v, attributes) }
    obj.`@music.name`.foreach { v => attributes = new UnprefixedAttribute("music.name", v, attributes) }
    obj.`@music.size`.foreach { v => attributes = new UnprefixedAttribute("music.size", v, attributes) }
    obj.`@ontheline`.foreach { v => attributes = new UnprefixedAttribute("ontheline", v, attributes) }
    obj.`@scale`.foreach { v => attributes = new UnprefixedAttribute("scale", v, attributes) }
    obj.`@aboveorder`.foreach { v => attributes = new UnprefixedAttribute("aboveorder", v, attributes) }
    obj.`@beloworder`.foreach { v => attributes = new UnprefixedAttribute("beloworder", v, attributes) }
    obj.`@betweenorder`.foreach { v => attributes = new UnprefixedAttribute("betweenorder", v, attributes) }
    obj.`@text.fam`.foreach { v => attributes = new UnprefixedAttribute("text.fam", v, attributes) }
    obj.`@text.name`.foreach { v => attributes = new UnprefixedAttribute("text.name", v, attributes) }
    obj.`@text.size`.foreach { v => attributes = new UnprefixedAttribute("text.size", v, attributes) }
    obj.`@text.style`.foreach { v => attributes = new UnprefixedAttribute("text.style", v, attributes) }
    obj.`@text.weight`.foreach { v => attributes = new UnprefixedAttribute("text.weight", v, attributes) }
    obj.`@visible`.foreach { v => attributes = new UnprefixedAttribute("visible", v, attributes) }
    obj.`@mensur.color`.foreach { v => attributes = new UnprefixedAttribute("mensur.color", v, attributes) }
    obj.`@mensur.dot`.foreach { v => attributes = new UnprefixedAttribute("mensur.dot", v, attributes) }
    obj.`@mensur.form`.foreach { v => attributes = new UnprefixedAttribute("mensur.form", v, attributes) }
    obj.`@mensur.loc`.foreach { v => attributes = new UnprefixedAttribute("mensur.loc", v, attributes) }
    obj.`@mensur.orient`.foreach { v => attributes = new UnprefixedAttribute("mensur.orient", v, attributes) }
    obj.`@mensur.sign`.foreach { v => attributes = new UnprefixedAttribute("mensur.sign", v, attributes) }
    obj.`@mensur.size`.foreach { v => attributes = new UnprefixedAttribute("mensur.size", v, attributes) }
    obj.`@mensur.slash`.foreach { v => attributes = new UnprefixedAttribute("mensur.slash", v, attributes) }
    obj.`@tab.align`.foreach { v => attributes = new UnprefixedAttribute("tab.align", v, attributes) }
    obj.`@tab.anchorline`.foreach { v => attributes = new UnprefixedAttribute("tab.anchorline", v, attributes) }
    obj.`@layerscheme`.foreach { v => attributes = new UnprefixedAttribute("layerscheme", v, attributes) }
    obj.`@lines.color`.foreach { v => attributes = new UnprefixedAttribute("lines.color", v, attributes) }
    obj.`@lines.visible`.foreach { v => attributes = new UnprefixedAttribute("lines.visible", v, attributes) }
    obj.`@spacing`.foreach { v => attributes = new UnprefixedAttribute("spacing", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "staffDef", attributes, TopScope, true, children*)
  }
}

object StaffGrpWriter extends MeiModelDummyWriter[StaffGrp] {
  def toXml(obj: StaffGrp): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }
    obj.`@instr`.foreach { v => attributes = new UnprefixedAttribute("instr", v, attributes) }
    obj.`@bar.len`.foreach { v => attributes = new UnprefixedAttribute("bar.len", v, attributes) }
    obj.`@bar.method`.foreach { v => attributes = new UnprefixedAttribute("bar.method", v, attributes) }
    obj.`@bar.place`.foreach { v => attributes = new UnprefixedAttribute("bar.place", v, attributes) }
    obj.`@symbol`.foreach { v => attributes = new UnprefixedAttribute("symbol", v, attributes) }
    obj.`@visible`.foreach { v => attributes = new UnprefixedAttribute("visible", v, attributes) }
    obj.`@bar.thru`.foreach { v => attributes = new UnprefixedAttribute("bar.thru", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "staffGrp", attributes, TopScope, true, children*)
  }
}

object StageDirWriter extends MeiModelDummyWriter[StageDir] {
  def toXml(obj: StageDir): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@tstamp2.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp2.ges", v, attributes) }
    obj.`@tstamp2.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp2.real", v, attributes) }
    obj.`@when`.foreach { v => attributes = new UnprefixedAttribute("when", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@plist`.foreach { v => attributes = new UnprefixedAttribute("plist", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    obj.`@startid`.foreach { v => attributes = new UnprefixedAttribute("startid", v, attributes) }
    obj.`@endid`.foreach { v => attributes = new UnprefixedAttribute("endid", v, attributes) }
    obj.`@tstamp2`.foreach { v => attributes = new UnprefixedAttribute("tstamp2", v, attributes) }
    obj.`@place`.foreach { v => attributes = new UnprefixedAttribute("place", v, attributes) }
    obj.`@ho`.foreach { v => attributes = new UnprefixedAttribute("ho", v, attributes) }
    obj.`@to`.foreach { v => attributes = new UnprefixedAttribute("to", v, attributes) }
    obj.`@vo`.foreach { v => attributes = new UnprefixedAttribute("vo", v, attributes) }
    obj.`@startho`.foreach { v => attributes = new UnprefixedAttribute("startho", v, attributes) }
    obj.`@endho`.foreach { v => attributes = new UnprefixedAttribute("endho", v, attributes) }
    obj.`@startto`.foreach { v => attributes = new UnprefixedAttribute("startto", v, attributes) }
    obj.`@endto`.foreach { v => attributes = new UnprefixedAttribute("endto", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "stageDir", attributes, TopScope, true, children*)
  }
}

object StampWriter extends MeiModelDummyWriter[Stamp] {
  def toXml(obj: Stamp): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "stamp", attributes, TopScope, true, children*)
  }
}

object StdValsWriter extends MeiModelDummyWriter[StdVals] {
  def toXml(obj: StdVals): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@data`.foreach { v => attributes = new UnprefixedAttribute("data", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "stdVals", attributes, TopScope, true, children*)
  }
}

object StemWriter extends MeiModelDummyWriter[Stem] {
  def toXml(obj: Stem): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@glyph.auth`.foreach { v => attributes = new UnprefixedAttribute("glyph.auth", v, attributes) }
    obj.`@glyph.uri`.foreach { v => attributes = new UnprefixedAttribute("glyph.uri", v, attributes) }
    obj.`@glyph.name`.foreach { v => attributes = new UnprefixedAttribute("glyph.name", v, attributes) }
    obj.`@glyph.num`.foreach { v => attributes = new UnprefixedAttribute("glyph.num", v, attributes) }
    obj.`@visible`.foreach { v => attributes = new UnprefixedAttribute("visible", v, attributes) }
    obj.`@pos`.foreach { v => attributes = new UnprefixedAttribute("pos", v, attributes) }
    obj.`@len`.foreach { v => attributes = new UnprefixedAttribute("len", v, attributes) }
    obj.`@form`.foreach { v => attributes = new UnprefixedAttribute("form", v, attributes) }
    obj.`@dir`.foreach { v => attributes = new UnprefixedAttribute("dir", v, attributes) }
    obj.`@flag.pos`.foreach { v => attributes = new UnprefixedAttribute("flag.pos", v, attributes) }
    obj.`@flag.form`.foreach { v => attributes = new UnprefixedAttribute("flag.form", v, attributes) }

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "stem", attributes, TopScope, true, children*)
  }
}

object StopWriter extends MeiModelDummyWriter[Stop] {
  def toXml(obj: Stop): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "stop", Null, TopScope, true, children*)
  }
}

object StreetWriter extends MeiModelDummyWriter[Street] {
  def toXml(obj: Street): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "street", attributes, TopScope, true, children*)
  }
}

object StringWriter extends MeiModelDummyWriter[String] {
  def toXml(obj: String): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@accid`.foreach { v => attributes = new UnprefixedAttribute("accid", v, attributes) }
    obj.`@pname`.foreach { v => attributes = new UnprefixedAttribute("pname", v, attributes) }
    obj.`@oct`.foreach { v => attributes = new UnprefixedAttribute("oct", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "string", attributes, TopScope, true, children*)
  }
}

object StyleWriter extends MeiModelDummyWriter[Style] {
  def toXml(obj: Style): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "style", Null, TopScope, true, children*)
  }
}

object StyleNameWriter extends MeiModelDummyWriter[StyleName] {
  def toXml(obj: StyleName): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@nonfiling`.foreach { v => attributes = new UnprefixedAttribute("nonfiling", v, attributes) }
    obj.`@nymref`.foreach { v => attributes = new UnprefixedAttribute("nymref", v, attributes) }
    obj.`@role`.foreach { v => attributes = new UnprefixedAttribute("role", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "styleName", attributes, TopScope, true, children*)
  }
}

object SubstWriter extends MeiModelDummyWriter[Subst] {
  def toXml(obj: Subst): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@instant`.foreach { v => attributes = new UnprefixedAttribute("instant", v, attributes) }
    obj.`@state`.foreach { v => attributes = new UnprefixedAttribute("state", v, attributes) }
    obj.`@hand`.foreach { v => attributes = new UnprefixedAttribute("hand", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }
    obj.`@seq`.foreach { v => attributes = new UnprefixedAttribute("seq", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "subst", attributes, TopScope, true, children*)
  }
}

object SuppliedWriter extends MeiModelDummyWriter[Supplied] {
  def toXml(obj: Supplied): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@agent`.foreach { v => attributes = new UnprefixedAttribute("agent", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@unit`.foreach { v => attributes = new UnprefixedAttribute("unit", v, attributes) }
    obj.`@atleast`.foreach { v => attributes = new UnprefixedAttribute("atleast", v, attributes) }
    obj.`@atmost`.foreach { v => attributes = new UnprefixedAttribute("atmost", v, attributes) }
    obj.`@min`.foreach { v => attributes = new UnprefixedAttribute("min", v, attributes) }
    obj.`@max`.foreach { v => attributes = new UnprefixedAttribute("max", v, attributes) }
    obj.`@confidence`.foreach { v => attributes = new UnprefixedAttribute("confidence", v, attributes) }
    obj.`@extent`.foreach { v => attributes = new UnprefixedAttribute("extent", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@reason`.foreach { v => attributes = new UnprefixedAttribute("reason", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "supplied", attributes, TopScope, true, children*)
  }
}

object SupportWriter extends MeiModelDummyWriter[Support] {
  def toXml(obj: Support): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "support", attributes, TopScope, true, children*)
  }
}

object SupportDescWriter extends MeiModelDummyWriter[SupportDesc] {
  def toXml(obj: SupportDesc): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@material`.foreach { v => attributes = new UnprefixedAttribute("material", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "supportDesc", attributes, TopScope, true, children*)
  }
}

object SurfaceWriter extends MeiModelDummyWriter[Surface] {
  def toXml(obj: Surface): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@ulx`.foreach { v => attributes = new UnprefixedAttribute("ulx", v, attributes) }
    obj.`@uly`.foreach { v => attributes = new UnprefixedAttribute("uly", v, attributes) }
    obj.`@lrx`.foreach { v => attributes = new UnprefixedAttribute("lrx", v, attributes) }
    obj.`@lry`.foreach { v => attributes = new UnprefixedAttribute("lry", v, attributes) }
    obj.`@rotate`.foreach { v => attributes = new UnprefixedAttribute("rotate", v, attributes) }
    obj.`@data`.foreach { v => attributes = new UnprefixedAttribute("data", v, attributes) }
    obj.`@decls`.foreach { v => attributes = new UnprefixedAttribute("decls", v, attributes) }
    obj.`@startid`.foreach { v => attributes = new UnprefixedAttribute("startid", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "surface", attributes, TopScope, true, children*)
  }
}

object SvgWriter extends MeiModelDummyWriter[Svg] {
  def toXml(obj: Svg): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "svg", Null, TopScope, true, children*)
  }
}

object SwitchWriter extends MeiModelDummyWriter[Switch] {
  def toXml(obj: Switch): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "switch", Null, TopScope, true, children*)
  }
}

object SylWriter extends MeiModelDummyWriter[Syl] {
  def toXml(obj: Syl): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@con`.foreach { v => attributes = new UnprefixedAttribute("con", v, attributes) }
    obj.`@wordpos`.foreach { v => attributes = new UnprefixedAttribute("wordpos", v, attributes) }
    obj.`@place`.foreach { v => attributes = new UnprefixedAttribute("place", v, attributes) }
    obj.`@fontfam`.foreach { v => attributes = new UnprefixedAttribute("fontfam", v, attributes) }
    obj.`@fontname`.foreach { v => attributes = new UnprefixedAttribute("fontname", v, attributes) }
    obj.`@fontsize`.foreach { v => attributes = new UnprefixedAttribute("fontsize", v, attributes) }
    obj.`@fontstyle`.foreach { v => attributes = new UnprefixedAttribute("fontstyle", v, attributes) }
    obj.`@fontweight`.foreach { v => attributes = new UnprefixedAttribute("fontweight", v, attributes) }
    obj.`@letterspacing`.foreach { v => attributes = new UnprefixedAttribute("letterspacing", v, attributes) }
    obj.`@lineheight`.foreach { v => attributes = new UnprefixedAttribute("lineheight", v, attributes) }
    obj.`@ho`.foreach { v => attributes = new UnprefixedAttribute("ho", v, attributes) }
    obj.`@to`.foreach { v => attributes = new UnprefixedAttribute("to", v, attributes) }
    obj.`@vo`.foreach { v => attributes = new UnprefixedAttribute("vo", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }
    obj.`@halign`.foreach { v => attributes = new UnprefixedAttribute("halign", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "syl", attributes, TopScope, true, children*)
  }
}

object SymNameWriter extends MeiModelDummyWriter[SymName] {
  def toXml(obj: SymName): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "symName", attributes, TopScope, true, children*)
  }
}

object SymPropWriter extends MeiModelDummyWriter[SymProp] {
  def toXml(obj: SymProp): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "symProp", attributes, TopScope, true, children*)
  }
}

object SymbolWriter extends MeiModelDummyWriter[Symbol] {
  def toXml(obj: Symbol): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "symbol", Null, TopScope, true, children*)
  }
}

object SymbolDefWriter extends MeiModelDummyWriter[SymbolDef] {
  def toXml(obj: SymbolDef): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@ulx`.foreach { v => attributes = new UnprefixedAttribute("ulx", v, attributes) }
    obj.`@uly`.foreach { v => attributes = new UnprefixedAttribute("uly", v, attributes) }
    obj.`@lrx`.foreach { v => attributes = new UnprefixedAttribute("lrx", v, attributes) }
    obj.`@lry`.foreach { v => attributes = new UnprefixedAttribute("lry", v, attributes) }
    obj.`@rotate`.foreach { v => attributes = new UnprefixedAttribute("rotate", v, attributes) }
    obj.`@select`.foreach { v => attributes = new UnprefixedAttribute("select", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "symbolDef", attributes, TopScope, true, children*)
  }
}

object SymbolTableWriter extends MeiModelDummyWriter[SymbolTable] {
  def toXml(obj: SymbolTable): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "symbolTable", attributes, TopScope, true, children*)
  }
}

object SysReqWriter extends MeiModelDummyWriter[SysReq] {
  def toXml(obj: SysReq): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "sysReq", attributes, TopScope, true, children*)
  }
}

object TabDurSymWriter extends MeiModelDummyWriter[TabDurSym] {
  def toXml(obj: TabDurSym): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@tab.fing`.foreach { v => attributes = new UnprefixedAttribute("tab.fing", v, attributes) }
    obj.`@tab.fret`.foreach { v => attributes = new UnprefixedAttribute("tab.fret", v, attributes) }
    obj.`@tab.line`.foreach { v => attributes = new UnprefixedAttribute("tab.line", v, attributes) }
    obj.`@tab.string`.foreach { v => attributes = new UnprefixedAttribute("tab.string", v, attributes) }
    obj.`@tab.course`.foreach { v => attributes = new UnprefixedAttribute("tab.course", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@altsym`.foreach { v => attributes = new UnprefixedAttribute("altsym", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@glyph.auth`.foreach { v => attributes = new UnprefixedAttribute("glyph.auth", v, attributes) }
    obj.`@glyph.uri`.foreach { v => attributes = new UnprefixedAttribute("glyph.uri", v, attributes) }
    obj.`@glyph.name`.foreach { v => attributes = new UnprefixedAttribute("glyph.name", v, attributes) }
    obj.`@glyph.num`.foreach { v => attributes = new UnprefixedAttribute("glyph.num", v, attributes) }
    obj.`@place`.foreach { v => attributes = new UnprefixedAttribute("place", v, attributes) }
    obj.`@fontfam`.foreach { v => attributes = new UnprefixedAttribute("fontfam", v, attributes) }
    obj.`@fontname`.foreach { v => attributes = new UnprefixedAttribute("fontname", v, attributes) }
    obj.`@fontsize`.foreach { v => attributes = new UnprefixedAttribute("fontsize", v, attributes) }
    obj.`@fontstyle`.foreach { v => attributes = new UnprefixedAttribute("fontstyle", v, attributes) }
    obj.`@fontweight`.foreach { v => attributes = new UnprefixedAttribute("fontweight", v, attributes) }
    obj.`@letterspacing`.foreach { v => attributes = new UnprefixedAttribute("letterspacing", v, attributes) }
    obj.`@lineheight`.foreach { v => attributes = new UnprefixedAttribute("lineheight", v, attributes) }
    obj.`@visible`.foreach { v => attributes = new UnprefixedAttribute("visible", v, attributes) }
    obj.`@ho`.foreach { v => attributes = new UnprefixedAttribute("ho", v, attributes) }
    obj.`@to`.foreach { v => attributes = new UnprefixedAttribute("to", v, attributes) }
    obj.`@vo`.foreach { v => attributes = new UnprefixedAttribute("vo", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "tabDurSym", attributes, TopScope, true, children*)
  }
}

object TabGrpWriter extends MeiModelDummyWriter[TabGrp] {
  def toXml(obj: TabGrp): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@dur`.foreach { v => attributes = new UnprefixedAttribute("dur", v, attributes) }
    obj.`@when`.foreach { v => attributes = new UnprefixedAttribute("when", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@visible`.foreach { v => attributes = new UnprefixedAttribute("visible", v, attributes) }
    obj.`@ho`.foreach { v => attributes = new UnprefixedAttribute("ho", v, attributes) }
    obj.`@to`.foreach { v => attributes = new UnprefixedAttribute("to", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }
    obj.`@dur.ges`.foreach { v => attributes = new UnprefixedAttribute("dur.ges", v, attributes) }
    obj.`@dots.ges`.foreach { v => attributes = new UnprefixedAttribute("dots.ges", v, attributes) }
    obj.`@dur.metrical`.foreach { v => attributes = new UnprefixedAttribute("dur.metrical", v, attributes) }
    obj.`@dur.ppq`.foreach { v => attributes = new UnprefixedAttribute("dur.ppq", v, attributes) }
    obj.`@dur.real`.foreach { v => attributes = new UnprefixedAttribute("dur.real", v, attributes) }
    obj.`@dur.recip`.foreach { v => attributes = new UnprefixedAttribute("dur.recip", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "tabGrp", attributes, TopScope, true, children*)
  }
}

object TableWriter extends MeiModelDummyWriter[Table] {
  def toXml(obj: Table): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "table", attributes, TopScope, true, children*)
  }
}

object TagUsageWriter extends MeiModelDummyWriter[TagUsage] {
  def toXml(obj: TagUsage): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    attributes = new UnprefixedAttribute("name", obj.`@name`, attributes)
    obj.`@context`.foreach { v => attributes = new UnprefixedAttribute("context", v, attributes) }
    obj.`@occurs`.foreach { v => attributes = new UnprefixedAttribute("occurs", v, attributes) }
    obj.`@withid`.foreach { v => attributes = new UnprefixedAttribute("withid", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "tagUsage", attributes, TopScope, true, children*)
  }
}

object TagsDeclWriter extends MeiModelDummyWriter[TagsDecl] {
  def toXml(obj: TagsDecl): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "tagsDecl", attributes, TopScope, true, children*)
  }
}

object TaxonomyWriter extends MeiModelDummyWriter[Taxonomy] {
  def toXml(obj: Taxonomy): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "taxonomy", attributes, TopScope, true, children*)
  }
}

object TdWriter extends MeiModelDummyWriter[Td] {
  def toXml(obj: Td): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }
    obj.`@colspan`.foreach { v => attributes = new UnprefixedAttribute("colspan", v, attributes) }
    obj.`@rowspan`.foreach { v => attributes = new UnprefixedAttribute("rowspan", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "td", attributes, TopScope, true, children*)
  }
}

object TempoWriter extends MeiModelDummyWriter[Tempo] {
  def toXml(obj: Tempo): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@midi.bpm`.foreach { v => attributes = new UnprefixedAttribute("midi.bpm", v, attributes) }
    obj.`@midi.mspb`.foreach { v => attributes = new UnprefixedAttribute("midi.mspb", v, attributes) }
    obj.`@when`.foreach { v => attributes = new UnprefixedAttribute("when", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@plist`.foreach { v => attributes = new UnprefixedAttribute("plist", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    obj.`@mm`.foreach { v => attributes = new UnprefixedAttribute("mm", v, attributes) }
    obj.`@mm.unit`.foreach { v => attributes = new UnprefixedAttribute("mm.unit", v, attributes) }
    obj.`@mm.dots`.foreach { v => attributes = new UnprefixedAttribute("mm.dots", v, attributes) }
    obj.`@startid`.foreach { v => attributes = new UnprefixedAttribute("startid", v, attributes) }
    obj.`@endid`.foreach { v => attributes = new UnprefixedAttribute("endid", v, attributes) }
    obj.`@tstamp2`.foreach { v => attributes = new UnprefixedAttribute("tstamp2", v, attributes) }
    obj.`@func`.foreach { v => attributes = new UnprefixedAttribute("func", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@lform`.foreach { v => attributes = new UnprefixedAttribute("lform", v, attributes) }
    obj.`@lwidth`.foreach { v => attributes = new UnprefixedAttribute("lwidth", v, attributes) }
    obj.`@lsegs`.foreach { v => attributes = new UnprefixedAttribute("lsegs", v, attributes) }
    obj.`@lendsym`.foreach { v => attributes = new UnprefixedAttribute("lendsym", v, attributes) }
    obj.`@lendsym.size`.foreach { v => attributes = new UnprefixedAttribute("lendsym.size", v, attributes) }
    obj.`@lstartsym`.foreach { v => attributes = new UnprefixedAttribute("lstartsym", v, attributes) }
    obj.`@lstartsym.size`.foreach { v => attributes = new UnprefixedAttribute("lstartsym.size", v, attributes) }
    obj.`@extender`.foreach { v => attributes = new UnprefixedAttribute("extender", v, attributes) }
    obj.`@place`.foreach { v => attributes = new UnprefixedAttribute("place", v, attributes) }
    obj.`@vgrp`.foreach { v => attributes = new UnprefixedAttribute("vgrp", v, attributes) }
    obj.`@ho`.foreach { v => attributes = new UnprefixedAttribute("ho", v, attributes) }
    obj.`@to`.foreach { v => attributes = new UnprefixedAttribute("to", v, attributes) }
    obj.`@vo`.foreach { v => attributes = new UnprefixedAttribute("vo", v, attributes) }
    obj.`@startho`.foreach { v => attributes = new UnprefixedAttribute("startho", v, attributes) }
    obj.`@endho`.foreach { v => attributes = new UnprefixedAttribute("endho", v, attributes) }
    obj.`@startto`.foreach { v => attributes = new UnprefixedAttribute("startto", v, attributes) }
    obj.`@endto`.foreach { v => attributes = new UnprefixedAttribute("endto", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "tempo", attributes, TopScope, true, children*)
  }
}

object TermWriter extends MeiModelDummyWriter[Term] {
  def toXml(obj: Term): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@data`.foreach { v => attributes = new UnprefixedAttribute("data", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "term", attributes, TopScope, true, children*)
  }
}

object TermListWriter extends MeiModelDummyWriter[TermList] {
  def toXml(obj: TermList): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xlink:actuate`.foreach { v => attributes = new UnprefixedAttribute("xlink:actuate", v, attributes) }
    obj.`@xlink:role`.foreach { v => attributes = new UnprefixedAttribute("xlink:role", v, attributes) }
    obj.`@xlink:show`.foreach { v => attributes = new UnprefixedAttribute("xlink:show", v, attributes) }
    obj.`@target`.foreach { v => attributes = new UnprefixedAttribute("target", v, attributes) }
    obj.`@targettype`.foreach { v => attributes = new UnprefixedAttribute("targettype", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "termList", attributes, TopScope, true, children*)
  }
}

object TextWriter extends MeiModelDummyWriter[Text] {
  def toXml(obj: Text): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "text", Null, TopScope, true, children*)
  }
}

object TextLangWriter extends MeiModelDummyWriter[TextLang] {
  def toXml(obj: TextLang): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@lang.main`.foreach { v => attributes = new UnprefixedAttribute("lang.main", v, attributes) }
    obj.`@lang.other`.foreach { v => attributes = new UnprefixedAttribute("lang.other", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "textLang", attributes, TopScope, true, children*)
  }
}

object TextPathWriter extends MeiModelDummyWriter[TextPath] {
  def toXml(obj: TextPath): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "textPath", Null, TopScope, true, children*)
  }
}

object ThWriter extends MeiModelDummyWriter[Th] {
  def toXml(obj: Th): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }
    obj.`@colspan`.foreach { v => attributes = new UnprefixedAttribute("colspan", v, attributes) }
    obj.`@rowspan`.foreach { v => attributes = new UnprefixedAttribute("rowspan", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "th", attributes, TopScope, true, children*)
  }
}

object TitleWriter extends MeiModelDummyWriter[Title] {
  def toXml(obj: Title): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "title", Null, TopScope, true, children*)
  }
}

object TitlePageWriter extends MeiModelDummyWriter[TitlePage] {
  def toXml(obj: TitlePage): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "titlePage", attributes, TopScope, true, children*)
  }
}

object TitlePartWriter extends MeiModelDummyWriter[TitlePart] {
  def toXml(obj: TitlePart): Elem = {
    var attributes: MetaData = Null
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@nonfiling`.foreach { v => attributes = new UnprefixedAttribute("nonfiling", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "titlePart", attributes, TopScope, true, children*)
  }
}

object TitleStmtWriter extends MeiModelDummyWriter[TitleStmt] {
  def toXml(obj: TitleStmt): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "titleStmt", attributes, TopScope, true, children*)
  }
}

object TrWriter extends MeiModelDummyWriter[Tr] {
  def toXml(obj: Tr): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "tr", attributes, TopScope, true, children*)
  }
}

object TrackConfigWriter extends MeiModelDummyWriter[TrackConfig] {
  def toXml(obj: TrackConfig): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@num`.foreach { v => attributes = new UnprefixedAttribute("num", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "trackConfig", attributes, TopScope, true, children*)
  }
}

object TreatHistWriter extends MeiModelDummyWriter[TreatHist] {
  def toXml(obj: TreatHist): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "treatHist", attributes, TopScope, true, children*)
  }
}

object TreatSchedWriter extends MeiModelDummyWriter[TreatSched] {
  def toXml(obj: TreatSched): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@enddate`.foreach { v => attributes = new UnprefixedAttribute("enddate", v, attributes) }
    obj.`@isodate`.foreach { v => attributes = new UnprefixedAttribute("isodate", v, attributes) }
    obj.`@notafter`.foreach { v => attributes = new UnprefixedAttribute("notafter", v, attributes) }
    obj.`@notbefore`.foreach { v => attributes = new UnprefixedAttribute("notbefore", v, attributes) }
    obj.`@startdate`.foreach { v => attributes = new UnprefixedAttribute("startdate", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "treatSched", attributes, TopScope, true, children*)
  }
}

object TrefWriter extends MeiModelDummyWriter[Tref] {
  def toXml(obj: Tref): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "tref", Null, TopScope, true, children*)
  }
}

object TrkNameWriter extends MeiModelDummyWriter[TrkName] {
  def toXml(obj: TrkName): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "trkName", attributes, TopScope, true, children*)
  }
}

object TspanWriter extends MeiModelDummyWriter[Tspan] {
  def toXml(obj: Tspan): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "tspan", Null, TopScope, true, children*)
  }
}

object TuningWriter extends MeiModelDummyWriter[Tuning] {
  def toXml(obj: Tuning): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@tuning.standard`.foreach { v => attributes = new UnprefixedAttribute("tuning.standard", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "tuning", attributes, TopScope, true, children*)
  }
}

object TypeDescWriter extends MeiModelDummyWriter[TypeDesc] {
  def toXml(obj: TypeDesc): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "typeDesc", attributes, TopScope, true, children*)
  }
}

object TypeNoteWriter extends MeiModelDummyWriter[TypeNote] {
  def toXml(obj: TypeNote): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "typeNote", attributes, TopScope, true, children*)
  }
}

object UnclearWriter extends MeiModelDummyWriter[Unclear] {
  def toXml(obj: Unclear): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@agent`.foreach { v => attributes = new UnprefixedAttribute("agent", v, attributes) }
    obj.`@source`.foreach { v => attributes = new UnprefixedAttribute("source", v, attributes) }
    obj.`@cert`.foreach { v => attributes = new UnprefixedAttribute("cert", v, attributes) }
    obj.`@evidence`.foreach { v => attributes = new UnprefixedAttribute("evidence", v, attributes) }
    obj.`@unit`.foreach { v => attributes = new UnprefixedAttribute("unit", v, attributes) }
    obj.`@atleast`.foreach { v => attributes = new UnprefixedAttribute("atleast", v, attributes) }
    obj.`@atmost`.foreach { v => attributes = new UnprefixedAttribute("atmost", v, attributes) }
    obj.`@min`.foreach { v => attributes = new UnprefixedAttribute("min", v, attributes) }
    obj.`@max`.foreach { v => attributes = new UnprefixedAttribute("max", v, attributes) }
    obj.`@confidence`.foreach { v => attributes = new UnprefixedAttribute("confidence", v, attributes) }
    obj.`@extent`.foreach { v => attributes = new UnprefixedAttribute("extent", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@hand`.foreach { v => attributes = new UnprefixedAttribute("hand", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@reason`.foreach { v => attributes = new UnprefixedAttribute("reason", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "unclear", attributes, TopScope, true, children*)
  }
}

object UnpubWriter extends MeiModelDummyWriter[Unpub] {
  def toXml(obj: Unpub): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "unpub", attributes, TopScope, true, children*)
  }
}

object UseWriter extends MeiModelDummyWriter[Use] {
  def toXml(obj: Use): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "use", Null, TopScope, true, children*)
  }
}

object UseRestrictWriter extends MeiModelDummyWriter[UseRestrict] {
  def toXml(obj: UseRestrict): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "useRestrict", attributes, TopScope, true, children*)
  }
}

object VelWriter extends MeiModelDummyWriter[Vel] {
  def toXml(obj: Vel): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@layer`.foreach { v => attributes = new UnprefixedAttribute("layer", v, attributes) }
    obj.`@part`.foreach { v => attributes = new UnprefixedAttribute("part", v, attributes) }
    obj.`@partstaff`.foreach { v => attributes = new UnprefixedAttribute("partstaff", v, attributes) }
    obj.`@staff`.foreach { v => attributes = new UnprefixedAttribute("staff", v, attributes) }
    obj.`@tstamp`.foreach { v => attributes = new UnprefixedAttribute("tstamp", v, attributes) }
    obj.`@tstamp.ges`.foreach { v => attributes = new UnprefixedAttribute("tstamp.ges", v, attributes) }
    obj.`@tstamp.real`.foreach { v => attributes = new UnprefixedAttribute("tstamp.real", v, attributes) }
    attributes = new UnprefixedAttribute("num", obj.`@num`, attributes)
    attributes = new UnprefixedAttribute("form", obj.`@form`, attributes)

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "vel", attributes, TopScope, true, children*)
  }
}

object VerseWriter extends MeiModelDummyWriter[Verse] {
  def toXml(obj: Verse): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@place`.foreach { v => attributes = new UnprefixedAttribute("place", v, attributes) }
    obj.`@fontfam`.foreach { v => attributes = new UnprefixedAttribute("fontfam", v, attributes) }
    obj.`@fontname`.foreach { v => attributes = new UnprefixedAttribute("fontname", v, attributes) }
    obj.`@fontsize`.foreach { v => attributes = new UnprefixedAttribute("fontsize", v, attributes) }
    obj.`@fontstyle`.foreach { v => attributes = new UnprefixedAttribute("fontstyle", v, attributes) }
    obj.`@fontweight`.foreach { v => attributes = new UnprefixedAttribute("fontweight", v, attributes) }
    obj.`@letterspacing`.foreach { v => attributes = new UnprefixedAttribute("letterspacing", v, attributes) }
    obj.`@lineheight`.foreach { v => attributes = new UnprefixedAttribute("lineheight", v, attributes) }
    obj.`@to`.foreach { v => attributes = new UnprefixedAttribute("to", v, attributes) }
    obj.`@vo`.foreach { v => attributes = new UnprefixedAttribute("vo", v, attributes) }
    obj.`@voltasym`.foreach { v => attributes = new UnprefixedAttribute("voltasym", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "verse", attributes, TopScope, true, children*)
  }
}

object ViewWriter extends MeiModelDummyWriter[View] {
  def toXml(obj: View): Elem = {

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "view", Null, TopScope, true, children*)
  }
}

object VkernWriter extends MeiModelDummyWriter[Vkern] {
  def toXml(obj: Vkern): Elem = {

    val children: scala.collection.immutable.List[Node] = scala.collection.immutable.Nil

    Elem(null, "vkern", Null, TopScope, true, children*)
  }
}

object VoltaWriter extends MeiModelDummyWriter[Volta] {
  def toXml(obj: Volta): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@color`.foreach { v => attributes = new UnprefixedAttribute("color", v, attributes) }
    obj.`@fontfam`.foreach { v => attributes = new UnprefixedAttribute("fontfam", v, attributes) }
    obj.`@fontname`.foreach { v => attributes = new UnprefixedAttribute("fontname", v, attributes) }
    obj.`@fontsize`.foreach { v => attributes = new UnprefixedAttribute("fontsize", v, attributes) }
    obj.`@fontstyle`.foreach { v => attributes = new UnprefixedAttribute("fontstyle", v, attributes) }
    obj.`@fontweight`.foreach { v => attributes = new UnprefixedAttribute("fontweight", v, attributes) }
    obj.`@letterspacing`.foreach { v => attributes = new UnprefixedAttribute("letterspacing", v, attributes) }
    obj.`@lineheight`.foreach { v => attributes = new UnprefixedAttribute("lineheight", v, attributes) }
    obj.`@to`.foreach { v => attributes = new UnprefixedAttribute("to", v, attributes) }
    obj.`@vo`.foreach { v => attributes = new UnprefixedAttribute("vo", v, attributes) }
    obj.`@x`.foreach { v => attributes = new UnprefixedAttribute("x", v, attributes) }
    obj.`@y`.foreach { v => attributes = new UnprefixedAttribute("y", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "volta", attributes, TopScope, true, children*)
  }
}

object WatermarkWriter extends MeiModelDummyWriter[Watermark] {
  def toXml(obj: Watermark): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@facs`.foreach { v => attributes = new UnprefixedAttribute("facs", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "watermark", attributes, TopScope, true, children*)
  }
}

object WhenWriter extends MeiModelDummyWriter[When] {
  def toXml(obj: When): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@data`.foreach { v => attributes = new UnprefixedAttribute("data", v, attributes) }
    obj.`@absolute`.foreach { v => attributes = new UnprefixedAttribute("absolute", v, attributes) }
    obj.`@interval`.foreach { v => attributes = new UnprefixedAttribute("interval", v, attributes) }
    obj.`@abstype`.foreach { v => attributes = new UnprefixedAttribute("abstype", v, attributes) }
    obj.`@inttype`.foreach { v => attributes = new UnprefixedAttribute("inttype", v, attributes) }
    obj.`@since`.foreach { v => attributes = new UnprefixedAttribute("since", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "when", attributes, TopScope, true, children*)
  }
}

object WidthWriter extends MeiModelDummyWriter[Width] {
  def toXml(obj: Width): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@xml:lang`.foreach { v => attributes = new UnprefixedAttribute("xml:lang", v, attributes) }
    obj.`@translit`.foreach { v => attributes = new UnprefixedAttribute("translit", v, attributes) }
    obj.`@unit`.foreach { v => attributes = new UnprefixedAttribute("unit", v, attributes) }
    obj.`@atleast`.foreach { v => attributes = new UnprefixedAttribute("atleast", v, attributes) }
    obj.`@atmost`.foreach { v => attributes = new UnprefixedAttribute("atmost", v, attributes) }
    obj.`@min`.foreach { v => attributes = new UnprefixedAttribute("min", v, attributes) }
    obj.`@max`.foreach { v => attributes = new UnprefixedAttribute("max", v, attributes) }
    obj.`@confidence`.foreach { v => attributes = new UnprefixedAttribute("confidence", v, attributes) }
    obj.`@quantity`.foreach { v => attributes = new UnprefixedAttribute("quantity", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "width", attributes, TopScope, true, children*)
  }
}

object WorkWriter extends MeiModelDummyWriter[Work] {
  def toXml(obj: Work): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@codedval`.foreach { v => attributes = new UnprefixedAttribute("codedval", v, attributes) }
    obj.`@auth`.foreach { v => attributes = new UnprefixedAttribute("auth", v, attributes) }
    obj.`@auth.uri`.foreach { v => attributes = new UnprefixedAttribute("auth.uri", v, attributes) }
    obj.`@analog`.foreach { v => attributes = new UnprefixedAttribute("analog", v, attributes) }
    obj.`@data`.foreach { v => attributes = new UnprefixedAttribute("data", v, attributes) }
    obj.`@xlink:actuate`.foreach { v => attributes = new UnprefixedAttribute("xlink:actuate", v, attributes) }
    obj.`@xlink:role`.foreach { v => attributes = new UnprefixedAttribute("xlink:role", v, attributes) }
    obj.`@xlink:show`.foreach { v => attributes = new UnprefixedAttribute("xlink:show", v, attributes) }
    obj.`@target`.foreach { v => attributes = new UnprefixedAttribute("target", v, attributes) }
    obj.`@targettype`.foreach { v => attributes = new UnprefixedAttribute("targettype", v, attributes) }
    obj.`@evaluate`.foreach { v => attributes = new UnprefixedAttribute("evaluate", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "work", attributes, TopScope, true, children*)
  }
}

object WorkListWriter extends MeiModelDummyWriter[WorkList] {
  def toXml(obj: WorkList): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "workList", attributes, TopScope, true, children*)
  }
}

object ZoneWriter extends MeiModelDummyWriter[Zone] {
  def toXml(obj: Zone): Elem = {
    var attributes: MetaData = Null
    obj.`@xml:id`.foreach { v => attributes = new UnprefixedAttribute("xml:id", v, attributes) }
    obj.`@xml:base`.foreach { v => attributes = new UnprefixedAttribute("xml:base", v, attributes) }
    obj.`@label`.foreach { v => attributes = new UnprefixedAttribute("label", v, attributes) }
    obj.`@copyof`.foreach { v => attributes = new UnprefixedAttribute("copyof", v, attributes) }
    obj.`@corresp`.foreach { v => attributes = new UnprefixedAttribute("corresp", v, attributes) }
    obj.`@follows`.foreach { v => attributes = new UnprefixedAttribute("follows", v, attributes) }
    obj.`@next`.foreach { v => attributes = new UnprefixedAttribute("next", v, attributes) }
    obj.`@precedes`.foreach { v => attributes = new UnprefixedAttribute("precedes", v, attributes) }
    obj.`@prev`.foreach { v => attributes = new UnprefixedAttribute("prev", v, attributes) }
    obj.`@sameas`.foreach { v => attributes = new UnprefixedAttribute("sameas", v, attributes) }
    obj.`@synch`.foreach { v => attributes = new UnprefixedAttribute("synch", v, attributes) }
    obj.`@n`.foreach { v => attributes = new UnprefixedAttribute("n", v, attributes) }
    obj.`@resp`.foreach { v => attributes = new UnprefixedAttribute("resp", v, attributes) }
    obj.`@class`.foreach { v => attributes = new UnprefixedAttribute("class", v, attributes) }
    obj.`@type`.foreach { v => attributes = new UnprefixedAttribute("type", v, attributes) }
    obj.`@ulx`.foreach { v => attributes = new UnprefixedAttribute("ulx", v, attributes) }
    obj.`@uly`.foreach { v => attributes = new UnprefixedAttribute("uly", v, attributes) }
    obj.`@lrx`.foreach { v => attributes = new UnprefixedAttribute("lrx", v, attributes) }
    obj.`@lry`.foreach { v => attributes = new UnprefixedAttribute("lry", v, attributes) }
    obj.`@rotate`.foreach { v => attributes = new UnprefixedAttribute("rotate", v, attributes) }
    obj.`@data`.foreach { v => attributes = new UnprefixedAttribute("data", v, attributes) }

    val children: scala.collection.immutable.List[Node] = obj.elements.flatMap { case x =>
      Some(MeiXmlWriter.toXml(x))
    }

    Elem(null, "zone", attributes, TopScope, true, children*)
  }
}
